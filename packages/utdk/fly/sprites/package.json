{
  "name": "@utdk/fly-sprites",
  "version": "0.1.0",
  "type": "module",
  "description": "fly.io Sprites provider: persistent, hardware-isolated Linux microVMs with a durable filesystem and a per-sprite URL. Credential: a bearer_token holding a Sprites API token.",
  "keywords": [
    "sandbox",
    "sprites",
    "fly.io",
    "microvm",
    "utdk"
  ],
  "utdk": {
    "provider": "fly/sprites",
    "handwritten": true,
    "interface": "sandbox",
    "auth": {
      "schemes": [
        {
          "id": "api_token",
          "type": "http",
          "scheme": "bearer",
          "description": "Sprites API token. Override the API root with the sandbox interface binding's `baseUrl`."
        }
      ]
    }
  }
}
