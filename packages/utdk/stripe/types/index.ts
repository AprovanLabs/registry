import type { Account, AccountLink, AccountSession, ApplePayDomain, ApplicationFee, AppsSecret, Balance, BalanceSettings, BalanceTransaction, BankAccount, BillingAlert, BillingCreditBalanceSummary, BillingCreditBalanceTransaction, BillingCreditGrant, BillingMeter, BillingMeterEvent, BillingMeterEventAdjustment, BillingMeterEventSummary, BillingPortalConfiguration, BillingPortalSession, Capability, Card, CashBalance, Charge, CheckoutSession, ClimateOrder, ClimateProduct, ClimateSupplier, ConfirmationToken, CountrySpec, Coupon, CreditNote, CreditNoteLineItem, Customer, CustomerBalanceTransaction, CustomerCashBalanceTransaction, CustomerSession, DeletedAccount, DeletedApplePayDomain, DeletedCoupon, DeletedCustomer, DeletedDiscount, DeletedExternalAccount, DeletedInvoice, DeletedInvoiceitem, DeletedPaymentSource, DeletedPerson, DeletedPlan, DeletedProduct, DeletedProductFeature, DeletedRadarValueList, DeletedRadarValueListItem, DeletedSubscriptionItem, DeletedTaxId, DeletedTerminalConfiguration, DeletedTerminalLocation, DeletedTerminalReader, DeletedTestHelpersTestClock, DeletedWebhookEndpoint, Discount, Dispute, EntitlementsActiveEntitlement, EntitlementsFeature, EphemeralKey, Event, ExchangeRate, ExternalAccount, FeeRefund, File, FileLink, FinancialConnectionsAccount, FinancialConnectionsAccountOwner, FinancialConnectionsSession, FinancialConnectionsTransaction, ForwardingRequest, FundingInstructions, IdentityVerificationReport, IdentityVerificationSession, Invoice, InvoicePayment, InvoiceRenderingTemplate, Invoiceitem, IssuingAuthorization, IssuingCard, IssuingCardholder, IssuingDispute, IssuingPersonalizationDesign, IssuingPhysicalBundle, IssuingSettlement, IssuingToken, IssuingTransaction, Item, LineItem, LoginLink, Mandate, PaymentAttemptRecord, PaymentIntent, PaymentIntentAmountDetailsLineItem, PaymentLink, PaymentMethod, PaymentMethodConfiguration, PaymentMethodDomain, PaymentRecord, PaymentSource, Payout, Person, Plan, Price, Product, ProductFeature, PromotionCode, Quote, RadarEarlyFraudWarning, RadarPaymentEvaluation, RadarValueList, RadarValueListItem, Refund, ReportingReportRun, ReportingReportType, Review, ScheduledQueryRun, SetupAttempt, SetupIntent, ShippingRate, SigmaSigmaApiQuery, Source, SourceMandateNotification, SourceTransaction, Subscription, SubscriptionItem, SubscriptionSchedule, TaxAssociation, TaxCalculation, TaxCalculationLineItem, TaxCode, TaxId, TaxRate, TaxRegistration, TaxSettings, TaxTransaction, TaxTransactionLineItem, TerminalConfiguration, TerminalConnectionToken, TerminalLocation, TerminalOnboardingLink, TerminalReader, TerminalRefund, TestHelpersTestClock, Token, Topup, Transfer, TransferReversal, TreasuryCreditReversal, TreasuryDebitReversal, TreasuryFinancialAccount, TreasuryFinancialAccountFeatures, TreasuryInboundTransfer, TreasuryOutboundPayment, TreasuryOutboundTransfer, TreasuryReceivedCredit, TreasuryReceivedDebit, TreasuryTransaction, TreasuryTransactionEntry, WebhookEndpoint } from "./schemas.js";

export type StripeClient = {
  /**
   * Retrieve account
   */
  getAccount: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Account>;

  /**
   * Create an account link
   */
  postAccountLinks: (input: {
    /** The identifier of the account to create an account link for. */
    account: string;
    /** The collect parameter is deprecated. Use `collection_options` instead. */
    collect?: "currently_due" | "eventually_due";
    /** Specifies the requirements that Stripe collects from connected accounts in the Connect Onboarding flow. */
    collection_options?: { fields?: "currently_due" | "eventually_due"; future_requirements?: "include" | "omit" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The URL the user will be redirected to if the account link is expired, has been previously-visited, or is otherwise invalid. The URL you specify should attempt to generate a new account link with the same parameters used to create the original account link, then redirect the user to the new account link's URL so they can continue with Connect Onboarding. If a new account link cannot be generated or the redirect fails you should display a useful error to the user. */
    refresh_url?: string;
    /** The URL that the user will be redirected to upon leaving or completing the linked flow. */
    return_url?: string;
    /** The type of account link the user is requesting.  You can create Account Links of type `account_update` only for connected accounts where your platform is responsible for collecting requirements, including Custom accounts. You can't create them for accounts that have access to a Stripe-hosted Dashboard. If you use [Connect embedded components](/connect/get-started-connect-embedded-components), you can include components that allow your connected accounts to update their own information. For an account without Stripe-hosted Dashboard access where Stripe is liable for negative balances, you must use embedded components. */
    type: "account_onboarding" | "account_update";
  }) => Promise<AccountLink>;

  /**
   * Create an Account Session
   */
  postAccountSessions: (input: {
    /** The identifier of the account to create an Account Session for. */
    account: string;
    /** Each key of the dictionary represents an embedded component, and each embedded component maps to its configuration (e.g. whether it has been enabled or not). */
    components: { account_management?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; external_account_collection?: boolean } }; account_onboarding?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; external_account_collection?: boolean } }; balance_report?: { enabled: boolean; features?: { [key: string]: unknown } }; balances?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; edit_payout_schedule?: boolean; external_account_collection?: boolean; instant_payouts?: boolean; standard_payouts?: boolean } }; disputes_list?: { enabled: boolean; features?: { capture_payments?: boolean; destination_on_behalf_of_charge_management?: boolean; dispute_management?: boolean; refund_management?: boolean } }; documents?: { enabled: boolean; features?: { [key: string]: unknown } }; financial_account?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; external_account_collection?: boolean; send_money?: boolean; transfer_balance?: boolean } }; financial_account_transactions?: { enabled: boolean; features?: { card_spend_dispute_management?: boolean } }; instant_payouts_promotion?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; external_account_collection?: boolean; instant_payouts?: boolean } }; issuing_card?: { enabled: boolean; features?: { card_management?: boolean; card_spend_dispute_management?: boolean; cardholder_management?: boolean; spend_control_management?: boolean } }; issuing_cards_list?: { enabled: boolean; features?: { card_management?: boolean; card_spend_dispute_management?: boolean; cardholder_management?: boolean; disable_stripe_user_authentication?: boolean; spend_control_management?: boolean } }; notification_banner?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; external_account_collection?: boolean } }; payment_details?: { enabled: boolean; features?: { capture_payments?: boolean; destination_on_behalf_of_charge_management?: boolean; dispute_management?: boolean; refund_management?: boolean } }; payment_disputes?: { enabled: boolean; features?: { destination_on_behalf_of_charge_management?: boolean; dispute_management?: boolean; refund_management?: boolean } }; payments?: { enabled: boolean; features?: { capture_payments?: boolean; destination_on_behalf_of_charge_management?: boolean; dispute_management?: boolean; refund_management?: boolean } }; payout_details?: { enabled: boolean; features?: { [key: string]: unknown } }; payout_reconciliation_report?: { enabled: boolean; features?: { [key: string]: unknown } }; payouts?: { enabled: boolean; features?: { disable_stripe_user_authentication?: boolean; edit_payout_schedule?: boolean; external_account_collection?: boolean; instant_payouts?: boolean; standard_payouts?: boolean } }; payouts_list?: { enabled: boolean; features?: { [key: string]: unknown } }; tax_registrations?: { enabled: boolean; features?: { [key: string]: unknown } }; tax_settings?: { enabled: boolean; features?: { [key: string]: unknown } } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<AccountSession>;

  /**
   * List all connected accounts
   */
  getAccounts: (input: {
    body?: { [key: string]: unknown };
    /** Only return connected accounts that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Account)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * <p>With <a href="/docs/connect">Connect</a>, you can create Stripe accounts for your users. To do this, you’ll first need to <a href="https://dashboard.stripe.com/account/applications/settings">register your platform</a>.</p>  <p>If you’ve already collected information for your connected accounts, you <a href="/docs/connect/best-practices#onboarding">can prefill that information</a> when creating the account. Connect Onboarding won’t ask for the prefilled information during account onboarding. You can prefill any information on the account.</p>
   */
  postAccounts: (input: {
    /** An [account token](https://api.stripe.com#create_account_token), used to securely provide details to the account. */
    account_token?: string;
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; account_type?: "checking" | "futsu" | "savings" | "toza"; country: string; currency?: string; documents?: { bank_account_ownership_verification?: { files?: (string)[] } }; object?: "bank_account"; routing_number?: string } | string;
    /** Business information about the account. */
    business_profile?: { annual_revenue?: { amount: number; currency: string; fiscal_year_end: string }; estimated_worker_count?: number; mcc?: string; minority_owned_business_designation?: ("lgbtqi_owned_business" | "minority_owned_business" | "none_of_these_apply" | "prefer_not_to_answer" | "women_owned_business")[]; monthly_estimated_revenue?: { amount: number; currency: string }; name?: string; product_description?: string; support_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; support_email?: string; support_phone?: string; support_url?: string | ""; url?: string };
    /** The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    business_type?: "company" | "government_entity" | "individual" | "non_profit";
    /** Each key of the dictionary represents a capability, and each capability maps to its settings (for example, whether it has been requested or not). Each capability is inactive until you have provided its specific requirements and Stripe has verified them. An account might have some of its requested capabilities be active and some be inactive.  Required when [account.controller.stripe_dashboard.type](/api/accounts/create#create_account-controller-dashboard-type) is `none`, which includes Custom accounts. */
    capabilities?: { acss_debit_payments?: { requested?: boolean }; affirm_payments?: { requested?: boolean }; afterpay_clearpay_payments?: { requested?: boolean }; alma_payments?: { requested?: boolean }; amazon_pay_payments?: { requested?: boolean }; app_distribution?: { requested?: boolean }; au_becs_debit_payments?: { requested?: boolean }; bacs_debit_payments?: { requested?: boolean }; bancontact_payments?: { requested?: boolean }; bank_transfer_payments?: { requested?: boolean }; billie_payments?: { requested?: boolean }; bizum_payments?: { requested?: boolean }; blik_payments?: { requested?: boolean }; boleto_payments?: { requested?: boolean }; card_issuing?: { requested?: boolean }; card_payments?: { requested?: boolean }; cartes_bancaires_payments?: { requested?: boolean }; cashapp_payments?: { requested?: boolean }; crypto_payments?: { requested?: boolean }; eps_payments?: { requested?: boolean }; fpx_payments?: { requested?: boolean }; gb_bank_transfer_payments?: { requested?: boolean }; giropay_payments?: { requested?: boolean }; grabpay_payments?: { requested?: boolean }; ideal_payments?: { requested?: boolean }; india_international_payments?: { requested?: boolean }; jcb_payments?: { requested?: boolean }; jp_bank_transfer_payments?: { requested?: boolean }; kakao_pay_payments?: { requested?: boolean }; klarna_payments?: { requested?: boolean }; konbini_payments?: { requested?: boolean }; kr_card_payments?: { requested?: boolean }; legacy_payments?: { requested?: boolean }; link_payments?: { requested?: boolean }; mb_way_payments?: { requested?: boolean }; mobilepay_payments?: { requested?: boolean }; multibanco_payments?: { requested?: boolean }; mx_bank_transfer_payments?: { requested?: boolean }; naver_pay_payments?: { requested?: boolean }; nz_bank_account_becs_debit_payments?: { requested?: boolean }; oxxo_payments?: { requested?: boolean }; p24_payments?: { requested?: boolean }; pay_by_bank_payments?: { requested?: boolean }; payco_payments?: { requested?: boolean }; paynow_payments?: { requested?: boolean }; payto_payments?: { requested?: boolean }; pix_payments?: { requested?: boolean }; promptpay_payments?: { requested?: boolean }; revolut_pay_payments?: { requested?: boolean }; samsung_pay_payments?: { requested?: boolean }; satispay_payments?: { requested?: boolean }; scalapay_payments?: { requested?: boolean }; sepa_bank_transfer_payments?: { requested?: boolean }; sepa_debit_payments?: { requested?: boolean }; sofort_payments?: { requested?: boolean }; sunbit_payments?: { requested?: boolean }; swish_payments?: { requested?: boolean }; tax_reporting_us_1099_k?: { requested?: boolean }; tax_reporting_us_1099_misc?: { requested?: boolean }; transfers?: { requested?: boolean }; treasury?: { requested?: boolean }; twint_payments?: { requested?: boolean }; upi_payments?: { requested?: boolean }; us_bank_account_ach_payments?: { requested?: boolean }; us_bank_transfer_payments?: { requested?: boolean }; zip_payments?: { requested?: boolean } };
    /** Information about the company or business. This field is available for any `business_type`. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    company?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; directors_provided?: boolean; directorship_declaration?: { date?: number; ip?: string; user_agent?: string }; executives_provided?: boolean; export_license_id?: string; export_purpose_code?: string; name?: string; name_kana?: string; name_kanji?: string; owners_provided?: boolean; ownership_declaration?: { date?: number; ip?: string; user_agent?: string }; ownership_exemption_reason?: "" | "qualified_entity_exceeds_ownership_threshold" | "qualifies_as_financial_institution"; phone?: string; registration_date?: { day: number; month: number; year: number } | ""; registration_number?: string; representative_declaration?: { date?: number; ip?: string; user_agent?: string }; structure?: "" | "free_zone_establishment" | "free_zone_llc" | "government_instrumentality" | "governmental_unit" | "incorporated_non_profit" | "incorporated_partnership" | "limited_liability_partnership" | "llc" | "multi_member_llc" | "private_company" | "private_corporation" | "private_partnership" | "public_company" | "public_corporation" | "public_partnership" | "registered_charity" | "single_member_llc" | "sole_establishment" | "sole_proprietorship" | "tax_exempt_government_instrumentality" | "unincorporated_association" | "unincorporated_non_profit" | "unincorporated_partnership"; tax_id?: string; tax_id_registrar?: string; vat_id?: string; verification?: { document?: { back?: string; front?: string } } };
    /** A hash of configuration describing the account controller's attributes. */
    controller?: { fees?: { payer?: "account" | "application" }; losses?: { payments?: "application" | "stripe" }; requirement_collection?: "application" | "stripe"; stripe_dashboard?: { type?: "express" | "full" | "none" } };
    /** The country in which the account holder resides, or in which the business is legally established. This should be an ISO 3166-1 alpha-2 country code. For example, if you are in the United States and the business for which you're creating an account is legally represented in Canada, you would use `CA` as the country for the account being created. Available countries include [Stripe's global markets](https://stripe.com/global) as well as countries where [cross-border payouts](https://stripe.com/docs/connect/cross-border-payouts) are supported. */
    country?: string;
    /** Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts). */
    default_currency?: string;
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { bank_account_ownership_verification?: { files?: (string)[] }; company_license?: { files?: (string)[] }; company_memorandum_of_association?: { files?: (string)[] }; company_ministerial_decree?: { files?: (string)[] }; company_registration_verification?: { files?: (string)[] }; company_tax_id_verification?: { files?: (string)[] }; proof_of_address?: { files?: (string)[] }; proof_of_registration?: { files?: (string)[]; signer?: { person?: string } }; proof_of_ultimate_beneficial_ownership?: { files?: (string)[]; signer?: { person?: string } } };
    /** The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    external_account?: string;
    /** A hash of account group type to tokens. These are account groups this account should be added to. */
    groups?: { payments_pricing?: string | "" };
    /** Information about the person represented by the account. This field is null unless `business_type` is set to `individual`. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    individual?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; dob?: { day: number; month: number; year: number } | ""; email?: string; first_name?: string; first_name_kana?: string; first_name_kanji?: string; full_name_aliases?: (string)[] | ""; gender?: string; id_number?: string; id_number_secondary?: string; last_name?: string; last_name_kana?: string; last_name_kanji?: string; maiden_name?: string; metadata?: { [key: string]: string | undefined } | ""; phone?: string; political_exposure?: "existing" | "none"; registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; relationship?: { director?: boolean; executive?: boolean; owner?: boolean; percent_ownership?: number | ""; title?: string }; ssn_last_4?: string; verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Options for customizing how the account functions within Stripe. */
    settings?: { bacs_debit_payments?: { display_name?: string }; branding?: { icon?: string; logo?: string; primary_color?: string; secondary_color?: string }; card_issuing?: { tos_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } }; card_payments?: { decline_on?: { avs_failure?: boolean; cvc_failure?: boolean }; statement_descriptor_prefix?: string; statement_descriptor_prefix_kana?: string | ""; statement_descriptor_prefix_kanji?: string | "" }; invoices?: { hosted_payment_method_save?: "always" | "never" | "offer" }; payments?: { statement_descriptor?: string; statement_descriptor_kana?: string; statement_descriptor_kanji?: string }; payouts?: { debit_negative_balances?: boolean; schedule?: { delay_days?: "minimum" | number; interval?: "daily" | "manual" | "monthly" | "weekly"; monthly_anchor?: number; monthly_payout_days?: (number)[]; weekly_anchor?: "friday" | "monday" | "saturday" | "sunday" | "thursday" | "tuesday" | "wednesday"; weekly_payout_days?: ("friday" | "monday" | "thursday" | "tuesday" | "wednesday")[] }; statement_descriptor?: string }; treasury?: { tos_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } } };
    /** Details on the account's acceptance of the [Stripe Services Agreement](/connect/updating-accounts#tos-acceptance). This property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. This property defaults to a `full` service agreement when empty. */
    tos_acceptance?: { date?: number; ip?: string; service_agreement?: string; user_agent?: string };
    /** The `type` parameter is deprecated. Use [`controller`](/api/accounts/create#create_account-controller) instead to configure dashboard access, fee payer, loss liability, and requirement collection. */
    type?: "custom" | "express" | "standard";
  }) => Promise<Account>;

  /**
   * Delete an account
   */
  deleteAccountsAccount: (input: {
    body?: { [key: string]: unknown };
    account: string;
  }) => Promise<DeletedAccount>;

  /**
   * Retrieve account
   */
  getAccountsAccount: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Account>;

  /**
   * Update an account
   */
  postAccountsAccount: (input: {
    /** An [account token](https://api.stripe.com#create_account_token), used to securely provide details to the account. */
    account_token?: string;
    /** Business information about the account. */
    business_profile?: { annual_revenue?: { amount: number; currency: string; fiscal_year_end: string }; estimated_worker_count?: number; mcc?: string; minority_owned_business_designation?: ("lgbtqi_owned_business" | "minority_owned_business" | "none_of_these_apply" | "prefer_not_to_answer" | "women_owned_business")[]; monthly_estimated_revenue?: { amount: number; currency: string }; name?: string; product_description?: string; support_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; support_email?: string; support_phone?: string; support_url?: string | ""; url?: string };
    /** The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    business_type?: "company" | "government_entity" | "individual" | "non_profit";
    /** Each key of the dictionary represents a capability, and each capability maps to its settings (for example, whether it has been requested or not). Each capability is inactive until you have provided its specific requirements and Stripe has verified them. An account might have some of its requested capabilities be active and some be inactive.  Required when [account.controller.stripe_dashboard.type](/api/accounts/create#create_account-controller-dashboard-type) is `none`, which includes Custom accounts. */
    capabilities?: { acss_debit_payments?: { requested?: boolean }; affirm_payments?: { requested?: boolean }; afterpay_clearpay_payments?: { requested?: boolean }; alma_payments?: { requested?: boolean }; amazon_pay_payments?: { requested?: boolean }; app_distribution?: { requested?: boolean }; au_becs_debit_payments?: { requested?: boolean }; bacs_debit_payments?: { requested?: boolean }; bancontact_payments?: { requested?: boolean }; bank_transfer_payments?: { requested?: boolean }; billie_payments?: { requested?: boolean }; bizum_payments?: { requested?: boolean }; blik_payments?: { requested?: boolean }; boleto_payments?: { requested?: boolean }; card_issuing?: { requested?: boolean }; card_payments?: { requested?: boolean }; cartes_bancaires_payments?: { requested?: boolean }; cashapp_payments?: { requested?: boolean }; crypto_payments?: { requested?: boolean }; eps_payments?: { requested?: boolean }; fpx_payments?: { requested?: boolean }; gb_bank_transfer_payments?: { requested?: boolean }; giropay_payments?: { requested?: boolean }; grabpay_payments?: { requested?: boolean }; ideal_payments?: { requested?: boolean }; india_international_payments?: { requested?: boolean }; jcb_payments?: { requested?: boolean }; jp_bank_transfer_payments?: { requested?: boolean }; kakao_pay_payments?: { requested?: boolean }; klarna_payments?: { requested?: boolean }; konbini_payments?: { requested?: boolean }; kr_card_payments?: { requested?: boolean }; legacy_payments?: { requested?: boolean }; link_payments?: { requested?: boolean }; mb_way_payments?: { requested?: boolean }; mobilepay_payments?: { requested?: boolean }; multibanco_payments?: { requested?: boolean }; mx_bank_transfer_payments?: { requested?: boolean }; naver_pay_payments?: { requested?: boolean }; nz_bank_account_becs_debit_payments?: { requested?: boolean }; oxxo_payments?: { requested?: boolean }; p24_payments?: { requested?: boolean }; pay_by_bank_payments?: { requested?: boolean }; payco_payments?: { requested?: boolean }; paynow_payments?: { requested?: boolean }; payto_payments?: { requested?: boolean }; pix_payments?: { requested?: boolean }; promptpay_payments?: { requested?: boolean }; revolut_pay_payments?: { requested?: boolean }; samsung_pay_payments?: { requested?: boolean }; satispay_payments?: { requested?: boolean }; scalapay_payments?: { requested?: boolean }; sepa_bank_transfer_payments?: { requested?: boolean }; sepa_debit_payments?: { requested?: boolean }; sofort_payments?: { requested?: boolean }; sunbit_payments?: { requested?: boolean }; swish_payments?: { requested?: boolean }; tax_reporting_us_1099_k?: { requested?: boolean }; tax_reporting_us_1099_misc?: { requested?: boolean }; transfers?: { requested?: boolean }; treasury?: { requested?: boolean }; twint_payments?: { requested?: boolean }; upi_payments?: { requested?: boolean }; us_bank_account_ach_payments?: { requested?: boolean }; us_bank_transfer_payments?: { requested?: boolean }; zip_payments?: { requested?: boolean } };
    /** Information about the company or business. This field is available for any `business_type`. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    company?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; directors_provided?: boolean; directorship_declaration?: { date?: number; ip?: string; user_agent?: string }; executives_provided?: boolean; export_license_id?: string; export_purpose_code?: string; name?: string; name_kana?: string; name_kanji?: string; owners_provided?: boolean; ownership_declaration?: { date?: number; ip?: string; user_agent?: string }; ownership_exemption_reason?: "" | "qualified_entity_exceeds_ownership_threshold" | "qualifies_as_financial_institution"; phone?: string; registration_date?: { day: number; month: number; year: number } | ""; registration_number?: string; representative_declaration?: { date?: number; ip?: string; user_agent?: string }; structure?: "" | "free_zone_establishment" | "free_zone_llc" | "government_instrumentality" | "governmental_unit" | "incorporated_non_profit" | "incorporated_partnership" | "limited_liability_partnership" | "llc" | "multi_member_llc" | "private_company" | "private_corporation" | "private_partnership" | "public_company" | "public_corporation" | "public_partnership" | "registered_charity" | "single_member_llc" | "sole_establishment" | "sole_proprietorship" | "tax_exempt_government_instrumentality" | "unincorporated_association" | "unincorporated_non_profit" | "unincorporated_partnership"; tax_id?: string; tax_id_registrar?: string; vat_id?: string; verification?: { document?: { back?: string; front?: string } } };
    /** Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts). */
    default_currency?: string;
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { bank_account_ownership_verification?: { files?: (string)[] }; company_license?: { files?: (string)[] }; company_memorandum_of_association?: { files?: (string)[] }; company_ministerial_decree?: { files?: (string)[] }; company_registration_verification?: { files?: (string)[] }; company_tax_id_verification?: { files?: (string)[] }; proof_of_address?: { files?: (string)[] }; proof_of_registration?: { files?: (string)[]; signer?: { person?: string } }; proof_of_ultimate_beneficial_ownership?: { files?: (string)[]; signer?: { person?: string } } };
    /** The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    external_account?: string;
    /** A hash of account group type to tokens. These are account groups this account should be added to. */
    groups?: { payments_pricing?: string | "" };
    /** Information about the person represented by the account. This field is null unless `business_type` is set to `individual`. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. */
    individual?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; dob?: { day: number; month: number; year: number } | ""; email?: string; first_name?: string; first_name_kana?: string; first_name_kanji?: string; full_name_aliases?: (string)[] | ""; gender?: string; id_number?: string; id_number_secondary?: string; last_name?: string; last_name_kana?: string; last_name_kanji?: string; maiden_name?: string; metadata?: { [key: string]: string | undefined } | ""; phone?: string; political_exposure?: "existing" | "none"; registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; relationship?: { director?: boolean; executive?: boolean; owner?: boolean; percent_ownership?: number | ""; title?: string }; ssn_last_4?: string; verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Options for customizing how the account functions within Stripe. */
    settings?: { bacs_debit_payments?: { display_name?: string }; branding?: { icon?: string; logo?: string; primary_color?: string; secondary_color?: string }; card_issuing?: { tos_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } }; card_payments?: { decline_on?: { avs_failure?: boolean; cvc_failure?: boolean }; statement_descriptor_prefix?: string; statement_descriptor_prefix_kana?: string | ""; statement_descriptor_prefix_kanji?: string | "" }; invoices?: { default_account_tax_ids?: (string)[] | ""; hosted_payment_method_save?: "always" | "never" | "offer" }; payments?: { statement_descriptor?: string; statement_descriptor_kana?: string; statement_descriptor_kanji?: string }; payouts?: { debit_negative_balances?: boolean; schedule?: { delay_days?: "minimum" | number; interval?: "daily" | "manual" | "monthly" | "weekly"; monthly_anchor?: number; monthly_payout_days?: (number)[]; weekly_anchor?: "friday" | "monday" | "saturday" | "sunday" | "thursday" | "tuesday" | "wednesday"; weekly_payout_days?: ("friday" | "monday" | "thursday" | "tuesday" | "wednesday")[] }; statement_descriptor?: string }; treasury?: { tos_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } } };
    /** Details on the account's acceptance of the [Stripe Services Agreement](/connect/updating-accounts#tos-acceptance). This property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. This property defaults to a `full` service agreement when empty. */
    tos_acceptance?: { date?: number; ip?: string; service_agreement?: string; user_agent?: string };
    account: string;
  }) => Promise<Account>;

  /**
   * Create an external account
   */
  postAccountsAccountBankAccounts: (input: {
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; account_type?: "checking" | "futsu" | "savings" | "toza"; country: string; currency?: string; documents?: { bank_account_ownership_verification?: { files?: (string)[] } }; object?: "bank_account"; routing_number?: string } | string;
    /** When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency. */
    default_for_currency?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A token, like the ones returned by [Stripe.js](https://docs.stripe.com/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead. */
    external_account?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    account: string;
  }) => Promise<ExternalAccount>;

  /**
   * Delete an external account
   */
  deleteAccountsAccountBankAccountsId: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Unique identifier for the external account to be deleted. */
    id: string;
  }) => Promise<DeletedExternalAccount>;

  /**
   * Retrieve an external account
   */
  getAccountsAccountBankAccountsId: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Unique identifier for the external account to be retrieved. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ExternalAccount>;

  /**
   * <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href="/api/accounts/object#account_object-controller-requirement_collection">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href="/connect/custom-accounts">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
   */
  postAccountsAccountBankAccountsId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "" | "company" | "individual";
    /** The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`. */
    account_type?: "checking" | "futsu" | "savings" | "toza";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** When set to true, this becomes the default external account for its currency. */
    default_for_currency?: boolean;
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { bank_account_ownership_verification?: { files?: (string)[] } };
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    account: string;
    id: string;
  }) => Promise<ExternalAccount>;

  /**
   * List all account capabilities
   */
  getAccountsAccountCapabilities: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<{ data: (Capability)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an Account Capability
   */
  getAccountsAccountCapabilitiesCapability: (input: {
    body?: { [key: string]: unknown };
    account: string;
    capability: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Capability>;

  /**
   * Update an Account Capability
   */
  postAccountsAccountCapabilitiesCapability: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** To request a new capability for an account, pass true. There can be a delay before the requested capability becomes active. If the capability has any activation requirements, the response includes them in the `requirements` arrays.  If a capability isn't permanent, you can remove it from the account by passing false. Some capabilities are permanent after they've been requested. Attempting to remove a permanent capability returns an error. */
    requested?: boolean;
    account: string;
    capability: string;
  }) => Promise<Capability>;

  /**
   * List all external accounts
   */
  getAccountsAccountExternalAccounts: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filter external accounts according to a particular object type. */
    object?: "bank_account" | "card";
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BankAccount | Card)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an external account
   */
  postAccountsAccountExternalAccounts: (input: {
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; account_type?: "checking" | "futsu" | "savings" | "toza"; country: string; currency?: string; documents?: { bank_account_ownership_verification?: { files?: (string)[] } }; object?: "bank_account"; routing_number?: string } | string;
    /** When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency. */
    default_for_currency?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A token, like the ones returned by [Stripe.js](https://docs.stripe.com/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead. */
    external_account?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    account: string;
  }) => Promise<ExternalAccount>;

  /**
   * Delete an external account
   */
  deleteAccountsAccountExternalAccountsId: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Unique identifier for the external account to be deleted. */
    id: string;
  }) => Promise<DeletedExternalAccount>;

  /**
   * Retrieve an external account
   */
  getAccountsAccountExternalAccountsId: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Unique identifier for the external account to be retrieved. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ExternalAccount>;

  /**
   * <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href="/api/accounts/object#account_object-controller-requirement_collection">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href="/connect/custom-accounts">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
   */
  postAccountsAccountExternalAccountsId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "" | "company" | "individual";
    /** The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`. */
    account_type?: "checking" | "futsu" | "savings" | "toza";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** When set to true, this becomes the default external account for its currency. */
    default_for_currency?: boolean;
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { bank_account_ownership_verification?: { files?: (string)[] } };
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    account: string;
    id: string;
  }) => Promise<ExternalAccount>;

  /**
   * Create a login link
   */
  postAccountsAccountLoginLinks: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    account: string;
  }) => Promise<LoginLink>;

  /**
   * List all persons
   */
  getAccountsAccountPeople: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filters on the list of people returned based on the person's relationship to the account's company. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; representative?: boolean };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Person)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a person
   */
  postAccountsAccountPeople: (input: {
    /** Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements. */
    additional_tos_acceptances?: { account?: { date?: number; ip?: string; user_agent?: string | "" } };
    /** The person's address. */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the person's address (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the person's address (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The person's date of birth. */
    dob?: { day: number; month: number; year: number } | "";
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { company_authorization?: { files?: (string | "")[] }; passport?: { files?: (string | "")[] }; visa?: { files?: (string | "")[] } };
    /** The person's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The person's first name. */
    first_name?: string;
    /** The Kana variation of the person's first name (Japan only). */
    first_name_kana?: string;
    /** The Kanji variation of the person's first name (Japan only). */
    first_name_kanji?: string;
    /** A list of alternate names or aliases that the person is known by. */
    full_name_aliases?: (string)[] | "";
    /** The person's gender (International regulations require either "male" or "female"). */
    gender?: string;
    /** The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number?: string;
    /** The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number_secondary?: string;
    /** The person's last name. */
    last_name?: string;
    /** The Kana variation of the person's last name (Japan only). */
    last_name_kana?: string;
    /** The Kanji variation of the person's last name (Japan only). */
    last_name_kanji?: string;
    /** The person's maiden name. */
    maiden_name?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable. */
    nationality?: string;
    /** A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person. */
    person_token?: string;
    /** The person's phone number. */
    phone?: string;
    /** Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction. */
    political_exposure?: "existing" | "none";
    /** The person's registered address. */
    registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The relationship that this person has with the account's legal entity. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; percent_ownership?: number | ""; representative?: boolean; title?: string };
    /** The last four digits of the person's Social Security number (U.S. only). */
    ssn_last_4?: string;
    /** Demographic data related to the person. */
    us_cfpb_data?: { ethnicity_details?: { ethnicity?: ("cuban" | "hispanic_or_latino" | "mexican" | "not_hispanic_or_latino" | "other_hispanic_or_latino" | "prefer_not_to_answer" | "puerto_rican")[]; ethnicity_other?: string }; race_details?: { race?: ("african_american" | "american_indian_or_alaska_native" | "asian" | "asian_indian" | "black_or_african_american" | "chinese" | "ethiopian" | "filipino" | "guamanian_or_chamorro" | "haitian" | "jamaican" | "japanese" | "korean" | "native_hawaiian" | "native_hawaiian_or_other_pacific_islander" | "nigerian" | "other_asian" | "other_black_or_african_american" | "other_pacific_islander" | "prefer_not_to_answer" | "samoan" | "somali" | "vietnamese" | "white")[]; race_other?: string }; self_identified_gender?: string };
    /** The person's verification status. */
    verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } };
    account: string;
  }) => Promise<Person>;

  /**
   * Delete a person
   */
  deleteAccountsAccountPeoplePerson: (input: {
    body?: { [key: string]: unknown };
    account: string;
    person: string;
  }) => Promise<DeletedPerson>;

  /**
   * Retrieve a person
   */
  getAccountsAccountPeoplePerson: (input: {
    body?: { [key: string]: unknown };
    account: string;
    person: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Person>;

  /**
   * Update a person
   */
  postAccountsAccountPeoplePerson: (input: {
    /** Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements. */
    additional_tos_acceptances?: { account?: { date?: number; ip?: string; user_agent?: string | "" } };
    /** The person's address. */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the person's address (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the person's address (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The person's date of birth. */
    dob?: { day: number; month: number; year: number } | "";
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { company_authorization?: { files?: (string | "")[] }; passport?: { files?: (string | "")[] }; visa?: { files?: (string | "")[] } };
    /** The person's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The person's first name. */
    first_name?: string;
    /** The Kana variation of the person's first name (Japan only). */
    first_name_kana?: string;
    /** The Kanji variation of the person's first name (Japan only). */
    first_name_kanji?: string;
    /** A list of alternate names or aliases that the person is known by. */
    full_name_aliases?: (string)[] | "";
    /** The person's gender (International regulations require either "male" or "female"). */
    gender?: string;
    /** The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number?: string;
    /** The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number_secondary?: string;
    /** The person's last name. */
    last_name?: string;
    /** The Kana variation of the person's last name (Japan only). */
    last_name_kana?: string;
    /** The Kanji variation of the person's last name (Japan only). */
    last_name_kanji?: string;
    /** The person's maiden name. */
    maiden_name?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable. */
    nationality?: string;
    /** A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person. */
    person_token?: string;
    /** The person's phone number. */
    phone?: string;
    /** Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction. */
    political_exposure?: "existing" | "none";
    /** The person's registered address. */
    registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The relationship that this person has with the account's legal entity. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; percent_ownership?: number | ""; representative?: boolean; title?: string };
    /** The last four digits of the person's Social Security number (U.S. only). */
    ssn_last_4?: string;
    /** Demographic data related to the person. */
    us_cfpb_data?: { ethnicity_details?: { ethnicity?: ("cuban" | "hispanic_or_latino" | "mexican" | "not_hispanic_or_latino" | "other_hispanic_or_latino" | "prefer_not_to_answer" | "puerto_rican")[]; ethnicity_other?: string }; race_details?: { race?: ("african_american" | "american_indian_or_alaska_native" | "asian" | "asian_indian" | "black_or_african_american" | "chinese" | "ethiopian" | "filipino" | "guamanian_or_chamorro" | "haitian" | "jamaican" | "japanese" | "korean" | "native_hawaiian" | "native_hawaiian_or_other_pacific_islander" | "nigerian" | "other_asian" | "other_black_or_african_american" | "other_pacific_islander" | "prefer_not_to_answer" | "samoan" | "somali" | "vietnamese" | "white")[]; race_other?: string }; self_identified_gender?: string };
    /** The person's verification status. */
    verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } };
    account: string;
    person: string;
  }) => Promise<Person>;

  /**
   * List all persons
   */
  getAccountsAccountPersons: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filters on the list of people returned based on the person's relationship to the account's company. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; representative?: boolean };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Person)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a person
   */
  postAccountsAccountPersons: (input: {
    /** Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements. */
    additional_tos_acceptances?: { account?: { date?: number; ip?: string; user_agent?: string | "" } };
    /** The person's address. */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the person's address (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the person's address (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The person's date of birth. */
    dob?: { day: number; month: number; year: number } | "";
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { company_authorization?: { files?: (string | "")[] }; passport?: { files?: (string | "")[] }; visa?: { files?: (string | "")[] } };
    /** The person's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The person's first name. */
    first_name?: string;
    /** The Kana variation of the person's first name (Japan only). */
    first_name_kana?: string;
    /** The Kanji variation of the person's first name (Japan only). */
    first_name_kanji?: string;
    /** A list of alternate names or aliases that the person is known by. */
    full_name_aliases?: (string)[] | "";
    /** The person's gender (International regulations require either "male" or "female"). */
    gender?: string;
    /** The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number?: string;
    /** The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number_secondary?: string;
    /** The person's last name. */
    last_name?: string;
    /** The Kana variation of the person's last name (Japan only). */
    last_name_kana?: string;
    /** The Kanji variation of the person's last name (Japan only). */
    last_name_kanji?: string;
    /** The person's maiden name. */
    maiden_name?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable. */
    nationality?: string;
    /** A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person. */
    person_token?: string;
    /** The person's phone number. */
    phone?: string;
    /** Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction. */
    political_exposure?: "existing" | "none";
    /** The person's registered address. */
    registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The relationship that this person has with the account's legal entity. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; percent_ownership?: number | ""; representative?: boolean; title?: string };
    /** The last four digits of the person's Social Security number (U.S. only). */
    ssn_last_4?: string;
    /** Demographic data related to the person. */
    us_cfpb_data?: { ethnicity_details?: { ethnicity?: ("cuban" | "hispanic_or_latino" | "mexican" | "not_hispanic_or_latino" | "other_hispanic_or_latino" | "prefer_not_to_answer" | "puerto_rican")[]; ethnicity_other?: string }; race_details?: { race?: ("african_american" | "american_indian_or_alaska_native" | "asian" | "asian_indian" | "black_or_african_american" | "chinese" | "ethiopian" | "filipino" | "guamanian_or_chamorro" | "haitian" | "jamaican" | "japanese" | "korean" | "native_hawaiian" | "native_hawaiian_or_other_pacific_islander" | "nigerian" | "other_asian" | "other_black_or_african_american" | "other_pacific_islander" | "prefer_not_to_answer" | "samoan" | "somali" | "vietnamese" | "white")[]; race_other?: string }; self_identified_gender?: string };
    /** The person's verification status. */
    verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } };
    account: string;
  }) => Promise<Person>;

  /**
   * Delete a person
   */
  deleteAccountsAccountPersonsPerson: (input: {
    body?: { [key: string]: unknown };
    account: string;
    person: string;
  }) => Promise<DeletedPerson>;

  /**
   * Retrieve a person
   */
  getAccountsAccountPersonsPerson: (input: {
    body?: { [key: string]: unknown };
    account: string;
    person: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Person>;

  /**
   * Update a person
   */
  postAccountsAccountPersonsPerson: (input: {
    /** Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements. */
    additional_tos_acceptances?: { account?: { date?: number; ip?: string; user_agent?: string | "" } };
    /** The person's address. */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the person's address (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the person's address (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The person's date of birth. */
    dob?: { day: number; month: number; year: number } | "";
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { company_authorization?: { files?: (string | "")[] }; passport?: { files?: (string | "")[] }; visa?: { files?: (string | "")[] } };
    /** The person's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The person's first name. */
    first_name?: string;
    /** The Kana variation of the person's first name (Japan only). */
    first_name_kana?: string;
    /** The Kanji variation of the person's first name (Japan only). */
    first_name_kanji?: string;
    /** A list of alternate names or aliases that the person is known by. */
    full_name_aliases?: (string)[] | "";
    /** The person's gender (International regulations require either "male" or "female"). */
    gender?: string;
    /** The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number?: string;
    /** The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii). */
    id_number_secondary?: string;
    /** The person's last name. */
    last_name?: string;
    /** The Kana variation of the person's last name (Japan only). */
    last_name_kana?: string;
    /** The Kanji variation of the person's last name (Japan only). */
    last_name_kanji?: string;
    /** The person's maiden name. */
    maiden_name?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable. */
    nationality?: string;
    /** A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person. */
    person_token?: string;
    /** The person's phone number. */
    phone?: string;
    /** Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction. */
    political_exposure?: "existing" | "none";
    /** The person's registered address. */
    registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The relationship that this person has with the account's legal entity. */
    relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; percent_ownership?: number | ""; representative?: boolean; title?: string };
    /** The last four digits of the person's Social Security number (U.S. only). */
    ssn_last_4?: string;
    /** Demographic data related to the person. */
    us_cfpb_data?: { ethnicity_details?: { ethnicity?: ("cuban" | "hispanic_or_latino" | "mexican" | "not_hispanic_or_latino" | "other_hispanic_or_latino" | "prefer_not_to_answer" | "puerto_rican")[]; ethnicity_other?: string }; race_details?: { race?: ("african_american" | "american_indian_or_alaska_native" | "asian" | "asian_indian" | "black_or_african_american" | "chinese" | "ethiopian" | "filipino" | "guamanian_or_chamorro" | "haitian" | "jamaican" | "japanese" | "korean" | "native_hawaiian" | "native_hawaiian_or_other_pacific_islander" | "nigerian" | "other_asian" | "other_black_or_african_american" | "other_pacific_islander" | "prefer_not_to_answer" | "samoan" | "somali" | "vietnamese" | "white")[]; race_other?: string }; self_identified_gender?: string };
    /** The person's verification status. */
    verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } };
    account: string;
    person: string;
  }) => Promise<Person>;

  /**
   * Reject an account
   */
  postAccountsAccountReject: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The reason for rejecting the account. Can be `fraud`, `terms_of_service`, or `other`. */
    reason: string;
    account: string;
  }) => Promise<Account>;

  /**
   * <p>List apple pay domains.</p>
   */
  getApplePayDomains: (input: {
    body?: { [key: string]: unknown };
    domain_name?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ApplePayDomain)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * <p>Create an apple pay domain.</p>
   */
  postApplePayDomains: (input: {
    domain_name: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ApplePayDomain>;

  /**
   * <p>Delete an apple pay domain.</p>
   */
  deleteApplePayDomainsDomain: (input: {
    body?: { [key: string]: unknown };
    domain: string;
  }) => Promise<DeletedApplePayDomain>;

  /**
   * <p>Retrieve an apple pay domain.</p>
   */
  getApplePayDomainsDomain: (input: {
    body?: { [key: string]: unknown };
    domain: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ApplePayDomain>;

  /**
   * List all application fees
   */
  getApplicationFees: (input: {
    body?: { [key: string]: unknown };
    /** Only return application fees for the charge specified by this charge ID. */
    charge?: string;
    /** Only return applications fees that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ApplicationFee)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an application fee refund
   */
  getApplicationFeesFeeRefundsId: (input: {
    body?: { [key: string]: unknown };
    fee: string;
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FeeRefund>;

  /**
   * Update an application fee refund
   */
  postApplicationFeesFeeRefundsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    fee: string;
    id: string;
  }) => Promise<FeeRefund>;

  /**
   * Retrieve an application fee
   */
  getApplicationFeesId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ApplicationFee>;

  postApplicationFeesIdRefund: (input: {
    amount?: number;
    directive?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<ApplicationFee>;

  /**
   * List all application fee refunds
   */
  getApplicationFeesIdRefunds: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FeeRefund)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an application fee refund
   */
  postApplicationFeesIdRefunds: (input: {
    /** A positive integer, in _cents (or local equivalent)_, representing how much of this fee to refund. Can refund only up to the remaining unrefunded amount of the fee. */
    amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    id: string;
  }) => Promise<FeeRefund>;

  /**
   * List secrets
   */
  getAppsSecrets: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user. */
    scope: { type: "account" | "user"; user?: string };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (AppsSecret)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Set a Secret
   */
  postAppsSecrets: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The Unix timestamp for the expiry time of the secret, after which the secret deletes. */
    expires_at?: number;
    /** A name for the secret that's unique within the scope. */
    name: string;
    /** The plaintext secret value to be stored. */
    payload: string;
    /** Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user. */
    scope: { type: "account" | "user"; user?: string };
  }) => Promise<AppsSecret>;

  /**
   * Delete a Secret
   */
  postAppsSecretsDelete: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A name for the secret that's unique within the scope. */
    name: string;
    /** Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user. */
    scope: { type: "account" | "user"; user?: string };
  }) => Promise<AppsSecret>;

  /**
   * Find a Secret
   */
  getAppsSecretsFind: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A name for the secret that's unique within the scope. */
    name: string;
    /** Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user. */
    scope: { type: "account" | "user"; user?: string };
  }) => Promise<AppsSecret>;

  /**
   * Retrieve balance
   */
  getBalance: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Balance>;

  /**
   * List all balance transactions
   */
  getBalanceHistory: (input: {
    body?: { [key: string]: unknown };
    /** Only return transactions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID. */
    payout?: string;
    /** Only returns transactions associated with the given object. */
    source?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `inbound_transfer`, `inbound_transfer_reversal`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `reserve_hold`, `reserve_release`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, `transfer_refund`, or `fee_credit_funding`. */
    type?: string;
  }) => Promise<{ data: (BalanceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a balance transaction
   */
  getBalanceHistoryId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BalanceTransaction>;

  /**
   * Retrieve balance settings
   */
  getBalanceSettings: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BalanceSettings>;

  /**
   * Update balance settings
   */
  postBalanceSettings: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Settings that apply to the [Payments Balance](https://docs.stripe.com/api/balance). */
    payments?: { debit_negative_balances?: boolean; payouts?: { automatic_transfer_rules_by_currency?: { [key: string]: ({ payout_method: string; transfer_up_to_amount?: number; type: "transfer_all" | "transfer_up_to_amount" })[] | "" | undefined } | ""; minimum_balance_by_currency?: { [key: string]: number | "" | undefined } | ""; schedule?: { interval?: "daily" | "manual" | "monthly" | "weekly"; monthly_payout_days?: (number)[]; weekly_payout_days?: ("friday" | "monday" | "thursday" | "tuesday" | "wednesday")[] }; statement_descriptor?: string }; settlement_timing?: { delay_days_override?: number | ""; start_of_day?: { hour?: number; minutes?: number; timezone?: string } | "" } };
  }) => Promise<BalanceSettings>;

  /**
   * List all balance transactions
   */
  getBalanceTransactions: (input: {
    body?: { [key: string]: unknown };
    /** Only return transactions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID. */
    payout?: string;
    /** Only returns transactions associated with the given object. */
    source?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `inbound_transfer`, `inbound_transfer_reversal`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `reserve_hold`, `reserve_release`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, `transfer_refund`, or `fee_credit_funding`. */
    type?: string;
  }) => Promise<{ data: (BalanceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a balance transaction
   */
  getBalanceTransactionsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BalanceTransaction>;

  /**
   * List billing alerts
   */
  getBillingAlerts: (input: {
    body?: { [key: string]: unknown };
    /** Filter results to only include this type of alert. */
    alert_type?: "usage_threshold";
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filter results to only include alerts with the given meter. */
    meter?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BillingAlert)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a billing alert
   */
  postBillingAlerts: (input: {
    /** The type of alert to create. */
    alert_type: "usage_threshold";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The title of the alert. */
    title: string;
    /** The configuration of the usage threshold. */
    usage_threshold?: { filters?: ({ customer?: string; type: "customer" })[]; gte: number; meter: string; recurrence: "one_time" };
  }) => Promise<BillingAlert>;

  /**
   * Retrieve a billing alert
   */
  getBillingAlertsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BillingAlert>;

  /**
   * Activate a billing alert
   */
  postBillingAlertsIdActivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingAlert>;

  /**
   * Archive a billing alert
   */
  postBillingAlertsIdArchive: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingAlert>;

  /**
   * Deactivate a billing alert
   */
  postBillingAlertsIdDeactivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingAlert>;

  /**
   * Retrieve the credit balance summary for a customer
   */
  getBillingCreditBalanceSummary: (input: {
    body?: { [key: string]: unknown };
    /** The customer whose credit balance summary you're retrieving. */
    customer?: string;
    /** The account representing the customer whose credit balance summary you're retrieving. */
    customer_account?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The filter criteria for the credit balance summary. */
    filter: { applicability_scope?: { price_type?: "metered"; prices?: ({ id: string })[] }; credit_grant?: string; type: "applicability_scope" | "credit_grant" };
  }) => Promise<BillingCreditBalanceSummary>;

  /**
   * List credit balance transactions
   */
  getBillingCreditBalanceTransactions: (input: {
    body?: { [key: string]: unknown };
    /** The credit grant for which to fetch credit balance transactions. */
    credit_grant?: string;
    /** The customer whose credit balance transactions you're retrieving. */
    customer?: string;
    /** The account representing the customer whose credit balance transactions you're retrieving. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BillingCreditBalanceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a credit balance transaction
   */
  getBillingCreditBalanceTransactionsId: (input: {
    body?: { [key: string]: unknown };
    /** Unique identifier for the object. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BillingCreditBalanceTransaction>;

  /**
   * List credit grants
   */
  getBillingCreditGrants: (input: {
    body?: { [key: string]: unknown };
    /** Only return credit grants for this customer. */
    customer?: string;
    /** Only return credit grants for this account representing the customer. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BillingCreditGrant)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a credit grant
   */
  postBillingCreditGrants: (input: {
    /** Amount of this credit grant. */
    amount: { monetary?: { currency: string; value: number }; type: "monetary" };
    /** Configuration specifying what this credit grant applies to. We currently only support `metered` prices that have a [Billing Meter](https://docs.stripe.com/api/billing/meter) attached to them. */
    applicability_config: { scope: { price_type?: "metered"; prices?: ({ id: string })[] } };
    /** The category of this credit grant. It defaults to `paid` if not specified. */
    category?: "paid" | "promotional";
    /** ID of the customer receiving the billing credits. */
    customer?: string;
    /** ID of the account representing the customer receiving the billing credits. */
    customer_account?: string;
    /** The time when the billing credits become effective-when they're eligible for use. It defaults to the current timestamp if not specified. */
    effective_at?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The time when the billing credits expire. If not specified, the billing credits don't expire. */
    expires_at?: number;
    /** Set of key-value pairs that you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format. */
    metadata?: { [key: string]: string | undefined };
    /** A descriptive name shown in the Dashboard. */
    name?: string;
    /** The desired priority for applying this credit grant. If not specified, it will be set to the default value of 50. The highest priority is 0 and the lowest is 100. */
    priority?: number;
  }) => Promise<BillingCreditGrant>;

  /**
   * Retrieve a credit grant
   */
  getBillingCreditGrantsId: (input: {
    body?: { [key: string]: unknown };
    /** Unique identifier for the object. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BillingCreditGrant>;

  /**
   * Update a credit grant
   */
  postBillingCreditGrantsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The time when the billing credits created by this credit grant expire. If set to empty, the billing credits never expire. */
    expires_at?: number | "";
    /** Set of key-value pairs you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format. */
    metadata?: { [key: string]: string | undefined };
    /** Unique identifier for the object. */
    id: string;
  }) => Promise<BillingCreditGrant>;

  /**
   * Expire a credit grant
   */
  postBillingCreditGrantsIdExpire: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Unique identifier for the object. */
    id: string;
  }) => Promise<BillingCreditGrant>;

  /**
   * Void a credit grant
   */
  postBillingCreditGrantsIdVoid: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Unique identifier for the object. */
    id: string;
  }) => Promise<BillingCreditGrant>;

  /**
   * Create a billing meter event adjustment
   */
  postBillingMeterEventAdjustments: (input: {
    /** Specifies which event to cancel. */
    cancel?: { identifier?: string };
    /** The name of the meter event. Corresponds with the `event_name` field on a meter. */
    event_name: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Specifies whether to cancel a single event or a range of events for a time period. Time period cancellation is not supported yet. */
    type: "cancel";
  }) => Promise<BillingMeterEventAdjustment>;

  /**
   * Create a billing meter event
   */
  postBillingMeterEvents: (input: {
    /** The name of the meter event. Corresponds with the `event_name` field on a meter. */
    event_name: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A unique identifier for the event. If not provided, one is generated. We recommend using UUID-like identifiers. Stripe enforces uniqueness within a rolling period of at least 24 hours. The enforcement of uniqueness primarily addresses issues arising from accidental retries or other problems occurring within extremely brief time intervals. This approach helps prevent duplicate entries and ensures data integrity in high-frequency operations. */
    identifier?: string;
    /** The payload of the event. This must contain the fields corresponding to a meter's `customer_mapping.event_payload_key` (default is `stripe_customer_id`) and `value_settings.event_payload_key` (default is `value`). Read more about the [payload](https://docs.stripe.com/billing/subscriptions/usage-based/meters/configure#meter-configuration-attributes). */
    payload: { [key: string]: string | undefined };
    /** The time of the event. Measured in seconds since the Unix epoch. Must be within the past 35 calendar days or up to 5 minutes in the future. Defaults to current timestamp if not specified. */
    timestamp?: number;
  }) => Promise<BillingMeterEvent>;

  /**
   * List billing meters
   */
  getBillingMeters: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Filter results to only include meters with the given status. */
    status?: "active" | "inactive";
  }) => Promise<{ data: (BillingMeter)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a billing meter
   */
  postBillingMeters: (input: {
    /** Fields that specify how to map a meter event to a customer. */
    customer_mapping?: { event_payload_key: string; type: "by_id" };
    /** The default settings to aggregate a meter's events with. */
    default_aggregation: { formula: "count" | "last" | "sum" };
    /** The meter’s name. Not visible to the customer. */
    display_name: string;
    /** The name of the meter event to record usage for. Corresponds with the `event_name` field on meter events. */
    event_name: string;
    /** The time window which meter events have been pre-aggregated for, if any. */
    event_time_window?: "day" | "hour";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Fields that specify how to calculate a meter event's value. */
    value_settings?: { event_payload_key: string };
  }) => Promise<BillingMeter>;

  /**
   * Retrieve a billing meter
   */
  getBillingMetersId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BillingMeter>;

  /**
   * Update a billing meter
   */
  postBillingMetersId: (input: {
    /** The meter’s name. Not visible to the customer. */
    display_name?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingMeter>;

  /**
   * Deactivate a billing meter
   */
  postBillingMetersIdDeactivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingMeter>;

  /**
   * List billing meter event summaries
   */
  getBillingMetersIdEventSummaries: (input: {
    body?: { [key: string]: unknown };
    /** Unique identifier for the object. */
    id: string;
    /** The customer for which to fetch event summaries. */
    customer: string;
    /** The timestamp from when to stop aggregating meter events (exclusive). Must be aligned with minute boundaries. */
    end_time: number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The timestamp from when to start aggregating meter events (inclusive). Must be aligned with minute boundaries. */
    start_time: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Specifies what granularity to use when generating event summaries. If not specified, a single event summary would be returned for the specified time range. For hourly granularity, start and end times must align with hour boundaries (e.g., 00:00, 01:00, ..., 23:00). For daily granularity, start and end times must align with UTC day boundaries (00:00 UTC). */
    value_grouping_window?: "day" | "hour";
  }) => Promise<{ data: (BillingMeterEventSummary)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Reactivate a billing meter
   */
  postBillingMetersIdReactivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<BillingMeter>;

  /**
   * List portal configurations
   */
  getBillingPortalConfigurations: (input: {
    body?: { [key: string]: unknown };
    /** Only return configurations that are active or inactive (e.g., pass `true` to only list active configurations). */
    active?: boolean;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return the default or non-default configurations (e.g., pass `true` to only list the default configuration). */
    is_default?: boolean;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BillingPortalConfiguration)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a portal configuration
   */
  postBillingPortalConfigurations: (input: {
    /** The business information shown to customers in the portal. */
    business_profile?: { headline?: string | ""; privacy_policy_url?: string; terms_of_service_url?: string };
    /** The default URL to redirect customers to when they click on the portal's link to return to your website. This can be [overriden](https://docs.stripe.com/api/customer_portal/sessions/create#create_portal_session-return_url) when creating the session. */
    default_return_url?: string | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about the features available in the portal. */
    features: { customer_update?: { allowed_updates?: ("address" | "email" | "name" | "phone" | "shipping" | "tax_id")[] | ""; enabled: boolean }; invoice_history?: { enabled: boolean }; payment_method_update?: { enabled: boolean; payment_method_configuration?: string | "" }; subscription_cancel?: { cancellation_reason?: { enabled: boolean; options: ("customer_service" | "low_quality" | "missing_features" | "other" | "switched_service" | "too_complex" | "too_expensive" | "unused")[] | "" }; enabled: boolean; mode?: "at_period_end" | "immediately"; proration_behavior?: "always_invoice" | "create_prorations" | "none" }; subscription_update?: { billing_cycle_anchor?: "now" | "unchanged"; default_allowed_updates?: ("price" | "promotion_code" | "quantity")[] | ""; enabled: boolean; products?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; prices: (string)[]; product: string })[] | ""; proration_behavior?: "always_invoice" | "create_prorations" | "none"; schedule_at_period_end?: { conditions?: ({ type: "decreasing_item_amount" | "shortening_interval" })[] }; trial_update_behavior?: "continue_trial" | "end_trial" } };
    /** The hosted login page for this configuration. Learn more about the portal login page in our [integration docs](https://stripe.com/docs/billing/subscriptions/integrating-customer-portal#share). */
    login_page?: { enabled: boolean };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The name of the configuration. */
    name?: string | "";
  }) => Promise<BillingPortalConfiguration>;

  /**
   * Retrieve a portal configuration
   */
  getBillingPortalConfigurationsConfiguration: (input: {
    body?: { [key: string]: unknown };
    configuration: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BillingPortalConfiguration>;

  /**
   * Update a portal configuration
   */
  postBillingPortalConfigurationsConfiguration: (input: {
    /** Whether the configuration is active and can be used to create portal sessions. */
    active?: boolean;
    /** The business information shown to customers in the portal. */
    business_profile?: { headline?: string | ""; privacy_policy_url?: string | ""; terms_of_service_url?: string | "" };
    /** The default URL to redirect customers to when they click on the portal's link to return to your website. This can be [overriden](https://docs.stripe.com/api/customer_portal/sessions/create#create_portal_session-return_url) when creating the session. */
    default_return_url?: string | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about the features available in the portal. */
    features?: { customer_update?: { allowed_updates?: ("address" | "email" | "name" | "phone" | "shipping" | "tax_id")[] | ""; enabled?: boolean }; invoice_history?: { enabled: boolean }; payment_method_update?: { enabled: boolean; payment_method_configuration?: string | "" }; subscription_cancel?: { cancellation_reason?: { enabled: boolean; options?: ("customer_service" | "low_quality" | "missing_features" | "other" | "switched_service" | "too_complex" | "too_expensive" | "unused")[] | "" }; enabled?: boolean; mode?: "at_period_end" | "immediately"; proration_behavior?: "always_invoice" | "create_prorations" | "none" }; subscription_update?: { billing_cycle_anchor?: "now" | "unchanged"; default_allowed_updates?: ("price" | "promotion_code" | "quantity")[] | ""; enabled?: boolean; products?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; prices: (string)[]; product: string })[] | ""; proration_behavior?: "always_invoice" | "create_prorations" | "none"; schedule_at_period_end?: { conditions?: ({ type: "decreasing_item_amount" | "shortening_interval" })[] | "" }; trial_update_behavior?: "continue_trial" | "end_trial" } };
    /** The hosted login page for this configuration. Learn more about the portal login page in our [integration docs](https://stripe.com/docs/billing/subscriptions/integrating-customer-portal#share). */
    login_page?: { enabled: boolean };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The name of the configuration. */
    name?: string | "";
    configuration: string;
  }) => Promise<BillingPortalConfiguration>;

  /**
   * Create a portal session
   */
  postBillingPortalSessions: (input: {
    /** The ID of an existing [configuration](https://docs.stripe.com/api/customer_portal/configurations) to use for this session, describing its functionality and features. If not specified, the session uses the default configuration. */
    configuration?: string;
    /** The ID of an existing customer. */
    customer?: string;
    /** The ID of an existing account. */
    customer_account?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about a specific flow for the customer to go through. See the [docs](https://docs.stripe.com/customer-management/portal-deep-links) to learn more about using customer portal deep links and flows. */
    flow_data?: { after_completion?: { hosted_confirmation?: { custom_message?: string }; redirect?: { return_url: string }; type: "hosted_confirmation" | "portal_homepage" | "redirect" }; subscription_cancel?: { retention?: { coupon_offer: { coupon: string }; type: "coupon_offer" }; subscription: string }; subscription_update?: { subscription: string }; subscription_update_confirm?: { discounts?: ({ coupon?: string; promotion_code?: string })[]; items: ({ id: string; price?: string; quantity?: number })[]; subscription: string }; type: "payment_method_update" | "subscription_cancel" | "subscription_update" | "subscription_update_confirm" };
    /** The IETF language tag of the locale customer portal is displayed in. If blank or auto, the customer’s `preferred_locales` or browser’s locale is used. */
    locale?: "auto" | "bg" | "cs" | "da" | "de" | "el" | "en" | "en-AU" | "en-CA" | "en-GB" | "en-IE" | "en-IN" | "en-NZ" | "en-SG" | "es" | "es-419" | "et" | "fi" | "fil" | "fr" | "fr-CA" | "hr" | "hu" | "id" | "it" | "ja" | "ko" | "lt" | "lv" | "ms" | "mt" | "nb" | "nl" | "pl" | "pt" | "pt-BR" | "ro" | "ru" | "sk" | "sl" | "sv" | "th" | "tr" | "vi" | "zh" | "zh-HK" | "zh-TW";
    /** The `on_behalf_of` account to use for this session. When specified, only subscriptions and invoices with this `on_behalf_of` account appear in the portal. For more information, see the [docs](https://docs.stripe.com/connect/separate-charges-and-transfers#settlement-merchant). Use the [Accounts API](https://docs.stripe.com/api/accounts/object#account_object-settings-branding) to modify the `on_behalf_of` account's branding settings, which the portal displays. */
    on_behalf_of?: string;
    /** The default URL to redirect customers to when they click on the portal's link to return to your website. */
    return_url?: string;
  }) => Promise<BillingPortalSession>;

  /**
   * List all charges
   */
  getCharges: (input: {
    body?: { [key: string]: unknown };
    /** Only return charges that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return charges for the customer specified by this customer ID. */
    customer?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID. */
    payment_intent?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return charges for this transfer group, limited to 100. */
    transfer_group?: string;
  }) => Promise<{ data: (Charge)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * <p>This method is no longer recommended—use the <a href="/docs/api/payment_intents">Payment Intents API</a> to initiate a new payment instead. Confirmation of the PaymentIntent creates the <code>Charge</code> object used to request payment.</p>
   */
  postCharges: (input: {
    /** Amount intended to be collected by this payment. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99). */
    amount?: number;
    application_fee?: number;
    /** A fee in cents (or local equivalent) that will be applied to the charge and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the `Stripe-Account` header in order to take an application fee. For more information, see the application fees [documentation](https://docs.stripe.com/connect/direct-charges#collect-fees). */
    application_fee_amount?: number;
    /** Whether to immediately capture the charge. Defaults to `true`. When `false`, the charge issues an authorization (or pre-authorization), and will need to be [captured](https://api.stripe.com#capture_charge) later. Uncaptured charges expire after a set number of days (7 by default). For more information, see the [authorizing charges and settling later](https://docs.stripe.com/charges/placing-a-hold) documentation. */
    capture?: boolean;
    /** A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js). */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; cvc?: string; exp_month: number; exp_year: number; metadata?: { [key: string]: string | undefined }; name?: string; number: string; object?: "card" } | string;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** The ID of an existing customer that will be charged in this request. */
    customer?: string;
    /** An arbitrary string which you can attach to a `Charge` object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing. */
    description?: string;
    destination?: { account: string; amount?: number } | string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The Stripe account ID for which these funds are intended. You can specify the business of record as the connected account using the `on_behalf_of` attribute on the charge. For details, see [Creating Separate Charges and Transfers](https://docs.stripe.com/connect/separate-charges-and-transfers#settlement-merchant). */
    on_behalf_of?: string;
    /** Options to configure Radar. See [Radar Session](https://docs.stripe.com/radar/radar-session) for more information. */
    radar_options?: { session?: string };
    /** The email address to which this charge's [receipt](https://docs.stripe.com/dashboard/receipts) will be sent. The receipt will not be sent until the charge is paid, and no receipts will be sent for test mode charges. If this charge is for a [Customer](https://docs.stripe.com/api/customers/object), the email address specified here will override the customer's email address. If `receipt_email` is specified for a charge in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails). */
    receipt_email?: string;
    /** Shipping information for the charge. Helps prevent fraud on charges for physical goods. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string };
    /** A payment source to be charged. This can be the ID of a [card](https://docs.stripe.com/api#cards) (i.e., credit or debit card), a [bank account](https://docs.stripe.com/api#bank_accounts), a [source](https://docs.stripe.com/api#sources), a [token](https://docs.stripe.com/api#tokens), or a [connected account](https://docs.stripe.com/connect/account-debits#charging-a-connected-account). For certain sources---namely, [cards](https://docs.stripe.com/api#cards), [bank accounts](https://docs.stripe.com/api#bank_accounts), and attached [sources](https://docs.stripe.com/api#sources)---you must also pass the ID of the associated customer. */
    source?: string;
    /** For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix. */
    statement_descriptor?: string;
    /** Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor. */
    statement_descriptor_suffix?: string;
    /** An optional dictionary including the account to automatically transfer to as part of a destination charge. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for details. */
    transfer_data?: { amount?: number; description?: string; destination: string };
    /** A string that identifies this transaction as part of a group. For details, see [Grouping transactions](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options). */
    transfer_group?: string;
  }) => Promise<Charge>;

  /**
   * Search charges
   */
  getChargesSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for charges](https://docs.stripe.com/search#query-fields-for-charges). */
    query: string;
  }) => Promise<{ data: (Charge)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Retrieve a charge
   */
  getChargesCharge: (input: {
    body?: { [key: string]: unknown };
    charge: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Charge>;

  /**
   * Update a charge
   */
  postChargesCharge: (input: {
    /** The ID of an existing customer that will be associated with this request. This field may only be updated if there is no existing associated customer with this charge. */
    customer?: string;
    /** An arbitrary string which you can attach to a charge object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A set of key-value pairs you can attach to a charge giving information about its riskiness. If you believe a charge is fraudulent, include a `user_report` key with a value of `fraudulent`. If you believe a charge is safe, include a `user_report` key with a value of `safe`. Stripe will use the information you send to improve our fraud detection algorithms. */
    fraud_details?: { user_report: "" | "fraudulent" | "safe" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** This is the email address that the receipt for this charge will be sent to. If this field is updated, then a new email receipt will be sent to the updated address. */
    receipt_email?: string;
    /** Shipping information for the charge. Helps prevent fraud on charges for physical goods. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string };
    /** A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for details. */
    transfer_group?: string;
    charge: string;
  }) => Promise<Charge>;

  /**
   * Capture a payment
   */
  postChargesChargeCapture: (input: {
    /** The amount to capture, which must be less than or equal to the original amount. */
    amount?: number;
    /** An application fee to add on to this charge. */
    application_fee?: number;
    /** An application fee amount to add on to this charge, which must be less than or equal to the original amount. */
    application_fee_amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The email address to send this charge's receipt to. This will override the previously-specified email address for this charge, if one was set. Receipts will not be sent in test mode. */
    receipt_email?: string;
    /** For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix. */
    statement_descriptor?: string;
    /** Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor. */
    statement_descriptor_suffix?: string;
    /** An optional dictionary including the account to automatically transfer to as part of a destination charge. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for details. */
    transfer_data?: { amount?: number };
    /** A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for details. */
    transfer_group?: string;
    charge: string;
  }) => Promise<Charge>;

  /**
   * <p>Retrieve a dispute for a specified charge.</p>
   */
  getChargesChargeDispute: (input: {
    body?: { [key: string]: unknown };
    charge: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Dispute>;

  postChargesChargeDispute: (input: {
    /** Evidence to upload, to respond to a dispute. Updating any field in the hash will submit all fields in the hash for review. The combined character count of all fields is limited to 150,000. */
    evidence?: { access_activity_log?: string; billing_address?: string; cancellation_policy?: string; cancellation_policy_disclosure?: string; cancellation_rebuttal?: string; customer_communication?: string; customer_email_address?: string; customer_name?: string; customer_purchase_ip?: string; customer_signature?: string; duplicate_charge_documentation?: string; duplicate_charge_explanation?: string; duplicate_charge_id?: string; enhanced_evidence?: { visa_compelling_evidence_3?: { disputed_transaction?: { customer_account_id?: string | ""; customer_device_fingerprint?: string | ""; customer_device_id?: string | ""; customer_email_address?: string | ""; customer_purchase_ip?: string | ""; merchandise_or_services?: "merchandise" | "services"; product_description?: string | ""; shipping_address?: { city?: string | ""; country?: string | ""; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" } }; prior_undisputed_transactions?: ({ charge: string; customer_account_id?: string | ""; customer_device_fingerprint?: string | ""; customer_device_id?: string | ""; customer_email_address?: string | ""; customer_purchase_ip?: string | ""; product_description?: string | ""; shipping_address?: { city?: string | ""; country?: string | ""; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" } })[] }; visa_compliance?: { fee_acknowledged?: boolean } } | ""; product_description?: string; receipt?: string; refund_policy?: string; refund_policy_disclosure?: string; refund_refusal_explanation?: string; service_date?: string; service_documentation?: string; shipping_address?: string; shipping_carrier?: string; shipping_date?: string; shipping_documentation?: string; shipping_tracking_number?: string; uncategorized_file?: string; uncategorized_text?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default). */
    submit?: boolean;
    charge: string;
  }) => Promise<Dispute>;

  postChargesChargeDisputeClose: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    charge: string;
  }) => Promise<Dispute>;

  /**
   * Create a refund
   */
  postChargesChargeRefund: (input: {
    /** A positive integer in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) representing how much of this charge to refund. Can refund only up to the remaining, unrefunded amount of the charge. */
    amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions. */
    instructions_email?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The identifier of the PaymentIntent to refund. */
    payment_intent?: string;
    /** String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://docs.stripe.com/radar/lists), and will also help us improve our fraud detection algorithms. */
    reason?: "duplicate" | "fraudulent" | "requested_by_customer";
    /** Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge. */
    refund_application_fee?: boolean;
    /** Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge. */
    reverse_transfer?: boolean;
    /** The identifier of the charge to refund. */
    charge: string;
  }) => Promise<Charge>;

  /**
   * List all refunds
   */
  getChargesChargeRefunds: (input: {
    body?: { [key: string]: unknown };
    charge: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Refund)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create customer balance refund
   */
  postChargesChargeRefunds: (input: {
    amount?: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Customer whose customer balance to refund from. */
    customer?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions. */
    instructions_email?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Origin of the refund */
    origin?: "customer_balance";
    /** The identifier of the PaymentIntent to refund. */
    payment_intent?: string;
    /** String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://docs.stripe.com/radar/lists), and will also help us improve our fraud detection algorithms. */
    reason?: "duplicate" | "fraudulent" | "requested_by_customer";
    /** Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge. */
    refund_application_fee?: boolean;
    /** Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge. */
    reverse_transfer?: boolean;
    /** The identifier of the charge to refund. */
    charge: string;
  }) => Promise<Refund>;

  /**
   * <p>Retrieves the details of an existing refund.</p>
   */
  getChargesChargeRefundsRefund: (input: {
    body?: { [key: string]: unknown };
    charge: string;
    refund: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Refund>;

  /**
   * <p>Update a specified refund.</p>
   */
  postChargesChargeRefundsRefund: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    metadata?: { [key: string]: string | undefined } | "";
    charge: string;
    refund: string;
  }) => Promise<Refund>;

  /**
   * List all Checkout Sessions
   */
  getCheckoutSessions: (input: {
    body?: { [key: string]: unknown };
    /** Only return Checkout Sessions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return the Checkout Sessions for the Customer specified. */
    customer?: string;
    /** Only return the Checkout Sessions for the Account specified. */
    customer_account?: string;
    /** Only return the Checkout Sessions for the Customer details specified. */
    customer_details?: { email: string };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return the Checkout Session for the PaymentIntent specified. */
    payment_intent?: string;
    /** Only return the Checkout Sessions for the Payment Link specified. */
    payment_link?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return the Checkout Sessions matching the given status. */
    status?: "complete" | "expired" | "open";
    /** Only return the Checkout Session for the subscription specified. */
    subscription?: string;
  }) => Promise<{ data: (CheckoutSession)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Checkout Session
   */
  postCheckoutSessions: (input: {
    /** Settings for price localization with [Adaptive Pricing](https://docs.stripe.com/payments/checkout/adaptive-pricing). */
    adaptive_pricing?: { enabled?: boolean };
    /** Configure actions after a Checkout Session has expired. You can't set this parameter if `ui_mode` is `elements`. */
    after_expiration?: { recovery?: { allow_promotion_codes?: boolean; enabled: boolean } };
    /** Enables user redeemable promotion codes. */
    allow_promotion_codes?: boolean;
    /** Settings for automatic tax lookup for this session and resulting payments, invoices, and subscriptions. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Specify whether Checkout should collect the customer's billing address. Defaults to `auto`. */
    billing_address_collection?: "auto" | "required";
    /** The branding settings for the Checkout Session. This parameter is not allowed if ui_mode is `elements`. */
    branding_settings?: { background_color?: string | ""; border_style?: "" | "pill" | "rectangular" | "rounded"; button_color?: string | ""; display_name?: string; font_family?: "" | "be_vietnam_pro" | "bitter" | "chakra_petch" | "default" | "hahmlet" | "inconsolata" | "inter" | "lato" | "lora" | "m_plus_1_code" | "montserrat" | "noto_sans" | "noto_sans_jp" | "noto_serif" | "nunito" | "open_sans" | "pridi" | "pt_sans" | "pt_serif" | "raleway" | "roboto" | "roboto_slab" | "source_sans_pro" | "titillium_web" | "ubuntu_mono" | "zen_maru_gothic"; icon?: { file?: string; type: "file" | "url"; url?: string }; logo?: { file?: string; type: "file" | "url"; url?: string } };
    /** If set, Checkout displays a back button and customers will be directed to this URL if they decide to cancel payment and return to your website. This parameter is not allowed if ui_mode is `embedded_page` or `elements`. */
    cancel_url?: string;
    /** A unique string to reference the Checkout Session. This can be a customer ID, a cart ID, or similar, and can be used to reconcile the session with your internal systems. */
    client_reference_id?: string;
    /** Configure fields for the Checkout Session to gather active consent from customers. */
    consent_collection?: { payment_method_reuse_agreement?: { position: "auto" | "hidden" }; promotions?: "auto" | "none"; terms_of_service?: "none" | "required" };
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Required in `setup` mode when `payment_method_types` is not set. */
    currency?: string;
    /** Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`. */
    custom_fields?: ({ dropdown?: { default_value?: string; options: ({ label: string; value: string })[] }; key: string; label: { custom: string; type: "custom" }; numeric?: { default_value?: string; maximum_length?: number; minimum_length?: number }; optional?: boolean; text?: { default_value?: string; maximum_length?: number; minimum_length?: number }; type: "dropdown" | "numeric" | "text" })[];
    /** Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`. */
    custom_text?: { after_submit?: { message: string } | ""; shipping_address?: { message: string } | ""; submit?: { message: string } | ""; terms_of_service_acceptance?: { message: string } | "" };
    /** ID of an existing Customer, if one exists. In `payment` mode, the customer’s most recently saved card payment method will be used to prefill the email, name, card details, and billing address on the Checkout page. In `subscription` mode, the customer’s [default payment method](https://docs.stripe.com/api/customers/update#update_customer-invoice_settings-default_payment_method) will be used if it’s a card, otherwise the most recently saved card will be used. A valid billing address, billing name and billing email are required on the payment method for Checkout to prefill the customer's card details.  If the Customer already has a valid [email](https://docs.stripe.com/api/customers/object#customer_object-email) set, the email will be prefilled and not editable in Checkout. If the Customer does not have a valid `email`, Checkout will set the email entered during the session on the Customer.  If blank for Checkout Sessions in `subscription` mode or with `customer_creation` set as `always` in `payment` mode, Checkout will create a new Customer object based on information provided during the payment flow.  You can set [`payment_intent_data.setup_future_usage`](https://docs.stripe.com/api/checkout/sessions/create#create_checkout_session-payment_intent_data-setup_future_usage) to have Checkout automatically attach the payment method to the Customer you pass in for future reuse. */
    customer?: string;
    /** ID of an existing Account, if one exists. Has the same behavior as `customer`. */
    customer_account?: string;
    /** Configure whether a Checkout Session creates a [Customer](https://docs.stripe.com/api/customers) during Session confirmation.  When a Customer is not created, you can still retrieve email, address, and other customer data entered in Checkout with [customer_details](https://docs.stripe.com/api/checkout/sessions/object#checkout_session_object-customer_details).  Sessions that don't create Customers instead are grouped by [guest customers](https://docs.stripe.com/payments/checkout/guest-customers) in the Dashboard. Promotion codes limited to first time customers will return invalid for these Sessions.  Can only be set in `payment` and `setup` mode. */
    customer_creation?: "always" | "if_required";
    /** If provided, this value will be used when the Customer object is created. If not provided, customers will be asked to enter their email address. Use this parameter to prefill customer data if you already have an email on file. To access information about the customer once a session is complete, use the `customer` field. */
    customer_email?: string;
    /** Controls what fields on Customer can be updated by the Checkout Session. Can only be provided when `customer` is provided. */
    customer_update?: { address?: "auto" | "never"; name?: "auto" | "never"; shipping?: "auto" | "never" };
    /** The coupon or promotion code to apply to this Session. Currently, only up to one may be specified. */
    discounts?: ({ coupon?: string; promotion_code?: string })[];
    /** A list of the types of payment methods (e.g., `card`) that should be excluded from this Checkout Session. This should only be used when payment methods for this Checkout Session are managed through the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The Epoch time in seconds at which the Checkout Session will expire. It can be anywhere from 30 minutes to 24 hours after Checkout Session creation. By default, this value is 24 hours from creation. */
    expires_at?: number;
    /** The integration identifier for this Checkout Session. Multiple Checkout Sessions can have the same integration identifier. */
    integration_identifier?: string;
    /** Generate a post-purchase Invoice for one-time payments. */
    invoice_creation?: { enabled: boolean; invoice_data?: { account_tax_ids?: (string)[] | ""; custom_fields?: ({ name: string; value: string })[] | ""; description?: string; footer?: string; issuer?: { account?: string; type: "account" | "self" }; metadata?: { [key: string]: string | undefined }; rendering_options?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; template?: string } | "" } };
    /** A list of items the customer is purchasing. Use this parameter to pass one-time or recurring [Prices](https://docs.stripe.com/api/prices). The parameter is required for `payment` and `subscription` mode.  For `payment` mode, there is a maximum of 100 line items, however it is recommended to consolidate line items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items with recurring Prices and 20 line items with one-time Prices. Line items with one-time Prices will be on the initial invoice only. */
    line_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; dynamic_tax_rates?: (string)[]; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] })[];
    /** The IETF language tag of the locale Checkout is displayed in. If blank or `auto`, the browser's locale is used. */
    locale?: "auto" | "bg" | "cs" | "da" | "de" | "el" | "en" | "en-GB" | "es" | "es-419" | "et" | "fi" | "fil" | "fr" | "fr-CA" | "hr" | "hu" | "id" | "it" | "ja" | "ko" | "lt" | "lv" | "ms" | "mt" | "nb" | "nl" | "pl" | "pt" | "pt-BR" | "ro" | "ru" | "sk" | "sl" | "sv" | "th" | "tr" | "vi" | "zh" | "zh-HK" | "zh-TW";
    /** Settings for Managed Payments for this Checkout Session and resulting [PaymentIntents](/api/payment_intents/object), [Invoices](/api/invoices/object), and [Subscriptions](/api/subscriptions/object). */
    managed_payments?: { enabled?: boolean };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The mode of the Checkout Session. Pass `subscription` if the Checkout Session includes at least one recurring item. */
    mode?: "payment" | "setup" | "subscription";
    /** Controls name collection settings for the session.  You can configure Checkout to collect your customers' business names, individual names, or both. Each name field can be either required or optional.  If a [Customer](https://docs.stripe.com/api/customers) is created or provided, the names can be saved to the Customer object as well.  You can't set this parameter if `ui_mode` is `custom`. */
    name_collection?: { business?: { enabled: boolean; optional?: boolean }; individual?: { enabled: boolean; optional?: boolean } };
    /** A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://docs.stripe.com/api/prices).  There is a maximum of 10 optional items allowed on a Checkout Session, and the existing limits on the number of line items allowed on a Checkout Session apply to the combined number of line items and optional items.  For `payment` mode, there is a maximum of 100 combined line items and optional items, however it is recommended to consolidate items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items and optional items with recurring Prices and 20 line items and optional items with one-time Prices.  You can't set this parameter if `ui_mode` is `custom`. */
    optional_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; price: string; quantity: number })[];
    /** Where the user is coming from. This informs the optimizations that are applied to the session. You can't set this parameter if `ui_mode` is `elements`. */
    origin_context?: "mobile_app" | "web";
    /** A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode. */
    payment_intent_data?: { application_fee_amount?: number; capture_method?: "automatic" | "automatic_async" | "manual"; description?: string; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string; receipt_email?: string; setup_future_usage?: "off_session" | "on_session"; shipping?: { address: { city?: string; country?: string; line1: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string }; statement_descriptor?: string; statement_descriptor_suffix?: string; transfer_data?: { amount?: number; destination: string }; transfer_group?: string };
    /** Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0. This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on configuring [subscriptions with a free trial](https://docs.stripe.com/payments/checkout/free-trials). */
    payment_method_collection?: "always" | "if_required";
    /** The ID of the payment method configuration to use with this Checkout session. */
    payment_method_configuration?: string;
    /** This parameter allows you to set some attributes on the payment method created during a Checkout session. */
    payment_method_data?: { allow_redisplay?: "always" | "limited" | "unspecified" };
    /** Payment-method-specific configuration. */
    payment_method_options?: { acss_debit?: { currency?: "cad" | "usd"; mandate_options?: { custom_mandate_url?: string | ""; default_for?: ("invoice" | "subscription")[]; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; setup_future_usage?: "none" | "off_session" | "on_session"; target_date?: string; verification_method?: "automatic" | "instant" | "microdeposits" }; affirm?: { capture_method?: "manual"; setup_future_usage?: "none" }; afterpay_clearpay?: { capture_method?: "manual"; setup_future_usage?: "none" }; alipay?: { setup_future_usage?: "none" }; alma?: { capture_method?: "manual" }; amazon_pay?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; au_becs_debit?: { setup_future_usage?: "none"; target_date?: string }; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "none" | "off_session" | "on_session"; target_date?: string }; bancontact?: { setup_future_usage?: "none" }; billie?: { capture_method?: "manual" }; boleto?: { expires_after_days?: number; setup_future_usage?: "none" | "off_session" | "on_session" }; card?: { capture_method?: "manual"; installments?: { enabled?: boolean }; request_extended_authorization?: "if_available" | "never"; request_incremental_authorization?: "if_available" | "never"; request_multicapture?: "if_available" | "never"; request_overcapture?: "if_available" | "never"; request_three_d_secure?: "any" | "automatic" | "challenge"; restrictions?: { brands_blocked?: ("american_express" | "discover_global_network" | "mastercard" | "visa")[] }; setup_future_usage?: "off_session" | "on_session"; statement_descriptor_suffix_kana?: string; statement_descriptor_suffix_kanji?: string }; cashapp?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" | "on_session" }; crypto?: { setup_future_usage?: "none" }; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; requested_address_types?: ("aba" | "iban" | "sepa" | "sort_code" | "spei" | "swift" | "zengin")[]; type: "eu_bank_transfer" | "gb_bank_transfer" | "jp_bank_transfer" | "mx_bank_transfer" | "us_bank_transfer" }; funding_type?: "bank_transfer"; setup_future_usage?: "none" }; demo_pay?: { setup_future_usage?: "none" | "off_session" }; eps?: { setup_future_usage?: "none" }; fpx?: { setup_future_usage?: "none" }; giropay?: { setup_future_usage?: "none" }; grabpay?: { setup_future_usage?: "none" }; ideal?: { setup_future_usage?: "none" }; kakao_pay?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; klarna?: { capture_method?: "manual"; setup_future_usage?: "none"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing: { amount: number; date: string }; reference: string })[] | "" }; konbini?: { expires_after_days?: number; setup_future_usage?: "none" }; kr_card?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; link?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; mobilepay?: { capture_method?: "manual"; setup_future_usage?: "none" }; multibanco?: { setup_future_usage?: "none" }; naver_pay?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; oxxo?: { expires_after_days?: number; setup_future_usage?: "none" }; p24?: { setup_future_usage?: "none"; tos_shown_and_accepted?: boolean }; pay_by_bank?: { [key: string]: unknown }; payco?: { capture_method?: "manual" }; paynow?: { setup_future_usage?: "none" }; paypal?: { capture_method?: "" | "manual"; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-DE" | "de-LU" | "el-GR" | "en-GB" | "en-US" | "es-ES" | "fi-FI" | "fr-BE" | "fr-FR" | "fr-LU" | "hu-HU" | "it-IT" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "sk-SK" | "sv-SE"; reference?: string; risk_correlation_id?: string; setup_future_usage?: "" | "none" | "off_session" }; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility"; start_date?: string | "" }; setup_future_usage?: "none" | "off_session" }; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string }; setup_future_usage?: "none" | "off_session" }; revolut_pay?: { capture_method?: "manual"; setup_future_usage?: "none" | "off_session" }; samsung_pay?: { capture_method?: "manual" }; satispay?: { capture_method?: "manual" }; scalapay?: { capture_method?: "manual" }; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "none" | "off_session" | "on_session"; target_date?: string }; sofort?: { setup_future_usage?: "none" }; swish?: { reference?: string }; twint?: { setup_future_usage?: "none" | "off_session" }; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" }; us_bank_account?: { financial_connections?: { permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; setup_future_usage?: "none" | "off_session" | "on_session"; target_date?: string; verification_method?: "automatic" | "instant" }; wechat_pay?: { app_id?: string; client: "android" | "ios" | "web"; setup_future_usage?: "none" } };
    /** A list of the types of payment methods (e.g., `card`) this Checkout Session can accept.  You can omit this attribute to manage your payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). See [Dynamic Payment Methods](https://docs.stripe.com/payments/payment-methods/integration-options#using-dynamic-payment-methods) for more details.  Read more about the supported payment methods and their requirements in our [payment method details guide](/docs/payments/checkout/payment-methods).  If multiple payment methods are passed, Checkout will dynamically reorder them to prioritize the most relevant payment methods based on the customer's location and other characteristics. */
    payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[];
    /** This property is used to set up permissions for various actions (e.g., update) on the CheckoutSession object. Can only be set when creating `embedded` or `custom` sessions.  For specific permissions, please refer to their dedicated subsections, such as `permissions.update_shipping_details`. */
    permissions?: { update_shipping_details?: "client_only" | "server_only" };
    /** Controls phone number collection settings for the session.  We recommend that you review your privacy policy and check with your legal contacts before using this feature. Learn more about [collecting phone numbers with Checkout](https://docs.stripe.com/payments/checkout/phone-numbers). */
    phone_number_collection?: { enabled: boolean };
    /** This parameter applies to `ui_mode: embedded_page`. Learn more about the [redirect behavior](https://docs.stripe.com/payments/checkout/custom-success-page?payment-ui=embedded-form) of embedded sessions. Defaults to `always`. */
    redirect_on_completion?: "always" | "if_required" | "never";
    /** The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. This parameter is required if `ui_mode` is `embedded_page` or `elements` and redirect-based payment methods are enabled on the session. */
    return_url?: string;
    /** Controls saved payment method settings for the session. Only available in `payment` and `subscription` mode. */
    saved_payment_method_options?: { allow_redisplay_filters?: ("always" | "limited" | "unspecified")[]; payment_method_remove?: "disabled" | "enabled"; payment_method_save?: "disabled" | "enabled" };
    /** A subset of parameters to be passed to SetupIntent creation for Checkout Sessions in `setup` mode. */
    setup_intent_data?: { description?: string; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string };
    /** When set, provides configuration for Checkout to collect a shipping address from a customer. */
    shipping_address_collection?: { allowed_countries: ("AC" | "AD" | "AE" | "AF" | "AG" | "AI" | "AL" | "AM" | "AO" | "AQ" | "AR" | "AT" | "AU" | "AW" | "AX" | "AZ" | "BA" | "BB" | "BD" | "BE" | "BF" | "BG" | "BH" | "BI" | "BJ" | "BL" | "BM" | "BN" | "BO" | "BQ" | "BR" | "BS" | "BT" | "BV" | "BW" | "BY" | "BZ" | "CA" | "CD" | "CF" | "CG" | "CH" | "CI" | "CK" | "CL" | "CM" | "CN" | "CO" | "CR" | "CV" | "CW" | "CY" | "CZ" | "DE" | "DJ" | "DK" | "DM" | "DO" | "DZ" | "EC" | "EE" | "EG" | "EH" | "ER" | "ES" | "ET" | "FI" | "FJ" | "FK" | "FO" | "FR" | "GA" | "GB" | "GD" | "GE" | "GF" | "GG" | "GH" | "GI" | "GL" | "GM" | "GN" | "GP" | "GQ" | "GR" | "GS" | "GT" | "GU" | "GW" | "GY" | "HK" | "HN" | "HR" | "HT" | "HU" | "ID" | "IE" | "IL" | "IM" | "IN" | "IO" | "IQ" | "IS" | "IT" | "JE" | "JM" | "JO" | "JP" | "KE" | "KG" | "KH" | "KI" | "KM" | "KN" | "KR" | "KW" | "KY" | "KZ" | "LA" | "LB" | "LC" | "LI" | "LK" | "LR" | "LS" | "LT" | "LU" | "LV" | "LY" | "MA" | "MC" | "MD" | "ME" | "MF" | "MG" | "MK" | "ML" | "MM" | "MN" | "MO" | "MQ" | "MR" | "MS" | "MT" | "MU" | "MV" | "MW" | "MX" | "MY" | "MZ" | "NA" | "NC" | "NE" | "NG" | "NI" | "NL" | "NO" | "NP" | "NR" | "NU" | "NZ" | "OM" | "PA" | "PE" | "PF" | "PG" | "PH" | "PK" | "PL" | "PM" | "PN" | "PR" | "PS" | "PT" | "PY" | "QA" | "RE" | "RO" | "RS" | "RU" | "RW" | "SA" | "SB" | "SC" | "SD" | "SE" | "SG" | "SH" | "SI" | "SJ" | "SK" | "SL" | "SM" | "SN" | "SO" | "SR" | "SS" | "ST" | "SV" | "SX" | "SZ" | "TA" | "TC" | "TD" | "TF" | "TG" | "TH" | "TJ" | "TK" | "TL" | "TM" | "TN" | "TO" | "TR" | "TT" | "TV" | "TW" | "TZ" | "UA" | "UG" | "US" | "UY" | "UZ" | "VA" | "VC" | "VE" | "VG" | "VN" | "VU" | "WF" | "WS" | "XK" | "YE" | "YT" | "ZA" | "ZM" | "ZW" | "ZZ")[] };
    /** The shipping rate options to apply to this Session. Up to a maximum of 5. */
    shipping_options?: ({ shipping_rate?: string; shipping_rate_data?: { delivery_estimate?: { maximum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number }; minimum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number } }; display_name: string; fixed_amount?: { amount: number; currency: string; currency_options?: { [key: string]: { amount: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } }; metadata?: { [key: string]: string | undefined }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tax_code?: string; type?: "fixed_amount" } })[];
    /** Describes the type of transaction being performed by Checkout in order to customize relevant text on the page, such as the submit button.  `submit_type` can only be specified on Checkout Sessions in `payment` or `subscription` mode. If blank or `auto`, `pay` is used. You can't set this parameter if `ui_mode` is `elements`. */
    submit_type?: "auto" | "book" | "donate" | "pay" | "subscribe";
    /** A subset of parameters to be passed to subscription creation for Checkout Sessions in `subscription` mode. */
    subscription_data?: { application_fee_percent?: number; billing_cycle_anchor?: number; billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" }; default_tax_rates?: (string)[]; description?: string; invoice_settings?: { issuer?: { account?: string; type: "account" | "self" } }; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string; pending_invoice_item_interval?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; proration_behavior?: "create_prorations" | "none"; transfer_data?: { amount_percent?: number; destination: string }; trial_end?: number; trial_period_days?: number; trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } } };
    /** The URL to which Stripe should send customers when payment or setup is complete. This parameter is not allowed if ui_mode is `embedded_page` or `elements`. If you'd like to use information from the successful Checkout Session on your page, read the guide on [customizing your success page](https://docs.stripe.com/payments/checkout/custom-success-page). */
    success_url?: string;
    /** Controls tax ID collection during checkout. */
    tax_id_collection?: { enabled: boolean; required?: "if_supported" | "never" };
    /** The UI mode of the Session. Defaults to `hosted_page`. */
    ui_mode?: "elements" | "embedded_page" | "hosted_page";
    /** Wallet-specific configuration. */
    wallet_options?: { link?: { display?: "auto" | "never" } };
  }) => Promise<CheckoutSession>;

  /**
   * Retrieve a Checkout Session
   */
  getCheckoutSessionsSession: (input: {
    body?: { [key: string]: unknown };
    session: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CheckoutSession>;

  /**
   * Update a Checkout Session
   */
  postCheckoutSessionsSession: (input: {
    /** Information about the customer collected within the Checkout Session. Can only be set when updating `embedded` or `custom` sessions. */
    collected_information?: { shipping_details?: { address: { city?: string; country: string; line1: string; line2?: string; postal_code?: string; state?: string }; name: string } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A list of items the customer is purchasing.  When updating line items, you must retransmit the entire array of line items.  To retain an existing line item, specify its `id`.  To update an existing line item, specify its `id` along with the new values of the fields to update.  To add a new line item, specify one of `price` or `price_data` and `quantity`.  To remove an existing line item, omit the line item's ID from the retransmitted array.  To reorder a line item, specify it at the desired position in the retransmitted array. */
    line_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; id?: string; metadata?: { [key: string]: string | undefined } | ""; price?: string; price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The shipping rate options to apply to this Session. Up to a maximum of 5. */
    shipping_options?: ({ shipping_rate?: string; shipping_rate_data?: { delivery_estimate?: { maximum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number }; minimum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number } }; display_name: string; fixed_amount?: { amount: number; currency: string; currency_options?: { [key: string]: { amount: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } }; metadata?: { [key: string]: string | undefined }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tax_code?: string; type?: "fixed_amount" } })[] | "";
    session: string;
  }) => Promise<CheckoutSession>;

  /**
   * Expire a Checkout Session
   */
  postCheckoutSessionsSessionExpire: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    session: string;
  }) => Promise<CheckoutSession>;

  /**
   * Retrieve a Checkout Session's line items
   */
  getCheckoutSessionsSessionLineItems: (input: {
    body?: { [key: string]: unknown };
    session: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Item)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * List orders
   */
  getClimateOrders: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ClimateOrder)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an order
   */
  postClimateOrders: (input: {
    /** Requested amount of carbon removal units. Either this or `metric_tons` must be specified. */
    amount?: number;
    /** Publicly sharable reference for the end beneficiary of carbon removal. Assumed to be the Stripe account if not set. */
    beneficiary?: { public_name: string };
    /** Request currency for the order as a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a supported [settlement currency for your account](https://stripe.com/docs/currencies). If omitted, the account's default currency will be used. */
    currency?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Requested number of tons for the order. Either this or `amount` must be specified. */
    metric_tons?: string;
    /** Unique identifier of the Climate product. */
    product: string;
  }) => Promise<ClimateOrder>;

  /**
   * Retrieve an order
   */
  getClimateOrdersOrder: (input: {
    body?: { [key: string]: unknown };
    /** Unique identifier of the order. */
    order: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ClimateOrder>;

  /**
   * Update an order
   */
  postClimateOrdersOrder: (input: {
    /** Publicly sharable reference for the end beneficiary of carbon removal. Assumed to be the Stripe account if not set. */
    beneficiary?: { public_name: string | "" } | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Unique identifier of the order. */
    order: string;
  }) => Promise<ClimateOrder>;

  /**
   * Cancel an order
   */
  postClimateOrdersOrderCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Unique identifier of the order. */
    order: string;
  }) => Promise<ClimateOrder>;

  /**
   * List products
   */
  getClimateProducts: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ClimateProduct)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a product
   */
  getClimateProductsProduct: (input: {
    body?: { [key: string]: unknown };
    product: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ClimateProduct>;

  /**
   * List suppliers
   */
  getClimateSuppliers: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ClimateSupplier)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a supplier
   */
  getClimateSuppliersSupplier: (input: {
    body?: { [key: string]: unknown };
    supplier: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ClimateSupplier>;

  /**
   * Retrieve a ConfirmationToken
   */
  getConfirmationTokensConfirmationToken: (input: {
    body?: { [key: string]: unknown };
    confirmation_token: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ConfirmationToken>;

  /**
   * List Country Specs
   */
  getCountrySpecs: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CountrySpec)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Country Spec
   */
  getCountrySpecsCountry: (input: {
    body?: { [key: string]: unknown };
    country: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CountrySpec>;

  /**
   * List all coupons
   */
  getCoupons: (input: {
    body?: { [key: string]: unknown };
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Coupon)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a coupon
   */
  postCoupons: (input: {
    /** A positive integer representing the amount to subtract from an invoice total (required if `percent_off` is not passed). */
    amount_off?: number;
    /** A hash containing directions for what this Coupon will apply discounts to. */
    applies_to?: { products?: (string)[] };
    /** Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) of the `amount_off` parameter (required if `amount_off` is passed). */
    currency?: string;
    /** Coupons defined in each available currency option (only supported if `amount_off` is passed). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies). */
    currency_options?: { [key: string]: { amount_off: number } | undefined };
    /** Specifies how long the discount will be in effect if used on a subscription. Defaults to `once`. */
    duration?: "forever" | "once" | "repeating";
    /** Required only if `duration` is `repeating`, in which case it must be a positive integer that specifies the number of months the discount will be in effect. */
    duration_in_months?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Unique string of your choice that will be used to identify this coupon when applying it to a customer. If you don't want to specify a particular code, you can leave the ID blank and we'll generate a random code for you. */
    id?: string;
    /** A positive integer specifying the number of times the coupon can be redeemed before it's no longer valid. For example, you might have a 50% off coupon that the first 20 readers of your blog can use. */
    max_redemptions?: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set. */
    name?: string;
    /** A positive float larger than 0, and smaller or equal to 100, that represents the discount the coupon will apply (required if `amount_off` is not passed). */
    percent_off?: number;
    /** Unix timestamp specifying the last time at which the coupon can be redeemed (cannot be set to more than 5 years in the future). After the redeem_by date, the coupon can no longer be applied to new customers. */
    redeem_by?: number;
  }) => Promise<Coupon>;

  /**
   * Delete a coupon
   */
  deleteCouponsCoupon: (input: {
    body?: { [key: string]: unknown };
    coupon: string;
  }) => Promise<DeletedCoupon>;

  /**
   * Retrieve a coupon
   */
  getCouponsCoupon: (input: {
    body?: { [key: string]: unknown };
    coupon: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Coupon>;

  /**
   * Update a coupon
   */
  postCouponsCoupon: (input: {
    /** Coupons defined in each available currency option (only supported if the coupon is amount-based). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies). */
    currency_options?: { [key: string]: { amount_off: number } | undefined };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set. */
    name?: string;
    coupon: string;
  }) => Promise<Coupon>;

  /**
   * List all credit notes
   */
  getCreditNotes: (input: {
    body?: { [key: string]: unknown };
    /** Only return credit notes that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return credit notes for the customer specified by this customer ID. */
    customer?: string;
    /** Only return credit notes for the account representing the customer specified by this account ID. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return credit notes for the invoice specified by this invoice ID. */
    invoice?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CreditNote)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a credit note
   */
  postCreditNotes: (input: {
    /** The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    amount?: number;
    /** The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice. */
    credit_amount?: number;
    /** The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF. */
    effective_at?: number;
    /** Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`. */
    email_type?: "credit_note" | "none";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** ID of the invoice. */
    invoice: string;
    /** Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    lines?: ({ amount?: number; description?: string; invoice_line_item?: string; metadata?: { [key: string]: string | undefined }; quantity?: number; tax_amounts?: ({ amount: number; tax_rate: string; taxable_amount: number })[] | ""; tax_rates?: (string)[] | ""; type: "custom_line_item" | "invoice_line_item"; unit_amount?: number; unit_amount_decimal?: string })[];
    /** The credit note's memo appears on the credit note PDF. */
    memo?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe. */
    out_of_band_amount?: number;
    /** Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory` */
    reason?: "duplicate" | "fraudulent" | "order_change" | "product_unsatisfactory";
    /** The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice. */
    refund_amount?: number;
    /** Refunds to link to this credit note. */
    refunds?: ({ amount_refunded?: number; payment_record_refund?: { payment_record: string; refund_group: string }; refund?: string; type?: "payment_record_refund" | "refund" })[];
    /** When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    shipping_cost?: { shipping_rate?: string };
  }) => Promise<CreditNote>;

  /**
   * Preview a credit note
   */
  getCreditNotesPreview: (input: {
    body?: { [key: string]: unknown };
    /** The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    amount?: number;
    /** The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice. */
    credit_amount?: number;
    /** The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF. */
    effective_at?: number;
    /** Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`. */
    email_type?: "credit_note" | "none";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** ID of the invoice. */
    invoice: string;
    /** Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    lines?: ({ amount?: number; description?: string; invoice_line_item?: string; metadata?: { [key: string]: string | undefined }; quantity?: number; tax_amounts?: ({ amount: number; tax_rate: string; taxable_amount: number })[] | ""; tax_rates?: (string)[] | ""; type: "custom_line_item" | "invoice_line_item"; unit_amount?: number; unit_amount_decimal?: string })[];
    /** The credit note's memo appears on the credit note PDF. */
    memo?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe. */
    out_of_band_amount?: number;
    /** Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory` */
    reason?: "duplicate" | "fraudulent" | "order_change" | "product_unsatisfactory";
    /** The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice. */
    refund_amount?: number;
    /** Refunds to link to this credit note. */
    refunds?: ({ amount_refunded?: number; payment_record_refund?: { payment_record: string; refund_group: string }; refund?: string; type?: "payment_record_refund" | "refund" })[];
    /** When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    shipping_cost?: { shipping_rate?: string };
  }) => Promise<CreditNote>;

  /**
   * Retrieve a credit note preview's line items
   */
  getCreditNotesPreviewLines: (input: {
    body?: { [key: string]: unknown };
    /** The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    amount?: number;
    /** The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice. */
    credit_amount?: number;
    /** The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF. */
    effective_at?: number;
    /** Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`. */
    email_type?: "credit_note" | "none";
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** ID of the invoice. */
    invoice: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    lines?: ({ amount?: number; description?: string; invoice_line_item?: string; metadata?: { [key: string]: string | undefined }; quantity?: number; tax_amounts?: ({ amount: number; tax_rate: string; taxable_amount: number })[] | ""; tax_rates?: (string)[] | ""; type: "custom_line_item" | "invoice_line_item"; unit_amount?: number; unit_amount_decimal?: string })[];
    /** The credit note's memo appears on the credit note PDF. */
    memo?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe. */
    out_of_band_amount?: number;
    /** Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory` */
    reason?: "duplicate" | "fraudulent" | "order_change" | "product_unsatisfactory";
    /** The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice. */
    refund_amount?: number;
    /** Refunds to link to this credit note. */
    refunds?: ({ amount_refunded?: number; payment_record_refund?: { payment_record: string; refund_group: string }; refund?: string; type?: "payment_record_refund" | "refund" })[];
    /** When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided. */
    shipping_cost?: { shipping_rate?: string };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CreditNoteLineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a credit note's line items
   */
  getCreditNotesCreditNoteLines: (input: {
    body?: { [key: string]: unknown };
    credit_note: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CreditNoteLineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a credit note
   */
  getCreditNotesId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CreditNote>;

  /**
   * Update a credit note
   */
  postCreditNotesId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Credit note memo. */
    memo?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    id: string;
  }) => Promise<CreditNote>;

  /**
   * Void a credit note
   */
  postCreditNotesIdVoid: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<CreditNote>;

  /**
   * Create a Customer Session
   */
  postCustomerSessions: (input: {
    /** Configuration for each component. At least 1 component must be enabled. */
    components: { buy_button?: { enabled: boolean }; customer_sheet?: { enabled: boolean; features?: { payment_method_allow_redisplay_filters?: ("always" | "limited" | "unspecified")[]; payment_method_remove?: "disabled" | "enabled" } }; mobile_payment_element?: { enabled: boolean; features?: { payment_method_allow_redisplay_filters?: ("always" | "limited" | "unspecified")[]; payment_method_redisplay?: "disabled" | "enabled"; payment_method_remove?: "disabled" | "enabled"; payment_method_save?: "disabled" | "enabled"; payment_method_save_allow_redisplay_override?: "always" | "limited" | "unspecified" } }; payment_element?: { enabled: boolean; features?: { payment_method_allow_redisplay_filters?: ("always" | "limited" | "unspecified")[]; payment_method_redisplay?: "disabled" | "enabled"; payment_method_redisplay_limit?: number; payment_method_remove?: "disabled" | "enabled"; payment_method_save?: "disabled" | "enabled"; payment_method_save_usage?: "off_session" | "on_session" } }; pricing_table?: { enabled: boolean } };
    /** The ID of an existing customer for which to create the Customer Session. */
    customer?: string;
    /** The ID of an existing Account for which to create the Customer Session. */
    customer_account?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CustomerSession>;

  /**
   * List all customers
   */
  getCustomers: (input: {
    body?: { [key: string]: unknown };
    /** Only return customers that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A case-sensitive filter on the list based on the customer's `email` field. The value must be a string. */
    email?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set. */
    test_clock?: string;
  }) => Promise<{ data: (Customer)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a customer
   */
  postCustomers: (input: {
    /** The customer's address. Learn about [country-specific requirements for calculating tax](https://docs.stripe.com/invoicing/taxes?dashboard-or-api=dashboard#set-up-customer). */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | "";
    /** An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice. */
    balance?: number;
    /** The customer's business name. This may be up to *150 characters*. */
    business_name?: string | "";
    /** Balance information and default balance settings for this customer. */
    cash_balance?: { settings?: { reconciliation_mode?: "automatic" | "manual" | "merchant_default" } };
    /** An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard. */
    description?: string;
    /** Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The customer's full name. This may be up to *150 characters*. */
    individual_name?: string | "";
    /** The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers. */
    invoice_prefix?: string;
    /** Default invoice settings for this customer. */
    invoice_settings?: { custom_fields?: ({ name: string; value: string })[] | ""; default_payment_method?: string; footer?: string; rendering_options?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; template?: string } | "" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The customer's full name or business name. */
    name?: string;
    /** The sequence to be used on the customer's next invoice. Defaults to 1. */
    next_invoice_sequence?: number;
    payment_method?: string;
    /** The customer's phone number. */
    phone?: string;
    /** Customer's preferred languages, ordered by preference. */
    preferred_locales?: (string)[];
    /** The customer's shipping information. Appears on invoices emailed to this customer. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string } | "";
    source?: string;
    /** Tax details about the customer. */
    tax?: { ip_address?: string | ""; validate_location?: "deferred" | "immediately" };
    /** The customer's tax exemption. One of `none`, `exempt`, or `reverse`. */
    tax_exempt?: "" | "exempt" | "none" | "reverse";
    /** The customer's tax IDs. */
    tax_id_data?: ({ type: "ad_nrt" | "ae_trn" | "al_tin" | "am_tin" | "ao_tin" | "ar_cuit" | "au_abn" | "au_arn" | "aw_tin" | "az_tin" | "ba_tin" | "bb_tin" | "bd_bin" | "bf_ifu" | "bg_uic" | "bh_vat" | "bj_ifu" | "bo_tin" | "br_cnpj" | "br_cpf" | "bs_tin" | "by_tin" | "ca_bn" | "ca_gst_hst" | "ca_pst_bc" | "ca_pst_mb" | "ca_pst_sk" | "ca_qst" | "cd_nif" | "ch_uid" | "ch_vat" | "cl_tin" | "cm_niu" | "cn_tin" | "co_nit" | "cr_tin" | "cv_nif" | "de_stn" | "do_rcn" | "ec_ruc" | "eg_tin" | "es_cif" | "et_tin" | "eu_oss_vat" | "eu_vat" | "fo_vat" | "gb_vat" | "ge_vat" | "gi_tin" | "gn_nif" | "hk_br" | "hr_oib" | "hu_tin" | "id_npwp" | "il_vat" | "in_gst" | "is_vat" | "it_cf" | "jp_cn" | "jp_rn" | "jp_trn" | "ke_pin" | "kg_tin" | "kh_tin" | "kr_brn" | "kz_bin" | "la_tin" | "li_uid" | "li_vat" | "lk_vat" | "ma_vat" | "md_vat" | "me_pib" | "mk_vat" | "mr_nif" | "mx_rfc" | "my_frp" | "my_itn" | "my_sst" | "ng_tin" | "no_vat" | "no_voec" | "np_pan" | "nz_gst" | "om_vat" | "pe_ruc" | "ph_tin" | "pl_nip" | "py_ruc" | "ro_tin" | "rs_pib" | "ru_inn" | "ru_kpp" | "sa_vat" | "sg_gst" | "sg_uen" | "si_tin" | "sn_ninea" | "sr_fin" | "sv_nit" | "th_vat" | "tj_tin" | "tr_tin" | "tw_vat" | "tz_vat" | "ua_vat" | "ug_tin" | "us_ein" | "uy_ruc" | "uz_tin" | "uz_vat" | "ve_rif" | "vn_tin" | "za_vat" | "zm_tin" | "zw_tin"; value: string })[];
    /** ID of the test clock to attach to the customer. */
    test_clock?: string;
  }) => Promise<Customer>;

  /**
   * Search customers
   */
  getCustomersSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for customers](https://docs.stripe.com/search#query-fields-for-customers). */
    query: string;
  }) => Promise<{ data: (Customer)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Delete a customer
   */
  deleteCustomersCustomer: (input: {
    body?: { [key: string]: unknown };
    customer: string;
  }) => Promise<DeletedCustomer>;

  /**
   * Retrieve a customer
   */
  getCustomersCustomer: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Customer | DeletedCustomer>;

  /**
   * Update a customer
   */
  postCustomersCustomer: (input: {
    /** The customer's address. Learn about [country-specific requirements for calculating tax](https://docs.stripe.com/invoicing/taxes?dashboard-or-api=dashboard#set-up-customer). */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | "";
    /** An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice. */
    balance?: number;
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; country: string; currency?: string; object?: "bank_account"; routing_number?: string } | string;
    /** The customer's business name. This may be up to *150 characters*. */
    business_name?: string | "";
    /** A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js). */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; cvc?: string; exp_month: number; exp_year: number; metadata?: { [key: string]: string | undefined }; name?: string; number: string; object?: "card" } | string;
    /** Balance information and default balance settings for this customer. */
    cash_balance?: { settings?: { reconciliation_mode?: "automatic" | "manual" | "merchant_default" } };
    /** ID of Alipay account to make the customer's new default for invoice payments. */
    default_alipay_account?: string;
    /** ID of bank account to make the customer's new default for invoice payments. */
    default_bank_account?: string;
    /** ID of card to make the customer's new default for invoice payments. */
    default_card?: string;
    /** If you are using payment methods created via the PaymentMethods API, see the [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/update#update_customer-invoice_settings-default_payment_method) parameter.  Provide the ID of a payment source already attached to this customer to make it this customer's default payment source.  If you want to add a new payment source and make it the default, see the [source](https://docs.stripe.com/api/customers/update#update_customer-source) property. */
    default_source?: string;
    /** An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard. */
    description?: string;
    /** Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The customer's full name. This may be up to *150 characters*. */
    individual_name?: string | "";
    /** The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers. */
    invoice_prefix?: string;
    /** Default invoice settings for this customer. */
    invoice_settings?: { custom_fields?: ({ name: string; value: string })[] | ""; default_payment_method?: string; footer?: string; rendering_options?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; template?: string } | "" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The customer's full name or business name. */
    name?: string;
    /** The sequence to be used on the customer's next invoice. Defaults to 1. */
    next_invoice_sequence?: number;
    /** The customer's phone number. */
    phone?: string;
    /** Customer's preferred languages, ordered by preference. */
    preferred_locales?: (string)[];
    /** The customer's shipping information. Appears on invoices emailed to this customer. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string } | "";
    source?: string;
    /** Tax details about the customer. */
    tax?: { ip_address?: string | ""; validate_location?: "auto" | "deferred" | "immediately" };
    /** The customer's tax exemption. One of `none`, `exempt`, or `reverse`. */
    tax_exempt?: "" | "exempt" | "none" | "reverse";
    customer: string;
  }) => Promise<Customer>;

  /**
   * List customer balance transactions
   */
  getCustomersCustomerBalanceTransactions: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** Only return customer balance transactions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return transactions that are related to the specified invoice. */
    invoice?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CustomerBalanceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a customer balance transaction
   */
  postCustomersCustomerBalanceTransactions: (input: {
    /** The integer amount in **cents (or local equivalent)** to apply to the customer's credit balance. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Specifies the [`invoice_credit_balance`](https://docs.stripe.com/api/customers/object#customer_object-invoice_credit_balance) that this transaction will apply to. If the customer's `currency` is not set, it will be updated to this value. */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    customer: string;
  }) => Promise<CustomerBalanceTransaction>;

  /**
   * Retrieve a customer balance transaction
   */
  getCustomersCustomerBalanceTransactionsTransaction: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CustomerBalanceTransaction>;

  /**
   * Update a customer credit balance transaction
   */
  postCustomersCustomerBalanceTransactionsTransaction: (input: {
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    customer: string;
    transaction: string;
  }) => Promise<CustomerBalanceTransaction>;

  /**
   * List all bank accounts
   */
  getCustomersCustomerBankAccounts: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BankAccount)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a card
   */
  postCustomersCustomerBankAccounts: (input: {
    /** A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details. */
    alipay_account?: string;
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; country: string; currency?: string; object?: "bank_account"; routing_number?: string } | string;
    /** A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js). */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; cvc?: string; exp_month: number; exp_year: number; metadata?: { [key: string]: string | undefined }; name?: string; number: string; object?: "card" } | string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Please refer to full [documentation](https://api.stripe.com) instead. */
    source?: string;
    customer: string;
  }) => Promise<PaymentSource>;

  /**
   * Delete a customer source
   */
  deleteCustomersCustomerBankAccountsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    customer: string;
    id: string;
  }) => Promise<PaymentSource | DeletedPaymentSource>;

  /**
   * Retrieve a bank account
   */
  getCustomersCustomerBankAccountsId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<BankAccount>;

  /**
   * <p>Update a specified source for a given customer.</p>
   */
  postCustomersCustomerBankAccountsId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "company" | "individual";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    owner?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string };
    customer: string;
    id: string;
  }) => Promise<Card | BankAccount | Source>;

  /**
   * Verify a bank account
   */
  postCustomersCustomerBankAccountsIdVerify: (input: {
    /** Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account. */
    amounts?: (number)[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    customer: string;
    id: string;
  }) => Promise<BankAccount>;

  /**
   * List all cards
   */
  getCustomersCustomerCards: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Card)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a card
   */
  postCustomersCustomerCards: (input: {
    /** A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details. */
    alipay_account?: string;
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; country: string; currency?: string; object?: "bank_account"; routing_number?: string } | string;
    /** A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js). */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; cvc?: string; exp_month: number; exp_year: number; metadata?: { [key: string]: string | undefined }; name?: string; number: string; object?: "card" } | string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Please refer to full [documentation](https://api.stripe.com) instead. */
    source?: string;
    customer: string;
  }) => Promise<PaymentSource>;

  /**
   * Delete a customer source
   */
  deleteCustomersCustomerCardsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    customer: string;
    id: string;
  }) => Promise<PaymentSource | DeletedPaymentSource>;

  /**
   * Retrieve a card
   */
  getCustomersCustomerCardsId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Card>;

  /**
   * <p>Update a specified source for a given customer.</p>
   */
  postCustomersCustomerCardsId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "company" | "individual";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    owner?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string };
    customer: string;
    id: string;
  }) => Promise<Card | BankAccount | Source>;

  /**
   * Retrieve a cash balance
   */
  getCustomersCustomerCashBalance: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CashBalance>;

  /**
   * Update a cash balance's settings
   */
  postCustomersCustomerCashBalance: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A hash of settings for this cash balance. */
    settings?: { reconciliation_mode?: "automatic" | "manual" | "merchant_default" };
    customer: string;
  }) => Promise<CashBalance>;

  /**
   * List cash balance transactions
   */
  getCustomersCustomerCashBalanceTransactions: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (CustomerCashBalanceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a cash balance transaction
   */
  getCustomersCustomerCashBalanceTransactionsTransaction: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<CustomerCashBalanceTransaction>;

  /**
   * Delete a customer discount
   */
  deleteCustomersCustomerDiscount: (input: {
    body?: { [key: string]: unknown };
    customer: string;
  }) => Promise<DeletedDiscount>;

  getCustomersCustomerDiscount: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Discount>;

  /**
   * Create or retrieve funding instructions for a customer cash balance
   */
  postCustomersCustomerFundingInstructions: (input: {
    /** Additional parameters for `bank_transfer` funding types */
    bank_transfer: { eu_bank_transfer?: { country: string }; requested_address_types?: ("iban" | "sort_code" | "spei" | "zengin")[]; type: "eu_bank_transfer" | "gb_bank_transfer" | "jp_bank_transfer" | "mx_bank_transfer" | "us_bank_transfer" };
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The `funding_type` to get the instructions for. */
    funding_type: "bank_transfer";
    customer: string;
  }) => Promise<FundingInstructions>;

  /**
   * List a Customer's PaymentMethods
   */
  getCustomersCustomerPaymentMethods: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. */
    allow_redisplay?: "always" | "limited" | "unspecified";
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request. */
    type?: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip";
  }) => Promise<{ data: (PaymentMethod)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Customer's PaymentMethod
   */
  getCustomersCustomerPaymentMethodsPaymentMethod: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    payment_method: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentMethod>;

  /**
   * <p>List sources for a specified customer.</p>
   */
  getCustomersCustomerSources: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filter sources according to a particular object type. */
    object?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (BankAccount | Card | Source)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a card
   */
  postCustomersCustomerSources: (input: {
    /** A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details. */
    alipay_account?: string;
    /** Either a token, like the ones returned by [Stripe.js](https://stripe.com/docs/js), or a dictionary containing a user's bank account details. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; country: string; currency?: string; object?: "bank_account"; routing_number?: string } | string;
    /** A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js). */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; cvc?: string; exp_month: number; exp_year: number; metadata?: { [key: string]: string | undefined }; name?: string; number: string; object?: "card" } | string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Please refer to full [documentation](https://api.stripe.com) instead. */
    source?: string;
    customer: string;
  }) => Promise<PaymentSource>;

  /**
   * Delete a customer source
   */
  deleteCustomersCustomerSourcesId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    customer: string;
    id: string;
  }) => Promise<PaymentSource | DeletedPaymentSource>;

  /**
   * <p>Retrieve a specified source for a given customer.</p>
   */
  getCustomersCustomerSourcesId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentSource>;

  /**
   * <p>Update a specified source for a given customer.</p>
   */
  postCustomersCustomerSourcesId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "company" | "individual";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    owner?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string };
    customer: string;
    id: string;
  }) => Promise<Card | BankAccount | Source>;

  /**
   * Verify a bank account
   */
  postCustomersCustomerSourcesIdVerify: (input: {
    /** Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account. */
    amounts?: (number)[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    customer: string;
    id: string;
  }) => Promise<BankAccount>;

  /**
   * List active subscriptions
   */
  getCustomersCustomerSubscriptions: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Subscription)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a subscription
   */
  postCustomersCustomerSubscriptions: (input: {
    /** A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items. */
    add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "now" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a platform account on a connected account in order to set an application fee percentage. For more information, see the application fees [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions). */
    application_fee_percent?: number | "";
    /** Automatic tax settings for this subscription. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor. */
    backdate_start_date?: number;
    /** A future timestamp in UTC format to anchor the subscription's [billing cycle](https://docs.stripe.com/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals. */
    billing_cycle_anchor?: number;
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | "";
    /** A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always cause a proration for that period. */
    cancel_at?: number | "max_billed_until" | "max_period_end" | "min_period_end";
    /** Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`. */
    cancel_at_period_end?: boolean;
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`. */
    days_until_due?: number;
    /** ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_payment_method?: string;
    /** ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_source?: string;
    /** The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription. */
    default_tax_rates?: (string)[] | "";
    /** The coupons to redeem into discounts for the subscription. If not specified or empty, inherits the discount from the subscription's customer. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { account_tax_ids?: (string)[] | ""; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of up to 20 subscription items, each with an attached price. */
    items?: ({ billing_thresholds?: { usage_gte: number } | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session). */
    off_session?: boolean;
    /** Controls how Stripe handles the first invoice when payment is required and `collection_method=charge_automatically`. Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** Payment settings to pass to invoices created by the subscription. */
    payment_settings?: { payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { expires_after_seconds?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly" } } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | ""; save_default_payment_method?: "off" | "on_subscription" };
    /** Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an invoice](/api/invoices/create) for the given subscription at the specified interval. */
    pending_invoice_item_interval?: { interval: "day" | "month" | "week" | "year"; interval_count?: number } | "";
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges. */
    transfer_data?: { amount_percent?: number; destination: string };
    /** Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. If set, trial_end will override the default trial period of the plan the customer is being subscribed to. The special value `now` can be provided to end the customer's trial immediately. Can be at most two years from `billing_cycle_anchor`. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_end?: "now" | number;
    /** Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_from_plan?: boolean;
    /** Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_period_days?: number;
    /** Settings related to subscription trials. */
    trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } };
    customer: string;
  }) => Promise<Subscription>;

  /**
   * Cancel a subscription
   */
  deleteCustomersCustomerSubscriptionsSubscriptionExposedId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Can be set to `true` if `at_period_end` is not set to `true`. Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items. */
    invoice_now?: boolean;
    /** Can be set to `true` if `at_period_end` is not set to `true`. Will generate a proration invoice item that credits remaining unused time until the subscription period end. */
    prorate?: boolean;
    customer: string;
    subscription_exposed_id: string;
  }) => Promise<Subscription>;

  /**
   * Retrieve a subscription
   */
  getCustomersCustomerSubscriptionsSubscriptionExposedId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    subscription_exposed_id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Subscription>;

  /**
   * Update a subscription on a customer
   */
  postCustomersCustomerSubscriptionsSubscriptionExposedId: (input: {
    /** A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items. */
    add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "now" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a platform account on a connected account in order to set an application fee percentage. For more information, see the application fees [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions). */
    application_fee_percent?: number | "";
    /** Automatic tax settings for this subscription. We recommend you only include this parameter when the existing value is being changed. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time. For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle). */
    billing_cycle_anchor?: "now" | "unchanged";
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | "";
    /** A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always cause a proration for that period. */
    cancel_at?: number | "" | "max_billed_until" | "max_period_end" | "min_period_end";
    /** Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`. */
    cancel_at_period_end?: boolean;
    /** Details about why this subscription was cancelled */
    cancellation_details?: { comment?: string | ""; feedback?: "" | "customer_service" | "low_quality" | "missing_features" | "other" | "switched_service" | "too_complex" | "too_expensive" | "unused" };
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`. */
    days_until_due?: number;
    /** ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_payment_method?: string;
    /** ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_source?: string | "";
    /** The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription. Pass an empty string to remove previously-defined tax rates. */
    default_tax_rates?: (string)[] | "";
    /** The coupons to redeem into discounts for the subscription. A populated array overwrites the existing discounts on the subscription. If not specified or empty array, it leaves the subscription's discounts unchanged. If empty string, it clears the subscription's discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { account_tax_ids?: (string)[] | ""; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of up to 20 subscription items, each with an attached price. */
    items?: ({ billing_thresholds?: { usage_gte: number } | ""; clear_usage?: boolean; deleted?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; id?: string; metadata?: { [key: string]: string | undefined } | ""; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session). */
    off_session?: boolean;
    /** If specified, payment collection for this subscription will be paused. Note that the subscription status will be unchanged and will not be updated to `paused`. Learn more about [pausing collection](https://docs.stripe.com/billing/subscriptions/pause-payment). */
    pause_collection?: { behavior: "keep_as_draft" | "mark_uncollectible" | "void"; resumes_at?: number } | "";
    /** Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** Payment settings to pass to invoices created by the subscription. */
    payment_settings?: { payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { expires_after_seconds?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly" } } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | ""; save_default_payment_method?: "off" | "on_subscription" };
    /** Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an invoice](/api/invoices/create) for the given subscription at the specified interval. */
    pending_invoice_item_interval?: { interval: "day" | "month" | "week" | "year"; interval_count?: number } | "";
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations. */
    proration_date?: number;
    /** If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges. This will be unset if you POST an empty value. */
    transfer_data?: { amount_percent?: number; destination: string } | "";
    /** Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. If set, trial_end will override the default trial period of the plan the customer is being subscribed to. The special value `now` can be provided to end the customer's trial immediately. Can be at most two years from `billing_cycle_anchor`. */
    trial_end?: "now" | number;
    /** Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_from_plan?: boolean;
    /** Settings related to subscription trials. */
    trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } };
    customer: string;
    subscription_exposed_id: string;
  }) => Promise<Subscription>;

  /**
   * Delete a customer discount
   */
  deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    subscription_exposed_id: string;
  }) => Promise<DeletedDiscount>;

  getCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    subscription_exposed_id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Discount>;

  /**
   * List all Customer tax IDs
   */
  getCustomersCustomerTaxIds: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxId)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Customer tax ID
   */
  postCustomersCustomerTaxIds: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `fo_vat`, `gb_vat`, `ge_vat`, `gi_tin`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `it_cf`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `lk_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `pl_nip`, `py_ruc`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin` */
    type: "ad_nrt" | "ae_trn" | "al_tin" | "am_tin" | "ao_tin" | "ar_cuit" | "au_abn" | "au_arn" | "aw_tin" | "az_tin" | "ba_tin" | "bb_tin" | "bd_bin" | "bf_ifu" | "bg_uic" | "bh_vat" | "bj_ifu" | "bo_tin" | "br_cnpj" | "br_cpf" | "bs_tin" | "by_tin" | "ca_bn" | "ca_gst_hst" | "ca_pst_bc" | "ca_pst_mb" | "ca_pst_sk" | "ca_qst" | "cd_nif" | "ch_uid" | "ch_vat" | "cl_tin" | "cm_niu" | "cn_tin" | "co_nit" | "cr_tin" | "cv_nif" | "de_stn" | "do_rcn" | "ec_ruc" | "eg_tin" | "es_cif" | "et_tin" | "eu_oss_vat" | "eu_vat" | "fo_vat" | "gb_vat" | "ge_vat" | "gi_tin" | "gn_nif" | "hk_br" | "hr_oib" | "hu_tin" | "id_npwp" | "il_vat" | "in_gst" | "is_vat" | "it_cf" | "jp_cn" | "jp_rn" | "jp_trn" | "ke_pin" | "kg_tin" | "kh_tin" | "kr_brn" | "kz_bin" | "la_tin" | "li_uid" | "li_vat" | "lk_vat" | "ma_vat" | "md_vat" | "me_pib" | "mk_vat" | "mr_nif" | "mx_rfc" | "my_frp" | "my_itn" | "my_sst" | "ng_tin" | "no_vat" | "no_voec" | "np_pan" | "nz_gst" | "om_vat" | "pe_ruc" | "ph_tin" | "pl_nip" | "py_ruc" | "ro_tin" | "rs_pib" | "ru_inn" | "ru_kpp" | "sa_vat" | "sg_gst" | "sg_uen" | "si_tin" | "sn_ninea" | "sr_fin" | "sv_nit" | "th_vat" | "tj_tin" | "tr_tin" | "tw_vat" | "tz_vat" | "ua_vat" | "ug_tin" | "us_ein" | "uy_ruc" | "uz_tin" | "uz_vat" | "ve_rif" | "vn_tin" | "za_vat" | "zm_tin" | "zw_tin";
    /** Value of the tax ID. */
    value: string;
    customer: string;
  }) => Promise<TaxId>;

  /**
   * Delete a Customer tax ID
   */
  deleteCustomersCustomerTaxIdsId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    id: string;
  }) => Promise<DeletedTaxId>;

  /**
   * Retrieve a Customer tax ID
   */
  getCustomersCustomerTaxIdsId: (input: {
    body?: { [key: string]: unknown };
    customer: string;
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxId>;

  /**
   * List all disputes
   */
  getDisputes: (input: {
    body?: { [key: string]: unknown };
    /** Only return disputes associated to the charge specified by this charge ID. */
    charge?: string;
    /** Only return disputes that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return disputes associated to the PaymentIntent specified by this PaymentIntent ID. */
    payment_intent?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Dispute)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a dispute
   */
  getDisputesDispute: (input: {
    body?: { [key: string]: unknown };
    dispute: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Dispute>;

  /**
   * Update a dispute
   */
  postDisputesDispute: (input: {
    /** Evidence to upload, to respond to a dispute. Updating any field in the hash will submit all fields in the hash for review. The combined character count of all fields is limited to 150,000. */
    evidence?: { access_activity_log?: string; billing_address?: string; cancellation_policy?: string; cancellation_policy_disclosure?: string; cancellation_rebuttal?: string; customer_communication?: string; customer_email_address?: string; customer_name?: string; customer_purchase_ip?: string; customer_signature?: string; duplicate_charge_documentation?: string; duplicate_charge_explanation?: string; duplicate_charge_id?: string; enhanced_evidence?: { visa_compelling_evidence_3?: { disputed_transaction?: { customer_account_id?: string | ""; customer_device_fingerprint?: string | ""; customer_device_id?: string | ""; customer_email_address?: string | ""; customer_purchase_ip?: string | ""; merchandise_or_services?: "merchandise" | "services"; product_description?: string | ""; shipping_address?: { city?: string | ""; country?: string | ""; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" } }; prior_undisputed_transactions?: ({ charge: string; customer_account_id?: string | ""; customer_device_fingerprint?: string | ""; customer_device_id?: string | ""; customer_email_address?: string | ""; customer_purchase_ip?: string | ""; product_description?: string | ""; shipping_address?: { city?: string | ""; country?: string | ""; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" } })[] }; visa_compliance?: { fee_acknowledged?: boolean } } | ""; product_description?: string; receipt?: string; refund_policy?: string; refund_policy_disclosure?: string; refund_refusal_explanation?: string; service_date?: string; service_documentation?: string; shipping_address?: string; shipping_carrier?: string; shipping_date?: string; shipping_documentation?: string; shipping_tracking_number?: string; uncategorized_file?: string; uncategorized_text?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default). */
    submit?: boolean;
    dispute: string;
  }) => Promise<Dispute>;

  /**
   * Close a dispute
   */
  postDisputesDisputeClose: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    dispute: string;
  }) => Promise<Dispute>;

  /**
   * List all active entitlements
   */
  getEntitlementsActiveEntitlements: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the customer. */
    customer: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (EntitlementsActiveEntitlement)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an active entitlement
   */
  getEntitlementsActiveEntitlementsId: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the entitlement. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<EntitlementsActiveEntitlement>;

  /**
   * List all features
   */
  getEntitlementsFeatures: (input: {
    body?: { [key: string]: unknown };
    /** If set, filter results to only include features with the given archive status. */
    archived?: boolean;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** If set, filter results to only include features with the given lookup_key. */
    lookup_key?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (EntitlementsFeature)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a feature
   */
  postEntitlementsFeatures: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A unique key you provide as your own system identifier. This may be up to 80 characters. */
    lookup_key: string;
    /** Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format. */
    metadata?: { [key: string]: string | undefined };
    /** The feature's name, for your own purpose, not meant to be displayable to the customer. */
    name: string;
  }) => Promise<EntitlementsFeature>;

  /**
   * Retrieve a feature
   */
  getEntitlementsFeaturesId: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the feature. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<EntitlementsFeature>;

  /**
   * Updates a feature
   */
  postEntitlementsFeaturesId: (input: {
    /** Inactive features cannot be attached to new products and will not be returned from the features list endpoint. */
    active?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The feature's name, for your own purpose, not meant to be displayable to the customer. */
    name?: string;
    id: string;
  }) => Promise<EntitlementsFeature>;

  /**
   * Create an ephemeral key
   */
  postEphemeralKeys: (input: {
    /** The ID of the Customer you'd like to modify using the resulting ephemeral key. */
    customer?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of the Issuing Card you'd like to access using the resulting ephemeral key. */
    issuing_card?: string;
    /** A single-use token, created by Stripe.js, used for creating ephemeral keys for Issuing Cards without exchanging sensitive information. */
    nonce?: string;
    /** The ID of the Identity VerificationSession you'd like to access using the resulting ephemeral key */
    verification_session?: string;
  }) => Promise<EphemeralKey>;

  /**
   * Immediately invalidate an ephemeral key
   */
  deleteEphemeralKeysKey: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    key: string;
  }) => Promise<EphemeralKey>;

  /**
   * List all events
   */
  getEvents: (input: {
    body?: { [key: string]: unknown };
    /** Only return events that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Filter events by whether all webhooks were successfully delivered. If false, events which are still pending or have failed all delivery attempts to a webhook endpoint will be returned. */
    delivery_success?: boolean;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** A string containing a specific event name, or group of events using * as a wildcard. The list will be filtered to include only events with a matching event property. */
    type?: string;
    /** An array of up to 20 strings containing specific event names. The list will be filtered to include only events with a matching event property. You may pass either `type` or `types`, but not both. */
    types?: (string)[];
  }) => Promise<{ data: (Event)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an event
   */
  getEventsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Event>;

  /**
   * List all exchange rates
   */
  getExchangeRates: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with the exchange rate for currency X your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and total number of supported payout currencies, and the default is the max. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with the exchange rate for currency X, your subsequent call can include `starting_after=X` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ExchangeRate)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an exchange rate
   */
  getExchangeRatesRateId: (input: {
    body?: { [key: string]: unknown };
    rate_id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ExchangeRate>;

  /**
   * <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href="/api/accounts/object#account_object-controller-requirement_collection">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href="/connect/custom-accounts">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
   */
  postExternalAccountsId: (input: {
    /** The name of the person or business that owns the bank account. */
    account_holder_name?: string;
    /** The type of entity that holds the account. This can be either `individual` or `company`. */
    account_holder_type?: "" | "company" | "individual";
    /** The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`. */
    account_type?: "checking" | "futsu" | "savings" | "toza";
    /** City/District/Suburb/Town/Village. */
    address_city?: string;
    /** Billing address country, if provided when creating card. */
    address_country?: string;
    /** Address line 1 (Street address/PO Box/Company name). */
    address_line1?: string;
    /** Address line 2 (Apartment/Suite/Unit/Building). */
    address_line2?: string;
    /** State/County/Province/Region. */
    address_state?: string;
    /** ZIP or postal code. */
    address_zip?: string;
    /** When set to true, this becomes the default external account for its currency. */
    default_for_currency?: boolean;
    /** Documents that may be submitted to satisfy various informational requests. */
    documents?: { bank_account_ownership_verification?: { files?: (string)[] } };
    /** Two digit number representing the card’s expiration month. */
    exp_month?: string;
    /** Four digit number representing the card’s expiration year. */
    exp_year?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Cardholder name. */
    name?: string;
    id: string;
  }) => Promise<ExternalAccount>;

  /**
   * List all file links
   */
  getFileLinks: (input: {
    body?: { [key: string]: unknown };
    /** Only return links that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Filter links by their expiration status. By default, Stripe returns all links. */
    expired?: boolean;
    /** Only return links for the given file. */
    file?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FileLink)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a file link
   */
  postFileLinks: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The link isn't usable after this future timestamp. */
    expires_at?: number;
    /** The ID of the file. The file's `purpose` must be one of the following: `business_icon`, `business_logo`, `customer_signature`, `dispute_evidence`, `finance_report_run`, `financial_account_statement`, `identity_document_downloadable`, `issuing_regulatory_reporting`, `pci_document`, `selfie`, `sigma_scheduled_query`, `tax_document_user_upload`, `terminal_android_apk`, or `terminal_reader_splashscreen`. */
    file: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
  }) => Promise<FileLink>;

  /**
   * Retrieve a file link
   */
  getFileLinksLink: (input: {
    body?: { [key: string]: unknown };
    link: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FileLink>;

  /**
   * Update a file link
   */
  postFileLinksLink: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A future timestamp after which the link will no longer be usable, or `now` to expire the link immediately. */
    expires_at?: "now" | number | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    link: string;
  }) => Promise<FileLink>;

  /**
   * List all files
   */
  getFiles: (input: {
    body?: { [key: string]: unknown };
    /** Only return files that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filter queries by the file purpose. If you don't provide a purpose, the queries return unfiltered files. */
    purpose?: "account_requirement" | "additional_verification" | "business_icon" | "business_logo" | "customer_signature" | "dispute_evidence" | "document_provider_identity_document" | "finance_report_run" | "financial_account_statement" | "identity_document" | "identity_document_downloadable" | "issuing_regulatory_reporting" | "pci_document" | "platform_terms_of_service" | "selfie" | "sigma_scheduled_query" | "tax_document_user_upload" | "terminal_android_apk" | "terminal_reader_splashscreen" | "terminal_wifi_certificate" | "terminal_wifi_private_key";
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (File)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a file
   */
  postFiles: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A file to upload. Make sure that the specifications follow RFC 2388, which defines file transfers for the `multipart/form-data` protocol. */
    file: string;
    /** Optional parameters that automatically create a [file link](https://api.stripe.com#file_links) for the newly created file. */
    file_link_data?: { create: boolean; expires_at?: number; metadata?: { [key: string]: string | undefined } | "" };
    /** The [purpose](https://docs.stripe.com/file-upload#uploading-a-file) of the uploaded file. */
    purpose: "account_requirement" | "additional_verification" | "business_icon" | "business_logo" | "customer_signature" | "dispute_evidence" | "identity_document" | "issuing_regulatory_reporting" | "pci_document" | "platform_terms_of_service" | "tax_document_user_upload" | "terminal_android_apk" | "terminal_reader_splashscreen" | "terminal_wifi_certificate" | "terminal_wifi_private_key";
  }) => Promise<File>;

  /**
   * Retrieve a file
   */
  getFilesFile: (input: {
    body?: { [key: string]: unknown };
    file: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<File>;

  /**
   * List Accounts
   */
  getFinancialConnectionsAccounts: (input: {
    body?: { [key: string]: unknown };
    /** If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive. */
    account_holder?: { account?: string; customer?: string; customer_account?: string };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** If present, only return accounts that were collected as part of the given session. */
    session?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FinancialConnectionsAccount)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an Account
   */
  getFinancialConnectionsAccountsAccount: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Disconnect an Account
   */
  postFinancialConnectionsAccountsAccountDisconnect: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * List Account Owners
   */
  getFinancialConnectionsAccountsAccountOwners: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The ID of the ownership object to fetch owners from. */
    ownership: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FinancialConnectionsAccountOwner)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Refresh Account data
   */
  postFinancialConnectionsAccountsAccountRefresh: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The list of account features that you would like to refresh. */
    features: ("balance" | "ownership" | "transactions")[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Subscribe to data refreshes for an Account
   */
  postFinancialConnectionsAccountsAccountSubscribe: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The list of account features to which you would like to subscribe. */
    features: ("transactions")[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Unsubscribe from data refreshes for an Account
   */
  postFinancialConnectionsAccountsAccountUnsubscribe: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The list of account features from which you would like to unsubscribe. */
    features: ("transactions")[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Create a Session
   */
  postFinancialConnectionsSessions: (input: {
    /** The account holder to link accounts for. */
    account_holder: { account?: string; customer?: string; customer_account?: string; type: "account" | "customer" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Filters to restrict the kinds of accounts to collect. */
    filters?: { account_subcategories?: ("checking" | "credit_card" | "line_of_credit" | "mortgage" | "savings")[]; countries?: (string)[] };
    /** List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`. */
    permissions: ("balances" | "ownership" | "payment_method" | "transactions")[];
    /** List of data features that you would like to retrieve upon account creation. */
    prefetch?: ("balances" | "ownership" | "transactions")[];
    /** For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app. */
    return_url?: string;
  }) => Promise<FinancialConnectionsSession>;

  /**
   * Retrieve a Session
   */
  getFinancialConnectionsSessionsSession: (input: {
    body?: { [key: string]: unknown };
    session: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FinancialConnectionsSession>;

  /**
   * List Transactions
   */
  getFinancialConnectionsTransactions: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the Financial Connections Account whose transactions will be retrieved. */
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** A filter on the list based on the object `transacted_at` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with the following options: */
    transacted_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A filter on the list based on the object `transaction_refresh` field. The value can be a dictionary with the following options: */
    transaction_refresh?: { after: string };
  }) => Promise<{ data: (FinancialConnectionsTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Transaction
   */
  getFinancialConnectionsTransactionsTransaction: (input: {
    body?: { [key: string]: unknown };
    transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FinancialConnectionsTransaction>;

  /**
   * List all ForwardingRequests
   */
  getForwardingRequests: (input: {
    body?: { [key: string]: unknown };
    /** Similar to other List endpoints, filters results based on created timestamp. You can pass gt, gte, lt, and lte timestamp values. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number };
    /** A pagination cursor to fetch the previous page of the list. The value must be a ForwardingRequest ID. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A pagination cursor to fetch the next page of the list. The value must be a ForwardingRequest ID. */
    starting_after?: string;
  }) => Promise<{ data: (ForwardingRequest)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a ForwardingRequest
   */
  postForwardingRequests: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The PaymentMethod to insert into the forwarded request. Forwarding previously consumed PaymentMethods is allowed. */
    payment_method: string;
    /** The field kinds to be replaced in the forwarded request. */
    replacements: ("card_cvc" | "card_expiry" | "card_number" | "cardholder_name" | "request_signature")[];
    /** The request body and headers to be sent to the destination endpoint. */
    request?: { body?: string; headers?: ({ name: string; value: string })[] };
    /** The destination URL for the forwarded request. Must be supported by the config. */
    url: string;
  }) => Promise<ForwardingRequest>;

  /**
   * Retrieve a ForwardingRequest
   */
  getForwardingRequestsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ForwardingRequest>;

  /**
   * List VerificationReports
   */
  getIdentityVerificationReports: (input: {
    body?: { [key: string]: unknown };
    /** A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems. */
    client_reference_id?: string;
    /** Only return VerificationReports that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return VerificationReports of this type */
    type?: "document" | "id_number";
    /** Only return VerificationReports created by this VerificationSession ID. It is allowed to provide a VerificationIntent ID. */
    verification_session?: string;
  }) => Promise<{ data: (IdentityVerificationReport)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a VerificationReport
   */
  getIdentityVerificationReportsReport: (input: {
    body?: { [key: string]: unknown };
    report: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IdentityVerificationReport>;

  /**
   * List VerificationSessions
   */
  getIdentityVerificationSessions: (input: {
    body?: { [key: string]: unknown };
    /** A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems. */
    client_reference_id?: string;
    /** Only return VerificationSessions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Customer ID */
    related_customer?: string;
    /** The ID of the Account representing a customer. */
    related_customer_account?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return VerificationSessions with this status. [Learn more about the lifecycle of sessions](https://docs.stripe.com/identity/how-sessions-work). */
    status?: "canceled" | "processing" | "requires_input" | "verified";
  }) => Promise<{ data: (IdentityVerificationSession)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a VerificationSession
   */
  postIdentityVerificationSessions: (input: {
    /** A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems. */
    client_reference_id?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** A set of options for the session’s verification checks. */
    options?: { document?: { allowed_types?: ("driving_license" | "id_card" | "passport")[]; require_id_number?: boolean; require_live_capture?: boolean; require_matching_selfie?: boolean } | "" };
    /** Details provided about the user being verified. These details might be shown to the user. */
    provided_details?: { email?: string; phone?: string };
    /** Customer ID */
    related_customer?: string;
    /** The ID of the Account representing a customer. */
    related_customer_account?: string;
    /** Tokens referencing a Person resource and its associated account. */
    related_person?: { account: string; person: string };
    /** The URL that the user will be redirected to upon completing the verification flow. */
    return_url?: string;
    /** The type of [verification check](https://docs.stripe.com/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`. */
    type?: "document" | "id_number";
    /** The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows. */
    verification_flow?: string;
  }) => Promise<IdentityVerificationSession>;

  /**
   * Retrieve a VerificationSession
   */
  getIdentityVerificationSessionsSession: (input: {
    body?: { [key: string]: unknown };
    session: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IdentityVerificationSession>;

  /**
   * Update a VerificationSession
   */
  postIdentityVerificationSessionsSession: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** A set of options for the session’s verification checks. */
    options?: { document?: { allowed_types?: ("driving_license" | "id_card" | "passport")[]; require_id_number?: boolean; require_live_capture?: boolean; require_matching_selfie?: boolean } | "" };
    /** Details provided about the user being verified. These details may be shown to the user. */
    provided_details?: { email?: string; phone?: string };
    /** The type of [verification check](https://docs.stripe.com/identity/verification-checks) to be performed. */
    type?: "document" | "id_number";
    session: string;
  }) => Promise<IdentityVerificationSession>;

  /**
   * Cancel a VerificationSession
   */
  postIdentityVerificationSessionsSessionCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    session: string;
  }) => Promise<IdentityVerificationSession>;

  /**
   * Redact a VerificationSession
   */
  postIdentityVerificationSessionsSessionRedact: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    session: string;
  }) => Promise<IdentityVerificationSession>;

  /**
   * List all payments for an invoice
   */
  getInvoicePayments: (input: {
    body?: { [key: string]: unknown };
    /** Only return invoice payments that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The identifier of the invoice whose payments to return. */
    invoice?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The payment details of the invoice payments to return. */
    payment?: { payment_intent?: string; payment_record?: string; type: "payment_intent" | "payment_record" };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The status of the invoice payments to return. */
    status?: "canceled" | "open" | "paid";
  }) => Promise<{ data: (InvoicePayment)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an InvoicePayment
   */
  getInvoicePaymentsInvoicePayment: (input: {
    body?: { [key: string]: unknown };
    invoice_payment: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<InvoicePayment>;

  /**
   * List all invoice rendering templates
   */
  getInvoiceRenderingTemplates: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    status?: "active" | "archived";
  }) => Promise<{ data: (InvoiceRenderingTemplate)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an invoice rendering template
   */
  getInvoiceRenderingTemplatesTemplate: (input: {
    body?: { [key: string]: unknown };
    template: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    version?: number;
  }) => Promise<InvoiceRenderingTemplate>;

  /**
   * Archive an invoice rendering template
   */
  postInvoiceRenderingTemplatesTemplateArchive: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    template: string;
  }) => Promise<InvoiceRenderingTemplate>;

  /**
   * Unarchive an invoice rendering template
   */
  postInvoiceRenderingTemplatesTemplateUnarchive: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    template: string;
  }) => Promise<InvoiceRenderingTemplate>;

  /**
   * List all invoice items
   */
  getInvoiceitems: (input: {
    body?: { [key: string]: unknown };
    /** Only return invoice items that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** The identifier of the customer whose invoice items to return. If none is provided, returns all invoice items. */
    customer?: string;
    /** The identifier of the account representing the customer whose invoice items to return. If none is provided, returns all invoice items. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed. */
    invoice?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied. */
    pending?: boolean;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Invoiceitem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an invoice item
   */
  postInvoiceitems: (input: {
    /** The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice. */
    amount?: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** The ID of the customer to bill for this invoice item. */
    customer?: string;
    /** The ID of the account representing the customer to bill for this invoice item. */
    customer_account?: string;
    /** An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking. */
    description?: string;
    /** Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. */
    discountable?: boolean;
    /** The coupons and promotion codes to redeem into discounts for the invoice item or invoice line item. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of an existing invoice to add this invoice item to. For subscription invoices, when left blank, the invoice item will be added to the next upcoming scheduled invoice. For standalone invoices, the invoice item won't be automatically added unless you pass `pending_invoice_item_behavior: 'include'` when creating the invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice. */
    invoice?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The period associated with this invoice item. When set to different values, the period will be rendered on the invoice. If you have [Stripe Revenue Recognition](https://docs.stripe.com/revenue-recognition) enabled, the period will be used to recognize and defer revenue. See the [Revenue Recognition documentation](https://docs.stripe.com/revenue-recognition/methodology/subscriptions-and-invoicing) for details. */
    period?: { end: number; start: number };
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline. */
    price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** The pricing information for the invoice item. */
    pricing?: { price?: string };
    /** Non-negative integer. The quantity of units for the invoice item. Use `quantity_decimal` instead to provide decimal precision. This field will be deprecated in favor of `quantity_decimal` in a future version. */
    quantity?: number;
    /** Non-negative decimal with at most 12 decimal places. The quantity of units for the invoice item. */
    quantity_decimal?: string;
    /** The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription. */
    subscription?: string;
    /** Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    /** A [tax code](https://docs.stripe.com/tax/tax-categories) ID. */
    tax_code?: string | "";
    /** The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item. */
    tax_rates?: (string)[];
    /** The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places. */
    unit_amount_decimal?: string;
  }) => Promise<Invoiceitem>;

  /**
   * Delete an invoice item
   */
  deleteInvoiceitemsInvoiceitem: (input: {
    body?: { [key: string]: unknown };
    invoiceitem: string;
  }) => Promise<DeletedInvoiceitem>;

  /**
   * Retrieve an invoice item
   */
  getInvoiceitemsInvoiceitem: (input: {
    body?: { [key: string]: unknown };
    invoiceitem: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Invoiceitem>;

  /**
   * Update an invoice item
   */
  postInvoiceitemsInvoiceitem: (input: {
    /** The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount. */
    amount?: number;
    /** An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking. */
    description?: string;
    /** Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations. */
    discountable?: boolean;
    /** The coupons, promotion codes & existing discounts which apply to the invoice item or invoice line item. Item discounts are applied before invoice discounts. Pass an empty string to remove previously-defined discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The period associated with this invoice item. When set to different values, the period will be rendered on the invoice. If you have [Stripe Revenue Recognition](https://docs.stripe.com/revenue-recognition) enabled, the period will be used to recognize and defer revenue. See the [Revenue Recognition documentation](https://docs.stripe.com/revenue-recognition/methodology/subscriptions-and-invoicing) for details. */
    period?: { end: number; start: number };
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline. */
    price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** The pricing information for the invoice item. */
    pricing?: { price?: string };
    /** Non-negative integer. The quantity of units for the invoice item. Use `quantity_decimal` instead to provide decimal precision. This field will be deprecated in favor of `quantity_decimal` in a future version. */
    quantity?: number;
    /** Non-negative decimal with at most 12 decimal places. The quantity of units for the line item. */
    quantity_decimal?: string;
    /** Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    /** A [tax code](https://docs.stripe.com/tax/tax-categories) ID. */
    tax_code?: string | "";
    /** The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item. Pass an empty string to remove previously-defined tax rates. */
    tax_rates?: (string)[] | "";
    /** The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places. */
    unit_amount_decimal?: string;
    invoiceitem: string;
  }) => Promise<Invoiceitem>;

  /**
   * List all invoices
   */
  getInvoices: (input: {
    body?: { [key: string]: unknown };
    /** The collection method of the invoice to retrieve. Either `charge_automatically` or `send_invoice`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Only return invoices that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return invoices for the customer specified by this customer ID. */
    customer?: string;
    /** Only return invoices for the account representing the customer specified by this account ID. */
    customer_account?: string;
    due_date?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://docs.stripe.com/billing/invoices/workflow#workflow-overview) */
    status?: "draft" | "open" | "paid" | "uncollectible" | "void";
    /** Only return invoices for the subscription specified by this subscription ID. */
    subscription?: string;
  }) => Promise<{ data: (Invoice)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an invoice
   */
  postInvoices: (input: {
    /** The account tax IDs associated with the invoice. Only editable when the invoice is a draft. */
    account_tax_ids?: (string)[] | "";
    /** A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://docs.stripe.com/billing/invoices/connect#collecting-fees). */
    application_fee_amount?: number;
    /** Controls whether Stripe performs [automatic collection](https://docs.stripe.com/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action. Defaults to false. */
    auto_advance?: boolean;
    /** Settings for automatic tax lookup for this invoice. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state. */
    automatically_finalizes_at?: number;
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this invoice using the default source attached to the customer. When sending an invoice, Stripe will email this invoice to the customer with payment instructions. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** The currency to create this invoice in. Defaults to that of `customer` if not specified. */
    currency?: string;
    /** A list of up to 4 custom fields to be displayed on the invoice. */
    custom_fields?: ({ name: string; value: string })[] | "";
    /** The ID of the customer to bill. */
    customer?: string;
    /** The ID of the account to bill. */
    customer_account?: string;
    /** The number of days from when the invoice is created until it is due. Valid only for invoices where `collection_method=send_invoice`. */
    days_until_due?: number;
    /** ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings. */
    default_payment_method?: string;
    /** ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source. */
    default_source?: string;
    /** The tax rates that will apply to any line item that does not have `tax_rates` set. */
    default_tax_rates?: (string)[];
    /** An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard. */
    description?: string;
    /** The coupons and promotion codes to redeem into discounts for the invoice. If not specified, inherits the discount from the invoice's customer. Pass an empty string to avoid inheriting any discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** The date on which payment for this invoice is due. Valid only for invoices where `collection_method=send_invoice`. */
    due_date?: number;
    /** The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the invoice PDF and receipt. */
    effective_at?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Footer to be displayed on the invoice. */
    footer?: string;
    /** Revise an existing invoice. The new invoice will be created in `status=draft`. See the [revision documentation](https://docs.stripe.com/invoicing/invoice-revisions) for more details. */
    from_invoice?: { action: "revision"; invoice: string };
    /** The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account. */
    issuer?: { account?: string; type: "account" | "self" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Set the number for this invoice. If no number is present then a number will be assigned automatically when the invoice is finalized. In many markets, regulations require invoices to be unique, sequential and / or gapless. You are responsible for ensuring this is true across all your different invoicing systems in the event that you edit the invoice number using our API. If you use only Stripe for your invoices and do not change invoice numbers, Stripe handles this aspect of compliance for you automatically. */
    number?: string;
    /** The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://docs.stripe.com/billing/invoices/connect) documentation for details. */
    on_behalf_of?: string;
    /** Configuration settings for the PaymentIntent that is generated when the invoice is finalized. */
    payment_settings?: { default_mandate?: string | ""; payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { installments?: { enabled?: boolean; plan?: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } | "" }; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | "" };
    /** How to handle pending invoice items on invoice creation. Defaults to `exclude` if the parameter is omitted. */
    pending_invoice_items_behavior?: "exclude" | "include";
    /** The rendering-related settings that control how the invoice is displayed on customer-facing surfaces such as PDF and Hosted Invoice Page. */
    rendering?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; pdf?: { page_size?: "a4" | "auto" | "letter" }; template?: string; template_version?: number | "" };
    /** Settings for the cost of shipping for this invoice. */
    shipping_cost?: { shipping_rate?: string; shipping_rate_data?: { delivery_estimate?: { maximum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number }; minimum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number } }; display_name: string; fixed_amount?: { amount: number; currency: string; currency_options?: { [key: string]: { amount: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } }; metadata?: { [key: string]: string | undefined }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tax_code?: string; type?: "fixed_amount" } };
    /** Shipping details for the invoice. The Invoice PDF will use the `shipping_details` value if it is set, otherwise the PDF will render the shipping address from the customer. */
    shipping_details?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string | "" };
    /** Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`. */
    statement_descriptor?: string;
    /** The ID of the subscription to invoice, if any. If set, the created invoice will only include pending invoice items for that subscription. The subscription's billing cycle and regular subscription events won't be affected. */
    subscription?: string;
    /** If specified, the funds from the invoice will be transferred to the destination and the ID of the resulting transfer will be found on the invoice's charge. */
    transfer_data?: { amount?: number; destination: string };
  }) => Promise<Invoice>;

  /**
   * Create a preview invoice
   */
  postInvoicesCreatePreview: (input: {
    /** Settings for automatic tax lookup for this invoice preview. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** The currency to preview this invoice in. Defaults to that of `customer` if not specified. */
    currency?: string;
    /** The identifier of the customer whose upcoming invoice you're retrieving. If `automatic_tax` is enabled then one of `customer`, `customer_details`, `subscription`, or `schedule` must be set. */
    customer?: string;
    /** The identifier of the account representing the customer whose upcoming invoice you're retrieving. If `automatic_tax` is enabled then one of `customer`, `customer_account`, `customer_details`, `subscription`, or `schedule` must be set. */
    customer_account?: string;
    /** Details about the customer you want to invoice or overrides for an existing customer. If `automatic_tax` is enabled then one of `customer`, `customer_details`, `subscription`, or `schedule` must be set. */
    customer_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string } | ""; tax?: { ip_address?: string | "" }; tax_exempt?: "" | "exempt" | "none" | "reverse"; tax_ids?: ({ type: "ad_nrt" | "ae_trn" | "al_tin" | "am_tin" | "ao_tin" | "ar_cuit" | "au_abn" | "au_arn" | "aw_tin" | "az_tin" | "ba_tin" | "bb_tin" | "bd_bin" | "bf_ifu" | "bg_uic" | "bh_vat" | "bj_ifu" | "bo_tin" | "br_cnpj" | "br_cpf" | "bs_tin" | "by_tin" | "ca_bn" | "ca_gst_hst" | "ca_pst_bc" | "ca_pst_mb" | "ca_pst_sk" | "ca_qst" | "cd_nif" | "ch_uid" | "ch_vat" | "cl_tin" | "cm_niu" | "cn_tin" | "co_nit" | "cr_tin" | "cv_nif" | "de_stn" | "do_rcn" | "ec_ruc" | "eg_tin" | "es_cif" | "et_tin" | "eu_oss_vat" | "eu_vat" | "fo_vat" | "gb_vat" | "ge_vat" | "gi_tin" | "gn_nif" | "hk_br" | "hr_oib" | "hu_tin" | "id_npwp" | "il_vat" | "in_gst" | "is_vat" | "it_cf" | "jp_cn" | "jp_rn" | "jp_trn" | "ke_pin" | "kg_tin" | "kh_tin" | "kr_brn" | "kz_bin" | "la_tin" | "li_uid" | "li_vat" | "lk_vat" | "ma_vat" | "md_vat" | "me_pib" | "mk_vat" | "mr_nif" | "mx_rfc" | "my_frp" | "my_itn" | "my_sst" | "ng_tin" | "no_vat" | "no_voec" | "np_pan" | "nz_gst" | "om_vat" | "pe_ruc" | "ph_tin" | "pl_nip" | "py_ruc" | "ro_tin" | "rs_pib" | "ru_inn" | "ru_kpp" | "sa_vat" | "sg_gst" | "sg_uen" | "si_tin" | "sn_ninea" | "sr_fin" | "sv_nit" | "th_vat" | "tj_tin" | "tr_tin" | "tw_vat" | "tz_vat" | "ua_vat" | "ug_tin" | "us_ein" | "uy_ruc" | "uz_tin" | "uz_vat" | "ve_rif" | "vn_tin" | "za_vat" | "zm_tin" | "zw_tin"; value: string })[] };
    /** The coupons to redeem into discounts for the invoice preview. If not specified, inherits the discount from the subscription or customer. This works for both coupons directly applied to an invoice and coupons applied to a subscription. Pass an empty string to avoid inheriting any discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** List of invoice items to add or update in the upcoming invoice preview (up to 250). */
    invoice_items?: ({ amount?: number; currency?: string; description?: string; discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; invoiceitem?: string; metadata?: { [key: string]: string | undefined } | ""; period?: { end: number; start: number }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; quantity_decimal?: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tax_code?: string | ""; tax_rates?: (string)[] | ""; unit_amount?: number; unit_amount_decimal?: string })[];
    /** The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account. */
    issuer?: { account?: string; type: "account" | "self" };
    /** The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://docs.stripe.com/billing/invoices/connect) documentation for details. */
    on_behalf_of?: string | "";
    /** Customizes the types of values to include when calculating the invoice. Defaults to `next` if unspecified. */
    preview_mode?: "next" | "recurring";
    /** The identifier of the schedule whose upcoming invoice you'd like to retrieve. Cannot be used with subscription or subscription fields. */
    schedule?: string;
    /** The schedule creation or modification params to apply as a preview. Cannot be used with `subscription` or `subscription_` prefixed fields. */
    schedule_details?: { billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" }; end_behavior?: "cancel" | "release"; phases?: ({ add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "phase_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "phase_start" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; application_fee_percent?: number; automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } }; billing_cycle_anchor?: "automatic" | "phase_start"; billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | ""; collection_method?: "charge_automatically" | "send_invoice"; default_payment_method?: string; default_tax_rates?: (string)[] | ""; description?: string | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; end_date?: number | "now"; invoice_settings?: { account_tax_ids?: (string)[] | ""; days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } }; items: ({ billing_thresholds?: { usage_gte: number } | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string; proration_behavior?: "always_invoice" | "create_prorations" | "none"; start_date?: number | "now"; transfer_data?: { amount_percent?: number; destination: string }; trial?: boolean; trial_end?: number | "now" })[]; proration_behavior?: "always_invoice" | "create_prorations" | "none" };
    /** The identifier of the subscription for which you'd like to retrieve the upcoming invoice. If not provided, but a `subscription_details.items` is provided, you will preview creating a subscription with those items. If neither `subscription` nor `subscription_details.items` is provided, you will retrieve the next upcoming invoice from among the customer's subscriptions. */
    subscription?: string;
    /** The subscription creation or modification params to apply as a preview. Cannot be used with `schedule` or `schedule_details` fields. */
    subscription_details?: { billing_cycle_anchor?: "now" | "unchanged" | number; billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" }; billing_schedules?: ({ applies_to?: ({ price?: string; type: "price" })[]; bill_until?: { duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; timestamp?: number; type: "duration" | "timestamp" }; key?: string })[] | ""; cancel_at?: number | "" | "max_billed_until" | "max_period_end" | "min_period_end"; cancel_at_period_end?: boolean; cancel_now?: boolean; default_tax_rates?: (string)[] | ""; items?: ({ billing_thresholds?: { usage_gte: number } | ""; clear_usage?: boolean; deleted?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; id?: string; metadata?: { [key: string]: string | undefined } | ""; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; proration_behavior?: "always_invoice" | "create_prorations" | "none"; proration_date?: number; resume_at?: "now"; start_date?: number; trial_end?: "now" | number };
  }) => Promise<Invoice>;

  /**
   * Search invoices
   */
  getInvoicesSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for invoices](https://docs.stripe.com/search#query-fields-for-invoices). */
    query: string;
  }) => Promise<{ data: (Invoice)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Delete a draft invoice
   */
  deleteInvoicesInvoice: (input: {
    body?: { [key: string]: unknown };
    invoice: string;
  }) => Promise<DeletedInvoice>;

  /**
   * Retrieve an invoice
   */
  getInvoicesInvoice: (input: {
    body?: { [key: string]: unknown };
    invoice: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Invoice>;

  /**
   * Update an invoice
   */
  postInvoicesInvoice: (input: {
    /** The account tax IDs associated with the invoice. Only editable when the invoice is a draft. */
    account_tax_ids?: (string)[] | "";
    /** A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://docs.stripe.com/billing/invoices/connect#collecting-fees). */
    application_fee_amount?: number;
    /** Controls whether Stripe performs [automatic collection](https://docs.stripe.com/invoicing/integration/automatic-advancement-collection) of the invoice. */
    auto_advance?: boolean;
    /** Settings for automatic tax lookup for this invoice. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state. To turn off automatic finalization, set `auto_advance` to false. */
    automatically_finalizes_at?: number;
    /** Either `charge_automatically` or `send_invoice`. This field can be updated only on `draft` invoices. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** A list of up to 4 custom fields to be displayed on the invoice. If a value for `custom_fields` is specified, the list specified will replace the existing custom field list on this invoice. Pass an empty string to remove previously-defined fields. */
    custom_fields?: ({ name: string; value: string })[] | "";
    /** The number of days from which the invoice is created until it is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices. */
    days_until_due?: number;
    /** ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings. */
    default_payment_method?: string;
    /** ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source. */
    default_source?: string | "";
    /** The tax rates that will apply to any line item that does not have `tax_rates` set. Pass an empty string to remove previously-defined tax rates. */
    default_tax_rates?: (string)[] | "";
    /** An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard. */
    description?: string;
    /** The discounts that will apply to the invoice. Pass an empty string to remove previously-defined discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** The date on which payment for this invoice is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices. */
    due_date?: number;
    /** The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the invoice PDF and receipt. */
    effective_at?: number | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Footer to be displayed on the invoice. */
    footer?: string;
    /** The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account. */
    issuer?: { account?: string; type: "account" | "self" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Set the number for this invoice. If no number is present then a number will be assigned automatically when the invoice is finalized. In many markets, regulations require invoices to be unique, sequential and / or gapless. You are responsible for ensuring this is true across all your different invoicing systems in the event that you edit the invoice number using our API. If you use only Stripe for your invoices and do not change invoice numbers, Stripe handles this aspect of compliance for you automatically. */
    number?: string | "";
    /** The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://docs.stripe.com/billing/invoices/connect) documentation for details. */
    on_behalf_of?: string | "";
    /** Configuration settings for the PaymentIntent that is generated when the invoice is finalized. */
    payment_settings?: { default_mandate?: string | ""; payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { installments?: { enabled?: boolean; plan?: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } | "" }; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | "" };
    /** The rendering-related settings that control how the invoice is displayed on customer-facing surfaces such as PDF and Hosted Invoice Page. */
    rendering?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; pdf?: { page_size?: "a4" | "auto" | "letter" }; template?: string; template_version?: number | "" };
    /** Settings for the cost of shipping for this invoice. */
    shipping_cost?: { shipping_rate?: string; shipping_rate_data?: { delivery_estimate?: { maximum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number }; minimum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number } }; display_name: string; fixed_amount?: { amount: number; currency: string; currency_options?: { [key: string]: { amount: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } }; metadata?: { [key: string]: string | undefined }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tax_code?: string; type?: "fixed_amount" } } | "";
    /** Shipping details for the invoice. The Invoice PDF will use the `shipping_details` value if it is set, otherwise the PDF will render the shipping address from the customer. */
    shipping_details?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string | "" } | "";
    /** Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`. */
    statement_descriptor?: string;
    /** If specified, the funds from the invoice will be transferred to the destination and the ID of the resulting transfer will be found on the invoice's charge. This will be unset if you POST an empty value. */
    transfer_data?: { amount?: number; destination: string } | "";
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Bulk add invoice line items
   */
  postInvoicesInvoiceAddLines: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    invoice_metadata?: { [key: string]: string | undefined } | "";
    /** The line items to add. */
    lines: ({ amount?: number; description?: string; discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; invoice_item?: string; metadata?: { [key: string]: string | undefined } | ""; period?: { end: number; start: number }; price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; pricing?: { price?: string }; quantity?: number; quantity_decimal?: string; tax_amounts?: ({ amount: number; tax_rate_data: { country?: string; description?: string; display_name: string; inclusive: boolean; jurisdiction?: string; jurisdiction_level?: "city" | "country" | "county" | "district" | "multiple" | "state"; percentage: number; state?: string; tax_type?: "amusement_tax" | "communications_tax" | "gst" | "hst" | "igst" | "jct" | "lease_tax" | "pst" | "qst" | "retail_delivery_fee" | "rst" | "sales_tax" | "service_tax" | "vat" }; taxability_reason?: "customer_exempt" | "not_collecting" | "not_subject_to_tax" | "not_supported" | "portion_product_exempt" | "portion_reduced_rated" | "portion_standard_rated" | "product_exempt" | "product_exempt_holiday" | "proportionally_rated" | "reduced_rated" | "reverse_charge" | "standard_rated" | "taxable_basis_reduced" | "zero_rated"; taxable_amount: number })[] | ""; tax_rates?: (string)[] | "" })[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Attach a payment to an Invoice
   */
  postInvoicesInvoiceAttachPayment: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of the PaymentIntent to attach to the invoice. */
    payment_intent?: string;
    /** The ID of the PaymentRecord to attach to the invoice. */
    payment_record?: string;
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Finalize an invoice
   */
  postInvoicesInvoiceFinalize: (input: {
    /** Controls whether Stripe performs [automatic collection](https://docs.stripe.com/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action. */
    auto_advance?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Retrieve an invoice's line items
   */
  getInvoicesInvoiceLines: (input: {
    body?: { [key: string]: unknown };
    invoice: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (LineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Update an invoice's line item
   */
  postInvoicesInvoiceLinesLineItemId: (input: {
    /** The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount. */
    amount?: number;
    /** An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking. */
    description?: string;
    /** Controls whether discounts apply to this line item. Defaults to false for prorations or negative line items, and true for all other line items. Cannot be set to true for prorations. */
    discountable?: boolean;
    /** The coupons, promotion codes & existing discounts which apply to the line item. Item discounts are applied before invoice discounts. Pass an empty string to remove previously-defined discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. For [type=subscription](/api/invoices/line_item) line items, the incoming metadata specified on the request is directly used to set this value, in contrast to [type=invoiceitem](/api/invoices/line_item) line items, where any existing metadata on the invoice line is merged with the incoming data. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The period associated with this invoice item. When set to different values, the period will be rendered on the invoice. If you have [Stripe Revenue Recognition](https://docs.stripe.com/revenue-recognition) enabled, the period will be used to recognize and defer revenue. See the [Revenue Recognition documentation](https://docs.stripe.com/revenue-recognition/methodology/subscriptions-and-invoicing) for details. */
    period?: { end: number; start: number };
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline. */
    price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** The pricing information for the invoice item. */
    pricing?: { price?: string };
    /** Non-negative integer. The quantity of units for the line item. Use `quantity_decimal` instead to provide decimal precision. This field will be deprecated in favor of `quantity_decimal` in a future version. */
    quantity?: number;
    /** Non-negative decimal with at most 12 decimal places. The quantity of units for the line item. */
    quantity_decimal?: string;
    /** A list of up to 10 tax amounts for this line item. This can be useful if you calculate taxes on your own or use a third-party to calculate them. You cannot set tax amounts if any line item has [tax_rates](https://docs.stripe.com/api/invoices/line_item#invoice_line_item_object-tax_rates) or if the invoice has [default_tax_rates](https://docs.stripe.com/api/invoices/object#invoice_object-default_tax_rates) or uses [automatic tax](https://docs.stripe.com/tax/invoicing). Pass an empty string to remove previously defined tax amounts. */
    tax_amounts?: ({ amount: number; tax_rate_data: { country?: string; description?: string; display_name: string; inclusive: boolean; jurisdiction?: string; jurisdiction_level?: "city" | "country" | "county" | "district" | "multiple" | "state"; percentage: number; state?: string; tax_type?: "amusement_tax" | "communications_tax" | "gst" | "hst" | "igst" | "jct" | "lease_tax" | "pst" | "qst" | "retail_delivery_fee" | "rst" | "sales_tax" | "service_tax" | "vat" }; taxability_reason?: "customer_exempt" | "not_collecting" | "not_subject_to_tax" | "not_supported" | "portion_product_exempt" | "portion_reduced_rated" | "portion_standard_rated" | "product_exempt" | "product_exempt_holiday" | "proportionally_rated" | "reduced_rated" | "reverse_charge" | "standard_rated" | "taxable_basis_reduced" | "zero_rated"; taxable_amount: number })[] | "";
    /** The tax rates which apply to the line item. When set, the `default_tax_rates` on the invoice do not apply to this line item. Pass an empty string to remove previously-defined tax rates. */
    tax_rates?: (string)[] | "";
    /** Invoice ID of line item */
    invoice: string;
    /** Invoice line item ID */
    line_item_id: string;
  }) => Promise<LineItem>;

  /**
   * Mark an invoice as uncollectible
   */
  postInvoicesInvoiceMarkUncollectible: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Pay an invoice
   */
  postInvoicesInvoicePay: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** In cases where the source used to pay the invoice has insufficient funds, passing `forgive=true` controls whether a charge should be attempted for the full amount available on the source, up to the amount to fully pay the invoice. This effectively forgives the difference between the amount available on the source and the amount due.   Passing `forgive=false` will fail the charge if the source hasn't been pre-funded with the right amount. An example for this case is with ACH Credit Transfers and wires: if the amount wired is less than the amount due by a small amount, you might want to forgive the difference. Defaults to `false`. */
    forgive?: boolean;
    /** ID of the mandate to be used for this invoice. It must correspond to the payment method used to pay the invoice, including the payment_method param or the invoice's default_payment_method or default_source, if set. */
    mandate?: string | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `true` (off-session). */
    off_session?: boolean;
    /** Boolean representing whether an invoice is paid outside of Stripe. This will result in no charge being made. Defaults to `false`. */
    paid_out_of_band?: boolean;
    /** A PaymentMethod to be charged. The PaymentMethod must be the ID of a PaymentMethod belonging to the customer associated with the invoice being paid. */
    payment_method?: string;
    /** A payment source to be charged. The source must be the ID of a source belonging to the customer associated with the invoice being paid. */
    source?: string;
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Bulk remove invoice line items
   */
  postInvoicesInvoiceRemoveLines: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    invoice_metadata?: { [key: string]: string | undefined } | "";
    /** The line items to remove. */
    lines: ({ behavior: "delete" | "unassign"; id: string })[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Send an invoice for manual payment
   */
  postInvoicesInvoiceSend: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Bulk update invoice line items
   */
  postInvoicesInvoiceUpdateLines: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. For [type=subscription](https://docs.stripe.com/api/invoices/line_item#invoice_line_item_object-type) line items, the incoming metadata specified on the request is directly used to set this value, in contrast to [type=invoiceitem](api/invoices/line_item#invoice_line_item_object-type) line items, where any existing metadata on the invoice line is merged with the incoming data. */
    invoice_metadata?: { [key: string]: string | undefined } | "";
    /** The line items to update. */
    lines: ({ amount?: number; description?: string; discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; id: string; metadata?: { [key: string]: string | undefined } | ""; period?: { end: number; start: number }; price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; pricing?: { price?: string }; quantity?: number; quantity_decimal?: string; tax_amounts?: ({ amount: number; tax_rate_data: { country?: string; description?: string; display_name: string; inclusive: boolean; jurisdiction?: string; jurisdiction_level?: "city" | "country" | "county" | "district" | "multiple" | "state"; percentage: number; state?: string; tax_type?: "amusement_tax" | "communications_tax" | "gst" | "hst" | "igst" | "jct" | "lease_tax" | "pst" | "qst" | "retail_delivery_fee" | "rst" | "sales_tax" | "service_tax" | "vat" }; taxability_reason?: "customer_exempt" | "not_collecting" | "not_subject_to_tax" | "not_supported" | "portion_product_exempt" | "portion_reduced_rated" | "portion_standard_rated" | "product_exempt" | "product_exempt_holiday" | "proportionally_rated" | "reduced_rated" | "reverse_charge" | "standard_rated" | "taxable_basis_reduced" | "zero_rated"; taxable_amount: number })[] | ""; tax_rates?: (string)[] | "" })[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * Void an invoice
   */
  postInvoicesInvoiceVoid: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    invoice: string;
  }) => Promise<Invoice>;

  /**
   * List all authorizations
   */
  getIssuingAuthorizations: (input: {
    body?: { [key: string]: unknown };
    /** Only return authorizations that belong to the given card. */
    card?: string;
    /** Only return authorizations that belong to the given cardholder. */
    cardholder?: string;
    /** Only return authorizations that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return authorizations with the given status. One of `pending`, `closed`, or `reversed`. */
    status?: "closed" | "expired" | "pending" | "reversed";
  }) => Promise<{ data: (IssuingAuthorization)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an authorization
   */
  getIssuingAuthorizationsAuthorization: (input: {
    body?: { [key: string]: unknown };
    authorization: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingAuthorization>;

  /**
   * Update an authorization
   */
  postIssuingAuthorizationsAuthorization: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Approve an authorization
   */
  postIssuingAuthorizationsAuthorizationApprove: (input: {
    /** If the authorization's `pending_request.is_amount_controllable` property is `true`, you may provide this value to control how much to hold for the authorization. Must be positive (use [`decline`](https://docs.stripe.com/api/issuing/authorizations/decline) to decline an authorization request). */
    amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Decline an authorization
   */
  postIssuingAuthorizationsAuthorizationDecline: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * List all cardholders
   */
  getIssuingCardholders: (input: {
    body?: { [key: string]: unknown };
    /** Only return cardholders that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return cardholders that have the given email address. */
    email?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return cardholders that have the given phone number. */
    phone_number?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return cardholders that have the given status. One of `active`, `inactive`, or `blocked`. */
    status?: "active" | "blocked" | "inactive";
    /** Only return cardholders that have the given type. One of `individual` or `company`. */
    type?: "company" | "individual";
  }) => Promise<{ data: (IssuingCardholder)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a cardholder
   */
  postIssuingCardholders: (input: {
    /** The cardholder's billing address. */
    billing: { address: { city: string; country: string; line1: string; line2?: string; postal_code: string; state?: string } };
    /** Additional information about a `company` cardholder. */
    company?: { tax_id?: string };
    /** The cardholder's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Additional information about an `individual` cardholder. */
    individual?: { card_issuing?: { user_terms_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } }; dob?: { day: number; month: number; year: number }; first_name?: string; last_name?: string; verification?: { document?: { back?: string; front?: string } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The cardholder's name. This will be printed on cards issued to them. The maximum length of this field is 24 characters. This field cannot contain any special characters or numbers. */
    name: string;
    /** The cardholder's phone number. This will be transformed to [E.164](https://en.wikipedia.org/wiki/E.164) if it is not provided in that format already. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://docs.stripe.com/issuing/3d-secure#when-is-3d-secure-applied) for more details. */
    phone_number?: string;
    /** The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`, `fr`, `it`, `pl`, or `sv`.  This changes the language of the [3D Secure flow](https://docs.stripe.com/issuing/3d-secure) and one-time password messages sent to the cardholder. */
    preferred_locales?: ("de" | "en" | "es" | "fr" | "it")[];
    /** Rules that control spending across this cardholder's cards. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spending-controls) for more details. */
    spending_controls?: { allowed_card_presences?: ("not_present" | "present")[]; allowed_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; allowed_merchant_countries?: (string)[]; blocked_card_presences?: ("not_present" | "present")[]; blocked_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; blocked_merchant_countries?: (string)[]; spending_limits?: ({ amount: number; categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; interval: "all_time" | "daily" | "monthly" | "per_authorization" | "weekly" | "yearly" })[]; spending_limits_currency?: string };
    /** Specifies whether to permit authorizations on this cardholder's cards. Defaults to `active`. */
    status?: "active" | "inactive";
    /** One of `individual` or `company`. See [Choose a cardholder type](https://docs.stripe.com/issuing/other/choose-cardholder) for more details. */
    type?: "company" | "individual";
  }) => Promise<IssuingCardholder>;

  /**
   * Retrieve a cardholder
   */
  getIssuingCardholdersCardholder: (input: {
    body?: { [key: string]: unknown };
    cardholder: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingCardholder>;

  /**
   * Update a cardholder
   */
  postIssuingCardholdersCardholder: (input: {
    /** The cardholder's billing address. */
    billing?: { address: { city: string; country: string; line1: string; line2?: string; postal_code: string; state?: string } };
    /** Additional information about a `company` cardholder. */
    company?: { tax_id?: string };
    /** The cardholder's email address. */
    email?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Additional information about an `individual` cardholder. */
    individual?: { card_issuing?: { user_terms_acceptance?: { date?: number; ip?: string; user_agent?: string | "" } }; dob?: { day: number; month: number; year: number }; first_name?: string; last_name?: string; verification?: { document?: { back?: string; front?: string } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://docs.stripe.com/issuing/3d-secure) for more details. */
    phone_number?: string;
    /** The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`, `fr`, `it`, `pl`, or `sv`.  This changes the language of the [3D Secure flow](https://docs.stripe.com/issuing/3d-secure) and one-time password messages sent to the cardholder. */
    preferred_locales?: ("de" | "en" | "es" | "fr" | "it")[];
    /** Rules that control spending across this cardholder's cards. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spending-controls) for more details. */
    spending_controls?: { allowed_card_presences?: ("not_present" | "present")[]; allowed_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; allowed_merchant_countries?: (string)[]; blocked_card_presences?: ("not_present" | "present")[]; blocked_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; blocked_merchant_countries?: (string)[]; spending_limits?: ({ amount: number; categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; interval: "all_time" | "daily" | "monthly" | "per_authorization" | "weekly" | "yearly" })[]; spending_limits_currency?: string };
    /** Specifies whether to permit authorizations on this cardholder's cards. */
    status?: "active" | "inactive";
    cardholder: string;
  }) => Promise<IssuingCardholder>;

  /**
   * List all cards
   */
  getIssuingCards: (input: {
    body?: { [key: string]: unknown };
    /** Only return cards belonging to the Cardholder with the provided ID. */
    cardholder?: string;
    /** Only return cards that were issued during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Only return cards that have the given expiration month. */
    exp_month?: number;
    /** Only return cards that have the given expiration year. */
    exp_year?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return cards that have the given last four digits. */
    last4?: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    personalization_design?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return cards that have the given status. One of `active`, `inactive`, or `canceled`. */
    status?: "active" | "canceled" | "inactive";
    /** Only return cards that have the given type. One of `virtual` or `physical`. */
    type?: "physical" | "virtual";
  }) => Promise<{ data: (IssuingCard)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a card
   */
  postIssuingCards: (input: {
    /** The [Cardholder](https://docs.stripe.com/api#issuing_cardholder_object) object with which the card will be associated. */
    cardholder?: string;
    /** The currency for the card. */
    currency: string;
    /** The desired expiration month (1-12) for this card if [specifying a custom expiration date](/issuing/cards/virtual/issue-cards?testing-method=with-code#exp-dates). */
    exp_month?: number;
    /** The desired 4-digit expiration year for this card if [specifying a custom expiration date](/issuing/cards/virtual/issue-cards?testing-method=with-code#exp-dates). */
    exp_year?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The new financial account ID the card will be associated with. This field allows a card to be reassigned to a different financial account. */
    financial_account?: string;
    /** Rules that control the lifecycle of this card, such as automatic cancellation. Refer to our [documentation](/issuing/controls/lifecycle-controls) for more details. */
    lifecycle_controls?: { cancel_after: { payment_count: number } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The personalization design object belonging to this card. */
    personalization_design?: string;
    /** The desired PIN for this card. */
    pin?: { encrypted_number?: string };
    /** The card this is meant to be a replacement for (if any). */
    replacement_for?: string;
    /** If `replacement_for` is specified, this should indicate why that card is being replaced. */
    replacement_reason?: "damaged" | "expired" | "lost" | "stolen";
    /** The second line to print on the card. Max length: 24 characters. */
    second_line?: string | "";
    /** The address where the card will be shipped. */
    shipping?: { address: { city: string; country: string; line1: string; line2?: string; postal_code: string; state?: string }; address_validation?: { mode: "disabled" | "normalization_only" | "validation_and_normalization" }; customs?: { eori_number?: string }; name: string; phone_number?: string; require_signature?: boolean; service?: "express" | "priority" | "standard"; type?: "bulk" | "individual" };
    /** Rules that control spending for this card. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spending-controls) for more details. */
    spending_controls?: { allowed_card_presences?: ("not_present" | "present")[]; allowed_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; allowed_merchant_countries?: (string)[]; blocked_card_presences?: ("not_present" | "present")[]; blocked_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; blocked_merchant_countries?: (string)[]; spending_limits?: ({ amount: number; categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; interval: "all_time" | "daily" | "monthly" | "per_authorization" | "weekly" | "yearly" })[] };
    /** Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`. */
    status?: "active" | "inactive";
    /** The type of card to issue. Possible values are `physical` or `virtual`. */
    type: "physical" | "virtual";
  }) => Promise<IssuingCard>;

  /**
   * Retrieve a card
   */
  getIssuingCardsCard: (input: {
    body?: { [key: string]: unknown };
    card: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingCard>;

  /**
   * Update a card
   */
  postIssuingCardsCard: (input: {
    /** Reason why the `status` of this card is `canceled`. */
    cancellation_reason?: "lost" | "stolen";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    personalization_design?: string;
    /** The desired new PIN for this card. */
    pin?: { encrypted_number?: string };
    /** Updated shipping information for the card. */
    shipping?: { address: { city: string; country: string; line1: string; line2?: string; postal_code: string; state?: string }; address_validation?: { mode: "disabled" | "normalization_only" | "validation_and_normalization" }; customs?: { eori_number?: string }; name: string; phone_number?: string; require_signature?: boolean; service?: "express" | "priority" | "standard"; type?: "bulk" | "individual" };
    /** Rules that control spending for this card. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spending-controls) for more details. */
    spending_controls?: { allowed_card_presences?: ("not_present" | "present")[]; allowed_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; allowed_merchant_countries?: (string)[]; blocked_card_presences?: ("not_present" | "present")[]; blocked_categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; blocked_merchant_countries?: (string)[]; spending_limits?: ({ amount: number; categories?: ("ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards")[]; interval: "all_time" | "daily" | "monthly" | "per_authorization" | "weekly" | "yearly" })[] };
    /** Dictates whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`. If this card is being canceled because it was lost or stolen, this information should be provided as `cancellation_reason`. */
    status?: "active" | "canceled" | "inactive";
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * List all disputes
   */
  getIssuingDisputes: (input: {
    body?: { [key: string]: unknown };
    /** Only return Issuing disputes that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Select Issuing disputes with the given status. */
    status?: "expired" | "lost" | "submitted" | "unsubmitted" | "won";
    /** Select the Issuing dispute for the given transaction. */
    transaction?: string;
  }) => Promise<{ data: (IssuingDispute)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a dispute
   */
  postIssuingDisputes: (input: {
    /** The dispute amount in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). If not set, defaults to the full transaction amount. */
    amount?: number;
    /** Evidence provided for the dispute. */
    evidence?: { canceled?: { additional_documentation?: string | ""; canceled_at?: number | ""; cancellation_policy_provided?: boolean | ""; cancellation_reason?: string | ""; expected_at?: number | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service"; return_status?: "" | "merchant_rejected" | "successful"; returned_at?: number | "" } | ""; duplicate?: { additional_documentation?: string | ""; card_statement?: string | ""; cash_receipt?: string | ""; check_image?: string | ""; explanation?: string | ""; original_transaction?: string } | ""; fraudulent?: { additional_documentation?: string | ""; explanation?: string | "" } | ""; merchandise_not_as_described?: { additional_documentation?: string | ""; explanation?: string | ""; received_at?: number | ""; return_description?: string | ""; return_status?: "" | "merchant_rejected" | "successful"; returned_at?: number | "" } | ""; no_valid_authorization?: { additional_documentation?: string | ""; explanation?: string | "" } | ""; not_received?: { additional_documentation?: string | ""; expected_at?: number | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service" } | ""; other?: { additional_documentation?: string | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service" } | ""; reason?: "canceled" | "duplicate" | "fraudulent" | "merchandise_not_as_described" | "no_valid_authorization" | "not_received" | "other" | "service_not_as_described"; service_not_as_described?: { additional_documentation?: string | ""; canceled_at?: number | ""; cancellation_reason?: string | ""; explanation?: string | ""; received_at?: number | "" } | "" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The ID of the issuing transaction to create a dispute for. For transaction on Treasury FinancialAccounts, use `treasury.received_debit`. */
    transaction?: string;
    /** Params for disputes related to Treasury FinancialAccounts */
    treasury?: { received_debit: string };
  }) => Promise<IssuingDispute>;

  /**
   * Retrieve a dispute
   */
  getIssuingDisputesDispute: (input: {
    body?: { [key: string]: unknown };
    dispute: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingDispute>;

  /**
   * Update a dispute
   */
  postIssuingDisputesDispute: (input: {
    /** The dispute amount in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    amount?: number;
    /** Evidence provided for the dispute. */
    evidence?: { canceled?: { additional_documentation?: string | ""; canceled_at?: number | ""; cancellation_policy_provided?: boolean | ""; cancellation_reason?: string | ""; expected_at?: number | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service"; return_status?: "" | "merchant_rejected" | "successful"; returned_at?: number | "" } | ""; duplicate?: { additional_documentation?: string | ""; card_statement?: string | ""; cash_receipt?: string | ""; check_image?: string | ""; explanation?: string | ""; original_transaction?: string } | ""; fraudulent?: { additional_documentation?: string | ""; explanation?: string | "" } | ""; merchandise_not_as_described?: { additional_documentation?: string | ""; explanation?: string | ""; received_at?: number | ""; return_description?: string | ""; return_status?: "" | "merchant_rejected" | "successful"; returned_at?: number | "" } | ""; no_valid_authorization?: { additional_documentation?: string | ""; explanation?: string | "" } | ""; not_received?: { additional_documentation?: string | ""; expected_at?: number | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service" } | ""; other?: { additional_documentation?: string | ""; explanation?: string | ""; product_description?: string | ""; product_type?: "" | "merchandise" | "service" } | ""; reason?: "canceled" | "duplicate" | "fraudulent" | "merchandise_not_as_described" | "no_valid_authorization" | "not_received" | "other" | "service_not_as_described"; service_not_as_described?: { additional_documentation?: string | ""; canceled_at?: number | ""; cancellation_reason?: string | ""; explanation?: string | ""; received_at?: number | "" } | "" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    dispute: string;
  }) => Promise<IssuingDispute>;

  /**
   * Submit a dispute
   */
  postIssuingDisputesDisputeSubmit: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    dispute: string;
  }) => Promise<IssuingDispute>;

  /**
   * List all personalization designs
   */
  getIssuingPersonalizationDesigns: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return personalization designs with the given lookup keys. */
    lookup_keys?: (string)[];
    /** Only return personalization designs with the given preferences. */
    preferences?: { is_default?: boolean; is_platform_default?: boolean };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return personalization designs with the given status. */
    status?: "active" | "inactive" | "rejected" | "review";
  }) => Promise<{ data: (IssuingPersonalizationDesign)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a personalization design
   */
  postIssuingPersonalizationDesigns: (input: {
    /** The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`. */
    card_logo?: string;
    /** Hash containing carrier text, for use with physical bundles that support carrier text. */
    carrier_text?: { footer_body?: string | ""; footer_title?: string | ""; header_body?: string | ""; header_title?: string | "" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters. */
    lookup_key?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Friendly display name. */
    name?: string;
    /** The physical bundle object belonging to this personalization design. */
    physical_bundle: string;
    /** Information on whether this personalization design is used to create cards when one is not specified. */
    preferences?: { is_default: boolean };
    /** If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design. */
    transfer_lookup_key?: boolean;
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * Retrieve a personalization design
   */
  getIssuingPersonalizationDesignsPersonalizationDesign: (input: {
    body?: { [key: string]: unknown };
    personalization_design: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * Update a personalization design
   */
  postIssuingPersonalizationDesignsPersonalizationDesign: (input: {
    /** The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`. */
    card_logo?: string | "";
    /** Hash containing carrier text, for use with physical bundles that support carrier text. */
    carrier_text?: { footer_body?: string | ""; footer_title?: string | ""; header_body?: string | ""; header_title?: string | "" } | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters. */
    lookup_key?: string | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Friendly display name. Providing an empty string will set the field to null. */
    name?: string | "";
    /** The physical bundle object belonging to this personalization design. */
    physical_bundle?: string;
    /** Information on whether this personalization design is used to create cards when one is not specified. */
    preferences?: { is_default: boolean };
    /** If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design. */
    transfer_lookup_key?: boolean;
    personalization_design: string;
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * List all physical bundles
   */
  getIssuingPhysicalBundles: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return physical bundles with the given status. */
    status?: "active" | "inactive" | "review";
    /** Only return physical bundles with the given type. */
    type?: "custom" | "standard";
  }) => Promise<{ data: (IssuingPhysicalBundle)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a physical bundle
   */
  getIssuingPhysicalBundlesPhysicalBundle: (input: {
    body?: { [key: string]: unknown };
    physical_bundle: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingPhysicalBundle>;

  /**
   * Retrieve a settlement
   */
  getIssuingSettlementsSettlement: (input: {
    body?: { [key: string]: unknown };
    settlement: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingSettlement>;

  /**
   * Update a settlement
   */
  postIssuingSettlementsSettlement: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    settlement: string;
  }) => Promise<IssuingSettlement>;

  /**
   * List all issuing tokens for card
   */
  getIssuingTokens: (input: {
    body?: { [key: string]: unknown };
    /** The Issuing card identifier to list tokens for. */
    card: string;
    /** Only return Issuing tokens that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Select Issuing tokens with the given status. */
    status?: "active" | "deleted" | "requested" | "suspended";
  }) => Promise<{ data: (IssuingToken)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an issuing token
   */
  getIssuingTokensToken: (input: {
    body?: { [key: string]: unknown };
    token: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingToken>;

  /**
   * Update a token status
   */
  postIssuingTokensToken: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Specifies which status the token should be updated to. */
    status: "active" | "deleted" | "suspended";
    token: string;
  }) => Promise<IssuingToken>;

  /**
   * List all transactions
   */
  getIssuingTransactions: (input: {
    body?: { [key: string]: unknown };
    /** Only return transactions that belong to the given card. */
    card?: string;
    /** Only return transactions that belong to the given cardholder. */
    cardholder?: string;
    /** Only return transactions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return transactions that have the given type. One of `capture` or `refund`. */
    type?: "capture" | "refund";
  }) => Promise<{ data: (IssuingTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a transaction
   */
  getIssuingTransactionsTransaction: (input: {
    body?: { [key: string]: unknown };
    transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<IssuingTransaction>;

  /**
   * Update a transaction
   */
  postIssuingTransactionsTransaction: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    transaction: string;
  }) => Promise<IssuingTransaction>;

  /**
   * Create a Session
   */
  postLinkAccountSessions: (input: {
    /** The account holder to link accounts for. */
    account_holder: { account?: string; customer?: string; customer_account?: string; type: "account" | "customer" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Filters to restrict the kinds of accounts to collect. */
    filters?: { account_subcategories?: ("checking" | "credit_card" | "line_of_credit" | "mortgage" | "savings")[]; countries?: (string)[] };
    /** List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`. */
    permissions: ("balances" | "ownership" | "payment_method" | "transactions")[];
    /** List of data features that you would like to retrieve upon account creation. */
    prefetch?: ("balances" | "ownership" | "transactions")[];
    /** For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app. */
    return_url?: string;
  }) => Promise<FinancialConnectionsSession>;

  /**
   * Retrieve a Session
   */
  getLinkAccountSessionsSession: (input: {
    body?: { [key: string]: unknown };
    session: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FinancialConnectionsSession>;

  /**
   * List Accounts
   */
  getLinkedAccounts: (input: {
    body?: { [key: string]: unknown };
    /** If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive. */
    account_holder?: { account?: string; customer?: string; customer_account?: string };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** If present, only return accounts that were collected as part of the given session. */
    session?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FinancialConnectionsAccount)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an Account
   */
  getLinkedAccountsAccount: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Disconnect an Account
   */
  postLinkedAccountsAccountDisconnect: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * List Account Owners
   */
  getLinkedAccountsAccountOwners: (input: {
    body?: { [key: string]: unknown };
    account: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The ID of the ownership object to fetch owners from. */
    ownership: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (FinancialConnectionsAccountOwner)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Refresh Account data
   */
  postLinkedAccountsAccountRefresh: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The list of account features that you would like to refresh. */
    features: ("balance" | "ownership" | "transactions")[];
    account: string;
  }) => Promise<FinancialConnectionsAccount>;

  /**
   * Retrieve a Mandate
   */
  getMandatesMandate: (input: {
    body?: { [key: string]: unknown };
    mandate: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Mandate>;

  /**
   * List Payment Attempt Records
   */
  getPaymentAttemptRecords: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The ID of the Payment Record. */
    payment_record: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentAttemptRecord)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Payment Attempt Record
   */
  getPaymentAttemptRecordsId: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the Payment Attempt Record. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentAttemptRecord>;

  /**
   * List all PaymentIntents
   */
  getPaymentIntents: (input: {
    body?: { [key: string]: unknown };
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return PaymentIntents for the customer that this customer ID specifies. */
    customer?: string;
    /** Only return PaymentIntents for the account representing the customer that this ID specifies. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentIntent)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a PaymentIntent
   */
  postPaymentIntents: (input: {
    /** Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99). */
    amount: number;
    /** Provides industry-specific information about the amount. */
    amount_details?: { discount_amount?: number | ""; enforce_arithmetic_validation?: boolean; line_items?: ({ discount_amount?: number; payment_method_options?: { card?: { commodity_code?: string }; card_present?: { commodity_code?: string }; klarna?: { image_url?: string; product_url?: string; reference?: string; subscription_reference?: string }; paypal?: { category?: "digital_goods" | "donation" | "physical_goods"; description?: string; sold_by?: string } }; product_code?: string; product_name: string; quantity: number; tax?: { total_tax_amount: number }; unit_cost: number; unit_of_measure?: string })[] | ""; shipping?: { amount?: number | ""; from_postal_code?: string | ""; to_postal_code?: string | "" } | ""; tax?: { total_tax_amount: number } | "" };
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    application_fee_amount?: number;
    /** When you enable this parameter, this PaymentIntent accepts payment methods that you enable in the Dashboard and that are compatible with this PaymentIntent's other parameters. */
    automatic_payment_methods?: { allow_redirects?: "always" | "never"; enabled: boolean };
    /** Controls when the funds will be captured from the customer's account. */
    capture_method?: "automatic" | "automatic_async" | "manual";
    /** Set to `true` to attempt to [confirm this PaymentIntent](https://docs.stripe.com/api/payment_intents/confirm) immediately. This parameter defaults to `false`. When creating and confirming a PaymentIntent at the same time, you can also provide the parameters available in the [Confirm API](https://docs.stripe.com/api/payment_intents/confirm). */
    confirm?: boolean;
    /** Describes whether we can confirm this PaymentIntent automatically, or if it requires customer action to confirm the payment. */
    confirmation_method?: "automatic" | "manual";
    /** ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence. */
    confirmation_token?: string;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead. */
    customer?: string;
    /** ID of the Account representing the customer that this PaymentIntent belongs to, if one exists.  Payment methods attached to other Accounts cannot be used with this PaymentIntent.  If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Account after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Account instead. */
    customer_account?: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. Use this parameter for simpler integrations that don't handle customer actions, such as [saving cards without authentication](https://docs.stripe.com/payments/save-card-without-authentication). This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-confirm). */
    error_on_requires_action?: boolean;
    /** The list of payment method types to exclude from use with this payment. */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Automations to be run during the PaymentIntent lifecycle */
    hooks?: { inputs?: { tax?: { calculation: string | "" } } };
    /** ID of the mandate that's used for this payment. This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-confirm). */
    mandate?: string;
    /** This hash contains details about the Mandate to create. This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-confirm). */
    mandate_data?: { customer_acceptance: { accepted_at?: number; offline?: { [key: string]: unknown }; online?: { ip_address: string; user_agent: string }; type: "offline" | "online" } } | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Set to `true` to indicate that the customer isn't in your checkout flow during this payment attempt and can't authenticate. Use this parameter in scenarios where you collect payment method details and [charge them later](https://docs.stripe.com/payments/save-during-payment). This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-confirm). */
    off_session?: boolean | "one_off" | "recurring";
    /** The Stripe account ID that these funds are intended for. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    on_behalf_of?: string;
    /** Provides industry-specific information about the charge. */
    payment_details?: { customer_reference?: string | ""; order_reference?: string | "" };
    /** ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://docs.stripe.com/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent.  If you omit this parameter with `confirm=true`, `customer.default_source` attaches as this PaymentIntent's payment instrument to improve migration for users of the Charges API. We recommend that you explicitly provide the `payment_method` moving forward. If the payment method is attached to a Customer, you must also provide the ID of that Customer as the [customer](https://api.stripe.com#create_payment_intent-customer) parameter of this PaymentIntent. */
    payment_method?: string;
    /** The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with this PaymentIntent. */
    payment_method_configuration?: string;
    /** If provided, this hash will be used to create a PaymentMethod. The new PaymentMethod will appear in the [payment_method](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-payment_method) property on the PaymentIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment method-specific configuration for this PaymentIntent. */
    payment_method_options?: { acss_debit?: { mandate_options?: { custom_mandate_url?: string | ""; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; affirm?: { capture_method?: "" | "manual"; preferred_locale?: string; setup_future_usage?: "none" } | ""; afterpay_clearpay?: { capture_method?: "" | "manual"; reference?: string; setup_future_usage?: "none" } | ""; alipay?: { setup_future_usage?: "" | "none" | "off_session" } | ""; alma?: { capture_method?: "" | "manual" } | ""; amazon_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; au_becs_debit?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl"; setup_future_usage?: "" | "none" | "off_session" } | ""; billie?: { capture_method?: "" | "manual" } | ""; bizum?: { [key: string]: unknown } | ""; blik?: { code?: string; setup_future_usage?: "" | "none" } | ""; boleto?: { expires_after_days?: number; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; card?: { capture_method?: "" | "manual"; cvc_token?: string; installments?: { enabled?: boolean; plan?: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } | "" }; mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_extended_authorization?: "if_available" | "never"; request_incremental_authorization?: "if_available" | "never"; request_multicapture?: "if_available" | "never"; request_overcapture?: "if_available" | "never"; request_three_d_secure?: "any" | "automatic" | "challenge"; require_cvc_recollection?: boolean; setup_future_usage?: "" | "none" | "off_session" | "on_session"; statement_descriptor_suffix_kana?: string | ""; statement_descriptor_suffix_kanji?: string | ""; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; exemption_indicator?: "low_risk" | "none"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id: string; version: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } } | ""; card_present?: { capture_method?: "manual" | "manual_preferred"; request_extended_authorization?: boolean; request_incremental_authorization_support?: boolean; routing?: { requested_priority?: "domestic" | "international" } } | ""; cashapp?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; crypto?: { setup_future_usage?: "none" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; requested_address_types?: ("aba" | "iban" | "sepa" | "sort_code" | "spei" | "swift" | "zengin")[]; type: "eu_bank_transfer" | "gb_bank_transfer" | "jp_bank_transfer" | "mx_bank_transfer" | "us_bank_transfer" }; funding_type?: "bank_transfer"; setup_future_usage?: "none" } | ""; eps?: { setup_future_usage?: "none" } | ""; fpx?: { setup_future_usage?: "none" } | ""; giropay?: { setup_future_usage?: "none" } | ""; grabpay?: { setup_future_usage?: "none" } | ""; ideal?: { setup_future_usage?: "" | "none" | "off_session" } | ""; interac_present?: { [key: string]: unknown } | ""; kakao_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; klarna?: { capture_method?: "" | "manual"; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; setup_future_usage?: "none" | "off_session" | "on_session"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing?: { amount: number; date: string }; reference: string })[] | "" } | ""; konbini?: { confirmation_number?: string | ""; expires_after_days?: number | ""; expires_at?: number | ""; product_description?: string | ""; setup_future_usage?: "none" } | ""; kr_card?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; link?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; mb_way?: { setup_future_usage?: "none" } | ""; mobilepay?: { capture_method?: "" | "manual"; setup_future_usage?: "none" } | ""; multibanco?: { setup_future_usage?: "none" } | ""; naver_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; nz_bank_account?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; oxxo?: { expires_after_days?: number; setup_future_usage?: "none" } | ""; p24?: { setup_future_usage?: "none"; tos_shown_and_accepted?: boolean } | ""; pay_by_bank?: { [key: string]: unknown } | ""; payco?: { capture_method?: "" | "manual" } | ""; paynow?: { setup_future_usage?: "none" } | ""; paypal?: { capture_method?: "" | "manual"; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-DE" | "de-LU" | "el-GR" | "en-GB" | "en-US" | "es-ES" | "fi-FI" | "fr-BE" | "fr-FR" | "fr-LU" | "hu-HU" | "it-IT" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "sk-SK" | "sv-SE"; reference?: string; risk_correlation_id?: string; setup_future_usage?: "" | "none" | "off_session" } | ""; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" }; setup_future_usage?: "" | "none" | "off_session" } | ""; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number; expires_at?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string }; setup_future_usage?: "none" | "off_session" } | ""; promptpay?: { setup_future_usage?: "none" } | ""; revolut_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; samsung_pay?: { capture_method?: "" | "manual" } | ""; satispay?: { capture_method?: "" | "manual" } | ""; scalapay?: { capture_method?: "" | "manual" } | ""; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; sofort?: { preferred_language?: "" | "de" | "en" | "es" | "fr" | "it" | "nl" | "pl"; setup_future_usage?: "" | "none" | "off_session" } | ""; swish?: { reference?: string | ""; setup_future_usage?: "none" } | ""; twint?: { setup_future_usage?: "none" | "off_session" } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; transaction_purpose?: "" | "goods" | "other" | "services" | "unspecified"; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; wechat_pay?: { app_id?: string; client?: "android" | "ios" | "web"; setup_future_usage?: "none" } | ""; zip?: { setup_future_usage?: "none" } | "" };
    /** The list of payment method types (for example, a card) that this PaymentIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type). */
    payment_method_types?: (string)[];
    /** Options to configure Radar. Learn more about [Radar Sessions](https://docs.stripe.com/radar/radar-session). */
    radar_options?: { session?: string };
    /** Email address to send the receipt to. If you specify `receipt_email` for a payment in live mode, you send a receipt regardless of your [email settings](https://dashboard.stripe.com/account/emails). */
    receipt_email?: string;
    /** The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-confirm). */
    return_url?: string;
    /** Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication). */
    setup_future_usage?: "off_session" | "on_session";
    /** Shipping information for this PaymentIntent. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string };
    /** Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead. */
    statement_descriptor?: string;
    /** Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. */
    statement_descriptor_suffix?: string;
    /** The parameters that you can use to automatically create a Transfer. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    transfer_data?: { amount?: number; description?: string; destination: string; metadata?: { [key: string]: string | undefined } | ""; payment_data?: { description?: string; metadata?: { [key: string]: string | undefined } | "" } };
    /** A string that identifies the resulting payment as part of a group. Learn more about the [use case for connected accounts](https://docs.stripe.com/connect/separate-charges-and-transfers). */
    transfer_group?: string;
    /** Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions. */
    use_stripe_sdk?: boolean;
  }) => Promise<PaymentIntent>;

  /**
   * Search PaymentIntents
   */
  getPaymentIntentsSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for payment intents](https://docs.stripe.com/search#query-fields-for-payment-intents). */
    query: string;
  }) => Promise<{ data: (PaymentIntent)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Retrieve a PaymentIntent
   */
  getPaymentIntentsIntent: (input: {
    body?: { [key: string]: unknown };
    intent: string;
    /** The client secret of the PaymentIntent. We require it if you use a publishable key to retrieve the source. */
    client_secret?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentIntent>;

  /**
   * Update a PaymentIntent
   */
  postPaymentIntentsIntent: (input: {
    /** Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99). */
    amount?: number;
    /** Provides industry-specific information about the amount. */
    amount_details?: { discount_amount?: number | ""; enforce_arithmetic_validation?: boolean; line_items?: ({ discount_amount?: number; payment_method_options?: { card?: { commodity_code?: string }; card_present?: { commodity_code?: string }; klarna?: { image_url?: string; product_url?: string; reference?: string; subscription_reference?: string }; paypal?: { category?: "digital_goods" | "donation" | "physical_goods"; description?: string; sold_by?: string } }; product_code?: string; product_name: string; quantity: number; tax?: { total_tax_amount: number }; unit_cost: number; unit_of_measure?: string })[] | ""; shipping?: { amount?: number | ""; from_postal_code?: string | ""; to_postal_code?: string | "" } | ""; tax?: { total_tax_amount: number } | "" } | "";
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    application_fee_amount?: number | "";
    /** Controls when the funds will be captured from the customer's account. */
    capture_method?: "automatic" | "automatic_async" | "manual";
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead. */
    customer?: string;
    /** ID of the Account representing the customer that this PaymentIntent belongs to, if one exists.  Payment methods attached to other Accounts cannot be used with this PaymentIntent.  If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Account after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Account instead. */
    customer_account?: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The list of payment method types to exclude from use with this payment. */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Automations to be run during the PaymentIntent lifecycle */
    hooks?: { inputs?: { tax?: { calculation: string | "" } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Provides industry-specific information about the charge. */
    payment_details?: { customer_reference?: string | ""; order_reference?: string | "" } | "";
    /** ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://docs.stripe.com/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. To unset this field to null, pass in an empty string. */
    payment_method?: string;
    /** The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with this PaymentIntent. */
    payment_method_configuration?: string;
    /** If provided, this hash will be used to create a PaymentMethod. The new PaymentMethod will appear in the [payment_method](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-payment_method) property on the PaymentIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment-method-specific configuration for this PaymentIntent. */
    payment_method_options?: { acss_debit?: { mandate_options?: { custom_mandate_url?: string | ""; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; affirm?: { capture_method?: "" | "manual"; preferred_locale?: string; setup_future_usage?: "none" } | ""; afterpay_clearpay?: { capture_method?: "" | "manual"; reference?: string; setup_future_usage?: "none" } | ""; alipay?: { setup_future_usage?: "" | "none" | "off_session" } | ""; alma?: { capture_method?: "" | "manual" } | ""; amazon_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; au_becs_debit?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl"; setup_future_usage?: "" | "none" | "off_session" } | ""; billie?: { capture_method?: "" | "manual" } | ""; bizum?: { [key: string]: unknown } | ""; blik?: { code?: string; setup_future_usage?: "" | "none" } | ""; boleto?: { expires_after_days?: number; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; card?: { capture_method?: "" | "manual"; cvc_token?: string; installments?: { enabled?: boolean; plan?: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } | "" }; mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_extended_authorization?: "if_available" | "never"; request_incremental_authorization?: "if_available" | "never"; request_multicapture?: "if_available" | "never"; request_overcapture?: "if_available" | "never"; request_three_d_secure?: "any" | "automatic" | "challenge"; require_cvc_recollection?: boolean; setup_future_usage?: "" | "none" | "off_session" | "on_session"; statement_descriptor_suffix_kana?: string | ""; statement_descriptor_suffix_kanji?: string | ""; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; exemption_indicator?: "low_risk" | "none"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id: string; version: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } } | ""; card_present?: { capture_method?: "manual" | "manual_preferred"; request_extended_authorization?: boolean; request_incremental_authorization_support?: boolean; routing?: { requested_priority?: "domestic" | "international" } } | ""; cashapp?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; crypto?: { setup_future_usage?: "none" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; requested_address_types?: ("aba" | "iban" | "sepa" | "sort_code" | "spei" | "swift" | "zengin")[]; type: "eu_bank_transfer" | "gb_bank_transfer" | "jp_bank_transfer" | "mx_bank_transfer" | "us_bank_transfer" }; funding_type?: "bank_transfer"; setup_future_usage?: "none" } | ""; eps?: { setup_future_usage?: "none" } | ""; fpx?: { setup_future_usage?: "none" } | ""; giropay?: { setup_future_usage?: "none" } | ""; grabpay?: { setup_future_usage?: "none" } | ""; ideal?: { setup_future_usage?: "" | "none" | "off_session" } | ""; interac_present?: { [key: string]: unknown } | ""; kakao_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; klarna?: { capture_method?: "" | "manual"; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; setup_future_usage?: "none" | "off_session" | "on_session"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing?: { amount: number; date: string }; reference: string })[] | "" } | ""; konbini?: { confirmation_number?: string | ""; expires_after_days?: number | ""; expires_at?: number | ""; product_description?: string | ""; setup_future_usage?: "none" } | ""; kr_card?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; link?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; mb_way?: { setup_future_usage?: "none" } | ""; mobilepay?: { capture_method?: "" | "manual"; setup_future_usage?: "none" } | ""; multibanco?: { setup_future_usage?: "none" } | ""; naver_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; nz_bank_account?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; oxxo?: { expires_after_days?: number; setup_future_usage?: "none" } | ""; p24?: { setup_future_usage?: "none"; tos_shown_and_accepted?: boolean } | ""; pay_by_bank?: { [key: string]: unknown } | ""; payco?: { capture_method?: "" | "manual" } | ""; paynow?: { setup_future_usage?: "none" } | ""; paypal?: { capture_method?: "" | "manual"; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-DE" | "de-LU" | "el-GR" | "en-GB" | "en-US" | "es-ES" | "fi-FI" | "fr-BE" | "fr-FR" | "fr-LU" | "hu-HU" | "it-IT" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "sk-SK" | "sv-SE"; reference?: string; risk_correlation_id?: string; setup_future_usage?: "" | "none" | "off_session" } | ""; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" }; setup_future_usage?: "" | "none" | "off_session" } | ""; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number; expires_at?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string }; setup_future_usage?: "none" | "off_session" } | ""; promptpay?: { setup_future_usage?: "none" } | ""; revolut_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; samsung_pay?: { capture_method?: "" | "manual" } | ""; satispay?: { capture_method?: "" | "manual" } | ""; scalapay?: { capture_method?: "" | "manual" } | ""; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; sofort?: { preferred_language?: "" | "de" | "en" | "es" | "fr" | "it" | "nl" | "pl"; setup_future_usage?: "" | "none" | "off_session" } | ""; swish?: { reference?: string | ""; setup_future_usage?: "none" } | ""; twint?: { setup_future_usage?: "none" | "off_session" } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; transaction_purpose?: "" | "goods" | "other" | "services" | "unspecified"; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; wechat_pay?: { app_id?: string; client?: "android" | "ios" | "web"; setup_future_usage?: "none" } | ""; zip?: { setup_future_usage?: "none" } | "" };
    /** The list of payment method types (for example, card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type). */
    payment_method_types?: (string)[];
    /** Email address that the receipt for the resulting payment will be sent to. If `receipt_email` is specified for a payment in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails). */
    receipt_email?: string | "";
    /** Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`. */
    setup_future_usage?: "" | "off_session" | "on_session";
    /** Shipping information for this PaymentIntent. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string } | "";
    /** Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead. */
    statement_descriptor?: string;
    /** Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. */
    statement_descriptor_suffix?: string;
    /** Use this parameter to automatically create a Transfer when the payment succeeds. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    transfer_data?: { amount?: number; description?: string; metadata?: { [key: string]: string | undefined } | ""; payment_data?: { description?: string; metadata?: { [key: string]: string | undefined } | "" } };
    /** A string that identifies the resulting payment as part of a group. You can only provide `transfer_group` if it hasn't been set. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    transfer_group?: string;
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * List all PaymentIntent LineItems
   */
  getPaymentIntentsIntentAmountDetailsLineItems: (input: {
    body?: { [key: string]: unknown };
    intent: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentIntentAmountDetailsLineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Reconcile a customer_balance PaymentIntent
   */
  postPaymentIntentsIntentApplyCustomerBalance: (input: {
    /** Amount that you intend to apply to this PaymentIntent from the customer’s cash balance. If the PaymentIntent was created by an Invoice, the full amount of the PaymentIntent is applied regardless of this parameter.  A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (for example, 100 cents to charge 1 USD or 100 to charge 100 JPY, a zero-decimal currency). The maximum amount is the amount of the PaymentIntent.  When you omit the amount, it defaults to the remaining amount requested on the PaymentIntent. */
    amount?: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * Cancel a PaymentIntent
   */
  postPaymentIntentsIntentCancel: (input: {
    /** Reason for canceling this PaymentIntent. Possible values are: `duplicate`, `fraudulent`, `requested_by_customer`, or `abandoned` */
    cancellation_reason?: "abandoned" | "duplicate" | "fraudulent" | "requested_by_customer";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * Capture a PaymentIntent
   */
  postPaymentIntentsIntentCapture: (input: {
    /** Provides industry-specific information about the amount. */
    amount_details?: { discount_amount?: number | ""; enforce_arithmetic_validation?: boolean; line_items?: ({ discount_amount?: number; payment_method_options?: { card?: { commodity_code?: string }; card_present?: { commodity_code?: string }; klarna?: { image_url?: string; product_url?: string; reference?: string; subscription_reference?: string }; paypal?: { category?: "digital_goods" | "donation" | "physical_goods"; description?: string; sold_by?: string } }; product_code?: string; product_name: string; quantity: number; tax?: { total_tax_amount: number }; unit_cost: number; unit_of_measure?: string })[] | ""; shipping?: { amount?: number | ""; from_postal_code?: string | ""; to_postal_code?: string | "" } | ""; tax?: { total_tax_amount: number } | "" };
    /** The amount to capture from the PaymentIntent, which must be less than or equal to the original amount. Defaults to the full `amount_capturable` if it's not provided. */
    amount_to_capture?: number;
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    application_fee_amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Defaults to `true`. When capturing a PaymentIntent, setting `final_capture` to `false` notifies Stripe to not release the remaining uncaptured funds to make sure that they're captured in future requests. You can only use this setting when [multicapture](https://docs.stripe.com/payments/multicapture) is available for PaymentIntents. */
    final_capture?: boolean;
    /** Automations to be run during the PaymentIntent lifecycle */
    hooks?: { inputs?: { tax?: { calculation: string | "" } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Provides industry-specific information about the charge. */
    payment_details?: { customer_reference?: string | ""; order_reference?: string | "" } | "";
    /** Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead. */
    statement_descriptor?: string;
    /** Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. */
    statement_descriptor_suffix?: string;
    /** The parameters that you can use to automatically create a transfer after the payment is captured. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    transfer_data?: { amount?: number };
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * Confirm a PaymentIntent
   */
  postPaymentIntentsIntentConfirm: (input: {
    /** Provides industry-specific information about the amount. */
    amount_details?: { discount_amount?: number | ""; enforce_arithmetic_validation?: boolean; line_items?: ({ discount_amount?: number; payment_method_options?: { card?: { commodity_code?: string }; card_present?: { commodity_code?: string }; klarna?: { image_url?: string; product_url?: string; reference?: string; subscription_reference?: string }; paypal?: { category?: "digital_goods" | "donation" | "physical_goods"; description?: string; sold_by?: string } }; product_code?: string; product_name: string; quantity: number; tax?: { total_tax_amount: number }; unit_cost: number; unit_of_measure?: string })[] | ""; shipping?: { amount?: number | ""; from_postal_code?: string | ""; to_postal_code?: string | "" } | ""; tax?: { total_tax_amount: number } | "" } | "";
    /** Amount to confirm on the PaymentIntent. Defaults to `amount` if not provided. */
    amount_to_confirm?: number;
    /** Controls when the funds will be captured from the customer's account. */
    capture_method?: "automatic" | "automatic_async" | "manual";
    /** The client secret of the PaymentIntent. */
    client_secret?: string;
    /** ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence. */
    confirmation_token?: string;
    /** Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. This parameter is intended for simpler integrations that do not handle customer actions, like [saving cards without authentication](https://docs.stripe.com/payments/save-card-without-authentication). */
    error_on_requires_action?: boolean;
    /** The list of payment method types to exclude from use with this payment. */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Automations to be run during the PaymentIntent lifecycle */
    hooks?: { inputs?: { tax?: { calculation: string | "" } } };
    /** ID of the mandate that's used for this payment. */
    mandate?: string;
    mandate_data?: { customer_acceptance: { accepted_at?: number; offline?: { [key: string]: unknown }; online?: { ip_address: string; user_agent: string }; type: "offline" | "online" } } | "" | { customer_acceptance: { online: { ip_address?: string; user_agent?: string }; type: "online" } };
    /** Set to `true` to indicate that the customer isn't in your checkout flow during this payment attempt and can't authenticate. Use this parameter in scenarios where you collect payment method details and [charge them later](https://docs.stripe.com/payments/save-during-payment). */
    off_session?: boolean | "one_off" | "recurring";
    /** Provides industry-specific information about the charge. */
    payment_details?: { customer_reference?: string | ""; order_reference?: string | "" } | "";
    /** ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://docs.stripe.com/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. If the payment method is attached to a Customer, it must match the [customer](https://api.stripe.com#create_payment_intent-customer) that is set on this PaymentIntent. */
    payment_method?: string;
    /** If provided, this hash will be used to create a PaymentMethod. The new PaymentMethod will appear in the [payment_method](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-payment_method) property on the PaymentIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment method-specific configuration for this PaymentIntent. */
    payment_method_options?: { acss_debit?: { mandate_options?: { custom_mandate_url?: string | ""; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; affirm?: { capture_method?: "" | "manual"; preferred_locale?: string; setup_future_usage?: "none" } | ""; afterpay_clearpay?: { capture_method?: "" | "manual"; reference?: string; setup_future_usage?: "none" } | ""; alipay?: { setup_future_usage?: "" | "none" | "off_session" } | ""; alma?: { capture_method?: "" | "manual" } | ""; amazon_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; au_becs_debit?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl"; setup_future_usage?: "" | "none" | "off_session" } | ""; billie?: { capture_method?: "" | "manual" } | ""; bizum?: { [key: string]: unknown } | ""; blik?: { code?: string; setup_future_usage?: "" | "none" } | ""; boleto?: { expires_after_days?: number; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; card?: { capture_method?: "" | "manual"; cvc_token?: string; installments?: { enabled?: boolean; plan?: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } | "" }; mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_extended_authorization?: "if_available" | "never"; request_incremental_authorization?: "if_available" | "never"; request_multicapture?: "if_available" | "never"; request_overcapture?: "if_available" | "never"; request_three_d_secure?: "any" | "automatic" | "challenge"; require_cvc_recollection?: boolean; setup_future_usage?: "" | "none" | "off_session" | "on_session"; statement_descriptor_suffix_kana?: string | ""; statement_descriptor_suffix_kanji?: string | ""; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; exemption_indicator?: "low_risk" | "none"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id: string; version: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } } | ""; card_present?: { capture_method?: "manual" | "manual_preferred"; request_extended_authorization?: boolean; request_incremental_authorization_support?: boolean; routing?: { requested_priority?: "domestic" | "international" } } | ""; cashapp?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; crypto?: { setup_future_usage?: "none" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; requested_address_types?: ("aba" | "iban" | "sepa" | "sort_code" | "spei" | "swift" | "zengin")[]; type: "eu_bank_transfer" | "gb_bank_transfer" | "jp_bank_transfer" | "mx_bank_transfer" | "us_bank_transfer" }; funding_type?: "bank_transfer"; setup_future_usage?: "none" } | ""; eps?: { setup_future_usage?: "none" } | ""; fpx?: { setup_future_usage?: "none" } | ""; giropay?: { setup_future_usage?: "none" } | ""; grabpay?: { setup_future_usage?: "none" } | ""; ideal?: { setup_future_usage?: "" | "none" | "off_session" } | ""; interac_present?: { [key: string]: unknown } | ""; kakao_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; klarna?: { capture_method?: "" | "manual"; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; setup_future_usage?: "none" | "off_session" | "on_session"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing?: { amount: number; date: string }; reference: string })[] | "" } | ""; konbini?: { confirmation_number?: string | ""; expires_after_days?: number | ""; expires_at?: number | ""; product_description?: string | ""; setup_future_usage?: "none" } | ""; kr_card?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; link?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; mb_way?: { setup_future_usage?: "none" } | ""; mobilepay?: { capture_method?: "" | "manual"; setup_future_usage?: "none" } | ""; multibanco?: { setup_future_usage?: "none" } | ""; naver_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; nz_bank_account?: { setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; oxxo?: { expires_after_days?: number; setup_future_usage?: "none" } | ""; p24?: { setup_future_usage?: "none"; tos_shown_and_accepted?: boolean } | ""; pay_by_bank?: { [key: string]: unknown } | ""; payco?: { capture_method?: "" | "manual" } | ""; paynow?: { setup_future_usage?: "none" } | ""; paypal?: { capture_method?: "" | "manual"; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-DE" | "de-LU" | "el-GR" | "en-GB" | "en-US" | "es-ES" | "fi-FI" | "fr-BE" | "fr-FR" | "fr-LU" | "hu-HU" | "it-IT" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "sk-SK" | "sv-SE"; reference?: string; risk_correlation_id?: string; setup_future_usage?: "" | "none" | "off_session" } | ""; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" }; setup_future_usage?: "" | "none" | "off_session" } | ""; pix?: { amount_includes_iof?: "always" | "never"; expires_after_seconds?: number; expires_at?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string }; setup_future_usage?: "none" | "off_session" } | ""; promptpay?: { setup_future_usage?: "none" } | ""; revolut_pay?: { capture_method?: "" | "manual"; setup_future_usage?: "" | "none" | "off_session" } | ""; samsung_pay?: { capture_method?: "" | "manual" } | ""; satispay?: { capture_method?: "" | "manual" } | ""; scalapay?: { capture_method?: "" | "manual" } | ""; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string } | ""; sofort?: { preferred_language?: "" | "de" | "en" | "es" | "fr" | "it" | "nl" | "pl"; setup_future_usage?: "" | "none" | "off_session" } | ""; swish?: { reference?: string | ""; setup_future_usage?: "none" } | ""; twint?: { setup_future_usage?: "none" | "off_session" } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; setup_future_usage?: "" | "none" | "off_session" | "on_session"; target_date?: string; transaction_purpose?: "" | "goods" | "other" | "services" | "unspecified"; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; wechat_pay?: { app_id?: string; client?: "android" | "ios" | "web"; setup_future_usage?: "none" } | ""; zip?: { setup_future_usage?: "none" } | "" };
    /** The list of payment method types (for example, a card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type). */
    payment_method_types?: (string)[];
    /** Options to configure Radar. Learn more about [Radar Sessions](https://docs.stripe.com/radar/radar-session). */
    radar_options?: { session?: string };
    /** Email address that the receipt for the resulting payment will be sent to. If `receipt_email` is specified for a payment in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails). */
    receipt_email?: string | "";
    /** The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods. */
    return_url?: string;
    /** Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`. */
    setup_future_usage?: "" | "off_session" | "on_session";
    /** Shipping information for this PaymentIntent. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name: string; phone?: string; tracking_number?: string } | "";
    /** Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions. */
    use_stripe_sdk?: boolean;
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * Increment an authorization
   */
  postPaymentIntentsIntentIncrementAuthorization: (input: {
    /** The updated total amount that you intend to collect from the cardholder. This amount must be greater than the currently authorized amount. */
    amount: number;
    /** Provides industry-specific information about the amount. */
    amount_details?: { discount_amount?: number | ""; enforce_arithmetic_validation?: boolean; line_items?: ({ discount_amount?: number; payment_method_options?: { card?: { commodity_code?: string }; card_present?: { commodity_code?: string }; klarna?: { image_url?: string; product_url?: string; reference?: string; subscription_reference?: string }; paypal?: { category?: "digital_goods" | "donation" | "physical_goods"; description?: string; sold_by?: string } }; product_code?: string; product_name: string; quantity: number; tax?: { total_tax_amount: number }; unit_cost: number; unit_of_measure?: string })[] | ""; shipping?: { amount?: number | ""; from_postal_code?: string | ""; to_postal_code?: string | "" } | ""; tax?: { total_tax_amount: number } | "" };
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    application_fee_amount?: number;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Automations to be run during the PaymentIntent lifecycle */
    hooks?: { inputs?: { tax?: { calculation: string | "" } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Provides industry-specific information about the charge. */
    payment_details?: { customer_reference?: string | ""; order_reference?: string | "" };
    /** Text that appears on the customer's statement as the statement descriptor for a non-card or card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors). */
    statement_descriptor?: string;
    /** The parameters used to automatically create a transfer after the payment is captured. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts). */
    transfer_data?: { amount?: number };
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * Verify microdeposits on a PaymentIntent
   */
  postPaymentIntentsIntentVerifyMicrodeposits: (input: {
    /** Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account. */
    amounts?: (number)[];
    /** The client secret of the PaymentIntent. */
    client_secret?: string;
    /** A six-character code starting with SM present in the microdeposit sent to the bank account. */
    descriptor_code?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    intent: string;
  }) => Promise<PaymentIntent>;

  /**
   * List all payment links
   */
  getPaymentLinks: (input: {
    body?: { [key: string]: unknown };
    /** Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links). */
    active?: boolean;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentLink)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a payment link
   */
  postPaymentLinks: (input: {
    /** Behavior after the purchase is complete. */
    after_completion?: { hosted_confirmation?: { custom_message?: string }; redirect?: { url: string }; type: "hosted_confirmation" | "redirect" };
    /** Enables user redeemable promotion codes. */
    allow_promotion_codes?: boolean;
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. Can only be applied when there are no line items with recurring prices. */
    application_fee_amount?: number;
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line item with a recurring price to use this field. */
    application_fee_percent?: number;
    /** Configuration for automatic tax collection. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Configuration for collecting the customer's billing address. Defaults to `auto`. */
    billing_address_collection?: "auto" | "required";
    /** Configure fields to gather active consent from customers. */
    consent_collection?: { payment_method_reuse_agreement?: { position: "auto" | "hidden" }; promotions?: "auto" | "none"; terms_of_service?: "none" | "required" };
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies) and supported by each line item's price. */
    currency?: string;
    /** Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`. */
    custom_fields?: ({ dropdown?: { default_value?: string; options: ({ label: string; value: string })[] }; key: string; label: { custom: string; type: "custom" }; numeric?: { default_value?: string; maximum_length?: number; minimum_length?: number }; optional?: boolean; text?: { default_value?: string; maximum_length?: number; minimum_length?: number }; type: "dropdown" | "numeric" | "text" })[];
    /** Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`. */
    custom_text?: { after_submit?: { message: string } | ""; shipping_address?: { message: string } | ""; submit?: { message: string } | ""; terms_of_service_acceptance?: { message: string } | "" };
    /** Configures whether [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link create a [Customer](https://docs.stripe.com/api/customers). */
    customer_creation?: "always" | "if_required";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The custom message to be displayed to a customer when a payment link is no longer active. */
    inactive_message?: string;
    /** Generate a post-purchase Invoice for one-time payments. */
    invoice_creation?: { enabled: boolean; invoice_data?: { account_tax_ids?: (string)[] | ""; custom_fields?: ({ name: string; value: string })[] | ""; description?: string; footer?: string; issuer?: { account?: string; type: "account" | "self" }; metadata?: { [key: string]: string | undefined } | ""; rendering_options?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; template?: string } | "" } };
    /** The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported. */
    line_items: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; price?: string; price_data?: { currency: string; product?: string; product_data?: { description?: string; images?: (string)[]; metadata?: { [key: string]: string | undefined }; name: string; tax_code?: string; unit_label?: string }; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity: number })[];
    /** Settings for Managed Payments for this Payment Link and resulting [CheckoutSessions](/api/checkout/sessions/object), [PaymentIntents](/api/payment_intents/object), [Invoices](/api/invoices/object), and [Subscriptions](/api/subscriptions/object). */
    managed_payments?: { enabled?: boolean };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link. */
    metadata?: { [key: string]: string | undefined };
    /** Controls settings applied for collecting the customer's name. */
    name_collection?: { business?: { enabled: boolean; optional?: boolean }; individual?: { enabled: boolean; optional?: boolean } };
    /** The account on behalf of which to charge. */
    on_behalf_of?: string;
    /** A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://docs.stripe.com/api/prices). There is a maximum of 10 optional items allowed on a payment link, and the existing limits on the number of line items allowed on a payment link apply to the combined number of line items and optional items. There is a maximum of 20 combined line items and optional items. */
    optional_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; price: string; quantity: number })[];
    /** A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode. */
    payment_intent_data?: { capture_method?: "automatic" | "automatic_async" | "manual"; description?: string; metadata?: { [key: string]: string | undefined }; setup_future_usage?: "off_session" | "on_session"; statement_descriptor?: string; statement_descriptor_suffix?: string; transfer_group?: string };
    /** Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://docs.stripe.com/payments/checkout/free-trials). */
    payment_method_collection?: "always" | "if_required";
    payment_method_options?: { card?: { restrictions?: { brands_blocked?: ("american_express" | "discover_global_network" | "mastercard" | "visa")[] } } };
    /** The list of payment method types that customers can use. If no value is passed, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods) (20+ payment methods [supported](https://docs.stripe.com/payments/payment-methods/integration-options#payment-method-product-support)). */
    payment_method_types?: ("affirm" | "afterpay_clearpay" | "alipay" | "alma" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "klarna" | "konbini" | "link" | "mb_way" | "mobilepay" | "multibanco" | "oxxo" | "p24" | "pay_by_bank" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "satispay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[];
    /** Controls phone number collection settings during checkout.  We recommend that you review your privacy policy and check with your legal contacts. */
    phone_number_collection?: { enabled: boolean };
    /** Settings that restrict the usage of a payment link. */
    restrictions?: { completed_sessions: { limit: number } };
    /** Configuration for collecting the customer's shipping address. */
    shipping_address_collection?: { allowed_countries: ("AC" | "AD" | "AE" | "AF" | "AG" | "AI" | "AL" | "AM" | "AO" | "AQ" | "AR" | "AT" | "AU" | "AW" | "AX" | "AZ" | "BA" | "BB" | "BD" | "BE" | "BF" | "BG" | "BH" | "BI" | "BJ" | "BL" | "BM" | "BN" | "BO" | "BQ" | "BR" | "BS" | "BT" | "BV" | "BW" | "BY" | "BZ" | "CA" | "CD" | "CF" | "CG" | "CH" | "CI" | "CK" | "CL" | "CM" | "CN" | "CO" | "CR" | "CV" | "CW" | "CY" | "CZ" | "DE" | "DJ" | "DK" | "DM" | "DO" | "DZ" | "EC" | "EE" | "EG" | "EH" | "ER" | "ES" | "ET" | "FI" | "FJ" | "FK" | "FO" | "FR" | "GA" | "GB" | "GD" | "GE" | "GF" | "GG" | "GH" | "GI" | "GL" | "GM" | "GN" | "GP" | "GQ" | "GR" | "GS" | "GT" | "GU" | "GW" | "GY" | "HK" | "HN" | "HR" | "HT" | "HU" | "ID" | "IE" | "IL" | "IM" | "IN" | "IO" | "IQ" | "IS" | "IT" | "JE" | "JM" | "JO" | "JP" | "KE" | "KG" | "KH" | "KI" | "KM" | "KN" | "KR" | "KW" | "KY" | "KZ" | "LA" | "LB" | "LC" | "LI" | "LK" | "LR" | "LS" | "LT" | "LU" | "LV" | "LY" | "MA" | "MC" | "MD" | "ME" | "MF" | "MG" | "MK" | "ML" | "MM" | "MN" | "MO" | "MQ" | "MR" | "MS" | "MT" | "MU" | "MV" | "MW" | "MX" | "MY" | "MZ" | "NA" | "NC" | "NE" | "NG" | "NI" | "NL" | "NO" | "NP" | "NR" | "NU" | "NZ" | "OM" | "PA" | "PE" | "PF" | "PG" | "PH" | "PK" | "PL" | "PM" | "PN" | "PR" | "PS" | "PT" | "PY" | "QA" | "RE" | "RO" | "RS" | "RU" | "RW" | "SA" | "SB" | "SC" | "SD" | "SE" | "SG" | "SH" | "SI" | "SJ" | "SK" | "SL" | "SM" | "SN" | "SO" | "SR" | "SS" | "ST" | "SV" | "SX" | "SZ" | "TA" | "TC" | "TD" | "TF" | "TG" | "TH" | "TJ" | "TK" | "TL" | "TM" | "TN" | "TO" | "TR" | "TT" | "TV" | "TW" | "TZ" | "UA" | "UG" | "US" | "UY" | "UZ" | "VA" | "VC" | "VE" | "VG" | "VN" | "VU" | "WF" | "WS" | "XK" | "YE" | "YT" | "ZA" | "ZM" | "ZW" | "ZZ")[] };
    /** The shipping rate options to apply to [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link. */
    shipping_options?: ({ shipping_rate?: string })[];
    /** Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://docs.stripe.com/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`). */
    submit_type?: "auto" | "book" | "donate" | "pay" | "subscribe";
    /** When creating a subscription, the specified configuration data will be used. There must be at least one line item with a recurring price to use `subscription_data`. */
    subscription_data?: { description?: string; invoice_settings?: { issuer?: { account?: string; type: "account" | "self" } }; metadata?: { [key: string]: string | undefined }; trial_period_days?: number; trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } } };
    /** Controls tax ID collection during checkout. */
    tax_id_collection?: { enabled: boolean; required?: "if_supported" | "never" };
    /** The account (if any) the payments will be attributed to for tax reporting, and where funds from each payment will be transferred to. */
    transfer_data?: { amount?: number; destination: string };
  }) => Promise<PaymentLink>;

  /**
   * Retrieve payment link
   */
  getPaymentLinksPaymentLink: (input: {
    body?: { [key: string]: unknown };
    payment_link: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentLink>;

  /**
   * Update a payment link
   */
  postPaymentLinksPaymentLink: (input: {
    /** Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that the link has been deactivated. */
    active?: boolean;
    /** Behavior after the purchase is complete. */
    after_completion?: { hosted_confirmation?: { custom_message?: string }; redirect?: { url: string }; type: "hosted_confirmation" | "redirect" };
    /** Enables user redeemable promotion codes. */
    allow_promotion_codes?: boolean;
    /** Configuration for automatic tax collection. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Configuration for collecting the customer's billing address. Defaults to `auto`. */
    billing_address_collection?: "auto" | "required";
    /** Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`. */
    custom_fields?: ({ dropdown?: { default_value?: string; options: ({ label: string; value: string })[] }; key: string; label: { custom: string; type: "custom" }; numeric?: { default_value?: string; maximum_length?: number; minimum_length?: number }; optional?: boolean; text?: { default_value?: string; maximum_length?: number; minimum_length?: number }; type: "dropdown" | "numeric" | "text" })[] | "";
    /** Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`. */
    custom_text?: { after_submit?: { message: string } | ""; shipping_address?: { message: string } | ""; submit?: { message: string } | ""; terms_of_service_acceptance?: { message: string } | "" };
    /** Configures whether [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link create a [Customer](https://docs.stripe.com/api/customers). */
    customer_creation?: "always" | "if_required";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The custom message to be displayed to a customer when a payment link is no longer active. */
    inactive_message?: string | "";
    /** Generate a post-purchase Invoice for one-time payments. */
    invoice_creation?: { enabled: boolean; invoice_data?: { account_tax_ids?: (string)[] | ""; custom_fields?: ({ name: string; value: string })[] | ""; description?: string; footer?: string; issuer?: { account?: string; type: "account" | "self" }; metadata?: { [key: string]: string | undefined } | ""; rendering_options?: { amount_tax_display?: "" | "exclude_tax" | "include_inclusive_tax"; template?: string } | "" } };
    /** The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported. */
    line_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; id: string; quantity?: number })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://docs.stripe.com/api/checkout/sessions) created by this payment link. */
    metadata?: { [key: string]: string | undefined };
    /** Controls settings applied for collecting the customer's name. */
    name_collection?: { business?: { enabled: boolean; optional?: boolean }; individual?: { enabled: boolean; optional?: boolean } } | "";
    /** A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://docs.stripe.com/api/prices). There is a maximum of 10 optional items allowed on a payment link, and the existing limits on the number of line items allowed on a payment link apply to the combined number of line items and optional items. There is a maximum of 20 combined line items and optional items. */
    optional_items?: ({ adjustable_quantity?: { enabled: boolean; maximum?: number; minimum?: number }; price: string; quantity: number })[] | "";
    /** A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode. */
    payment_intent_data?: { description?: string | ""; metadata?: { [key: string]: string | undefined } | ""; statement_descriptor?: string | ""; statement_descriptor_suffix?: string | ""; transfer_group?: string | "" };
    /** Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://docs.stripe.com/payments/checkout/free-trials). */
    payment_method_collection?: "always" | "if_required";
    /** Payment-method-specific configuration. */
    payment_method_options?: { card?: { restrictions?: { brands_blocked?: ("american_express" | "discover_global_network" | "mastercard" | "visa")[] | "" } | "" } | "" } | "";
    /** The list of payment method types that customers can use. Pass an empty string to enable dynamic payment methods that use your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). */
    payment_method_types?: ("affirm" | "afterpay_clearpay" | "alipay" | "alma" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "klarna" | "konbini" | "link" | "mb_way" | "mobilepay" | "multibanco" | "oxxo" | "p24" | "pay_by_bank" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "satispay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[] | "";
    /** Controls phone number collection settings during checkout.  We recommend that you review your privacy policy and check with your legal contacts. */
    phone_number_collection?: { enabled: boolean };
    /** Settings that restrict the usage of a payment link. */
    restrictions?: { completed_sessions: { limit: number } } | "";
    /** Configuration for collecting the customer's shipping address. */
    shipping_address_collection?: { allowed_countries: ("AC" | "AD" | "AE" | "AF" | "AG" | "AI" | "AL" | "AM" | "AO" | "AQ" | "AR" | "AT" | "AU" | "AW" | "AX" | "AZ" | "BA" | "BB" | "BD" | "BE" | "BF" | "BG" | "BH" | "BI" | "BJ" | "BL" | "BM" | "BN" | "BO" | "BQ" | "BR" | "BS" | "BT" | "BV" | "BW" | "BY" | "BZ" | "CA" | "CD" | "CF" | "CG" | "CH" | "CI" | "CK" | "CL" | "CM" | "CN" | "CO" | "CR" | "CV" | "CW" | "CY" | "CZ" | "DE" | "DJ" | "DK" | "DM" | "DO" | "DZ" | "EC" | "EE" | "EG" | "EH" | "ER" | "ES" | "ET" | "FI" | "FJ" | "FK" | "FO" | "FR" | "GA" | "GB" | "GD" | "GE" | "GF" | "GG" | "GH" | "GI" | "GL" | "GM" | "GN" | "GP" | "GQ" | "GR" | "GS" | "GT" | "GU" | "GW" | "GY" | "HK" | "HN" | "HR" | "HT" | "HU" | "ID" | "IE" | "IL" | "IM" | "IN" | "IO" | "IQ" | "IS" | "IT" | "JE" | "JM" | "JO" | "JP" | "KE" | "KG" | "KH" | "KI" | "KM" | "KN" | "KR" | "KW" | "KY" | "KZ" | "LA" | "LB" | "LC" | "LI" | "LK" | "LR" | "LS" | "LT" | "LU" | "LV" | "LY" | "MA" | "MC" | "MD" | "ME" | "MF" | "MG" | "MK" | "ML" | "MM" | "MN" | "MO" | "MQ" | "MR" | "MS" | "MT" | "MU" | "MV" | "MW" | "MX" | "MY" | "MZ" | "NA" | "NC" | "NE" | "NG" | "NI" | "NL" | "NO" | "NP" | "NR" | "NU" | "NZ" | "OM" | "PA" | "PE" | "PF" | "PG" | "PH" | "PK" | "PL" | "PM" | "PN" | "PR" | "PS" | "PT" | "PY" | "QA" | "RE" | "RO" | "RS" | "RU" | "RW" | "SA" | "SB" | "SC" | "SD" | "SE" | "SG" | "SH" | "SI" | "SJ" | "SK" | "SL" | "SM" | "SN" | "SO" | "SR" | "SS" | "ST" | "SV" | "SX" | "SZ" | "TA" | "TC" | "TD" | "TF" | "TG" | "TH" | "TJ" | "TK" | "TL" | "TM" | "TN" | "TO" | "TR" | "TT" | "TV" | "TW" | "TZ" | "UA" | "UG" | "US" | "UY" | "UZ" | "VA" | "VC" | "VE" | "VG" | "VN" | "VU" | "WF" | "WS" | "XK" | "YE" | "YT" | "ZA" | "ZM" | "ZW" | "ZZ")[] } | "";
    /** Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://docs.stripe.com/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`). */
    submit_type?: "auto" | "book" | "donate" | "pay" | "subscribe";
    /** When creating a subscription, the specified configuration data will be used. There must be at least one line item with a recurring price to use `subscription_data`. */
    subscription_data?: { invoice_settings?: { issuer?: { account?: string; type: "account" | "self" } }; metadata?: { [key: string]: string | undefined } | ""; trial_period_days?: number | ""; trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } } | "" };
    /** Controls tax ID collection during checkout. */
    tax_id_collection?: { enabled: boolean; required?: "if_supported" | "never" };
    payment_link: string;
  }) => Promise<PaymentLink>;

  /**
   * Retrieve a payment link's line items
   */
  getPaymentLinksPaymentLinkLineItems: (input: {
    body?: { [key: string]: unknown };
    payment_link: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Item)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * List payment method configurations
   */
  getPaymentMethodConfigurations: (input: {
    body?: { [key: string]: unknown };
    /** Whether the configuration is active. */
    active?: boolean;
    /** The Connect application to filter by. */
    application?: string | "";
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentMethodConfiguration)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a payment method configuration
   */
  postPaymentMethodConfigurations: (input: {
    /** Canadian pre-authorized debit payments, check this [page](https://docs.stripe.com/payments/acss-debit) for more details like country availability. */
    acss_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** [Affirm](https://www.affirm.com/) gives your customers a way to split purchases over a series of payments. Depending on the purchase, they can pay with four interest-free payments (Split Pay) or pay over a longer term (Installments), which might include interest. Check this [page](https://docs.stripe.com/payments/affirm) for more details like country availability. */
    affirm?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Afterpay gives your customers a way to pay for purchases in installments, check this [page](https://docs.stripe.com/payments/afterpay-clearpay) for more details like country availability. Afterpay is particularly popular among businesses selling fashion, beauty, and sports products. */
    afterpay_clearpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Alipay is a digital wallet in China that has more than a billion active users worldwide. Alipay users can pay on the web or on a mobile device using login credentials or their Alipay app. Alipay has a low dispute rate and reduces fraud by authenticating payments using the customer's login credentials. Check this [page](https://docs.stripe.com/payments/alipay) for more details. */
    alipay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Alma is a Buy Now, Pay Later payment method that offers customers the ability to pay in 2, 3, or 4 installments. */
    alma?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Amazon Pay is a wallet payment method that lets your customers check out the same way as on Amazon. */
    amazon_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users can accept [Apple Pay](https://stripe.com/payments/apple-pay) in iOS applications in iOS 9 and later, and on the web in Safari starting with iOS 10 or macOS Sierra. There are no additional fees to process Apple Pay payments, and the [pricing](https://stripe.com/pricing) is the same as other card transactions. Check this [page](https://docs.stripe.com/apple-pay) for more details. */
    apple_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Apple Pay Later, a payment method for customers to buy now and pay later, gives your customers a way to split purchases into four installments across six weeks. */
    apple_pay_later?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Australia can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with an Australian bank account. Check this [page](https://docs.stripe.com/payments/au-becs-debit) for more details. */
    au_becs_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in the UK can accept Bacs Direct Debit payments from customers with a UK bank account, check this [page](https://docs.stripe.com/payments/payment-methods/bacs-debit) for more details. */
    bacs_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Bancontact is the most popular online payment method in Belgium, with over 15 million cards in circulation. [Customers](https://docs.stripe.com/api/customers) use a Bancontact card or mobile app linked to a Belgian bank account to make online payments that are secure, guaranteed, and confirmed immediately. Check this [page](https://docs.stripe.com/payments/bancontact) for more details. */
    bancontact?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Billie is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) payment method that offers businesses Pay by Invoice where they offer payment terms ranging from 7-120 days. Customers are redirected from your website or app, authorize the payment with Billie, then return to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    billie?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** To enable Bizum, buyers need a Spanish IBAN from a bank connected to Bizum. Within their banking app, they can enable Bizum and link their mobile number to their IBAN. */
    bizum?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** BLIK is a [single use](https://docs.stripe.com/payments/payment-methods#usage) payment method that requires customers to authenticate their payments. When customers want to pay online using BLIK, they request a six-digit code from their banking application and enter it into the payment collection form. Check this [page](https://docs.stripe.com/payments/blik) for more details. */
    blik?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Boleto is an official (regulated by the Central Bank of Brazil) payment method in Brazil. Check this [page](https://docs.stripe.com/payments/boleto) for more details. */
    boleto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cards are a popular way for consumers and businesses to pay online or in person. Stripe supports global and local card networks. */
    card?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cartes Bancaires is France's local card network. More than 95% of these cards are co-branded with either Visa or Mastercard, meaning you can process these cards over either Cartes Bancaires or the Visa or Mastercard networks. Check this [page](https://docs.stripe.com/payments/cartes-bancaires) for more details. */
    cartes_bancaires?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cash App is a popular consumer app in the US that allows customers to bank, invest, send, and receive money using their digital wallet. Check this [page](https://docs.stripe.com/payments/cash-app-pay) for more details. */
    cashapp?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** [Stablecoin payments](https://docs.stripe.com/payments/stablecoin-payments) enable customers to pay in stablecoins like USDC from 100s of wallets including Phantom and Metamask. */
    crypto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Uses a customer’s [cash balance](https://docs.stripe.com/payments/customer-balance) for the payment. The cash balance can be funded via a bank transfer. Check this [page](https://docs.stripe.com/payments/bank-transfers) for more details. */
    customer_balance?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** EPS is an Austria-based payment method that allows customers to complete transactions online using their bank credentials. EPS is supported by all Austrian banks and is accepted by over 80% of Austrian online retailers. Check this [page](https://docs.stripe.com/payments/eps) for more details. */
    eps?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Financial Process Exchange (FPX) is a Malaysia-based payment method that allows customers to complete transactions online using their bank credentials. Bank Negara Malaysia (BNM), the Central Bank of Malaysia, and eleven other major Malaysian financial institutions are members of the PayNet Group, which owns and operates FPX. It is one of the most popular online payment methods in Malaysia, with nearly 90 million transactions in 2018 according to BNM. Check this [page](https://docs.stripe.com/payments/fpx) for more details. */
    fpx?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Meal vouchers in France, or “titres-restaurant”, is a local benefits program commonly offered by employers for their employees to purchase prepared food and beverages on working days. Check this [page](https://docs.stripe.com/payments/meal-vouchers/fr-meal-vouchers) for more details. */
    fr_meal_voucher_conecs?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** giropay is a German payment method based on online banking, introduced in 2006. It allows customers to complete transactions online using their online banking environment, with funds debited from their bank account. Depending on their bank, customers confirm payments on giropay using a second factor of authentication or a PIN. giropay accounts for 10% of online checkouts in Germany. Check this [page](https://docs.stripe.com/payments/giropay) for more details. */
    giropay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Google Pay allows customers to make payments in your app or website using any credit or debit card saved to their Google Account, including those from Google Play, YouTube, Chrome, or an Android device. Use the Google Pay API to request any credit or debit card stored in your customer's Google account. Check this [page](https://docs.stripe.com/google-pay) for more details. */
    google_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** GrabPay is a payment method developed by [Grab](https://www.grab.com/sg/consumer/finance/pay/). GrabPay is a digital wallet - customers maintain a balance in their wallets that they pay out with. Check this [page](https://docs.stripe.com/payments/grabpay) for more details. */
    grabpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** iDEAL is a Netherlands-based payment method that allows customers to complete transactions online using their bank credentials. All major Dutch banks are members of Currence, the scheme that operates iDEAL, making it the most popular online payment method in the Netherlands with a share of online transactions close to 55%. Check this [page](https://docs.stripe.com/payments/ideal) for more details. */
    ideal?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** JCB is a credit card company based in Japan. JCB is currently available in Japan to businesses approved by JCB, and available to all businesses in Australia, Canada, Hong Kong, Japan, New Zealand, Singapore, Switzerland, United Kingdom, United States, and all countries in the European Economic Area except Iceland. Check this [page](https://support.stripe.com/questions/accepting-japan-credit-bureau-%28jcb%29-payments) for more details. */
    jcb?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Kakao Pay is a popular local wallet available in South Korea. */
    kakao_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Klarna gives customers a range of [payment options](https://docs.stripe.com/payments/klarna#payment-options) during checkout. Available payment options vary depending on the customer's billing address and the transaction amount. These payment options make it convenient for customers to purchase items in all price ranges. Check this [page](https://docs.stripe.com/payments/klarna) for more details. */
    klarna?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Konbini allows customers in Japan to pay for bills and online purchases at convenience stores with cash. Check this [page](https://docs.stripe.com/payments/konbini) for more details. */
    konbini?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Korean cards let users pay using locally issued cards from South Korea. */
    kr_card?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** [Link](https://docs.stripe.com/payments/link) is a payment method network. With Link, users save their payment details once, then reuse that information to pay with one click for any business on the network. */
    link?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** MB WAY is the most popular wallet in Portugal. After entering their phone number in your checkout, customers approve the payment directly in their MB WAY app. Check this [page](https://docs.stripe.com/payments/mb-way) for more details. */
    mb_way?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** MobilePay is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) card wallet payment method used in Denmark and Finland. It allows customers to [authenticate and approve](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using the MobilePay app. Check this [page](https://docs.stripe.com/payments/mobilepay) for more details. */
    mobilepay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Europe and the United States can accept Multibanco payments from customers in Portugal using [Sources](https://stripe.com/docs/sources)—a single integration path for creating payments using any supported method. */
    multibanco?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Configuration name. */
    name?: string;
    /** Naver Pay is a popular local wallet available in South Korea. */
    naver_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in New Zealand can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with a New Zeland bank account. Check this [page](https://docs.stripe.com/payments/nz-bank-account) for more details. */
    nz_bank_account?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** OXXO is a Mexican chain of convenience stores with thousands of locations across Latin America and represents nearly 20% of online transactions in Mexico. OXXO allows customers to pay bills and online purchases in-store with cash. Check this [page](https://docs.stripe.com/payments/oxxo) for more details. */
    oxxo?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Przelewy24 is a Poland-based payment method aggregator that allows customers to complete transactions online using bank transfers and other methods. Bank transfers account for 30% of online payments in Poland and Przelewy24 provides a way for customers to pay with over 165 banks. Check this [page](https://docs.stripe.com/payments/p24) for more details. */
    p24?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Configuration's parent configuration. Specify to create a child configuration. */
    parent?: string;
    /** Pay by bank is a redirect payment method backed by bank transfers. A customer is redirected to their bank to authorize a bank transfer for a given amount. This removes a lot of the error risks inherent in waiting for the customer to initiate a transfer themselves, and is less expensive than card payments. */
    pay_by_bank?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PAYCO is a [single-use](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea. */
    payco?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayNow is a Singapore-based payment method that allows customers to make a payment using their preferred app from participating banks and participating non-bank financial institutions. Check this [page](https://docs.stripe.com/payments/paynow) for more details. */
    paynow?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayPal, a digital wallet popular with customers in Europe, allows your customers worldwide to pay using their PayPal account. Check this [page](https://docs.stripe.com/payments/paypal) for more details. */
    paypal?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayTo is a [real-time](https://docs.stripe.com/payments/real-time) payment method that enables customers in Australia to pay by providing their bank account details. Customers must accept a mandate authorizing you to debit their account. Check this [page](https://docs.stripe.com/payments/payto) for more details. */
    payto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Pix is a payment method popular in Brazil. When paying with Pix, customers authenticate and approve payments by scanning a QR code in their preferred banking app. Check this [page](https://docs.stripe.com/payments/pix) for more details. */
    pix?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PromptPay is a Thailand-based payment method that allows customers to make a payment using their preferred app from participating banks. Check this [page](https://docs.stripe.com/payments/promptpay) for more details. */
    promptpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Revolut Pay, developed by Revolut, a global finance app, is a digital wallet payment method. Revolut Pay uses the customer’s stored balance or cards to fund the payment, and offers the option for non-Revolut customers to save their details after their first purchase. */
    revolut_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Samsung Pay is a [single-use](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea. */
    samsung_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Satispay is a [single-use](/payments/payment-methods#usage) payment method where customers are required to [authenticate](/payments/payment-methods#customer-actions) their payment. Customers pay by being redirected from your website or app, authorizing the payment with Satispay, then returning to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    satispay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Scalapay is a [single-use](/payments/payment-methods#usage) payment method that lets customers pay in 3 or 4 installments. Customers are redirected from your website or app, authorize the payment with Scalapay, then return to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    scalapay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** The [Single Euro Payments Area (SEPA)](https://en.wikipedia.org/wiki/Single_Euro_Payments_Area) is an initiative of the European Union to simplify payments within and across member countries. SEPA established and enforced banking standards to allow for the direct debiting of every EUR-denominated bank account within the SEPA region, check this [page](https://docs.stripe.com/payments/sepa-debit) for more details. */
    sepa_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Europe and the United States can use the [Payment Intents API](https://stripe.com/docs/payments/payment-intents)—a single integration path for creating payments using any supported method—to accept [Sofort](https://www.sofort.com/) payments from customers. Check this [page](https://docs.stripe.com/payments/sofort) for more details. */
    sofort?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Sunbit is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) payment method where customers choose to pay in 3, 6, or 12 installments. Customers are redirected from your website or app, authorize the payment with Sunbit, then return to your website or app. You get [immediate notification](https://docs.stripe.com/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    sunbit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Swish is a [real-time](https://docs.stripe.com/payments/real-time) payment method popular in Sweden. It allows customers to [authenticate and approve](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using the Swish mobile app and the Swedish BankID mobile app. Check this [page](https://docs.stripe.com/payments/swish) for more details. */
    swish?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Twint is a payment method popular in Switzerland. It allows customers to pay using their mobile phone. Check this [page](https://docs.stripe.com/payments/twint) for more details. */
    twint?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Unified Payment Interface (UPI) is India's leading payment method with exponential growth since it launched in 2016. */
    upi?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in the United States can accept ACH direct debit payments from customers with a US bank account using the Automated Clearing House (ACH) payments system operated by Nacha. Check this [page](https://docs.stripe.com/payments/ach-direct-debit) for more details. */
    us_bank_account?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** WeChat, owned by Tencent, is China's leading mobile app with over 1 billion monthly active users. Chinese consumers can use WeChat Pay to pay for goods and services inside of businesses' apps and websites. WeChat Pay users buy most frequently in gaming, e-commerce, travel, online education, and food/nutrition. Check this [page](https://docs.stripe.com/payments/wechat-pay) for more details. */
    wechat_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Zip gives your customers a way to split purchases over a series of payments. Check this [page](https://docs.stripe.com/payments/zip) for more details like country availability. */
    zip?: { display_preference?: { preference?: "none" | "off" | "on" } };
  }) => Promise<PaymentMethodConfiguration>;

  /**
   * Retrieve payment method configuration
   */
  getPaymentMethodConfigurationsConfiguration: (input: {
    body?: { [key: string]: unknown };
    configuration: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentMethodConfiguration>;

  /**
   * Update payment method configuration
   */
  postPaymentMethodConfigurationsConfiguration: (input: {
    /** Canadian pre-authorized debit payments, check this [page](https://docs.stripe.com/payments/acss-debit) for more details like country availability. */
    acss_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Whether the configuration can be used for new payments. */
    active?: boolean;
    /** [Affirm](https://www.affirm.com/) gives your customers a way to split purchases over a series of payments. Depending on the purchase, they can pay with four interest-free payments (Split Pay) or pay over a longer term (Installments), which might include interest. Check this [page](https://docs.stripe.com/payments/affirm) for more details like country availability. */
    affirm?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Afterpay gives your customers a way to pay for purchases in installments, check this [page](https://docs.stripe.com/payments/afterpay-clearpay) for more details like country availability. Afterpay is particularly popular among businesses selling fashion, beauty, and sports products. */
    afterpay_clearpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Alipay is a digital wallet in China that has more than a billion active users worldwide. Alipay users can pay on the web or on a mobile device using login credentials or their Alipay app. Alipay has a low dispute rate and reduces fraud by authenticating payments using the customer's login credentials. Check this [page](https://docs.stripe.com/payments/alipay) for more details. */
    alipay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Alma is a Buy Now, Pay Later payment method that offers customers the ability to pay in 2, 3, or 4 installments. */
    alma?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Amazon Pay is a wallet payment method that lets your customers check out the same way as on Amazon. */
    amazon_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users can accept [Apple Pay](https://stripe.com/payments/apple-pay) in iOS applications in iOS 9 and later, and on the web in Safari starting with iOS 10 or macOS Sierra. There are no additional fees to process Apple Pay payments, and the [pricing](https://stripe.com/pricing) is the same as other card transactions. Check this [page](https://docs.stripe.com/apple-pay) for more details. */
    apple_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Apple Pay Later, a payment method for customers to buy now and pay later, gives your customers a way to split purchases into four installments across six weeks. */
    apple_pay_later?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Australia can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with an Australian bank account. Check this [page](https://docs.stripe.com/payments/au-becs-debit) for more details. */
    au_becs_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in the UK can accept Bacs Direct Debit payments from customers with a UK bank account, check this [page](https://docs.stripe.com/payments/payment-methods/bacs-debit) for more details. */
    bacs_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Bancontact is the most popular online payment method in Belgium, with over 15 million cards in circulation. [Customers](https://docs.stripe.com/api/customers) use a Bancontact card or mobile app linked to a Belgian bank account to make online payments that are secure, guaranteed, and confirmed immediately. Check this [page](https://docs.stripe.com/payments/bancontact) for more details. */
    bancontact?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Billie is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) payment method that offers businesses Pay by Invoice where they offer payment terms ranging from 7-120 days. Customers are redirected from your website or app, authorize the payment with Billie, then return to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    billie?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** To enable Bizum, buyers need a Spanish IBAN from a bank connected to Bizum. Within their banking app, they can enable Bizum and link their mobile number to their IBAN. */
    bizum?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** BLIK is a [single use](https://docs.stripe.com/payments/payment-methods#usage) payment method that requires customers to authenticate their payments. When customers want to pay online using BLIK, they request a six-digit code from their banking application and enter it into the payment collection form. Check this [page](https://docs.stripe.com/payments/blik) for more details. */
    blik?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Boleto is an official (regulated by the Central Bank of Brazil) payment method in Brazil. Check this [page](https://docs.stripe.com/payments/boleto) for more details. */
    boleto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cards are a popular way for consumers and businesses to pay online or in person. Stripe supports global and local card networks. */
    card?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cartes Bancaires is France's local card network. More than 95% of these cards are co-branded with either Visa or Mastercard, meaning you can process these cards over either Cartes Bancaires or the Visa or Mastercard networks. Check this [page](https://docs.stripe.com/payments/cartes-bancaires) for more details. */
    cartes_bancaires?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Cash App is a popular consumer app in the US that allows customers to bank, invest, send, and receive money using their digital wallet. Check this [page](https://docs.stripe.com/payments/cash-app-pay) for more details. */
    cashapp?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** [Stablecoin payments](https://docs.stripe.com/payments/stablecoin-payments) enable customers to pay in stablecoins like USDC from 100s of wallets including Phantom and Metamask. */
    crypto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Uses a customer’s [cash balance](https://docs.stripe.com/payments/customer-balance) for the payment. The cash balance can be funded via a bank transfer. Check this [page](https://docs.stripe.com/payments/bank-transfers) for more details. */
    customer_balance?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** EPS is an Austria-based payment method that allows customers to complete transactions online using their bank credentials. EPS is supported by all Austrian banks and is accepted by over 80% of Austrian online retailers. Check this [page](https://docs.stripe.com/payments/eps) for more details. */
    eps?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Financial Process Exchange (FPX) is a Malaysia-based payment method that allows customers to complete transactions online using their bank credentials. Bank Negara Malaysia (BNM), the Central Bank of Malaysia, and eleven other major Malaysian financial institutions are members of the PayNet Group, which owns and operates FPX. It is one of the most popular online payment methods in Malaysia, with nearly 90 million transactions in 2018 according to BNM. Check this [page](https://docs.stripe.com/payments/fpx) for more details. */
    fpx?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Meal vouchers in France, or “titres-restaurant”, is a local benefits program commonly offered by employers for their employees to purchase prepared food and beverages on working days. Check this [page](https://docs.stripe.com/payments/meal-vouchers/fr-meal-vouchers) for more details. */
    fr_meal_voucher_conecs?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** giropay is a German payment method based on online banking, introduced in 2006. It allows customers to complete transactions online using their online banking environment, with funds debited from their bank account. Depending on their bank, customers confirm payments on giropay using a second factor of authentication or a PIN. giropay accounts for 10% of online checkouts in Germany. Check this [page](https://docs.stripe.com/payments/giropay) for more details. */
    giropay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Google Pay allows customers to make payments in your app or website using any credit or debit card saved to their Google Account, including those from Google Play, YouTube, Chrome, or an Android device. Use the Google Pay API to request any credit or debit card stored in your customer's Google account. Check this [page](https://docs.stripe.com/google-pay) for more details. */
    google_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** GrabPay is a payment method developed by [Grab](https://www.grab.com/sg/consumer/finance/pay/). GrabPay is a digital wallet - customers maintain a balance in their wallets that they pay out with. Check this [page](https://docs.stripe.com/payments/grabpay) for more details. */
    grabpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** iDEAL is a Netherlands-based payment method that allows customers to complete transactions online using their bank credentials. All major Dutch banks are members of Currence, the scheme that operates iDEAL, making it the most popular online payment method in the Netherlands with a share of online transactions close to 55%. Check this [page](https://docs.stripe.com/payments/ideal) for more details. */
    ideal?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** JCB is a credit card company based in Japan. JCB is currently available in Japan to businesses approved by JCB, and available to all businesses in Australia, Canada, Hong Kong, Japan, New Zealand, Singapore, Switzerland, United Kingdom, United States, and all countries in the European Economic Area except Iceland. Check this [page](https://support.stripe.com/questions/accepting-japan-credit-bureau-%28jcb%29-payments) for more details. */
    jcb?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Kakao Pay is a popular local wallet available in South Korea. */
    kakao_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Klarna gives customers a range of [payment options](https://docs.stripe.com/payments/klarna#payment-options) during checkout. Available payment options vary depending on the customer's billing address and the transaction amount. These payment options make it convenient for customers to purchase items in all price ranges. Check this [page](https://docs.stripe.com/payments/klarna) for more details. */
    klarna?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Konbini allows customers in Japan to pay for bills and online purchases at convenience stores with cash. Check this [page](https://docs.stripe.com/payments/konbini) for more details. */
    konbini?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Korean cards let users pay using locally issued cards from South Korea. */
    kr_card?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** [Link](https://docs.stripe.com/payments/link) is a payment method network. With Link, users save their payment details once, then reuse that information to pay with one click for any business on the network. */
    link?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** MB WAY is the most popular wallet in Portugal. After entering their phone number in your checkout, customers approve the payment directly in their MB WAY app. Check this [page](https://docs.stripe.com/payments/mb-way) for more details. */
    mb_way?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** MobilePay is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) card wallet payment method used in Denmark and Finland. It allows customers to [authenticate and approve](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using the MobilePay app. Check this [page](https://docs.stripe.com/payments/mobilepay) for more details. */
    mobilepay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Europe and the United States can accept Multibanco payments from customers in Portugal using [Sources](https://stripe.com/docs/sources)—a single integration path for creating payments using any supported method. */
    multibanco?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Configuration name. */
    name?: string;
    /** Naver Pay is a popular local wallet available in South Korea. */
    naver_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in New Zealand can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with a New Zeland bank account. Check this [page](https://docs.stripe.com/payments/nz-bank-account) for more details. */
    nz_bank_account?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** OXXO is a Mexican chain of convenience stores with thousands of locations across Latin America and represents nearly 20% of online transactions in Mexico. OXXO allows customers to pay bills and online purchases in-store with cash. Check this [page](https://docs.stripe.com/payments/oxxo) for more details. */
    oxxo?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Przelewy24 is a Poland-based payment method aggregator that allows customers to complete transactions online using bank transfers and other methods. Bank transfers account for 30% of online payments in Poland and Przelewy24 provides a way for customers to pay with over 165 banks. Check this [page](https://docs.stripe.com/payments/p24) for more details. */
    p24?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Pay by bank is a redirect payment method backed by bank transfers. A customer is redirected to their bank to authorize a bank transfer for a given amount. This removes a lot of the error risks inherent in waiting for the customer to initiate a transfer themselves, and is less expensive than card payments. */
    pay_by_bank?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PAYCO is a [single-use](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea. */
    payco?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayNow is a Singapore-based payment method that allows customers to make a payment using their preferred app from participating banks and participating non-bank financial institutions. Check this [page](https://docs.stripe.com/payments/paynow) for more details. */
    paynow?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayPal, a digital wallet popular with customers in Europe, allows your customers worldwide to pay using their PayPal account. Check this [page](https://docs.stripe.com/payments/paypal) for more details. */
    paypal?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PayTo is a [real-time](https://docs.stripe.com/payments/real-time) payment method that enables customers in Australia to pay by providing their bank account details. Customers must accept a mandate authorizing you to debit their account. Check this [page](https://docs.stripe.com/payments/payto) for more details. */
    payto?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Pix is a payment method popular in Brazil. When paying with Pix, customers authenticate and approve payments by scanning a QR code in their preferred banking app. Check this [page](https://docs.stripe.com/payments/pix) for more details. */
    pix?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** PromptPay is a Thailand-based payment method that allows customers to make a payment using their preferred app from participating banks. Check this [page](https://docs.stripe.com/payments/promptpay) for more details. */
    promptpay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Revolut Pay, developed by Revolut, a global finance app, is a digital wallet payment method. Revolut Pay uses the customer’s stored balance or cards to fund the payment, and offers the option for non-Revolut customers to save their details after their first purchase. */
    revolut_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Samsung Pay is a [single-use](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea. */
    samsung_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Satispay is a [single-use](/payments/payment-methods#usage) payment method where customers are required to [authenticate](/payments/payment-methods#customer-actions) their payment. Customers pay by being redirected from your website or app, authorizing the payment with Satispay, then returning to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    satispay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Scalapay is a [single-use](/payments/payment-methods#usage) payment method that lets customers pay in 3 or 4 installments. Customers are redirected from your website or app, authorize the payment with Scalapay, then return to your website or app. You get [immediate notification](/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    scalapay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** The [Single Euro Payments Area (SEPA)](https://en.wikipedia.org/wiki/Single_Euro_Payments_Area) is an initiative of the European Union to simplify payments within and across member countries. SEPA established and enforced banking standards to allow for the direct debiting of every EUR-denominated bank account within the SEPA region, check this [page](https://docs.stripe.com/payments/sepa-debit) for more details. */
    sepa_debit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in Europe and the United States can use the [Payment Intents API](https://stripe.com/docs/payments/payment-intents)—a single integration path for creating payments using any supported method—to accept [Sofort](https://www.sofort.com/) payments from customers. Check this [page](https://docs.stripe.com/payments/sofort) for more details. */
    sofort?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Sunbit is a [single-use](https://docs.stripe.com/payments/payment-methods#usage) payment method where customers choose to pay in 3, 6, or 12 installments. Customers are redirected from your website or app, authorize the payment with Sunbit, then return to your website or app. You get [immediate notification](https://docs.stripe.com/payments/payment-methods#payment-notification) of whether the payment succeeded or failed. */
    sunbit?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Swish is a [real-time](https://docs.stripe.com/payments/real-time) payment method popular in Sweden. It allows customers to [authenticate and approve](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using the Swish mobile app and the Swedish BankID mobile app. Check this [page](https://docs.stripe.com/payments/swish) for more details. */
    swish?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Twint is a payment method popular in Switzerland. It allows customers to pay using their mobile phone. Check this [page](https://docs.stripe.com/payments/twint) for more details. */
    twint?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Unified Payment Interface (UPI) is India's leading payment method with exponential growth since it launched in 2016. */
    upi?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Stripe users in the United States can accept ACH direct debit payments from customers with a US bank account using the Automated Clearing House (ACH) payments system operated by Nacha. Check this [page](https://docs.stripe.com/payments/ach-direct-debit) for more details. */
    us_bank_account?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** WeChat, owned by Tencent, is China's leading mobile app with over 1 billion monthly active users. Chinese consumers can use WeChat Pay to pay for goods and services inside of businesses' apps and websites. WeChat Pay users buy most frequently in gaming, e-commerce, travel, online education, and food/nutrition. Check this [page](https://docs.stripe.com/payments/wechat-pay) for more details. */
    wechat_pay?: { display_preference?: { preference?: "none" | "off" | "on" } };
    /** Zip gives your customers a way to split purchases over a series of payments. Check this [page](https://docs.stripe.com/payments/zip) for more details like country availability. */
    zip?: { display_preference?: { preference?: "none" | "off" | "on" } };
    configuration: string;
  }) => Promise<PaymentMethodConfiguration>;

  /**
   * List payment method domains
   */
  getPaymentMethodDomains: (input: {
    body?: { [key: string]: unknown };
    /** The domain name that this payment method domain object represents. */
    domain_name?: string;
    /** Whether this payment method domain is enabled. If the domain is not enabled, payment methods will not appear in Elements or Embedded Checkout */
    enabled?: boolean;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PaymentMethodDomain)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a payment method domain
   */
  postPaymentMethodDomains: (input: {
    /** The domain name that this payment method domain object represents. */
    domain_name: string;
    /** Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout. */
    enabled?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentMethodDomain>;

  /**
   * Retrieve a payment method domain
   */
  getPaymentMethodDomainsPaymentMethodDomain: (input: {
    body?: { [key: string]: unknown };
    payment_method_domain: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentMethodDomain>;

  /**
   * Update a payment method domain
   */
  postPaymentMethodDomainsPaymentMethodDomain: (input: {
    /** Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout. */
    enabled?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    payment_method_domain: string;
  }) => Promise<PaymentMethodDomain>;

  /**
   * Validate an existing payment method domain
   */
  postPaymentMethodDomainsPaymentMethodDomainValidate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    payment_method_domain: string;
  }) => Promise<PaymentMethodDomain>;

  /**
   * List PaymentMethods
   */
  getPaymentMethods: (input: {
    body?: { [key: string]: unknown };
    /** This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. */
    allow_redisplay?: "always" | "limited" | "unspecified";
    /** The ID of the customer whose PaymentMethods will be retrieved. */
    customer?: string;
    /** The ID of the Account whose PaymentMethods will be retrieved. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Filters the list by the object `type` field. Unfiltered, the list returns all payment method types except `custom`. If your integration expects only one type of payment method in the response, specify that type value in the request to reduce your payload. */
    type?: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip";
  }) => Promise<{ data: (PaymentMethod)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Shares a PaymentMethod
   */
  postPaymentMethods: (input: {
    /** If this is an `acss_debit` PaymentMethod, this hash contains details about the ACSS Debit payment method. */
    acss_debit?: { account_number: string; institution_number: string; transit_number: string };
    /** If this is an `affirm` PaymentMethod, this hash contains details about the Affirm payment method. */
    affirm?: { [key: string]: unknown };
    /** If this is an `AfterpayClearpay` PaymentMethod, this hash contains details about the AfterpayClearpay payment method. */
    afterpay_clearpay?: { [key: string]: unknown };
    /** If this is an `Alipay` PaymentMethod, this hash contains details about the Alipay payment method. */
    alipay?: { [key: string]: unknown };
    /** This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`. */
    allow_redisplay?: "always" | "limited" | "unspecified";
    /** If this is a Alma PaymentMethod, this hash contains details about the Alma payment method. */
    alma?: { [key: string]: unknown };
    /** If this is a AmazonPay PaymentMethod, this hash contains details about the AmazonPay payment method. */
    amazon_pay?: { [key: string]: unknown };
    /** If this is an `au_becs_debit` PaymentMethod, this hash contains details about the bank account. */
    au_becs_debit?: { account_number: string; bsb_number: string };
    /** If this is a `bacs_debit` PaymentMethod, this hash contains details about the Bacs Direct Debit bank account. */
    bacs_debit?: { account_number?: string; sort_code?: string };
    /** If this is a `bancontact` PaymentMethod, this hash contains details about the Bancontact payment method. */
    bancontact?: { [key: string]: unknown };
    /** If this is a `billie` PaymentMethod, this hash contains details about the Billie payment method. */
    billie?: { [key: string]: unknown };
    /** Billing information associated with the PaymentMethod that may be used or required by particular types of payment methods. */
    billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string };
    /** If this is a `bizum` PaymentMethod, this hash contains details about the Bizum payment method. */
    bizum?: { [key: string]: unknown };
    /** If this is a `blik` PaymentMethod, this hash contains details about the BLIK payment method. */
    blik?: { [key: string]: unknown };
    /** If this is a `boleto` PaymentMethod, this hash contains details about the Boleto payment method. */
    boleto?: { tax_id: string };
    /** If this is a `card` PaymentMethod, this hash contains the user's card details. For backwards compatibility, you can alternatively provide a Stripe token (e.g., for Apple Pay, Amex Express Checkout, or legacy Checkout) into the card hash with format `card: {token: "tok_visa"}`. When providing a card number, you must meet the requirements for [PCI compliance](https://stripe.com/docs/security#validating-pci-compliance). We strongly recommend using Stripe.js instead of interacting with this API directly. */
    card?: { cvc?: string; exp_month: number; exp_year: number; networks?: { preferred?: "cartes_bancaires" | "mastercard" | "visa" }; number: string } | { token: string };
    /** If this is a `cashapp` PaymentMethod, this hash contains details about the Cash App Pay payment method. */
    cashapp?: { [key: string]: unknown };
    /** If this is a Crypto PaymentMethod, this hash contains details about the Crypto payment method. */
    crypto?: { [key: string]: unknown };
    /** If this is a `custom` PaymentMethod, this hash contains details about the Custom payment method. */
    custom?: { type: string };
    /** The `Customer` to whom the original PaymentMethod is attached. */
    customer?: string;
    /** If this is a `customer_balance` PaymentMethod, this hash contains details about the CustomerBalance payment method. */
    customer_balance?: { [key: string]: unknown };
    /** If this is an `eps` PaymentMethod, this hash contains details about the EPS payment method. */
    eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** If this is an `fpx` PaymentMethod, this hash contains details about the FPX payment method. */
    fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" };
    /** If this is a `giropay` PaymentMethod, this hash contains details about the Giropay payment method. */
    giropay?: { [key: string]: unknown };
    /** If this is a `grabpay` PaymentMethod, this hash contains details about the GrabPay payment method. */
    grabpay?: { [key: string]: unknown };
    /** If this is an `ideal` PaymentMethod, this hash contains details about the iDEAL payment method. */
    ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" };
    /** If this is an `interac_present` PaymentMethod, this hash contains details about the Interac Present payment method. */
    interac_present?: { [key: string]: unknown };
    /** If this is a `kakao_pay` PaymentMethod, this hash contains details about the Kakao Pay payment method. */
    kakao_pay?: { [key: string]: unknown };
    /** If this is a `klarna` PaymentMethod, this hash contains details about the Klarna payment method. */
    klarna?: { dob?: { day: number; month: number; year: number } };
    /** If this is a `konbini` PaymentMethod, this hash contains details about the Konbini payment method. */
    konbini?: { [key: string]: unknown };
    /** If this is a `kr_card` PaymentMethod, this hash contains details about the Korean Card payment method. */
    kr_card?: { [key: string]: unknown };
    /** If this is an `Link` PaymentMethod, this hash contains details about the Link payment method (Link is also known as Onelink in the UK). */
    link?: { [key: string]: unknown };
    /** If this is a MB WAY PaymentMethod, this hash contains details about the MB WAY payment method. */
    mb_way?: { [key: string]: unknown };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** If this is a `mobilepay` PaymentMethod, this hash contains details about the MobilePay payment method. */
    mobilepay?: { [key: string]: unknown };
    /** If this is a `multibanco` PaymentMethod, this hash contains details about the Multibanco payment method. */
    multibanco?: { [key: string]: unknown };
    /** If this is a `naver_pay` PaymentMethod, this hash contains details about the Naver Pay payment method. */
    naver_pay?: { funding?: "card" | "points" };
    /** If this is an nz_bank_account PaymentMethod, this hash contains details about the nz_bank_account payment method. */
    nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string };
    /** If this is an `oxxo` PaymentMethod, this hash contains details about the OXXO payment method. */
    oxxo?: { [key: string]: unknown };
    /** If this is a `p24` PaymentMethod, this hash contains details about the P24 payment method. */
    p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" };
    /** If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method. */
    pay_by_bank?: { [key: string]: unknown };
    /** If this is a `payco` PaymentMethod, this hash contains details about the PAYCO payment method. */
    payco?: { [key: string]: unknown };
    /** The PaymentMethod to share. */
    payment_method?: string;
    /** If this is a `paynow` PaymentMethod, this hash contains details about the PayNow payment method. */
    paynow?: { [key: string]: unknown };
    /** If this is a `paypal` PaymentMethod, this hash contains details about the PayPal payment method. */
    paypal?: { [key: string]: unknown };
    /** If this is a `payto` PaymentMethod, this hash contains details about the PayTo payment method. */
    payto?: { account_number?: string; bsb_number?: string; pay_id?: string };
    /** If this is a `pix` PaymentMethod, this hash contains details about the Pix payment method. */
    pix?: { [key: string]: unknown };
    /** If this is a `promptpay` PaymentMethod, this hash contains details about the PromptPay payment method. */
    promptpay?: { [key: string]: unknown };
    /** Options to configure Radar. See [Radar Session](https://docs.stripe.com/radar/radar-session) for more information. */
    radar_options?: { session?: string };
    /** If this is a `revolut_pay` PaymentMethod, this hash contains details about the Revolut Pay payment method. */
    revolut_pay?: { [key: string]: unknown };
    /** If this is a `samsung_pay` PaymentMethod, this hash contains details about the SamsungPay payment method. */
    samsung_pay?: { [key: string]: unknown };
    /** If this is a `satispay` PaymentMethod, this hash contains details about the Satispay payment method. */
    satispay?: { [key: string]: unknown };
    /** If this is a Scalapay PaymentMethod, this hash contains details about the Scalapay payment method. */
    scalapay?: { [key: string]: unknown };
    /** If this is a `sepa_debit` PaymentMethod, this hash contains details about the SEPA debit bank account. */
    sepa_debit?: { iban: string };
    /** If this is a `sofort` PaymentMethod, this hash contains details about the SOFORT payment method. */
    sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" };
    /** If this is a Sunbit PaymentMethod, this hash contains details about the Sunbit payment method. */
    sunbit?: { [key: string]: unknown };
    /** If this is a `swish` PaymentMethod, this hash contains details about the Swish payment method. */
    swish?: { [key: string]: unknown };
    /** If this is a TWINT PaymentMethod, this hash contains details about the TWINT payment method. */
    twint?: { [key: string]: unknown };
    /** The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type. */
    type?: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip";
    /** If this is a `upi` PaymentMethod, this hash contains details about the UPI payment method. */
    upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } };
    /** If this is an `us_bank_account` PaymentMethod, this hash contains details about the US bank account payment method. */
    us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string };
    /** If this is an `wechat_pay` PaymentMethod, this hash contains details about the wechat_pay payment method. */
    wechat_pay?: { [key: string]: unknown };
    /** If this is a `zip` PaymentMethod, this hash contains details about the Zip payment method. */
    zip?: { [key: string]: unknown };
  }) => Promise<PaymentMethod>;

  /**
   * Retrieve a PaymentMethod
   */
  getPaymentMethodsPaymentMethod: (input: {
    body?: { [key: string]: unknown };
    payment_method: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentMethod>;

  /**
   * Update a PaymentMethod
   */
  postPaymentMethodsPaymentMethod: (input: {
    /** This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`. */
    allow_redisplay?: "always" | "limited" | "unspecified";
    /** Billing information associated with the PaymentMethod that may be used or required by particular types of payment methods. */
    billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string };
    /** If this is a `card` PaymentMethod, this hash contains the user's card details. */
    card?: { exp_month?: number; exp_year?: number; networks?: { preferred?: "" | "cartes_bancaires" | "mastercard" | "visa" } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** If this is a `payto` PaymentMethod, this hash contains details about the PayTo payment method. */
    payto?: { account_number?: string; bsb_number?: string; pay_id?: string };
    /** If this is an `us_bank_account` PaymentMethod, this hash contains details about the US bank account payment method. */
    us_bank_account?: { account_holder_type?: "company" | "individual"; account_type?: "checking" | "savings" };
    payment_method: string;
  }) => Promise<PaymentMethod>;

  /**
   * Attach a PaymentMethod to a Customer
   */
  postPaymentMethodsPaymentMethodAttach: (input: {
    /** The ID of the customer to which to attach the PaymentMethod. */
    customer?: string;
    /** The ID of the Account representing the customer to which to attach the PaymentMethod. */
    customer_account?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    payment_method: string;
  }) => Promise<PaymentMethod>;

  /**
   * Detach a PaymentMethod from a Customer
   */
  postPaymentMethodsPaymentMethodDetach: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    payment_method: string;
  }) => Promise<PaymentMethod>;

  /**
   * Report a payment
   */
  postPaymentRecordsReportPayment: (input: {
    /** The amount you initially requested for this payment. */
    amount_requested: { currency: string; value: number };
    /** Customer information for this payment. */
    customer_details?: { customer?: string; email?: string; name?: string; phone?: string };
    /** Indicates whether the customer was present in your checkout flow during this payment. */
    customer_presence?: "off_session" | "on_session";
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about the payment attempt failure. */
    failed?: { failed_at: number };
    /** Information about the payment attempt guarantee. */
    guaranteed?: { guaranteed_at: number };
    /** When the reported payment was initiated. Measured in seconds since the Unix epoch. */
    initiated_at: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The outcome of the reported payment. */
    outcome?: "failed" | "guaranteed";
    /** Information about the Payment Method debited for this payment. */
    payment_method_details: { billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string }; custom?: { display_name?: string; type?: string }; payment_method?: string; type?: "custom" };
    /** Processor information for this payment. */
    processor_details?: { custom?: { payment_reference: string }; type: "custom" };
    /** Shipping information for this payment. */
    shipping_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name?: string; phone?: string };
  }) => Promise<PaymentRecord>;

  /**
   * Retrieve a Payment Record
   */
  getPaymentRecordsId: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the Payment Record. */
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PaymentRecord>;

  /**
   * Report a payment attempt
   */
  postPaymentRecordsIdReportPaymentAttempt: (input: {
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about the payment attempt failure. */
    failed?: { failed_at: number };
    /** Information about the payment attempt guarantee. */
    guaranteed?: { guaranteed_at: number };
    /** When the reported payment was initiated. Measured in seconds since the Unix epoch. */
    initiated_at: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The outcome of the reported payment. */
    outcome?: "failed" | "guaranteed";
    /** Information about the Payment Method debited for this payment. */
    payment_method_details?: { billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string }; custom?: { display_name?: string; type?: string }; payment_method?: string; type?: "custom" };
    /** Shipping information for this payment. */
    shipping_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name?: string; phone?: string };
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * Report payment attempt canceled
   */
  postPaymentRecordsIdReportPaymentAttemptCanceled: (input: {
    /** When the reported payment was canceled. Measured in seconds since the Unix epoch. */
    canceled_at: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * Report payment attempt failed
   */
  postPaymentRecordsIdReportPaymentAttemptFailed: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** When the reported payment failed. Measured in seconds since the Unix epoch. */
    failed_at: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * Report payment attempt guaranteed
   */
  postPaymentRecordsIdReportPaymentAttemptGuaranteed: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** When the reported payment was guaranteed. Measured in seconds since the Unix epoch. */
    guaranteed_at: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * Report payment attempt informational
   */
  postPaymentRecordsIdReportPaymentAttemptInformational: (input: {
    /** Customer information for this payment. */
    customer_details?: { customer?: string; email?: string; name?: string; phone?: string };
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Shipping information for this payment. */
    shipping_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name?: string; phone?: string } | "";
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * Report a refund
   */
  postPaymentRecordsIdReportRefund: (input: {
    /** A positive integer in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) representing how much of this payment to refund. Can refund only up to the remaining, unrefunded amount of the payment. */
    amount?: { currency: string; value: number };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** When the reported refund was initiated. Measured in seconds since the Unix epoch. */
    initiated_at?: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The outcome of the reported refund. */
    outcome: "refunded";
    /** Processor information for this refund. */
    processor_details: { custom?: { refund_reference: string }; type: "custom" };
    /** Information about the payment attempt refund. */
    refunded: { refunded_at: number };
    /** The ID of the Payment Record. */
    id: string;
  }) => Promise<PaymentRecord>;

  /**
   * List all payouts
   */
  getPayouts: (input: {
    body?: { [key: string]: unknown };
    /** Only return payouts that are expected to arrive during the given date interval. */
    arrival_date?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return payouts that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** The ID of an external account - only return payouts sent to this external account. */
    destination?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return payouts that have the given status: `pending`, `paid`, `failed`, or `canceled`. */
    status?: string;
  }) => Promise<{ data: (Payout)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a payout
   */
  postPayouts: (input: {
    /** A positive integer in cents representing how much to payout. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The ID of a bank account or a card to send the payout to. If you don't provide a destination, we use the default external account for the specified currency. */
    destination?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The method used to send this payout, which is `standard` or `instant`. We support `instant` for payouts to debit cards and bank accounts in certain countries. Learn more about [bank support for Instant Payouts](https://stripe.com/docs/payouts/instant-payouts-banks). */
    method?: "instant" | "standard";
    /** The ID of a v2 FinancialAccount to send funds to. */
    payout_method?: string;
    /** The balance type of your Stripe balance to draw this payout from. Balances for different payment sources are kept separately. You can find the amounts with the Balances API. One of `bank_account`, `card`, or `fpx`. */
    source_type?: "bank_account" | "card" | "fpx";
    /** A string that displays on the recipient's bank or card statement (up to 22 characters). A `statement_descriptor` that's longer than 22 characters return an error. Most banks truncate this information and display it inconsistently. Some banks might not display it at all. For US ACH payouts, this maps to the ACH Company Entry Description field, which the NACHA standard limits to 10 characters. Stripe truncates descriptors longer than 10 characters for US ACH payouts. */
    statement_descriptor?: string;
  }) => Promise<Payout>;

  /**
   * Retrieve a payout
   */
  getPayoutsPayout: (input: {
    body?: { [key: string]: unknown };
    payout: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Payout>;

  /**
   * Update a payout
   */
  postPayoutsPayout: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    payout: string;
  }) => Promise<Payout>;

  /**
   * Cancel a payout
   */
  postPayoutsPayoutCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    payout: string;
  }) => Promise<Payout>;

  /**
   * Reverse a payout
   */
  postPayoutsPayoutReverse: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    payout: string;
  }) => Promise<Payout>;

  /**
   * List all plans
   */
  getPlans: (input: {
    body?: { [key: string]: unknown };
    /** Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans). */
    active?: boolean;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return plans for the given product. */
    product?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Plan)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a plan
   */
  postPlans: (input: {
    /** Whether the plan is currently available for new subscriptions. Defaults to `true`. */
    active?: boolean;
    /** A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to charge on a recurring basis. */
    amount?: number;
    /** Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount` and `amount_decimal` can be set. */
    amount_decimal?: string;
    /** Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes. */
    billing_scheme?: "per_unit" | "tiered";
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer. You can optionally override this ID, but the ID must be unique across all plans in your Stripe account. You can, however, use the same plan ID in both live and test modes. */
    id?: string;
    /** Specifies billing frequency. Either `day`, `week`, `month` or `year`. */
    interval: "day" | "month" | "week" | "year";
    /** The number of intervals between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36 months, or 156 weeks). */
    interval_count?: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The meter tracking the usage of a metered price */
    meter?: string;
    /** A brief description of the plan, hidden from customers. */
    nickname?: string;
    product?: { active?: boolean; id?: string; metadata?: { [key: string]: string | undefined }; name: string; statement_descriptor?: string; tax_code?: string; unit_label?: string } | string;
    /** Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`. */
    tiers?: ({ flat_amount?: number; flat_amount_decimal?: string; unit_amount?: number; unit_amount_decimal?: string; up_to: "inf" | number })[];
    /** Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows. */
    tiers_mode?: "graduated" | "volume";
    /** Apply a transformation to the reported usage or set quantity before computing the billed price. Cannot be combined with `tiers`. */
    transform_usage?: { divide_by: number; round: "down" | "up" };
    /** Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan). */
    trial_period_days?: number;
    /** Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`. */
    usage_type?: "licensed" | "metered";
  }) => Promise<Plan>;

  /**
   * Delete a plan
   */
  deletePlansPlan: (input: {
    body?: { [key: string]: unknown };
    plan: string;
  }) => Promise<DeletedPlan>;

  /**
   * Retrieve a plan
   */
  getPlansPlan: (input: {
    body?: { [key: string]: unknown };
    plan: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Plan>;

  /**
   * Update a plan
   */
  postPlansPlan: (input: {
    /** Whether the plan is currently available for new subscriptions. */
    active?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** A brief description of the plan, hidden from customers. */
    nickname?: string;
    /** The product the plan belongs to. This cannot be changed once it has been used in a subscription or subscription schedule. */
    product?: string;
    /** Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan). */
    trial_period_days?: number;
    plan: string;
  }) => Promise<Plan>;

  /**
   * List all prices
   */
  getPrices: (input: {
    body?: { [key: string]: unknown };
    /** Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices). */
    active?: boolean;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return prices for the given currency. */
    currency?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return the price with these lookup_keys, if any exist. You can specify up to 10 lookup_keys. */
    lookup_keys?: (string)[];
    /** Only return prices for the given product. */
    product?: string;
    /** Only return prices with these recurring fields. */
    recurring?: { interval?: "day" | "month" | "week" | "year"; meter?: string; usage_type?: "licensed" | "metered" };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return prices of type `recurring` or `one_time`. */
    type?: "one_time" | "recurring";
  }) => Promise<{ data: (Price)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a price
   */
  postPrices: (input: {
    /** Whether the price can be used for new purchases. Defaults to `true`. */
    active?: boolean;
    /** Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes. */
    billing_scheme?: "per_unit" | "tiered";
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** Prices defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies). */
    currency_options?: { [key: string]: { custom_unit_amount?: { enabled: boolean; maximum?: number; minimum?: number; preset?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tiers?: ({ flat_amount?: number; flat_amount_decimal?: string; unit_amount?: number; unit_amount_decimal?: string; up_to: "inf" | number })[]; unit_amount?: number; unit_amount_decimal?: string } | undefined };
    /** When set, provides configuration for the amount to be adjusted by the customer during Checkout Sessions and Payment Links. */
    custom_unit_amount?: { enabled: boolean; maximum?: number; minimum?: number; preset?: number };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters. */
    lookup_key?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** A brief description of the price, hidden from customers. */
    nickname?: string;
    /** The ID of the [Product](https://docs.stripe.com/api/products) that this [Price](https://docs.stripe.com/api/prices) will belong to. */
    product?: string;
    /** These fields can be used to create a new product that this price will belong to. */
    product_data?: { active?: boolean; id?: string; metadata?: { [key: string]: string | undefined }; name: string; statement_descriptor?: string; tax_code?: string; unit_label?: string };
    /** The recurring components of a price such as `interval` and `usage_type`. */
    recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number; meter?: string; usage_type?: "licensed" | "metered" };
    /** Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    /** Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`. */
    tiers?: ({ flat_amount?: number; flat_amount_decimal?: string; unit_amount?: number; unit_amount_decimal?: string; up_to: "inf" | number })[];
    /** Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows. */
    tiers_mode?: "graduated" | "volume";
    /** If set to true, will atomically remove the lookup key from the existing price, and assign it to this price. */
    transfer_lookup_key?: boolean;
    /** Apply a transformation to the reported usage or set quantity before computing the billed price. Cannot be combined with `tiers`. */
    transform_quantity?: { divide_by: number; round: "down" | "up" };
    /** A positive integer in cents (or local equivalent) (or 0 for a free price) representing how much to charge. One of `unit_amount`, `unit_amount_decimal`, or `custom_unit_amount` is required, unless `billing_scheme=tiered`. */
    unit_amount?: number;
    /** Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set. */
    unit_amount_decimal?: string;
  }) => Promise<Price>;

  /**
   * Search prices
   */
  getPricesSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for prices](https://docs.stripe.com/search#query-fields-for-prices). */
    query: string;
  }) => Promise<{ data: (Price)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Retrieve a price
   */
  getPricesPrice: (input: {
    body?: { [key: string]: unknown };
    price: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Price>;

  /**
   * Update a price
   */
  postPricesPrice: (input: {
    /** Whether the price can be used for new purchases. Defaults to `true`. */
    active?: boolean;
    /** Prices defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies). */
    currency_options?: { [key: string]: { custom_unit_amount?: { enabled: boolean; maximum?: number; minimum?: number; preset?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tiers?: ({ flat_amount?: number; flat_amount_decimal?: string; unit_amount?: number; unit_amount_decimal?: string; up_to: "inf" | number })[]; unit_amount?: number; unit_amount_decimal?: string } | undefined } | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters. */
    lookup_key?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** A brief description of the price, hidden from customers. */
    nickname?: string;
    /** Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    /** If set to true, will atomically remove the lookup key from the existing price, and assign it to this price. */
    transfer_lookup_key?: boolean;
    price: string;
  }) => Promise<Price>;

  /**
   * List all products
   */
  getProducts: (input: {
    body?: { [key: string]: unknown };
    /** Only return products that are active or inactive (e.g., pass `false` to list all inactive products). */
    active?: boolean;
    /** Only return products that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Only return products with the given IDs. Cannot be used with [starting_after](https://api.stripe.com#list_products-starting_after) or [ending_before](https://api.stripe.com#list_products-ending_before). */
    ids?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return products that can be shipped (i.e., physical, not digital products). */
    shippable?: boolean;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return products with the given url. */
    url?: string;
  }) => Promise<{ data: (Product)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a product
   */
  postProducts: (input: {
    /** Whether the product is currently available for purchase. Defaults to `true`. */
    active?: boolean;
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object. This Price will be set as the default price for this product. */
    default_price_data?: { currency: string; currency_options?: { [key: string]: { custom_unit_amount?: { enabled: boolean; maximum?: number; minimum?: number; preset?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; tiers?: ({ flat_amount?: number; flat_amount_decimal?: string; unit_amount?: number; unit_amount_decimal?: string; up_to: "inf" | number })[]; unit_amount?: number; unit_amount_decimal?: string } | undefined }; custom_unit_amount?: { enabled: boolean; maximum?: number; minimum?: number; preset?: number }; metadata?: { [key: string]: string | undefined }; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** An identifier will be randomly generated by Stripe. You can optionally override this ID, but the ID must be unique across all products in your Stripe account. */
    id?: string;
    /** A list of up to 8 URLs of images for this product, meant to be displayable to the customer. */
    images?: (string)[];
    /** A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://docs.stripe.com/payments/checkout/pricing-table). */
    marketing_features?: ({ name: string })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The product's name, meant to be displayable to the customer. */
    name: string;
    /** The dimensions of this product for shipping purposes. */
    package_dimensions?: { height: number; length: number; weight: number; width: number };
    /** Whether this product is shipped (i.e., physical goods). */
    shippable?: boolean;
    /** An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\`, `"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. Only used for subscription payments. */
    statement_descriptor?: string;
    /** A [tax code](https://docs.stripe.com/tax/tax-categories) ID. */
    tax_code?: string;
    /** A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal. */
    unit_label?: string;
    /** A URL of a publicly-accessible webpage for this product. */
    url?: string;
  }) => Promise<Product>;

  /**
   * Search products
   */
  getProductsSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for products](https://docs.stripe.com/search#query-fields-for-products). */
    query: string;
  }) => Promise<{ data: (Product)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Delete a product
   */
  deleteProductsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
  }) => Promise<DeletedProduct>;

  /**
   * Retrieve a product
   */
  getProductsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Product>;

  /**
   * Update a product
   */
  postProductsId: (input: {
    /** Whether the product is available for purchase. */
    active?: boolean;
    /** The ID of the [Price](https://docs.stripe.com/api/prices) object that is the default price for this product. */
    default_price?: string;
    /** The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes. */
    description?: string | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A list of up to 8 URLs of images for this product, meant to be displayable to the customer. */
    images?: (string)[] | "";
    /** A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://docs.stripe.com/payments/checkout/pricing-table). */
    marketing_features?: ({ name: string })[] | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The product's name, meant to be displayable to the customer. */
    name?: string;
    /** The dimensions of this product for shipping purposes. */
    package_dimensions?: { height: number; length: number; weight: number; width: number } | "";
    /** Whether this product is shipped (i.e., physical goods). */
    shippable?: boolean;
    /** An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\`, `"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. May only be set if `type=service`. Only used for subscription payments. */
    statement_descriptor?: string;
    /** A [tax code](https://docs.stripe.com/tax/tax-categories) ID. */
    tax_code?: string | "";
    /** A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal. May only be set if `type=service`. */
    unit_label?: string | "";
    /** A URL of a publicly-accessible webpage for this product. */
    url?: string | "";
    id: string;
  }) => Promise<Product>;

  /**
   * List all features attached to a product
   */
  getProductsProductFeatures: (input: {
    body?: { [key: string]: unknown };
    product: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ProductFeature)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Attach a feature to a product
   */
  postProductsProductFeatures: (input: {
    /** The ID of the [Feature](https://docs.stripe.com/api/entitlements/feature) object attached to this product. */
    entitlement_feature: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    product: string;
  }) => Promise<ProductFeature>;

  /**
   * Remove a feature from a product
   */
  deleteProductsProductFeaturesId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    product: string;
  }) => Promise<DeletedProductFeature>;

  /**
   * Retrieve a product_feature
   */
  getProductsProductFeaturesId: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the product_feature. */
    id: string;
    /** The ID of the product. */
    product: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ProductFeature>;

  /**
   * List all promotion codes
   */
  getPromotionCodes: (input: {
    body?: { [key: string]: unknown };
    /** Filter promotion codes by whether they are active. */
    active?: boolean;
    /** Only return promotion codes that have this case-insensitive code. */
    code?: string;
    /** Only return promotion codes for this coupon. */
    coupon?: string;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return promotion codes that are restricted to this customer. */
    customer?: string;
    /** Only return promotion codes that are restricted to this account representing the customer. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (PromotionCode)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a promotion code
   */
  postPromotionCodes: (input: {
    /** Whether the promotion code is currently active. */
    active?: boolean;
    /** The customer-facing code. Regardless of case, this code must be unique across all active promotion codes for a specific customer. Valid characters are lower case letters (a-z), upper case letters (A-Z), digits (0-9), and dashes (-).  If left blank, we will generate one automatically. */
    code?: string;
    /** The customer who can use this promotion code. If not set, all customers can use the promotion code. */
    customer?: string;
    /** The account representing the customer who can use this promotion code. If not set, all customers can use the promotion code. */
    customer_account?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The timestamp at which this promotion code will expire. If the coupon has specified a `redeems_by`, then this value cannot be after the coupon's `redeems_by`. */
    expires_at?: number;
    /** A positive integer specifying the number of times the promotion code can be redeemed. If the coupon has specified a `max_redemptions`, then this value cannot be greater than the coupon's `max_redemptions`. */
    max_redemptions?: number;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The promotion referenced by this promotion code. */
    promotion: { coupon?: string; type: "coupon" };
    /** Settings that restrict the redemption of the promotion code. */
    restrictions?: { currency_options?: { [key: string]: { minimum_amount?: number } | undefined }; first_time_transaction?: boolean; minimum_amount?: number; minimum_amount_currency?: string };
  }) => Promise<PromotionCode>;

  /**
   * Retrieve a promotion code
   */
  getPromotionCodesPromotionCode: (input: {
    body?: { [key: string]: unknown };
    promotion_code: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<PromotionCode>;

  /**
   * Update a promotion code
   */
  postPromotionCodesPromotionCode: (input: {
    /** Whether the promotion code is currently active. A promotion code can only be reactivated when the coupon is still valid and the promotion code is otherwise redeemable. */
    active?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Settings that restrict the redemption of the promotion code. */
    restrictions?: { currency_options?: { [key: string]: { minimum_amount?: number } | undefined } };
    promotion_code: string;
  }) => Promise<PromotionCode>;

  /**
   * List all quotes
   */
  getQuotes: (input: {
    body?: { [key: string]: unknown };
    /** The ID of the customer whose quotes you're retrieving. */
    customer?: string;
    /** The ID of the account representing the customer whose quotes you're retrieving. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The status of the quote. */
    status?: "accepted" | "canceled" | "draft" | "open";
    /** Provides a list of quotes that are associated with the specified test clock. The response will not include quotes with test clocks if this and the customer parameter is not set. */
    test_clock?: string;
  }) => Promise<{ data: (Quote)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a quote
   */
  postQuotes: (input: {
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. There cannot be any line items with recurring prices when using this field. */
    application_fee_amount?: number | "";
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line item with a recurring price to use this field. */
    application_fee_percent?: number | "";
    /** Settings for automatic tax lookup for this quote and resulting invoices and subscriptions. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed. */
    customer?: string;
    /** The account for which this quote belongs to. A customer or account is required before finalizing the quote. Once specified, it cannot be changed. */
    customer_account?: string;
    /** The tax rates that will apply to any line item that does not have `tax_rates` set. */
    default_tax_rates?: (string)[] | "";
    /** A description that will be displayed on the quote PDF. If no value is passed, the default description configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used. */
    description?: string | "";
    /** The discounts applied to the quote. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. If no value is passed, the default expiration date configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used. */
    expires_at?: number;
    /** A footer that will be displayed on the quote PDF. If no value is passed, the default footer configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used. */
    footer?: string | "";
    /** Clone an existing quote. The new quote will be created in `status=draft`. When using this parameter, you cannot specify any other parameters except for `expires_at`. */
    from_quote?: { is_revision?: boolean; quote: string };
    /** A header that will be displayed on the quote PDF. If no value is passed, the default header configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used. */
    header?: string | "";
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost. */
    line_items?: ({ discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; price?: string; price_data?: { currency: string; product: string; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The account on behalf of which to charge. */
    on_behalf_of?: string | "";
    /** When creating a subscription or subscription schedule, the specified configuration data will be used. There must be at least one line item with a recurring price for a subscription or subscription schedule to be created. A subscription schedule is created if `subscription_data[effective_date]` is present and in the future, otherwise a subscription is created. */
    subscription_data?: { billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" }; description?: string; effective_date?: "current_period_end" | number | ""; metadata?: { [key: string]: string | undefined }; trial_period_days?: number | "" };
    /** ID of the test clock to attach to the quote. */
    test_clock?: string;
    /** The data with which to automatically create a Transfer for each of the invoices. */
    transfer_data?: { amount?: number; amount_percent?: number; destination: string } | "";
  }) => Promise<Quote>;

  /**
   * Retrieve a quote
   */
  getQuotesQuote: (input: {
    body?: { [key: string]: unknown };
    quote: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Quote>;

  /**
   * Update a quote
   */
  postQuotesQuote: (input: {
    /** The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. There cannot be any line items with recurring prices when using this field. */
    application_fee_amount?: number | "";
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line item with a recurring price to use this field. */
    application_fee_percent?: number | "";
    /** Settings for automatic tax lookup for this quote and resulting invoices and subscriptions. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed. */
    customer?: string;
    /** The account for which this quote belongs to. A customer or account is required before finalizing the quote. Once specified, it cannot be changed. */
    customer_account?: string;
    /** The tax rates that will apply to any line item that does not have `tax_rates` set. */
    default_tax_rates?: (string)[] | "";
    /** A description that will be displayed on the quote PDF. */
    description?: string | "";
    /** The discounts applied to the quote. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. */
    expires_at?: number;
    /** A footer that will be displayed on the quote PDF. */
    footer?: string | "";
    /** A header that will be displayed on the quote PDF. */
    header?: string | "";
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost. */
    line_items?: ({ discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; id?: string; price?: string; price_data?: { currency: string; product: string; recurring?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The account on behalf of which to charge. */
    on_behalf_of?: string | "";
    /** When creating a subscription or subscription schedule, the specified configuration data will be used. There must be at least one line item with a recurring price for a subscription or subscription schedule to be created. A subscription schedule is created if `subscription_data[effective_date]` is present and in the future, otherwise a subscription is created. */
    subscription_data?: { description?: string | ""; effective_date?: "current_period_end" | number | ""; metadata?: { [key: string]: string | undefined }; trial_period_days?: number | "" };
    /** The data with which to automatically create a Transfer for each of the invoices. */
    transfer_data?: { amount?: number; amount_percent?: number; destination: string } | "";
    quote: string;
  }) => Promise<Quote>;

  /**
   * Accept a quote
   */
  postQuotesQuoteAccept: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    quote: string;
  }) => Promise<Quote>;

  /**
   * Cancel a quote
   */
  postQuotesQuoteCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    quote: string;
  }) => Promise<Quote>;

  /**
   * Retrieve a quote's upfront line items
   */
  getQuotesQuoteComputedUpfrontLineItems: (input: {
    body?: { [key: string]: unknown };
    quote: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Item)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Finalize a quote
   */
  postQuotesQuoteFinalize: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. */
    expires_at?: number;
    quote: string;
  }) => Promise<Quote>;

  /**
   * Retrieve a quote's line items
   */
  getQuotesQuoteLineItems: (input: {
    body?: { [key: string]: unknown };
    quote: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Item)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Download quote PDF
   */
  getQuotesQuotePdf: (input: {
    body?: { [key: string]: unknown };
    quote: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<string>;

  /**
   * List all early fraud warnings
   */
  getRadarEarlyFraudWarnings: (input: {
    body?: { [key: string]: unknown };
    /** Only return early fraud warnings for the charge specified by this charge ID. */
    charge?: string;
    /** Only return early fraud warnings that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return early fraud warnings for charges that were created by the PaymentIntent specified by this PaymentIntent ID. */
    payment_intent?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (RadarEarlyFraudWarning)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve an early fraud warning
   */
  getRadarEarlyFraudWarningsEarlyFraudWarning: (input: {
    body?: { [key: string]: unknown };
    early_fraud_warning: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<RadarEarlyFraudWarning>;

  /**
   * Create a Payment Evaluation
   */
  postRadarPaymentEvaluations: (input: {
    /** Details about the Client Device Metadata to associate with the payment evaluation. */
    client_device_metadata_details?: { radar_session: string };
    /** Details about the customer associated with the payment evaluation. */
    customer_details: { customer?: string; customer_account?: string; email?: string; name?: string; phone?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Details about the payment. */
    payment_details: { amount: number; currency: string; description?: string; money_movement_details?: { card?: { customer_presence?: "off_session" | "on_session"; payment_type?: "one_off" | "recurring" | "setup_one_off" | "setup_recurring" }; money_movement_type: "card" }; payment_method_details: { billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string }; payment_method: string }; shipping_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name?: string; phone?: string }; statement_descriptor?: string };
  }) => Promise<RadarPaymentEvaluation>;

  /**
   * List all value list items
   */
  getRadarValueListItems: (input: {
    body?: { [key: string]: unknown };
    /** Only return items that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Return items belonging to the parent list whose value matches the specified value (using an "is like" match). */
    value?: string;
    /** Identifier for the parent value list this item belongs to. */
    value_list: string;
  }) => Promise<{ data: (RadarValueListItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a value list item
   */
  postRadarValueListItems: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The value of the item (whose type must match the type of the parent value list). */
    value: string;
    /** The identifier of the value list which the created item will be added to. */
    value_list: string;
  }) => Promise<RadarValueListItem>;

  /**
   * Delete a value list item
   */
  deleteRadarValueListItemsItem: (input: {
    body?: { [key: string]: unknown };
    item: string;
  }) => Promise<DeletedRadarValueListItem>;

  /**
   * Retrieve a value list item
   */
  getRadarValueListItemsItem: (input: {
    body?: { [key: string]: unknown };
    item: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<RadarValueListItem>;

  /**
   * List all value lists
   */
  getRadarValueLists: (input: {
    body?: { [key: string]: unknown };
    /** The alias used to reference the value list when writing rules. */
    alias?: string;
    /** A value contained within a value list - returns all value lists containing this value. */
    contains?: string;
    /** Only return value lists that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (RadarValueList)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a value list
   */
  postRadarValueLists: (input: {
    /** The name of the value list for use in rules. */
    alias: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Type of the items in the value list. One of `card_fingerprint`, `card_bin`, `crypto_fingerprint`, `email`, `ip_address`, `country`, `string`, `case_sensitive_string`, `customer_id`, `account`, `sepa_debit_fingerprint`, or `us_bank_account_fingerprint`. Use `string` if the item type is unknown or mixed. */
    item_type?: "account" | "card_bin" | "card_fingerprint" | "case_sensitive_string" | "country" | "crypto_fingerprint" | "customer_id" | "email" | "ip_address" | "sepa_debit_fingerprint" | "string" | "us_bank_account_fingerprint";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The human-readable name of the value list. */
    name: string;
  }) => Promise<RadarValueList>;

  /**
   * Delete a value list
   */
  deleteRadarValueListsValueList: (input: {
    body?: { [key: string]: unknown };
    value_list: string;
  }) => Promise<DeletedRadarValueList>;

  /**
   * Retrieve a value list
   */
  getRadarValueListsValueList: (input: {
    body?: { [key: string]: unknown };
    value_list: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<RadarValueList>;

  /**
   * Update a value list
   */
  postRadarValueListsValueList: (input: {
    /** The name of the value list for use in rules. */
    alias?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The human-readable name of the value list. */
    name?: string;
    value_list: string;
  }) => Promise<RadarValueList>;

  /**
   * List all refunds
   */
  getRefunds: (input: {
    body?: { [key: string]: unknown };
    /** Only return refunds for the charge specified by this charge ID. */
    charge?: string;
    /** Only return refunds that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return refunds for the PaymentIntent specified by this ID. */
    payment_intent?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Refund)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create customer balance refund
   */
  postRefunds: (input: {
    amount?: number;
    /** The identifier of the charge to refund. */
    charge?: string;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Customer whose customer balance to refund from. */
    customer?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions. */
    instructions_email?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Origin of the refund */
    origin?: "customer_balance";
    /** The identifier of the PaymentIntent to refund. */
    payment_intent?: string;
    /** String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://docs.stripe.com/radar/lists), and will also help us improve our fraud detection algorithms. */
    reason?: "duplicate" | "fraudulent" | "requested_by_customer";
    /** Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge. */
    refund_application_fee?: boolean;
    /** Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge. */
    reverse_transfer?: boolean;
  }) => Promise<Refund>;

  /**
   * Retrieve a refund
   */
  getRefundsRefund: (input: {
    body?: { [key: string]: unknown };
    refund: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Refund>;

  /**
   * Update a refund
   */
  postRefundsRefund: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    refund: string;
  }) => Promise<Refund>;

  /**
   * Cancel a refund
   */
  postRefundsRefundCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    refund: string;
  }) => Promise<Refund>;

  /**
   * List all Report Runs
   */
  getReportingReportRuns: (input: {
    body?: { [key: string]: unknown };
    /** Only return Report Runs that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ReportingReportRun)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Report Run
   */
  postReportingReportRuns: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Parameters specifying how the report should be run. Different Report Types have different required and optional parameters, listed in the [API Access to Reports](https://docs.stripe.com/reporting/statements/api) documentation. */
    parameters?: { columns?: (string)[]; connected_account?: string; currency?: string; interval_end?: number; interval_start?: number; payout?: string; reporting_category?: "advance" | "advance_funding" | "anticipation_repayment" | "charge" | "charge_failure" | "climate_order_purchase" | "climate_order_refund" | "connect_collection_transfer" | "connect_reserved_funds" | "contribution" | "dispute" | "dispute_reversal" | "fee" | "financing_paydown" | "financing_paydown_reversal" | "financing_payout" | "financing_payout_reversal" | "issuing_authorization_hold" | "issuing_authorization_release" | "issuing_dispute" | "issuing_transaction" | "network_cost" | "other_adjustment" | "partial_capture_reversal" | "payout" | "payout_reversal" | "platform_earning" | "platform_earning_refund" | "refund" | "refund_failure" | "risk_reserved_funds" | "tax" | "topup" | "topup_reversal" | "transfer" | "transfer_reversal" | "unreconciled_customer_funds"; timezone?: "Africa/Abidjan" | "Africa/Accra" | "Africa/Addis_Ababa" | "Africa/Algiers" | "Africa/Asmara" | "Africa/Asmera" | "Africa/Bamako" | "Africa/Bangui" | "Africa/Banjul" | "Africa/Bissau" | "Africa/Blantyre" | "Africa/Brazzaville" | "Africa/Bujumbura" | "Africa/Cairo" | "Africa/Casablanca" | "Africa/Ceuta" | "Africa/Conakry" | "Africa/Dakar" | "Africa/Dar_es_Salaam" | "Africa/Djibouti" | "Africa/Douala" | "Africa/El_Aaiun" | "Africa/Freetown" | "Africa/Gaborone" | "Africa/Harare" | "Africa/Johannesburg" | "Africa/Juba" | "Africa/Kampala" | "Africa/Khartoum" | "Africa/Kigali" | "Africa/Kinshasa" | "Africa/Lagos" | "Africa/Libreville" | "Africa/Lome" | "Africa/Luanda" | "Africa/Lubumbashi" | "Africa/Lusaka" | "Africa/Malabo" | "Africa/Maputo" | "Africa/Maseru" | "Africa/Mbabane" | "Africa/Mogadishu" | "Africa/Monrovia" | "Africa/Nairobi" | "Africa/Ndjamena" | "Africa/Niamey" | "Africa/Nouakchott" | "Africa/Ouagadougou" | "Africa/Porto-Novo" | "Africa/Sao_Tome" | "Africa/Timbuktu" | "Africa/Tripoli" | "Africa/Tunis" | "Africa/Windhoek" | "America/Adak" | "America/Anchorage" | "America/Anguilla" | "America/Antigua" | "America/Araguaina" | "America/Argentina/Buenos_Aires" | "America/Argentina/Catamarca" | "America/Argentina/ComodRivadavia" | "America/Argentina/Cordoba" | "America/Argentina/Jujuy" | "America/Argentina/La_Rioja" | "America/Argentina/Mendoza" | "America/Argentina/Rio_Gallegos" | "America/Argentina/Salta" | "America/Argentina/San_Juan" | "America/Argentina/San_Luis" | "America/Argentina/Tucuman" | "America/Argentina/Ushuaia" | "America/Aruba" | "America/Asuncion" | "America/Atikokan" | "America/Atka" | "America/Bahia" | "America/Bahia_Banderas" | "America/Barbados" | "America/Belem" | "America/Belize" | "America/Blanc-Sablon" | "America/Boa_Vista" | "America/Bogota" | "America/Boise" | "America/Buenos_Aires" | "America/Cambridge_Bay" | "America/Campo_Grande" | "America/Cancun" | "America/Caracas" | "America/Catamarca" | "America/Cayenne" | "America/Cayman" | "America/Chicago" | "America/Chihuahua" | "America/Ciudad_Juarez" | "America/Coral_Harbour" | "America/Cordoba" | "America/Costa_Rica" | "America/Coyhaique" | "America/Creston" | "America/Cuiaba" | "America/Curacao" | "America/Danmarkshavn" | "America/Dawson" | "America/Dawson_Creek" | "America/Denver" | "America/Detroit" | "America/Dominica" | "America/Edmonton" | "America/Eirunepe" | "America/El_Salvador" | "America/Ensenada" | "America/Fort_Nelson" | "America/Fort_Wayne" | "America/Fortaleza" | "America/Glace_Bay" | "America/Godthab" | "America/Goose_Bay" | "America/Grand_Turk" | "America/Grenada" | "America/Guadeloupe" | "America/Guatemala" | "America/Guayaquil" | "America/Guyana" | "America/Halifax" | "America/Havana" | "America/Hermosillo" | "America/Indiana/Indianapolis" | "America/Indiana/Knox" | "America/Indiana/Marengo" | "America/Indiana/Petersburg" | "America/Indiana/Tell_City" | "America/Indiana/Vevay" | "America/Indiana/Vincennes" | "America/Indiana/Winamac" | "America/Indianapolis" | "America/Inuvik" | "America/Iqaluit" | "America/Jamaica" | "America/Jujuy" | "America/Juneau" | "America/Kentucky/Louisville" | "America/Kentucky/Monticello" | "America/Knox_IN" | "America/Kralendijk" | "America/La_Paz" | "America/Lima" | "America/Los_Angeles" | "America/Louisville" | "America/Lower_Princes" | "America/Maceio" | "America/Managua" | "America/Manaus" | "America/Marigot" | "America/Martinique" | "America/Matamoros" | "America/Mazatlan" | "America/Mendoza" | "America/Menominee" | "America/Merida" | "America/Metlakatla" | "America/Mexico_City" | "America/Miquelon" | "America/Moncton" | "America/Monterrey" | "America/Montevideo" | "America/Montreal" | "America/Montserrat" | "America/Nassau" | "America/New_York" | "America/Nipigon" | "America/Nome" | "America/Noronha" | "America/North_Dakota/Beulah" | "America/North_Dakota/Center" | "America/North_Dakota/New_Salem" | "America/Nuuk" | "America/Ojinaga" | "America/Panama" | "America/Pangnirtung" | "America/Paramaribo" | "America/Phoenix" | "America/Port-au-Prince" | "America/Port_of_Spain" | "America/Porto_Acre" | "America/Porto_Velho" | "America/Puerto_Rico" | "America/Punta_Arenas" | "America/Rainy_River" | "America/Rankin_Inlet" | "America/Recife" | "America/Regina" | "America/Resolute" | "America/Rio_Branco" | "America/Rosario" | "America/Santa_Isabel" | "America/Santarem" | "America/Santiago" | "America/Santo_Domingo" | "America/Sao_Paulo" | "America/Scoresbysund" | "America/Shiprock" | "America/Sitka" | "America/St_Barthelemy" | "America/St_Johns" | "America/St_Kitts" | "America/St_Lucia" | "America/St_Thomas" | "America/St_Vincent" | "America/Swift_Current" | "America/Tegucigalpa" | "America/Thule" | "America/Thunder_Bay" | "America/Tijuana" | "America/Toronto" | "America/Tortola" | "America/Vancouver" | "America/Virgin" | "America/Whitehorse" | "America/Winnipeg" | "America/Yakutat" | "America/Yellowknife" | "Antarctica/Casey" | "Antarctica/Davis" | "Antarctica/DumontDUrville" | "Antarctica/Macquarie" | "Antarctica/Mawson" | "Antarctica/McMurdo" | "Antarctica/Palmer" | "Antarctica/Rothera" | "Antarctica/South_Pole" | "Antarctica/Syowa" | "Antarctica/Troll" | "Antarctica/Vostok" | "Arctic/Longyearbyen" | "Asia/Aden" | "Asia/Almaty" | "Asia/Amman" | "Asia/Anadyr" | "Asia/Aqtau" | "Asia/Aqtobe" | "Asia/Ashgabat" | "Asia/Ashkhabad" | "Asia/Atyrau" | "Asia/Baghdad" | "Asia/Bahrain" | "Asia/Baku" | "Asia/Bangkok" | "Asia/Barnaul" | "Asia/Beirut" | "Asia/Bishkek" | "Asia/Brunei" | "Asia/Calcutta" | "Asia/Chita" | "Asia/Choibalsan" | "Asia/Chongqing" | "Asia/Chungking" | "Asia/Colombo" | "Asia/Dacca" | "Asia/Damascus" | "Asia/Dhaka" | "Asia/Dili" | "Asia/Dubai" | "Asia/Dushanbe" | "Asia/Famagusta" | "Asia/Gaza" | "Asia/Harbin" | "Asia/Hebron" | "Asia/Ho_Chi_Minh" | "Asia/Hong_Kong" | "Asia/Hovd" | "Asia/Irkutsk" | "Asia/Istanbul" | "Asia/Jakarta" | "Asia/Jayapura" | "Asia/Jerusalem" | "Asia/Kabul" | "Asia/Kamchatka" | "Asia/Karachi" | "Asia/Kashgar" | "Asia/Kathmandu" | "Asia/Katmandu" | "Asia/Khandyga" | "Asia/Kolkata" | "Asia/Krasnoyarsk" | "Asia/Kuala_Lumpur" | "Asia/Kuching" | "Asia/Kuwait" | "Asia/Macao" | "Asia/Macau" | "Asia/Magadan" | "Asia/Makassar" | "Asia/Manila" | "Asia/Muscat" | "Asia/Nicosia" | "Asia/Novokuznetsk" | "Asia/Novosibirsk" | "Asia/Omsk" | "Asia/Oral" | "Asia/Phnom_Penh" | "Asia/Pontianak" | "Asia/Pyongyang" | "Asia/Qatar" | "Asia/Qostanay" | "Asia/Qyzylorda" | "Asia/Rangoon" | "Asia/Riyadh" | "Asia/Saigon" | "Asia/Sakhalin" | "Asia/Samarkand" | "Asia/Seoul" | "Asia/Shanghai" | "Asia/Singapore" | "Asia/Srednekolymsk" | "Asia/Taipei" | "Asia/Tashkent" | "Asia/Tbilisi" | "Asia/Tehran" | "Asia/Tel_Aviv" | "Asia/Thimbu" | "Asia/Thimphu" | "Asia/Tokyo" | "Asia/Tomsk" | "Asia/Ujung_Pandang" | "Asia/Ulaanbaatar" | "Asia/Ulan_Bator" | "Asia/Urumqi" | "Asia/Ust-Nera" | "Asia/Vientiane" | "Asia/Vladivostok" | "Asia/Yakutsk" | "Asia/Yangon" | "Asia/Yekaterinburg" | "Asia/Yerevan" | "Atlantic/Azores" | "Atlantic/Bermuda" | "Atlantic/Canary" | "Atlantic/Cape_Verde" | "Atlantic/Faeroe" | "Atlantic/Faroe" | "Atlantic/Jan_Mayen" | "Atlantic/Madeira" | "Atlantic/Reykjavik" | "Atlantic/South_Georgia" | "Atlantic/St_Helena" | "Atlantic/Stanley" | "Australia/ACT" | "Australia/Adelaide" | "Australia/Brisbane" | "Australia/Broken_Hill" | "Australia/Canberra" | "Australia/Currie" | "Australia/Darwin" | "Australia/Eucla" | "Australia/Hobart" | "Australia/LHI" | "Australia/Lindeman" | "Australia/Lord_Howe" | "Australia/Melbourne" | "Australia/NSW" | "Australia/North" | "Australia/Perth" | "Australia/Queensland" | "Australia/South" | "Australia/Sydney" | "Australia/Tasmania" | "Australia/Victoria" | "Australia/West" | "Australia/Yancowinna" | "Brazil/Acre" | "Brazil/DeNoronha" | "Brazil/East" | "Brazil/West" | "CET" | "CST6CDT" | "Canada/Atlantic" | "Canada/Central" | "Canada/Eastern" | "Canada/Mountain" | "Canada/Newfoundland" | "Canada/Pacific" | "Canada/Saskatchewan" | "Canada/Yukon" | "Chile/Continental" | "Chile/EasterIsland" | "Cuba" | "EET" | "EST" | "EST5EDT" | "Egypt" | "Eire" | "Etc/GMT" | "Etc/GMT+0" | "Etc/GMT+1" | "Etc/GMT+10" | "Etc/GMT+11" | "Etc/GMT+12" | "Etc/GMT+2" | "Etc/GMT+3" | "Etc/GMT+4" | "Etc/GMT+5" | "Etc/GMT+6" | "Etc/GMT+7" | "Etc/GMT+8" | "Etc/GMT+9" | "Etc/GMT-0" | "Etc/GMT-1" | "Etc/GMT-10" | "Etc/GMT-11" | "Etc/GMT-12" | "Etc/GMT-13" | "Etc/GMT-14" | "Etc/GMT-2" | "Etc/GMT-3" | "Etc/GMT-4" | "Etc/GMT-5" | "Etc/GMT-6" | "Etc/GMT-7" | "Etc/GMT-8" | "Etc/GMT-9" | "Etc/GMT0" | "Etc/Greenwich" | "Etc/UCT" | "Etc/UTC" | "Etc/Universal" | "Etc/Zulu" | "Europe/Amsterdam" | "Europe/Andorra" | "Europe/Astrakhan" | "Europe/Athens" | "Europe/Belfast" | "Europe/Belgrade" | "Europe/Berlin" | "Europe/Bratislava" | "Europe/Brussels" | "Europe/Bucharest" | "Europe/Budapest" | "Europe/Busingen" | "Europe/Chisinau" | "Europe/Copenhagen" | "Europe/Dublin" | "Europe/Gibraltar" | "Europe/Guernsey" | "Europe/Helsinki" | "Europe/Isle_of_Man" | "Europe/Istanbul" | "Europe/Jersey" | "Europe/Kaliningrad" | "Europe/Kiev" | "Europe/Kirov" | "Europe/Kyiv" | "Europe/Lisbon" | "Europe/Ljubljana" | "Europe/London" | "Europe/Luxembourg" | "Europe/Madrid" | "Europe/Malta" | "Europe/Mariehamn" | "Europe/Minsk" | "Europe/Monaco" | "Europe/Moscow" | "Europe/Nicosia" | "Europe/Oslo" | "Europe/Paris" | "Europe/Podgorica" | "Europe/Prague" | "Europe/Riga" | "Europe/Rome" | "Europe/Samara" | "Europe/San_Marino" | "Europe/Sarajevo" | "Europe/Saratov" | "Europe/Simferopol" | "Europe/Skopje" | "Europe/Sofia" | "Europe/Stockholm" | "Europe/Tallinn" | "Europe/Tirane" | "Europe/Tiraspol" | "Europe/Ulyanovsk" | "Europe/Uzhgorod" | "Europe/Vaduz" | "Europe/Vatican" | "Europe/Vienna" | "Europe/Vilnius" | "Europe/Volgograd" | "Europe/Warsaw" | "Europe/Zagreb" | "Europe/Zaporozhye" | "Europe/Zurich" | "Factory" | "GB" | "GB-Eire" | "GMT" | "GMT+0" | "GMT-0" | "GMT0" | "Greenwich" | "HST" | "Hongkong" | "Iceland" | "Indian/Antananarivo" | "Indian/Chagos" | "Indian/Christmas" | "Indian/Cocos" | "Indian/Comoro" | "Indian/Kerguelen" | "Indian/Mahe" | "Indian/Maldives" | "Indian/Mauritius" | "Indian/Mayotte" | "Indian/Reunion" | "Iran" | "Israel" | "Jamaica" | "Japan" | "Kwajalein" | "Libya" | "MET" | "MST" | "MST7MDT" | "Mexico/BajaNorte" | "Mexico/BajaSur" | "Mexico/General" | "NZ" | "NZ-CHAT" | "Navajo" | "PRC" | "PST8PDT" | "Pacific/Apia" | "Pacific/Auckland" | "Pacific/Bougainville" | "Pacific/Chatham" | "Pacific/Chuuk" | "Pacific/Easter" | "Pacific/Efate" | "Pacific/Enderbury" | "Pacific/Fakaofo" | "Pacific/Fiji" | "Pacific/Funafuti" | "Pacific/Galapagos" | "Pacific/Gambier" | "Pacific/Guadalcanal" | "Pacific/Guam" | "Pacific/Honolulu" | "Pacific/Johnston" | "Pacific/Kanton" | "Pacific/Kiritimati" | "Pacific/Kosrae" | "Pacific/Kwajalein" | "Pacific/Majuro" | "Pacific/Marquesas" | "Pacific/Midway" | "Pacific/Nauru" | "Pacific/Niue" | "Pacific/Norfolk" | "Pacific/Noumea" | "Pacific/Pago_Pago" | "Pacific/Palau" | "Pacific/Pitcairn" | "Pacific/Pohnpei" | "Pacific/Ponape" | "Pacific/Port_Moresby" | "Pacific/Rarotonga" | "Pacific/Saipan" | "Pacific/Samoa" | "Pacific/Tahiti" | "Pacific/Tarawa" | "Pacific/Tongatapu" | "Pacific/Truk" | "Pacific/Wake" | "Pacific/Wallis" | "Pacific/Yap" | "Poland" | "Portugal" | "ROC" | "ROK" | "Singapore" | "Turkey" | "UCT" | "US/Alaska" | "US/Aleutian" | "US/Arizona" | "US/Central" | "US/East-Indiana" | "US/Eastern" | "US/Hawaii" | "US/Indiana-Starke" | "US/Michigan" | "US/Mountain" | "US/Pacific" | "US/Pacific-New" | "US/Samoa" | "UTC" | "Universal" | "W-SU" | "WET" | "Zulu" };
    /** The ID of the [report type](https://docs.stripe.com/reporting/statements/api#report-types) to run, such as `"balance.summary.1"`. */
    report_type: string;
  }) => Promise<ReportingReportRun>;

  /**
   * Retrieve a Report Run
   */
  getReportingReportRunsReportRun: (input: {
    body?: { [key: string]: unknown };
    report_run: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ReportingReportRun>;

  /**
   * List all Report Types
   */
  getReportingReportTypes: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<{ data: (ReportingReportType)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Report Type
   */
  getReportingReportTypesReportType: (input: {
    body?: { [key: string]: unknown };
    report_type: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ReportingReportType>;

  /**
   * List all open reviews
   */
  getReviews: (input: {
    body?: { [key: string]: unknown };
    /** Only return reviews that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (Review)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a review
   */
  getReviewsReview: (input: {
    body?: { [key: string]: unknown };
    review: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Review>;

  /**
   * Approve a review
   */
  postReviewsReviewApprove: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    review: string;
  }) => Promise<Review>;

  /**
   * List all SetupAttempts
   */
  getSetupAttempts: (input: {
    body?: { [key: string]: unknown };
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return SetupAttempts created by the SetupIntent specified by this ID. */
    setup_intent: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (SetupAttempt)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * List all SetupIntents
   */
  getSetupIntents: (input: {
    body?: { [key: string]: unknown };
    /** If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer. */
    attach_to_self?: boolean;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return SetupIntents for the customer specified by this customer ID. */
    customer?: string;
    /** Only return SetupIntents for the account specified by this customer ID. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return SetupIntents that associate with the specified payment method. */
    payment_method?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (SetupIntent)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a SetupIntent
   */
  postSetupIntents: (input: {
    /** If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer. */
    attach_to_self?: boolean;
    /** When you enable this parameter, this SetupIntent accepts payment methods that you enable in the Dashboard and that are compatible with its other parameters. */
    automatic_payment_methods?: { allow_redirects?: "always" | "never"; enabled: boolean };
    /** Set to `true` to attempt to confirm this SetupIntent immediately. This parameter defaults to `false`. If a card is the attached payment method, you can provide a `return_url` in case further authentication is necessary. */
    confirm?: boolean;
    /** ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence. */
    confirmation_token?: string;
    /** ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent. */
    customer?: string;
    /** ID of the Account this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be used with this SetupIntent. */
    customer_account?: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The list of payment method types to exclude from use with this SetupIntent. */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes. */
    flow_directions?: ("inbound" | "outbound")[];
    /** This hash contains details about the mandate to create. This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/setup_intents/create#create_setup_intent-confirm). */
    mandate_data?: { customer_acceptance: { accepted_at?: number; offline?: { [key: string]: unknown }; online?: { ip_address: string; user_agent: string }; type: "offline" | "online" } } | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The Stripe account ID created for this SetupIntent. */
    on_behalf_of?: string;
    /** ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. */
    payment_method?: string;
    /** The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with this SetupIntent. */
    payment_method_configuration?: string;
    /** When included, this hash creates a PaymentMethod that is set as the [`payment_method`](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment method-specific configuration for this SetupIntent. */
    payment_method_options?: { acss_debit?: { currency?: "cad" | "usd"; mandate_options?: { custom_mandate_url?: string | ""; default_for?: ("invoice" | "subscription")[]; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" }; amazon_pay?: { [key: string]: unknown }; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" } }; bizum?: { [key: string]: unknown }; card?: { mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; currency: string; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge"; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram?: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id?: string; version?: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } }; card_present?: { [key: string]: unknown }; klarna?: { currency?: string; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing: { amount: number; date: string }; reference: string })[] | "" }; link?: { [key: string]: unknown }; paypal?: { billing_agreement_id?: string }; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility"; start_date?: string | "" } }; pix?: { mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string } }; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" } }; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" }; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } };
    /** The list of payment method types (for example, card) that this SetupIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type). */
    payment_method_types?: (string)[];
    /** The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. To redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://docs.stripe.com/api/setup_intents/create#create_setup_intent-confirm). */
    return_url?: string;
    /** If you populate this hash, this SetupIntent generates a `single_use` mandate after successful completion.  Single-use mandates are only valid for the following payment methods: `acss_debit`, `alipay`, `au_becs_debit`, `bacs_debit`, `bancontact`, `boleto`, `ideal`, `link`, `sepa_debit`, and `us_bank_account`. */
    single_use?: { amount: number; currency: string };
    /** Indicates how the payment method is intended to be used in the future. If not provided, this value defaults to `off_session`. */
    usage?: "off_session" | "on_session";
    /** Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions. */
    use_stripe_sdk?: boolean;
  }) => Promise<SetupIntent>;

  /**
   * Retrieve a SetupIntent
   */
  getSetupIntentsIntent: (input: {
    body?: { [key: string]: unknown };
    intent: string;
    /** The client secret of the SetupIntent. We require this string if you use a publishable key to retrieve the SetupIntent. */
    client_secret?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<SetupIntent>;

  /**
   * Update a SetupIntent
   */
  postSetupIntentsIntent: (input: {
    /** If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer. */
    attach_to_self?: boolean;
    /** ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent. */
    customer?: string;
    /** ID of the Account this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be used with this SetupIntent. */
    customer_account?: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The list of payment method types to exclude from use with this SetupIntent. */
    excluded_payment_method_types?: ("acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "card" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip")[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes. */
    flow_directions?: ("inbound" | "outbound")[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. To unset this field to null, pass in an empty string. */
    payment_method?: string;
    /** The ID of the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) to use with this SetupIntent. */
    payment_method_configuration?: string;
    /** When included, this hash creates a PaymentMethod that is set as the [`payment_method`](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment method-specific configuration for this SetupIntent. */
    payment_method_options?: { acss_debit?: { currency?: "cad" | "usd"; mandate_options?: { custom_mandate_url?: string | ""; default_for?: ("invoice" | "subscription")[]; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" }; amazon_pay?: { [key: string]: unknown }; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" } }; bizum?: { [key: string]: unknown }; card?: { mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; currency: string; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge"; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram?: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id?: string; version?: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } }; card_present?: { [key: string]: unknown }; klarna?: { currency?: string; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing: { amount: number; date: string }; reference: string })[] | "" }; link?: { [key: string]: unknown }; paypal?: { billing_agreement_id?: string }; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility"; start_date?: string | "" } }; pix?: { mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string } }; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" } }; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" }; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } };
    /** The list of payment method types (for example, card) that this SetupIntent can set up. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type). */
    payment_method_types?: (string)[];
    intent: string;
  }) => Promise<SetupIntent>;

  /**
   * Cancel a SetupIntent
   */
  postSetupIntentsIntentCancel: (input: {
    /** Reason for canceling this SetupIntent. Possible values are: `abandoned`, `requested_by_customer`, or `duplicate` */
    cancellation_reason?: "abandoned" | "duplicate" | "requested_by_customer";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    intent: string;
  }) => Promise<SetupIntent>;

  /**
   * Confirm a SetupIntent
   */
  postSetupIntentsIntentConfirm: (input: {
    /** The client secret of the SetupIntent. */
    client_secret?: string;
    /** ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence. */
    confirmation_token?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    mandate_data?: { customer_acceptance: { accepted_at?: number; offline?: { [key: string]: unknown }; online?: { ip_address: string; user_agent: string }; type: "offline" | "online" } } | "" | { customer_acceptance: { online: { ip_address?: string; user_agent?: string }; type: "online" } };
    /** ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. */
    payment_method?: string;
    /** When included, this hash creates a PaymentMethod that is set as the [`payment_method`](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment method-specific configuration for this SetupIntent. */
    payment_method_options?: { acss_debit?: { currency?: "cad" | "usd"; mandate_options?: { custom_mandate_url?: string | ""; default_for?: ("invoice" | "subscription")[]; interval_description?: string; payment_schedule?: "combined" | "interval" | "sporadic"; transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" }; amazon_pay?: { [key: string]: unknown }; bacs_debit?: { mandate_options?: { reference_prefix?: string | "" } }; bizum?: { [key: string]: unknown }; card?: { mandate_options?: { amount: number; amount_type: "fixed" | "maximum"; currency: string; description?: string; end_date?: number; interval: "day" | "month" | "sporadic" | "week" | "year"; interval_count?: number; reference: string; start_date: number; supported_types?: ("india")[] }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge"; three_d_secure?: { ares_trans_status?: "A" | "C" | "I" | "N" | "R" | "U" | "Y"; cryptogram?: string; electronic_commerce_indicator?: "01" | "02" | "05" | "06" | "07"; network_options?: { cartes_bancaires?: { cb_avalgo: "0" | "1" | "2" | "3" | "4" | "A"; cb_exemption?: string; cb_score?: number } }; requestor_challenge_indicator?: string; transaction_id?: string; version?: "1.0.2" | "2.1.0" | "2.2.0" | "2.3.0" | "2.3.1" } }; card_present?: { [key: string]: unknown }; klarna?: { currency?: string; on_demand?: { average_amount?: number; maximum_amount?: number; minimum_amount?: number; purchase_interval?: "day" | "month" | "week" | "year"; purchase_interval_count?: number }; preferred_locale?: "cs-CZ" | "da-DK" | "de-AT" | "de-CH" | "de-DE" | "el-GR" | "en-AT" | "en-AU" | "en-BE" | "en-CA" | "en-CH" | "en-CZ" | "en-DE" | "en-DK" | "en-ES" | "en-FI" | "en-FR" | "en-GB" | "en-GR" | "en-IE" | "en-IT" | "en-NL" | "en-NO" | "en-NZ" | "en-PL" | "en-PT" | "en-RO" | "en-SE" | "en-US" | "es-ES" | "es-US" | "fi-FI" | "fr-BE" | "fr-CA" | "fr-CH" | "fr-FR" | "it-CH" | "it-IT" | "nb-NO" | "nl-BE" | "nl-NL" | "pl-PL" | "pt-PT" | "ro-RO" | "sv-FI" | "sv-SE"; subscriptions?: ({ interval: "day" | "month" | "week" | "year"; interval_count?: number; name?: string; next_billing: { amount: number; date: string }; reference: string })[] | "" }; link?: { [key: string]: unknown }; paypal?: { billing_agreement_id?: string }; payto?: { mandate_options?: { amount?: number | ""; amount_type?: "" | "fixed" | "maximum"; end_date?: string | ""; payment_schedule?: "" | "adhoc" | "annual" | "daily" | "fortnightly" | "monthly" | "quarterly" | "semi_annual" | "weekly"; payments_per_period?: number | ""; purpose?: "" | "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility"; start_date?: string | "" } }; pix?: { mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; amount_type?: "fixed" | "maximum"; currency?: string; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly"; reference?: string; start_date?: string } }; sepa_debit?: { mandate_options?: { reference_prefix?: string | "" } }; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number }; setup_future_usage?: "" | "none" | "off_session" | "on_session" }; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[]; return_url?: string }; mandate_options?: { collection_method?: "" | "paper" }; networks?: { requested?: ("ach" | "us_domestic_wire")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } };
    /** The URL to redirect your customer back to after they authenticate on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods. */
    return_url?: string;
    /** Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions. */
    use_stripe_sdk?: boolean;
    intent: string;
  }) => Promise<SetupIntent>;

  /**
   * Verify microdeposits on a SetupIntent
   */
  postSetupIntentsIntentVerifyMicrodeposits: (input: {
    /** Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account. */
    amounts?: (number)[];
    /** The client secret of the SetupIntent. */
    client_secret?: string;
    /** A six-character code starting with SM present in the microdeposit sent to the bank account. */
    descriptor_code?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    intent: string;
  }) => Promise<SetupIntent>;

  /**
   * List all shipping rates
   */
  getShippingRates: (input: {
    body?: { [key: string]: unknown };
    /** Only return shipping rates that are active or inactive. */
    active?: boolean;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return shipping rates for the given currency. */
    currency?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ShippingRate)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a shipping rate
   */
  postShippingRates: (input: {
    /** The estimated range for how long shipping will take, meant to be displayable to the customer. This will appear on CheckoutSessions. */
    delivery_estimate?: { maximum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number }; minimum?: { unit: "business_day" | "day" | "hour" | "month" | "week"; value: number } };
    /** The name of the shipping rate, meant to be displayable to the customer. This will appear on CheckoutSessions. */
    display_name: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Describes a fixed amount to charge for shipping. Must be present if type is `fixed_amount`. */
    fixed_amount?: { amount: number; currency: string; currency_options?: { [key: string]: { amount: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    /** A [tax code](https://docs.stripe.com/tax/tax-categories) ID. The Shipping tax code is `txcd_92010001`. */
    tax_code?: string;
    /** The type of calculation to use on the shipping rate. */
    type?: "fixed_amount";
  }) => Promise<ShippingRate>;

  /**
   * Retrieve a shipping rate
   */
  getShippingRatesShippingRateToken: (input: {
    body?: { [key: string]: unknown };
    shipping_rate_token: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ShippingRate>;

  /**
   * Update a shipping rate
   */
  postShippingRatesShippingRateToken: (input: {
    /** Whether the shipping rate can be used for new purchases. Defaults to `true`. */
    active?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Describes a fixed amount to charge for shipping. Must be present if type is `fixed_amount`. */
    fixed_amount?: { currency_options?: { [key: string]: { amount?: number; tax_behavior?: "exclusive" | "inclusive" | "unspecified" } | undefined } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. */
    tax_behavior?: "exclusive" | "inclusive" | "unspecified";
    shipping_rate_token: string;
  }) => Promise<ShippingRate>;

  /**
   * Update an existing Sigma Query
   */
  postSigmaSavedQueriesId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The name of the query to update. */
    name?: string;
    /** The sql statement to update the specified query statement with. This should be a valid Trino SQL statement that can be run in Sigma. */
    sql?: string;
    /** The `id` of the saved query to update. This should be a valid `id` that was previously created. */
    id: string;
  }) => Promise<SigmaSigmaApiQuery>;

  /**
   * List all scheduled query runs
   */
  getSigmaScheduledQueryRuns: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (ScheduledQueryRun)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a scheduled query run
   */
  getSigmaScheduledQueryRunsScheduledQueryRun: (input: {
    body?: { [key: string]: unknown };
    scheduled_query_run: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<ScheduledQueryRun>;

  /**
   * Shares a source
   */
  postSources: (input: {
    /** Amount associated with the source. This is the amount for which the source will be chargeable once ready. Required for `single_use` sources. Not supported for `receiver` type sources, where charge amount may not be specified until funds land. */
    amount?: number;
    /** Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) associated with the source. This is the currency for which the source will be chargeable once ready. */
    currency?: string;
    /** The `Customer` to whom the original source is attached to. Must be set when the original source is not a `Source` (e.g., `Card`). */
    customer?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The authentication `flow` of the source to create. `flow` is one of `redirect`, `receiver`, `code_verification`, `none`. It is generally inferred unless a type supports multiple flows. */
    flow?: "code_verification" | "none" | "receiver" | "redirect";
    /** Information about a mandate possibility attached to a source object (generally for bank debits) as well as its acceptance status. */
    mandate?: { acceptance?: { date?: number; ip?: string; offline?: { contact_email: string }; online?: { date?: number; ip?: string; user_agent?: string }; status: "accepted" | "pending" | "refused" | "revoked"; type?: "offline" | "online"; user_agent?: string }; amount?: number | ""; currency?: string; interval?: "one_time" | "scheduled" | "variable"; notification_method?: "deprecated_none" | "email" | "manual" | "none" | "stripe_email" };
    metadata?: { [key: string]: string | undefined };
    /** The source to share. */
    original_source?: string;
    /** Information about the owner of the payment instrument that may be used or required by particular source types. */
    owner?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string };
    /** Optional parameters for the receiver flow. Can be set only if the source is a receiver (`flow` is `receiver`). */
    receiver?: { refund_attributes_method?: "email" | "manual" | "none" };
    /** Parameters required for the redirect flow. Required if the source is authenticated by a redirect (`flow` is `redirect`). */
    redirect?: { return_url: string };
    /** Information about the items and shipping associated with the source. Required for transactional credit (for example Klarna) sources before you can charge it. */
    source_order?: { items?: ({ amount?: number; currency?: string; description?: string; parent?: string; quantity?: number; type?: "discount" | "shipping" | "sku" | "tax" })[]; shipping?: { address: { city?: string; country?: string; line1: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name?: string; phone?: string; tracking_number?: string } };
    /** An arbitrary string to be displayed on your customer's statement. As an example, if your website is `RunClub` and the item you're charging for is a race ticket, you may want to specify a `statement_descriptor` of `RunClub 5K race ticket.` While many payment types will display this information, some may not display it at all. */
    statement_descriptor?: string;
    /** An optional token used to create the source. When passed, token properties will override source parameters. */
    token?: string;
    /** The `type` of the source to create. Required unless `customer` and `original_source` are specified (see the [Cloning card Sources](https://docs.stripe.com/sources/connect#cloning-card-sources) guide) */
    type?: string;
    usage?: "reusable" | "single_use";
  }) => Promise<Source>;

  /**
   * Retrieve a source
   */
  getSourcesSource: (input: {
    body?: { [key: string]: unknown };
    source: string;
    /** The client secret of the source. Required if a publishable key is used to retrieve the source. */
    client_secret?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Source>;

  /**
   * Update a source
   */
  postSourcesSource: (input: {
    /** Amount associated with the source. */
    amount?: number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information about a mandate possibility attached to a source object (generally for bank debits) as well as its acceptance status. */
    mandate?: { acceptance?: { date?: number; ip?: string; offline?: { contact_email: string }; online?: { date?: number; ip?: string; user_agent?: string }; status: "accepted" | "pending" | "refused" | "revoked"; type?: "offline" | "online"; user_agent?: string }; amount?: number | ""; currency?: string; interval?: "one_time" | "scheduled" | "variable"; notification_method?: "deprecated_none" | "email" | "manual" | "none" | "stripe_email" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Information about the owner of the payment instrument that may be used or required by particular source types. */
    owner?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; email?: string; name?: string; phone?: string };
    /** Information about the items and shipping associated with the source. Required for transactional credit (for example Klarna) sources before you can charge it. */
    source_order?: { items?: ({ amount?: number; currency?: string; description?: string; parent?: string; quantity?: number; type?: "discount" | "shipping" | "sku" | "tax" })[]; shipping?: { address: { city?: string; country?: string; line1: string; line2?: string; postal_code?: string; state?: string }; carrier?: string; name?: string; phone?: string; tracking_number?: string } };
    source: string;
  }) => Promise<Source>;

  /**
   * Retrieve a Source MandateNotification
   */
  getSourcesSourceMandateNotificationsMandateNotification: (input: {
    body?: { [key: string]: unknown };
    mandate_notification: string;
    source: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<SourceMandateNotification>;

  /**
   * <p>List source transactions for a given source.</p>
   */
  getSourcesSourceSourceTransactions: (input: {
    body?: { [key: string]: unknown };
    source: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (SourceTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a source transaction
   */
  getSourcesSourceSourceTransactionsSourceTransaction: (input: {
    body?: { [key: string]: unknown };
    source: string;
    source_transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<SourceTransaction>;

  /**
   * <p>Verify a given source.</p>
   */
  postSourcesSourceVerify: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The values needed to verify the source. */
    values: (string)[];
    source: string;
  }) => Promise<Source>;

  /**
   * List all subscription items
   */
  getSubscriptionItems: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The ID of the subscription whose items will be retrieved. */
    subscription: string;
  }) => Promise<{ data: (SubscriptionItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a subscription item
   */
  postSubscriptionItems: (input: {
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. Pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { usage_gte: number } | "";
    /** The coupons to redeem into discounts for the subscription item. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** The ID of the price object. */
    price?: string;
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline. */
    price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](/api/invoices/create_preview) endpoint. */
    proration_date?: number;
    /** The quantity you'd like to apply to the subscription item you're creating. */
    quantity?: number;
    /** The identifier of the subscription to modify. */
    subscription: string;
    /** A list of [Tax Rate](https://docs.stripe.com/api/tax_rates) ids. These Tax Rates will override the [`default_tax_rates`](https://docs.stripe.com/api/subscriptions/create#create_subscription-default_tax_rates) on the Subscription. When updating, pass an empty string to remove previously-defined tax rates. */
    tax_rates?: (string)[] | "";
  }) => Promise<SubscriptionItem>;

  /**
   * Delete a subscription item
   */
  deleteSubscriptionItemsItem: (input: {
    /** Delete all usage for the given subscription item. Allowed only when the current plan's `usage_type` is `metered`. */
    clear_usage?: boolean;
    /** Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](/api/invoices/create_preview) endpoint. */
    proration_date?: number;
    item: string;
  }) => Promise<DeletedSubscriptionItem>;

  /**
   * Retrieve a subscription item
   */
  getSubscriptionItemsItem: (input: {
    body?: { [key: string]: unknown };
    item: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<SubscriptionItem>;

  /**
   * Update a subscription item
   */
  postSubscriptionItemsItem: (input: {
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. Pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { usage_gte: number } | "";
    /** The coupons to redeem into discounts for the subscription item. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session). */
    off_session?: boolean;
    /** Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** The ID of the price object. One of `price` or `price_data` is required. When changing a subscription item's price, `quantity` is set to 1 unless a `quantity` parameter is provided. */
    price?: string;
    /** Data used to generate a new [Price](https://docs.stripe.com/api/prices) object inline. One of `price` or `price_data` is required. */
    price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string };
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](/api/invoices/create_preview) endpoint. */
    proration_date?: number;
    /** The quantity you'd like to apply to the subscription item you're creating. */
    quantity?: number;
    /** A list of [Tax Rate](https://docs.stripe.com/api/tax_rates) ids. These Tax Rates will override the [`default_tax_rates`](https://docs.stripe.com/api/subscriptions/create#create_subscription-default_tax_rates) on the Subscription. When updating, pass an empty string to remove previously-defined tax rates. */
    tax_rates?: (string)[] | "";
    item: string;
  }) => Promise<SubscriptionItem>;

  /**
   * List all schedules
   */
  getSubscriptionSchedules: (input: {
    body?: { [key: string]: unknown };
    /** Only return subscription schedules that were created canceled the given date interval. */
    canceled_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscription schedules that completed during the given date interval. */
    completed_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscription schedules that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscription schedules for the given customer. */
    customer?: string;
    /** Only return subscription schedules for the given account. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return subscription schedules that were released during the given date interval. */
    released_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscription schedules that have not started yet. */
    scheduled?: boolean;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (SubscriptionSchedule)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a schedule
   */
  postSubscriptionSchedules: (input: {
    /** Controls how prorations and invoices for subscriptions are calculated and orchestrated. */
    billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" };
    /** The identifier of the customer to create the subscription schedule for. */
    customer?: string;
    /** The identifier of the account to create the subscription schedule for. */
    customer_account?: string;
    /** Object representing the subscription schedule's default settings. */
    default_settings?: { application_fee_percent?: number; automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } }; billing_cycle_anchor?: "automatic" | "phase_start"; billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | ""; collection_method?: "charge_automatically" | "send_invoice"; default_payment_method?: string; description?: string | ""; invoice_settings?: { account_tax_ids?: (string)[] | ""; days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } }; on_behalf_of?: string | ""; transfer_data?: { amount_percent?: number; destination: string } | "" };
    /** Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription. */
    end_behavior?: "cancel" | "none" | "release" | "renew";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Migrate an existing subscription to be managed by a subscription schedule. If this parameter is set, a subscription schedule will be created using the subscription's item(s), set to auto-renew using the subscription's interval. When using this parameter, other parameters (such as phase values) cannot be set. To create a subscription schedule with other modifications, we recommend making two separate API calls. */
    from_subscription?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase. */
    phases?: ({ add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "phase_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "phase_start" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; application_fee_percent?: number; automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } }; billing_cycle_anchor?: "automatic" | "phase_start"; billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | ""; collection_method?: "charge_automatically" | "send_invoice"; currency?: string; default_payment_method?: string; default_tax_rates?: (string)[] | ""; description?: string | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; end_date?: number; invoice_settings?: { account_tax_ids?: (string)[] | ""; days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } }; items: ({ billing_thresholds?: { usage_gte: number } | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string; proration_behavior?: "always_invoice" | "create_prorations" | "none"; transfer_data?: { amount_percent?: number; destination: string }; trial?: boolean; trial_end?: number })[];
    /** When the subscription schedule starts. We recommend using `now` so that it starts the subscription immediately. You can also use a Unix timestamp to backdate the subscription so that it starts on a past date, or set a future date for the subscription to start on. */
    start_date?: number | "now";
  }) => Promise<SubscriptionSchedule>;

  /**
   * Retrieve a schedule
   */
  getSubscriptionSchedulesSchedule: (input: {
    body?: { [key: string]: unknown };
    schedule: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<SubscriptionSchedule>;

  /**
   * Update a schedule
   */
  postSubscriptionSchedulesSchedule: (input: {
    /** Object representing the subscription schedule's default settings. */
    default_settings?: { application_fee_percent?: number; automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } }; billing_cycle_anchor?: "automatic" | "phase_start"; billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | ""; collection_method?: "charge_automatically" | "send_invoice"; default_payment_method?: string; description?: string | ""; invoice_settings?: { account_tax_ids?: (string)[] | ""; days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } }; on_behalf_of?: string | ""; transfer_data?: { amount_percent?: number; destination: string } | "" };
    /** Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription. */
    end_behavior?: "cancel" | "none" | "release" | "renew";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase. Note that past phases can be omitted. */
    phases?: ({ add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "phase_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "phase_start" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; application_fee_percent?: number; automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } }; billing_cycle_anchor?: "automatic" | "phase_start"; billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | ""; collection_method?: "charge_automatically" | "send_invoice"; default_payment_method?: string; default_tax_rates?: (string)[] | ""; description?: string | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; end_date?: number | "now"; invoice_settings?: { account_tax_ids?: (string)[] | ""; days_until_due?: number; issuer?: { account?: string; type: "account" | "self" } }; items: ({ billing_thresholds?: { usage_gte: number } | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[]; metadata?: { [key: string]: string | undefined }; on_behalf_of?: string; proration_behavior?: "always_invoice" | "create_prorations" | "none"; start_date?: number | "now"; transfer_data?: { amount_percent?: number; destination: string }; trial?: boolean; trial_end?: number | "now" })[];
    /** If the update changes the billing configuration (item price, quantity, etc.) of the current phase, indicates how prorations from this change should be handled. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    schedule: string;
  }) => Promise<SubscriptionSchedule>;

  /**
   * Cancel a schedule
   */
  postSubscriptionSchedulesScheduleCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** If the subscription schedule is `active`, indicates if a final invoice will be generated that contains any un-invoiced metered usage and new/pending proration invoice items. Defaults to `true`. */
    invoice_now?: boolean;
    /** If the subscription schedule is `active`, indicates if the cancellation should be prorated. Defaults to `true`. */
    prorate?: boolean;
    schedule: string;
  }) => Promise<SubscriptionSchedule>;

  /**
   * Release a schedule
   */
  postSubscriptionSchedulesScheduleRelease: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Keep any cancellation on the subscription that the schedule has set */
    preserve_cancel_date?: boolean;
    schedule: string;
  }) => Promise<SubscriptionSchedule>;

  /**
   * List subscriptions
   */
  getSubscriptions: (input: {
    body?: { [key: string]: unknown };
    /** Filter subscriptions by their automatic tax settings. */
    automatic_tax?: { enabled: boolean };
    /** The collection method of the subscriptions to retrieve. Either `charge_automatically` or `send_invoice`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Only return subscriptions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscriptions whose minimum item current_period_end falls within the given date interval. */
    current_period_end?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return subscriptions whose maximum item current_period_start falls within the given date interval. */
    current_period_start?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** The ID of the customer whose subscriptions you're retrieving. */
    customer?: string;
    /** The ID of the account representing the customer whose subscriptions you're retrieving. */
    customer_account?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Filter for subscriptions that contain this recurring price ID. */
    price?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The status of the subscriptions to retrieve. Passing in a value of `canceled` will return all canceled subscriptions, including those belonging to deleted customers. Pass `ended` to find subscriptions that are canceled and subscriptions that are expired due to [incomplete payment](https://docs.stripe.com/billing/subscriptions/overview#subscription-statuses). Passing in a value of `all` will return subscriptions of all statuses. If no value is supplied, all subscriptions that have not been canceled are returned. */
    status?: "active" | "all" | "canceled" | "ended" | "incomplete" | "incomplete_expired" | "past_due" | "paused" | "trialing" | "unpaid";
    /** Filter for subscriptions that are associated with the specified test clock. The response will not include subscriptions with test clocks if this and the customer parameter is not set. */
    test_clock?: string;
  }) => Promise<{ data: (Subscription)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a subscription
   */
  postSubscriptions: (input: {
    /** A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items. */
    add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "now" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a platform account on a connected account in order to set an application fee percentage. For more information, see the application fees [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions). */
    application_fee_percent?: number | "";
    /** Automatic tax settings for this subscription. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor. */
    backdate_start_date?: number;
    /** A future timestamp in UTC format to anchor the subscription's [billing cycle](https://docs.stripe.com/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals. */
    billing_cycle_anchor?: number;
    /** Mutually exclusive with billing_cycle_anchor and only valid with monthly and yearly price intervals. When provided, the billing_cycle_anchor is set to the next occurrence of the day_of_month at the hour, minute, and second UTC. */
    billing_cycle_anchor_config?: { day_of_month: number; hour?: number; minute?: number; month?: number; second?: number };
    /** Controls how prorations and invoices for subscriptions are calculated and orchestrated. */
    billing_mode?: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "classic" | "flexible" };
    /** Sets the billing schedules for the subscription. */
    billing_schedules?: ({ applies_to?: ({ price?: string; type: "price" })[]; bill_until: { duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; timestamp?: number; type: "duration" | "timestamp" }; key?: string })[];
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | "";
    /** A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always cause a proration for that period. */
    cancel_at?: number | "max_billed_until" | "max_period_end" | "min_period_end";
    /** Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`. */
    cancel_at_period_end?: boolean;
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** The identifier of the customer to subscribe. */
    customer?: string;
    /** The identifier of the account representing the customer to subscribe. */
    customer_account?: string;
    /** Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`. */
    days_until_due?: number;
    /** ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_payment_method?: string;
    /** ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_source?: string;
    /** The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription. */
    default_tax_rates?: (string)[] | "";
    /** The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs. */
    description?: string;
    /** The coupons to redeem into discounts for the subscription. If not specified or empty, inherits the discount from the subscription's customer. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { account_tax_ids?: (string)[] | ""; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of up to 20 subscription items, each with an attached price. */
    items?: ({ billing_thresholds?: { usage_gte: number } | ""; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; metadata?: { [key: string]: string | undefined }; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session). */
    off_session?: boolean;
    /** The account on behalf of which to charge, for each of the subscription's invoices. */
    on_behalf_of?: string | "";
    /** Controls how Stripe handles the first invoice when payment is required and `collection_method=charge_automatically`. Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** Payment settings to pass to invoices created by the subscription. */
    payment_settings?: { payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { expires_after_seconds?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly" } } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | ""; save_default_payment_method?: "off" | "on_subscription" };
    /** Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an invoice](/api/invoices/create) for the given subscription at the specified interval. */
    pending_invoice_item_interval?: { interval: "day" | "month" | "week" | "year"; interval_count?: number } | "";
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges. */
    transfer_data?: { amount_percent?: number; destination: string };
    /** Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. If set, trial_end will override the default trial period of the plan the customer is being subscribed to. The special value `now` can be provided to end the customer's trial immediately. Can be at most two years from `billing_cycle_anchor`. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_end?: "now" | number;
    /** Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_from_plan?: boolean;
    /** Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_period_days?: number;
    /** Settings related to subscription trials. */
    trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } };
  }) => Promise<Subscription>;

  /**
   * Search subscriptions
   */
  getSubscriptionsSearch: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results. */
    page?: string;
    /** The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for subscriptions](https://docs.stripe.com/search#query-fields-for-subscriptions). */
    query: string;
  }) => Promise<{ data: (Subscription)[]; has_more: boolean; next_page?: string | null; object: "search_result"; total_count?: number; url: string }>;

  /**
   * Cancel a subscription
   */
  deleteSubscriptionsSubscriptionExposedId: (input: {
    /** Details about why this subscription was cancelled */
    cancellation_details?: { comment?: string | ""; feedback?: "" | "customer_service" | "low_quality" | "missing_features" | "other" | "switched_service" | "too_complex" | "too_expensive" | "unused" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items. Defaults to `false`. */
    invoice_now?: boolean;
    /** Will generate a proration invoice item that credits remaining unused time until the subscription period end. Defaults to `false`. */
    prorate?: boolean;
    subscription_exposed_id: string;
  }) => Promise<Subscription>;

  /**
   * Retrieve a subscription
   */
  getSubscriptionsSubscriptionExposedId: (input: {
    body?: { [key: string]: unknown };
    subscription_exposed_id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Subscription>;

  /**
   * Update a subscription
   */
  postSubscriptionsSubscriptionExposedId: (input: {
    /** A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items. */
    add_invoice_items?: ({ discountable?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[]; metadata?: { [key: string]: string | undefined }; period?: { end: { timestamp?: number; type: "min_item_period_end" | "timestamp" }; start: { timestamp?: number; type: "max_item_period_start" | "now" | "timestamp" } }; price?: string; price_data?: { currency: string; product: string; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a platform account on a connected account in order to set an application fee percentage. For more information, see the application fees [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions). */
    application_fee_percent?: number | "";
    /** Automatic tax settings for this subscription. We recommend you only include this parameter when the existing value is being changed. */
    automatic_tax?: { enabled: boolean; liability?: { account?: string; type: "account" | "self" } };
    /** Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time (in UTC). For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle). */
    billing_cycle_anchor?: "now" | "unchanged";
    /** Sets the billing schedules for the subscription. */
    billing_schedules?: ({ applies_to?: ({ price?: string; type: "price" })[]; bill_until?: { duration?: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; timestamp?: number; type: "duration" | "timestamp" }; key?: string })[] | "";
    /** Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds. */
    billing_thresholds?: { amount_gte?: number; reset_billing_cycle_anchor?: boolean } | "";
    /** A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always cause a proration for that period. */
    cancel_at?: number | "" | "max_billed_until" | "max_period_end" | "min_period_end";
    /** Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`. */
    cancel_at_period_end?: boolean;
    /** Details about why this subscription was cancelled */
    cancellation_details?: { comment?: string | ""; feedback?: "" | "customer_service" | "low_quality" | "missing_features" | "other" | "switched_service" | "too_complex" | "too_expensive" | "unused" };
    /** Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`. */
    collection_method?: "charge_automatically" | "send_invoice";
    /** Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`. */
    days_until_due?: number;
    /** ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_payment_method?: string;
    /** ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source). */
    default_source?: string | "";
    /** The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription. Pass an empty string to remove previously-defined tax rates. */
    default_tax_rates?: (string)[] | "";
    /** The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs. */
    description?: string | "";
    /** The coupons to redeem into discounts for the subscription. A populated array overwrites the existing discounts on the subscription. If not specified or empty array, it leaves the subscription's discounts unchanged. If empty string, it clears the subscription's discounts. */
    discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** All invoices will be billed using the specified settings. */
    invoice_settings?: { account_tax_ids?: (string)[] | ""; issuer?: { account?: string; type: "account" | "self" } };
    /** A list of up to 20 subscription items, each with an attached price. */
    items?: ({ billing_thresholds?: { usage_gte: number } | ""; clear_usage?: boolean; deleted?: boolean; discounts?: ({ coupon?: string; discount?: string; promotion_code?: string })[] | ""; id?: string; metadata?: { [key: string]: string | undefined } | ""; price?: string; price_data?: { currency: string; product: string; recurring: { interval: "day" | "month" | "week" | "year"; interval_count?: number }; tax_behavior?: "exclusive" | "inclusive" | "unspecified"; unit_amount?: number; unit_amount_decimal?: string }; quantity?: number; tax_rates?: (string)[] | "" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session). */
    off_session?: boolean;
    /** The account on behalf of which to charge, for each of the subscription's invoices. */
    on_behalf_of?: string | "";
    /** If specified, payment collection for this subscription will be paused. Note that the subscription status will be unchanged and will not be updated to `paused`. Learn more about [pausing collection](https://docs.stripe.com/billing/subscriptions/pause-payment). */
    pause_collection?: { behavior: "keep_as_draft" | "mark_uncollectible" | "void"; resumes_at?: number } | "";
    /** Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`. */
    payment_behavior?: "allow_incomplete" | "default_incomplete" | "error_if_incomplete" | "pending_if_incomplete";
    /** Payment settings to pass to invoices created by the subscription. */
    payment_settings?: { payment_method_options?: { acss_debit?: { mandate_options?: { transaction_type?: "business" | "personal" }; verification_method?: "automatic" | "instant" | "microdeposits" } | ""; bancontact?: { preferred_language?: "de" | "en" | "fr" | "nl" } | ""; card?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string }; network?: "amex" | "cartes_bancaires" | "diners" | "discover" | "eftpos_au" | "girocard" | "interac" | "jcb" | "link" | "mastercard" | "unionpay" | "unknown" | "visa"; request_three_d_secure?: "any" | "automatic" | "challenge" } | ""; customer_balance?: { bank_transfer?: { eu_bank_transfer?: { country: string }; type?: string }; funding_type?: string } | ""; konbini?: { [key: string]: unknown } | ""; payto?: { mandate_options?: { amount?: number; purpose?: "dependant_support" | "government" | "loan" | "mortgage" | "other" | "pension" | "personal" | "retail" | "salary" | "tax" | "utility" } } | ""; pix?: { expires_after_seconds?: number; mandate_options?: { amount?: number; amount_includes_iof?: "always" | "never"; end_date?: string; payment_schedule?: "halfyearly" | "monthly" | "quarterly" | "weekly" | "yearly" } } | ""; sepa_debit?: { [key: string]: unknown } | ""; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } } | ""; us_bank_account?: { financial_connections?: { filters?: { account_subcategories?: ("checking" | "savings")[] }; permissions?: ("balances" | "ownership" | "payment_method" | "transactions")[]; prefetch?: ("balances" | "ownership" | "transactions")[] }; verification_method?: "automatic" | "instant" | "microdeposits" } | "" }; payment_method_types?: ("ach_credit_transfer" | "ach_debit" | "acss_debit" | "affirm" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "boleto" | "card" | "cashapp" | "crypto" | "custom" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "jp_credit_transfer" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "multibanco" | "naver_pay" | "nz_bank_account" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "sepa_credit_transfer" | "sepa_debit" | "sofort" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay")[] | ""; save_default_payment_method?: "off" | "on_subscription" };
    /** Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an invoice](/api/invoices/create) for the given subscription at the specified interval. */
    pending_invoice_item_interval?: { interval: "day" | "month" | "week" | "year"; interval_count?: number } | "";
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations. */
    proration_date?: number;
    /** If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges. This will be unset if you POST an empty value. */
    transfer_data?: { amount_percent?: number; destination: string } | "";
    /** Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. If set, `trial_end` will override the default trial period of the plan the customer is being subscribed to. The `billing_cycle_anchor` will be updated to the `trial_end` value. The special value `now` can be provided to end the customer's trial immediately. Can be at most two years from `billing_cycle_anchor`. */
    trial_end?: "now" | number;
    /** Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more. */
    trial_from_plan?: boolean;
    /** Settings related to subscription trials. */
    trial_settings?: { end_behavior: { missing_payment_method: "cancel" | "create_invoice" | "pause" } };
    subscription_exposed_id: string;
  }) => Promise<Subscription>;

  /**
   * Delete a subscription discount
   */
  deleteSubscriptionsSubscriptionExposedIdDiscount: (input: {
    body?: { [key: string]: unknown };
    subscription_exposed_id: string;
  }) => Promise<DeletedDiscount>;

  /**
   * Migrate a subscription
   */
  postSubscriptionsSubscriptionMigrate: (input: {
    /** Controls how prorations and invoices for subscriptions are calculated and orchestrated. */
    billing_mode: { flexible?: { proration_discounts?: "included" | "itemized" }; type: "flexible" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    subscription: string;
  }) => Promise<Subscription>;

  /**
   * Resume a subscription
   */
  postSubscriptionsSubscriptionResume: (input: {
    /** The billing cycle anchor that applies when the subscription is resumed. Either `now` or `unchanged`. The default is `now`. For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle). */
    billing_cycle_anchor?: "now" | "unchanged";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Determines how to handle [prorations](https://docs.stripe.com/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor` being `unchanged`. When the `billing_cycle_anchor` is set to `now` (default value), no prorations are generated. If no value is passed, the default is `create_prorations`. */
    proration_behavior?: "always_invoice" | "create_prorations" | "none";
    /** If set, prorations will be calculated as though the subscription was resumed at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. */
    proration_date?: number;
    subscription: string;
  }) => Promise<Subscription>;

  /**
   * Find a Tax Association
   */
  getTaxAssociationsFind: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Valid [PaymentIntent](https://docs.stripe.com/api/payment_intents/object) id */
    payment_intent: string;
  }) => Promise<TaxAssociation>;

  /**
   * Create a Calculation
   */
  postTaxCalculations: (input: {
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** The ID of an existing customer to use for this calculation. If provided, the customer's address and tax IDs are copied to `customer_details`. */
    customer?: string;
    /** Details about the customer, including address and tax IDs. */
    customer_details?: { address?: { city?: string | ""; country: string; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" }; address_source?: "billing" | "shipping"; ip_address?: string; tax_ids?: ({ type: "ad_nrt" | "ae_trn" | "al_tin" | "am_tin" | "ao_tin" | "ar_cuit" | "au_abn" | "au_arn" | "aw_tin" | "az_tin" | "ba_tin" | "bb_tin" | "bd_bin" | "bf_ifu" | "bg_uic" | "bh_vat" | "bj_ifu" | "bo_tin" | "br_cnpj" | "br_cpf" | "bs_tin" | "by_tin" | "ca_bn" | "ca_gst_hst" | "ca_pst_bc" | "ca_pst_mb" | "ca_pst_sk" | "ca_qst" | "cd_nif" | "ch_uid" | "ch_vat" | "cl_tin" | "cm_niu" | "cn_tin" | "co_nit" | "cr_tin" | "cv_nif" | "de_stn" | "do_rcn" | "ec_ruc" | "eg_tin" | "es_cif" | "et_tin" | "eu_oss_vat" | "eu_vat" | "fo_vat" | "gb_vat" | "ge_vat" | "gi_tin" | "gn_nif" | "hk_br" | "hr_oib" | "hu_tin" | "id_npwp" | "il_vat" | "in_gst" | "is_vat" | "it_cf" | "jp_cn" | "jp_rn" | "jp_trn" | "ke_pin" | "kg_tin" | "kh_tin" | "kr_brn" | "kz_bin" | "la_tin" | "li_uid" | "li_vat" | "lk_vat" | "ma_vat" | "md_vat" | "me_pib" | "mk_vat" | "mr_nif" | "mx_rfc" | "my_frp" | "my_itn" | "my_sst" | "ng_tin" | "no_vat" | "no_voec" | "np_pan" | "nz_gst" | "om_vat" | "pe_ruc" | "ph_tin" | "pl_nip" | "py_ruc" | "ro_tin" | "rs_pib" | "ru_inn" | "ru_kpp" | "sa_vat" | "sg_gst" | "sg_uen" | "si_tin" | "sn_ninea" | "sr_fin" | "sv_nit" | "th_vat" | "tj_tin" | "tr_tin" | "tw_vat" | "tz_vat" | "ua_vat" | "ug_tin" | "us_ein" | "uy_ruc" | "uz_tin" | "uz_vat" | "ve_rif" | "vn_tin" | "za_vat" | "zm_tin" | "zw_tin"; value: string })[]; taxability_override?: "customer_exempt" | "none" | "reverse_charge" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A list of items the customer is purchasing. */
    line_items: ({ amount: number; metadata?: { [key: string]: string | undefined }; product?: string; quantity?: number; reference?: string; tax_behavior?: "exclusive" | "inclusive"; tax_code?: string })[];
    /** Details about the address from which the goods are being shipped. */
    ship_from_details?: { address: { city?: string | ""; country: string; line1?: string | ""; line2?: string | ""; postal_code?: string | ""; state?: string | "" } };
    /** Shipping cost details to be used for the calculation. */
    shipping_cost?: { amount?: number; shipping_rate?: string; tax_behavior?: "exclusive" | "inclusive"; tax_code?: string };
    /** Timestamp of date at which the tax rules and rates in effect applies for the calculation. Measured in seconds since the Unix epoch. Can be up to 48 hours in the past, and up to 48 hours in the future. */
    tax_date?: number;
  }) => Promise<TaxCalculation>;

  /**
   * Retrieve a Calculation
   */
  getTaxCalculationsCalculation: (input: {
    body?: { [key: string]: unknown };
    calculation: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxCalculation>;

  /**
   * Retrieve a Calculation's line items
   */
  getTaxCalculationsCalculationLineItems: (input: {
    body?: { [key: string]: unknown };
    calculation: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxCalculationLineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * List registrations
   */
  getTaxRegistrations: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** The status of the Tax Registration. */
    status?: "active" | "all" | "expired" | "scheduled";
  }) => Promise<{ data: (TaxRegistration)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a registration
   */
  postTaxRegistrations: (input: {
    /** Time at which the Tax Registration becomes active. It can be either `now` to indicate the current time, or a future timestamp measured in seconds since the Unix epoch. */
    active_from: "now" | number;
    /** Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)). */
    country: string;
    /** Specific options for a registration in the specified `country`. */
    country_options: { ae?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; al?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; am?: { type: "simplified" }; ao?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; at?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; au?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; aw?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; az?: { type: "simplified" }; ba?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; bb?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; bd?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; be?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; bf?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; bg?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; bh?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; bj?: { type: "simplified" }; bs?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; by?: { type: "simplified" }; ca?: { province_standard?: { province: string }; type: "province_standard" | "simplified" | "standard" }; cd?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; ch?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; cl?: { type: "simplified" }; cm?: { type: "simplified" }; co?: { type: "simplified" }; cr?: { type: "simplified" }; cv?: { type: "simplified" }; cy?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; cz?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; de?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; dk?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; ec?: { type: "simplified" }; ee?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; eg?: { type: "simplified" }; es?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; et?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; fi?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; fr?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; gb?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; ge?: { type: "simplified" }; gn?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; gr?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; hr?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; hu?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; id?: { type: "simplified" }; ie?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; in?: { type: "simplified" }; is?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; it?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; jp?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; ke?: { type: "simplified" }; kg?: { type: "simplified" }; kh?: { type: "simplified" }; kr?: { type: "simplified" }; kz?: { type: "simplified" }; la?: { type: "simplified" }; lk?: { type: "simplified" }; lt?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; lu?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; lv?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; ma?: { type: "simplified" }; md?: { type: "simplified" }; me?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; mk?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; mr?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; mt?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; mx?: { type: "simplified" }; my?: { type: "simplified" }; ng?: { type: "simplified" }; nl?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; no?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; np?: { type: "simplified" }; nz?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; om?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; pe?: { type: "simplified" }; ph?: { type: "simplified" }; pl?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; pt?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; ro?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; rs?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; ru?: { type: "simplified" }; sa?: { type: "simplified" }; se?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; sg?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; si?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; sk?: { standard?: { place_of_supply_scheme: "inbound_goods" | "small_seller" | "standard" }; type: "ioss" | "oss_non_union" | "oss_union" | "standard" }; sn?: { type: "simplified" }; sr?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; th?: { type: "simplified" }; tj?: { type: "simplified" }; tr?: { type: "simplified" }; tw?: { type: "simplified" }; tz?: { type: "simplified" }; ua?: { type: "simplified" }; ug?: { type: "simplified" }; us?: { local_amusement_tax?: { jurisdiction: string }; local_lease_tax?: { jurisdiction: string }; state: string; state_sales_tax?: { elections: ({ jurisdiction?: string; type: "local_use_tax" | "simplified_sellers_use_tax" | "single_local_use_tax" })[] }; type: "local_amusement_tax" | "local_lease_tax" | "state_communications_tax" | "state_retail_delivery_fee" | "state_sales_tax" }; uy?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; uz?: { type: "simplified" }; vn?: { type: "simplified" }; za?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" }; zm?: { type: "simplified" }; zw?: { standard?: { place_of_supply_scheme?: "inbound_goods" | "standard" }; type: "standard" } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** If set, the Tax Registration stops being active at this time. If not set, the Tax Registration will be active indefinitely. Timestamp measured in seconds since the Unix epoch. */
    expires_at?: number;
  }) => Promise<TaxRegistration>;

  /**
   * Retrieve a registration
   */
  getTaxRegistrationsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxRegistration>;

  /**
   * Update a registration
   */
  postTaxRegistrationsId: (input: {
    /** Time at which the registration becomes active. It can be either `now` to indicate the current time, or a timestamp measured in seconds since the Unix epoch. */
    active_from?: "now" | number;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** If set, the registration stops being active at this time. If not set, the registration will be active indefinitely. It can be either `now` to indicate the current time, or a timestamp measured in seconds since the Unix epoch. */
    expires_at?: "now" | number | "";
    id: string;
  }) => Promise<TaxRegistration>;

  /**
   * Retrieve settings
   */
  getTaxSettings: (input: {
    body?: { [key: string]: unknown };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxSettings>;

  /**
   * Update settings
   */
  postTaxSettings: (input: {
    /** Default configuration to be used on Stripe Tax calculations. */
    defaults?: { tax_behavior?: "exclusive" | "inclusive" | "inferred_by_currency"; tax_code?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The place where your business is located. */
    head_office?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } };
  }) => Promise<TaxSettings>;

  /**
   * Create a Transaction from a Calculation
   */
  postTaxTransactionsCreateFromCalculation: (input: {
    /** Tax Calculation ID to be used as input when creating the transaction. */
    calculation: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The Unix timestamp representing when the tax liability is assumed or reduced, which determines the liability posting period and handling in tax liability reports. The timestamp must fall within the `tax_date` and the current time, unless the `tax_date` is scheduled in advance. Defaults to the current time. */
    posted_at?: number;
    /** A custom order or sale identifier, such as 'myOrder_123'. Must be unique across all transactions, including reversals. */
    reference: string;
  }) => Promise<TaxTransaction>;

  /**
   * Create a reversal Transaction
   */
  postTaxTransactionsCreateReversal: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A flat amount to reverse across the entire transaction, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units) in negative. This value represents the total amount to refund from the transaction, including taxes. */
    flat_amount?: number;
    /** The line item amounts to reverse. */
    line_items?: ({ amount: number; amount_tax: number; metadata?: { [key: string]: string | undefined }; original_line_item: string; quantity?: number; reference: string })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** If `partial`, the provided line item or shipping cost amounts are reversed. If `full`, the original transaction is fully reversed. */
    mode: "full" | "partial";
    /** The ID of the Transaction to partially or fully reverse. */
    original_transaction: string;
    /** A custom identifier for this reversal, such as `myOrder_123-refund_1`, which must be unique across all transactions. The reference helps identify this reversal transaction in exported [tax reports](https://docs.stripe.com/tax/reports). */
    reference: string;
    /** The shipping cost to reverse. */
    shipping_cost?: { amount: number; amount_tax: number };
  }) => Promise<TaxTransaction>;

  /**
   * Retrieve a Transaction
   */
  getTaxTransactionsTransaction: (input: {
    body?: { [key: string]: unknown };
    transaction: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxTransaction>;

  /**
   * Retrieve a Transaction's line items
   */
  getTaxTransactionsTransactionLineItems: (input: {
    body?: { [key: string]: unknown };
    transaction: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxTransactionLineItem)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * List all tax codes
   */
  getTaxCodes: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxCode)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a tax code
   */
  getTaxCodesId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxCode>;

  /**
   * List all tax IDs
   */
  getTaxIds: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The account or customer the tax ID belongs to. Defaults to `owner[type]=self`. */
    owner?: { account?: string; customer?: string; customer_account?: string; type: "account" | "application" | "customer" | "self" };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxId)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a tax ID
   */
  postTaxIds: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The account or customer the tax ID belongs to. Defaults to `owner[type]=self`. */
    owner?: { account?: string; customer?: string; customer_account?: string; type: "account" | "application" | "customer" | "self" };
    /** Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `fo_vat`, `gb_vat`, `ge_vat`, `gi_tin`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `it_cf`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `lk_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `pl_nip`, `py_ruc`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin` */
    type: "ad_nrt" | "ae_trn" | "al_tin" | "am_tin" | "ao_tin" | "ar_cuit" | "au_abn" | "au_arn" | "aw_tin" | "az_tin" | "ba_tin" | "bb_tin" | "bd_bin" | "bf_ifu" | "bg_uic" | "bh_vat" | "bj_ifu" | "bo_tin" | "br_cnpj" | "br_cpf" | "bs_tin" | "by_tin" | "ca_bn" | "ca_gst_hst" | "ca_pst_bc" | "ca_pst_mb" | "ca_pst_sk" | "ca_qst" | "cd_nif" | "ch_uid" | "ch_vat" | "cl_tin" | "cm_niu" | "cn_tin" | "co_nit" | "cr_tin" | "cv_nif" | "de_stn" | "do_rcn" | "ec_ruc" | "eg_tin" | "es_cif" | "et_tin" | "eu_oss_vat" | "eu_vat" | "fo_vat" | "gb_vat" | "ge_vat" | "gi_tin" | "gn_nif" | "hk_br" | "hr_oib" | "hu_tin" | "id_npwp" | "il_vat" | "in_gst" | "is_vat" | "it_cf" | "jp_cn" | "jp_rn" | "jp_trn" | "ke_pin" | "kg_tin" | "kh_tin" | "kr_brn" | "kz_bin" | "la_tin" | "li_uid" | "li_vat" | "lk_vat" | "ma_vat" | "md_vat" | "me_pib" | "mk_vat" | "mr_nif" | "mx_rfc" | "my_frp" | "my_itn" | "my_sst" | "ng_tin" | "no_vat" | "no_voec" | "np_pan" | "nz_gst" | "om_vat" | "pe_ruc" | "ph_tin" | "pl_nip" | "py_ruc" | "ro_tin" | "rs_pib" | "ru_inn" | "ru_kpp" | "sa_vat" | "sg_gst" | "sg_uen" | "si_tin" | "sn_ninea" | "sr_fin" | "sv_nit" | "th_vat" | "tj_tin" | "tr_tin" | "tw_vat" | "tz_vat" | "ua_vat" | "ug_tin" | "us_ein" | "uy_ruc" | "uz_tin" | "uz_vat" | "ve_rif" | "vn_tin" | "za_vat" | "zm_tin" | "zw_tin";
    /** Value of the tax ID. */
    value: string;
  }) => Promise<TaxId>;

  /**
   * Delete a tax ID
   */
  deleteTaxIdsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
  }) => Promise<DeletedTaxId>;

  /**
   * Retrieve a tax ID
   */
  getTaxIdsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxId>;

  /**
   * List all tax rates
   */
  getTaxRates: (input: {
    body?: { [key: string]: unknown };
    /** Optional flag to filter by tax rates that are either active or inactive (archived). */
    active?: boolean;
    /** Optional range for filtering created date. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Optional flag to filter by tax rates that are inclusive (or those that are not inclusive). */
    inclusive?: boolean;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TaxRate)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a tax rate
   */
  postTaxRates: (input: {
    /** Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set. */
    active?: boolean;
    /** Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)). */
    country?: string;
    /** An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers. */
    description?: string;
    /** The display name of the tax rate, which will be shown to users. */
    display_name: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** This specifies if the tax rate is inclusive or exclusive. */
    inclusive: boolean;
    /** The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice. */
    jurisdiction?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** This represents the tax rate percent out of 100. */
    percentage: number;
    /** [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, "NY" for New York, United States. */
    state?: string;
    /** The high-level tax type, such as `vat` or `sales_tax`. */
    tax_type?: "amusement_tax" | "communications_tax" | "gst" | "hst" | "igst" | "jct" | "lease_tax" | "pst" | "qst" | "retail_delivery_fee" | "rst" | "sales_tax" | "service_tax" | "vat";
  }) => Promise<TaxRate>;

  /**
   * Retrieve a tax rate
   */
  getTaxRatesTaxRate: (input: {
    body?: { [key: string]: unknown };
    tax_rate: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TaxRate>;

  /**
   * Update a tax rate
   */
  postTaxRatesTaxRate: (input: {
    /** Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set. */
    active?: boolean;
    /** Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)). */
    country?: string;
    /** An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers. */
    description?: string;
    /** The display name of the tax rate, which will be shown to users. */
    display_name?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice. */
    jurisdiction?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, "NY" for New York, United States. */
    state?: string;
    /** The high-level tax type, such as `vat` or `sales_tax`. */
    tax_type?: "amusement_tax" | "communications_tax" | "gst" | "hst" | "igst" | "jct" | "lease_tax" | "pst" | "qst" | "retail_delivery_fee" | "rst" | "sales_tax" | "service_tax" | "vat";
    tax_rate: string;
  }) => Promise<TaxRate>;

  /**
   * List all Configurations
   */
  getTerminalConfigurations: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** if present, only return the account default or non-default configurations. */
    is_account_default?: boolean;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TerminalConfiguration)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Configuration
   */
  postTerminalConfigurations: (input: {
    /** An object containing device type specific settings for BBPOS WisePad 3 readers. */
    bbpos_wisepad3?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for BBPOS WisePOS E readers. */
    bbpos_wisepos_e?: { splashscreen?: string | "" };
    /** Configuration for cellular connectivity. */
    cellular?: { enabled: boolean } | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Name of the configuration */
    name?: string;
    /** Configurations for collecting transactions offline. */
    offline?: { enabled: boolean } | "";
    /** Reboot time settings for readers. that support customized reboot time configuration. */
    reboot_window?: { end_hour: number; start_hour: number };
    /** An object containing device type specific settings for Stripe S700 readers. */
    stripe_s700?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for Stripe S710 readers. */
    stripe_s710?: { splashscreen?: string | "" };
    /** Tipping configurations for readers that support on-reader tips. */
    tipping?: { aed?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; aud?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; cad?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; chf?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; czk?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; dkk?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; eur?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; gbp?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; gip?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; hkd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; huf?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; jpy?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; mxn?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; myr?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; nok?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; nzd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; pln?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; ron?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; sek?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; sgd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; usd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number } } | "";
    /** An object containing device type specific settings for Verifone M425 readers. */
    verifone_m425?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for Verifone P400 readers. */
    verifone_p400?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for Verifone P630 readers. */
    verifone_p630?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for Verifone UX700 readers. */
    verifone_ux700?: { splashscreen?: string | "" };
    /** An object containing device type specific settings for Verifone V660p readers. */
    verifone_v660p?: { splashscreen?: string | "" };
    /** Configurations for connecting to a WiFi network. */
    wifi?: { enterprise_eap_peap?: { ca_certificate_file?: string; password: string; ssid: string; username: string }; enterprise_eap_tls?: { ca_certificate_file?: string; client_certificate_file: string; private_key_file: string; private_key_file_password?: string; ssid: string }; personal_psk?: { password: string; ssid: string }; type: "enterprise_eap_peap" | "enterprise_eap_tls" | "personal_psk" } | "";
  }) => Promise<TerminalConfiguration>;

  /**
   * Delete a Configuration
   */
  deleteTerminalConfigurationsConfiguration: (input: {
    body?: { [key: string]: unknown };
    configuration: string;
  }) => Promise<DeletedTerminalConfiguration>;

  /**
   * Retrieve a Configuration
   */
  getTerminalConfigurationsConfiguration: (input: {
    body?: { [key: string]: unknown };
    configuration: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TerminalConfiguration | DeletedTerminalConfiguration>;

  /**
   * Update a Configuration
   */
  postTerminalConfigurationsConfiguration: (input: {
    /** An object containing device type specific settings for BBPOS WisePad 3 readers. */
    bbpos_wisepad3?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for BBPOS WisePOS E readers. */
    bbpos_wisepos_e?: { splashscreen?: string | "" } | "";
    /** Configuration for cellular connectivity. */
    cellular?: { enabled: boolean } | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Name of the configuration */
    name?: string;
    /** Configurations for collecting transactions offline. */
    offline?: { enabled: boolean } | "";
    /** Reboot time settings for readers. that support customized reboot time configuration. */
    reboot_window?: { end_hour: number; start_hour: number } | "";
    /** An object containing device type specific settings for Stripe S700 readers. */
    stripe_s700?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for Stripe S710 readers. */
    stripe_s710?: { splashscreen?: string | "" } | "";
    /** Tipping configurations for readers that support on-reader tips. */
    tipping?: { aed?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; aud?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; cad?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; chf?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; czk?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; dkk?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; eur?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; gbp?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; gip?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; hkd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; huf?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; jpy?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; mxn?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; myr?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; nok?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; nzd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; pln?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; ron?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; sek?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; sgd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number }; usd?: { fixed_amounts?: (number)[]; percentages?: (number)[]; smart_tip_threshold?: number } } | "";
    /** An object containing device type specific settings for Verifone M425 readers. */
    verifone_m425?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for Verifone P400 readers. */
    verifone_p400?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for Verifone P630 readers. */
    verifone_p630?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for Verifone UX700 readers. */
    verifone_ux700?: { splashscreen?: string | "" } | "";
    /** An object containing device type specific settings for Verifone V660p readers. */
    verifone_v660p?: { splashscreen?: string | "" } | "";
    /** Configurations for connecting to a WiFi network. */
    wifi?: { enterprise_eap_peap?: { ca_certificate_file?: string; password: string; ssid: string; username: string }; enterprise_eap_tls?: { ca_certificate_file?: string; client_certificate_file: string; private_key_file: string; private_key_file_password?: string; ssid: string }; personal_psk?: { password: string; ssid: string }; type: "enterprise_eap_peap" | "enterprise_eap_tls" | "personal_psk" } | "";
    configuration: string;
  }) => Promise<TerminalConfiguration | DeletedTerminalConfiguration>;

  /**
   * Create a Connection Token
   */
  postTerminalConnectionTokens: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The id of the location that this connection token is scoped to. If specified the connection token will only be usable with readers assigned to that location, otherwise the connection token will be usable with all readers. Note that location scoping only applies to internet-connected readers. For more details, see [the docs on scoping connection tokens](https://docs.stripe.com/terminal/fleet/locations-and-zones?dashboard-or-api=api#connection-tokens). */
    location?: string;
  }) => Promise<TerminalConnectionToken>;

  /**
   * List all Locations
   */
  getTerminalLocations: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TerminalLocation)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Location
   */
  postTerminalLocations: (input: {
    /** The full address of the location. */
    address?: { city?: string; country: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the full address of the location (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the full address of the location (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The ID of a configuration that will be used to customize all readers in this location. */
    configuration_overrides?: string;
    /** A name for the location. Maximum length is 1000 characters. */
    display_name?: string;
    /** The Kana variation of the name for the location (Japan only). Maximum length is 1000 characters. */
    display_name_kana?: string;
    /** The Kanji variation of the name for the location (Japan only). Maximum length is 1000 characters. */
    display_name_kanji?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The phone number for the location. */
    phone?: string;
  }) => Promise<TerminalLocation>;

  /**
   * Delete a Location
   */
  deleteTerminalLocationsLocation: (input: {
    body?: { [key: string]: unknown };
    location: string;
  }) => Promise<DeletedTerminalLocation>;

  /**
   * Retrieve a Location
   */
  getTerminalLocationsLocation: (input: {
    body?: { [key: string]: unknown };
    location: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TerminalLocation | DeletedTerminalLocation>;

  /**
   * Update a Location
   */
  postTerminalLocationsLocation: (input: {
    /** The full address of the location. You can't change the location's `country`. If you need to modify the `country` field, create a new `Location` object and re-register any existing readers to that location. */
    address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string };
    /** The Kana variation of the full address of the location (Japan only). */
    address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The Kanji variation of the full address of the location (Japan only). */
    address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string };
    /** The ID of a configuration that will be used to customize all readers in this location. */
    configuration_overrides?: string | "";
    /** A name for the location. */
    display_name?: string | "";
    /** The Kana variation of the name for the location (Japan only). */
    display_name_kana?: string | "";
    /** The Kanji variation of the name for the location (Japan only). */
    display_name_kanji?: string | "";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The phone number for the location. */
    phone?: string | "";
    location: string;
  }) => Promise<TerminalLocation | DeletedTerminalLocation>;

  /**
   * Create an Onboarding Link
   */
  postTerminalOnboardingLinks: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Specific fields needed to generate the desired link type. */
    link_options: { apple_terms_and_conditions?: { allow_relinking?: boolean; merchant_display_name: string } };
    /** The type of link being generated. */
    link_type: "apple_terms_and_conditions";
    /** Stripe account ID to generate the link for. */
    on_behalf_of?: string;
  }) => Promise<TerminalOnboardingLink>;

  /**
   * List all Readers
   */
  getTerminalReaders: (input: {
    body?: { [key: string]: unknown };
    /** Filters readers by device type */
    device_type?: "bbpos_chipper2x" | "bbpos_wisepad3" | "bbpos_wisepos_e" | "mobile_phone_reader" | "simulated_stripe_s700" | "simulated_stripe_s710" | "simulated_verifone_m425" | "simulated_verifone_p630" | "simulated_verifone_ux700" | "simulated_verifone_v660p" | "simulated_wisepos_e" | "stripe_m2" | "stripe_s700" | "stripe_s710" | "verifone_P400" | "verifone_m425" | "verifone_p630" | "verifone_ux700" | "verifone_v660p";
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A location ID to filter the response list to only readers at the specific location */
    location?: string;
    /** Filters readers by serial number */
    serial_number?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** A status filter to filter readers to only offline or online readers */
    status?: "offline" | "online";
  }) => Promise<{ data: (TerminalReader)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a Reader
   */
  postTerminalReaders: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Custom label given to the reader for easier identification. If no label is specified, the registration code will be used. */
    label?: string;
    /** The location to assign the reader to. */
    location?: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** A code generated by the reader used for registering to an account. */
    registration_code: string;
  }) => Promise<TerminalReader>;

  /**
   * Delete a Reader
   */
  deleteTerminalReadersReader: (input: {
    body?: { [key: string]: unknown };
    reader: string;
  }) => Promise<DeletedTerminalReader>;

  /**
   * Retrieve a Reader
   */
  getTerminalReadersReader: (input: {
    body?: { [key: string]: unknown };
    reader: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TerminalReader | DeletedTerminalReader>;

  /**
   * Update a Reader
   */
  postTerminalReadersReader: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The new label of the reader. */
    label?: string | "";
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    reader: string;
  }) => Promise<TerminalReader | DeletedTerminalReader>;

  /**
   * Cancel the current reader action
   */
  postTerminalReadersReaderCancelAction: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Collect inputs using a Reader
   */
  postTerminalReadersReaderCollectInputs: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** List of inputs to be collected from the customer using the Reader. Maximum 5 inputs. */
    inputs: ({ custom_text: { description?: string; skip_button?: string; submit_button?: string; title: string }; required?: boolean; selection?: { choices: ({ id: string; style?: "primary" | "secondary"; text: string })[] }; toggles?: ({ default_value?: "disabled" | "enabled"; description?: string; title?: string })[]; type: "email" | "numeric" | "phone" | "selection" | "signature" | "text" })[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Hand off a PaymentIntent to a Reader and collect card details
   */
  postTerminalReadersReaderCollectPaymentMethod: (input: {
    /** Configuration overrides for this collection, such as tipping, surcharging, and customer cancellation settings. */
    collect_config?: { allow_redisplay?: "always" | "limited" | "unspecified"; enable_customer_cancellation?: boolean; skip_tipping?: boolean; tipping?: { amount_eligible?: number } };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of the PaymentIntent to collect a payment method for. */
    payment_intent: string;
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Confirm a PaymentIntent on the Reader
   */
  postTerminalReadersReaderConfirmPaymentIntent: (input: {
    /** Configuration overrides for this confirmation, such as surcharge settings and return URL. */
    confirm_config?: { return_url?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of the PaymentIntent to confirm. */
    payment_intent: string;
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Hand-off a PaymentIntent to a Reader
   */
  postTerminalReadersReaderProcessPaymentIntent: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The ID of the PaymentIntent to process on the reader. */
    payment_intent: string;
    /** Configuration overrides for this transaction, such as tipping and customer cancellation settings. */
    process_config?: { allow_redisplay?: "always" | "limited" | "unspecified"; enable_customer_cancellation?: boolean; return_url?: string; skip_tipping?: boolean; tipping?: { amount_eligible?: number } };
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Hand-off a SetupIntent to a Reader
   */
  postTerminalReadersReaderProcessSetupIntent: (input: {
    /** This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. */
    allow_redisplay: "always" | "limited" | "unspecified";
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Configuration overrides for this setup, such as MOTO and customer cancellation settings. */
    process_config?: { enable_customer_cancellation?: boolean };
    /** The ID of the SetupIntent to process on the reader. */
    setup_intent: string;
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Refund a Charge or a PaymentIntent in-person
   */
  postTerminalReadersReaderRefundPayment: (input: {
    /** A positive integer in __cents__ representing how much of this charge to refund. */
    amount?: number;
    /** ID of the Charge to refund. */
    charge?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** ID of the PaymentIntent to refund. */
    payment_intent?: string;
    /** Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge. */
    refund_application_fee?: boolean;
    /** Configuration overrides for this refund, such as customer cancellation settings. */
    refund_payment_config?: { enable_customer_cancellation?: boolean };
    /** Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount). A transfer can be reversed only by the application that created the charge. */
    reverse_transfer?: boolean;
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Set reader display
   */
  postTerminalReadersReaderSetReaderDisplay: (input: {
    /** Cart details to display on the reader screen, including line items, amounts, and currency. */
    cart?: { currency: string; line_items: ({ amount: number; description: string; quantity: number })[]; tax?: number; total: number };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Type of information to display. Only `cart` is currently supported. */
    type: "cart";
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Create a refund using a Terminal-supported device.
   */
  postTerminalRefunds: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TerminalRefund>;

  /**
   * Create a test Confirmation Token
   */
  postTestHelpersConfirmationTokens: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** ID of an existing PaymentMethod. */
    payment_method?: string;
    /** If provided, this hash will be used to create a PaymentMethod. */
    payment_method_data?: { acss_debit?: { account_number: string; institution_number: string; transit_number: string }; affirm?: { [key: string]: unknown }; afterpay_clearpay?: { [key: string]: unknown }; alipay?: { [key: string]: unknown }; allow_redisplay?: "always" | "limited" | "unspecified"; alma?: { [key: string]: unknown }; amazon_pay?: { [key: string]: unknown }; au_becs_debit?: { account_number: string; bsb_number: string }; bacs_debit?: { account_number?: string; sort_code?: string }; bancontact?: { [key: string]: unknown }; billie?: { [key: string]: unknown }; billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | ""; tax_id?: string }; bizum?: { [key: string]: unknown }; blik?: { [key: string]: unknown }; boleto?: { tax_id: string }; cashapp?: { [key: string]: unknown }; crypto?: { [key: string]: unknown }; customer_balance?: { [key: string]: unknown }; eps?: { bank?: "arzte_und_apotheker_bank" | "austrian_anadi_bank_ag" | "bank_austria" | "bankhaus_carl_spangler" | "bankhaus_schelhammer_und_schattera_ag" | "bawag_psk_ag" | "bks_bank_ag" | "brull_kallmus_bank_ag" | "btv_vier_lander_bank" | "capital_bank_grawe_gruppe_ag" | "deutsche_bank_ag" | "dolomitenbank" | "easybank_ag" | "erste_bank_und_sparkassen" | "hypo_alpeadriabank_international_ag" | "hypo_bank_burgenland_aktiengesellschaft" | "hypo_noe_lb_fur_niederosterreich_u_wien" | "hypo_oberosterreich_salzburg_steiermark" | "hypo_tirol_bank_ag" | "hypo_vorarlberg_bank_ag" | "marchfelder_bank" | "oberbank_ag" | "raiffeisen_bankengruppe_osterreich" | "schoellerbank_ag" | "sparda_bank_wien" | "volksbank_gruppe" | "volkskreditbank_ag" | "vr_bank_braunau" }; fpx?: { bank: "affin_bank" | "agrobank" | "alliance_bank" | "ambank" | "bank_islam" | "bank_muamalat" | "bank_of_china" | "bank_rakyat" | "bsn" | "cimb" | "deutsche_bank" | "hong_leong_bank" | "hsbc" | "kfh" | "maybank2e" | "maybank2u" | "ocbc" | "pb_enterprise" | "public_bank" | "rhb" | "standard_chartered" | "uob" }; giropay?: { [key: string]: unknown }; grabpay?: { [key: string]: unknown }; ideal?: { bank?: "abn_amro" | "adyen" | "asn_bank" | "bunq" | "buut" | "finom" | "handelsbanken" | "ing" | "knab" | "mollie" | "moneyou" | "n26" | "nn" | "rabobank" | "regiobank" | "revolut" | "sns_bank" | "triodos_bank" | "van_lanschot" | "yoursafe" }; interac_present?: { [key: string]: unknown }; kakao_pay?: { [key: string]: unknown }; klarna?: { dob?: { day: number; month: number; year: number } }; konbini?: { [key: string]: unknown }; kr_card?: { [key: string]: unknown }; link?: { [key: string]: unknown }; mb_way?: { [key: string]: unknown }; metadata?: { [key: string]: string | undefined }; mobilepay?: { [key: string]: unknown }; multibanco?: { [key: string]: unknown }; naver_pay?: { funding?: "card" | "points" }; nz_bank_account?: { account_holder_name?: string; account_number: string; bank_code: string; branch_code: string; reference?: string; suffix: string }; oxxo?: { [key: string]: unknown }; p24?: { bank?: "alior_bank" | "bank_millennium" | "bank_nowy_bfg_sa" | "bank_pekao_sa" | "banki_spbdzielcze" | "blik" | "bnp_paribas" | "boz" | "citi_handlowy" | "credit_agricole" | "envelobank" | "etransfer_pocztowy24" | "getin_bank" | "ideabank" | "ing" | "inteligo" | "mbank_mtransfer" | "nest_przelew" | "noble_pay" | "pbac_z_ipko" | "plus_bank" | "santander_przelew24" | "tmobile_usbugi_bankowe" | "toyota_bank" | "velobank" | "volkswagen_bank" }; pay_by_bank?: { [key: string]: unknown }; payco?: { [key: string]: unknown }; paynow?: { [key: string]: unknown }; paypal?: { [key: string]: unknown }; payto?: { account_number?: string; bsb_number?: string; pay_id?: string }; pix?: { [key: string]: unknown }; promptpay?: { [key: string]: unknown }; radar_options?: { session?: string }; revolut_pay?: { [key: string]: unknown }; samsung_pay?: { [key: string]: unknown }; satispay?: { [key: string]: unknown }; scalapay?: { [key: string]: unknown }; sepa_debit?: { iban: string }; sofort?: { country: "AT" | "BE" | "DE" | "ES" | "IT" | "NL" }; sunbit?: { [key: string]: unknown }; swish?: { [key: string]: unknown }; twint?: { [key: string]: unknown }; type: "acss_debit" | "affirm" | "afterpay_clearpay" | "alipay" | "alma" | "amazon_pay" | "au_becs_debit" | "bacs_debit" | "bancontact" | "billie" | "bizum" | "blik" | "boleto" | "cashapp" | "crypto" | "customer_balance" | "eps" | "fpx" | "giropay" | "grabpay" | "ideal" | "kakao_pay" | "klarna" | "konbini" | "kr_card" | "link" | "mb_way" | "mobilepay" | "multibanco" | "naver_pay" | "nz_bank_account" | "oxxo" | "p24" | "pay_by_bank" | "payco" | "paynow" | "paypal" | "payto" | "pix" | "promptpay" | "revolut_pay" | "samsung_pay" | "satispay" | "scalapay" | "sepa_debit" | "sofort" | "sunbit" | "swish" | "twint" | "upi" | "us_bank_account" | "wechat_pay" | "zip"; upi?: { mandate_options?: { amount?: number; amount_type?: "fixed" | "maximum"; description?: string; end_date?: number } }; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string }; wechat_pay?: { [key: string]: unknown }; zip?: { [key: string]: unknown } };
    /** Payment-method-specific configuration for this ConfirmationToken. */
    payment_method_options?: { card?: { installments?: { plan: { count?: number; interval?: "month"; type: "bonus" | "fixed_count" | "revolving" } } } };
    /** Return URL used to confirm the Intent. */
    return_url?: string;
    /** Indicates that you intend to make future payments with this ConfirmationToken's payment method.  The presence of this property will [attach the payment method](https://docs.stripe.com/payments/save-during-payment) to the PaymentIntent's Customer, if present, after the PaymentIntent is confirmed and any required actions from the user are complete. */
    setup_future_usage?: "off_session" | "on_session";
    /** Shipping information for this ConfirmationToken. */
    shipping?: { address: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; name: string; phone?: string | "" };
  }) => Promise<ConfirmationToken>;

  /**
   * Fund a test mode cash balance
   */
  postTestHelpersCustomersCustomerFundCashBalance: (input: {
    /** Amount to be used for this test cash balance transaction. A positive integer representing how much to fund in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to fund $1.00 or 100 to fund ¥100, a zero-decimal currency). */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A description of the test funding. This simulates free-text references supplied by customers when making bank transfers to their cash balance. You can use this to test how Stripe's [reconciliation algorithm](https://docs.stripe.com/payments/customer-balance/reconciliation) applies to different user inputs. */
    reference?: string;
    customer: string;
  }) => Promise<CustomerCashBalanceTransaction>;

  /**
   * Create a test-mode authorization
   */
  postTestHelpersIssuingAuthorizations: (input: {
    /** The total amount to attempt to authorize. This amount is in the provided currency, or defaults to the card's currency, and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    amount?: number;
    /** Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    amount_details?: { atm_fee?: number; cashback_amount?: number };
    /** How the card details were provided. Defaults to online. */
    authorization_method?: "chip" | "contactless" | "keyed_in" | "online" | "swipe";
    /** Card associated with this authorization. */
    card: string;
    /** The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Fleet-specific information for authorizations using Fleet cards. */
    fleet?: { cardholder_prompt_data?: { driver_id?: string; odometer?: number; unspecified_id?: string; user_id?: string; vehicle_number?: string }; purchase_type?: "fuel_and_non_fuel_purchase" | "fuel_purchase" | "non_fuel_purchase"; reported_breakdown?: { fuel?: { gross_amount_decimal?: string }; non_fuel?: { gross_amount_decimal?: string }; tax?: { local_amount_decimal?: string; national_amount_decimal?: string } }; service_type?: "full_service" | "non_fuel_transaction" | "self_service" };
    /** Probability that this transaction can be disputed in the event of fraud. Assessed by comparing the characteristics of the authorization to card network rules. */
    fraud_disputability_likelihood?: "neutral" | "unknown" | "very_likely" | "very_unlikely";
    /** Information about fuel that was purchased with this transaction. */
    fuel?: { industry_product_code?: string; quantity_decimal?: string; type?: "diesel" | "other" | "unleaded_plus" | "unleaded_regular" | "unleaded_super"; unit?: "charging_minute" | "imperial_gallon" | "kilogram" | "kilowatt_hour" | "liter" | "other" | "pound" | "us_gallon"; unit_cost_decimal?: string };
    /** If set `true`, you may provide [amount](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization. */
    is_amount_controllable?: boolean;
    /** The total amount to attempt to authorize. This amount is in the provided merchant currency, and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    merchant_amount?: number;
    /** The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    merchant_currency?: string;
    /** Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened. */
    merchant_data?: { category?: "ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards"; city?: string; country?: string; name?: string; network_id?: string; postal_code?: string; state?: string; terminal_id?: string; url?: string };
    /** Details about the authorization, such as identifiers, set by the card network. */
    network_data?: { acquiring_institution_id?: string };
    /** Stripe’s assessment of the fraud risk for this authorization. */
    risk_assessment?: { card_testing_risk?: { invalid_account_number_decline_rate_past_hour?: number; invalid_credentials_decline_rate_past_hour?: number; level: "elevated" | "highest" | "low" | "normal" | "not_assessed" | "unknown" }; fraud_risk?: { level: "elevated" | "highest" | "low" | "normal" | "not_assessed" | "unknown"; score?: number }; merchant_dispute_risk?: { dispute_rate?: number; level: "elevated" | "highest" | "low" | "normal" | "not_assessed" | "unknown" } };
    /** Verifications that Stripe performed on information that the cardholder provided to the merchant. */
    verification_data?: { address_line1_check?: "match" | "mismatch" | "not_provided"; address_postal_code_check?: "match" | "mismatch" | "not_provided"; authentication_exemption?: { claimed_by: "acquirer" | "issuer"; type: "low_value_transaction" | "transaction_risk_analysis" | "unknown" }; cvc_check?: "match" | "mismatch" | "not_provided"; expiry_check?: "match" | "mismatch" | "not_provided"; three_d_secure?: { result: "attempt_acknowledged" | "authenticated" | "failed" | "required" } };
    /** The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as `null` when no digital wallet was utilized. */
    wallet?: "apple_pay" | "google_pay" | "samsung_pay";
  }) => Promise<IssuingAuthorization>;

  /**
   * Capture a test-mode authorization
   */
  postTestHelpersIssuingAuthorizationsAuthorizationCapture: (input: {
    /** The amount to capture from the authorization. If not provided, the full amount of the authorization will be captured. This amount is in the authorization currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    capture_amount?: number;
    /** Whether to close the authorization after capture. Defaults to true. Set to false to enable multi-capture flows. */
    close_authorization?: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Additional purchase information that is optionally provided by the merchant. */
    purchase_details?: { fleet?: { cardholder_prompt_data?: { driver_id?: string; odometer?: number; unspecified_id?: string; user_id?: string; vehicle_number?: string }; purchase_type?: "fuel_and_non_fuel_purchase" | "fuel_purchase" | "non_fuel_purchase"; reported_breakdown?: { fuel?: { gross_amount_decimal?: string }; non_fuel?: { gross_amount_decimal?: string }; tax?: { local_amount_decimal?: string; national_amount_decimal?: string } }; service_type?: "full_service" | "non_fuel_transaction" | "self_service" }; flight?: { departure_at?: number; passenger_name?: string; refundable?: boolean; segments?: ({ arrival_airport_code?: string; carrier?: string; departure_airport_code?: string; flight_number?: string; service_class?: string; stopover_allowed?: boolean })[]; travel_agency?: string }; fuel?: { industry_product_code?: string; quantity_decimal?: string; type?: "diesel" | "other" | "unleaded_plus" | "unleaded_regular" | "unleaded_super"; unit?: "charging_minute" | "imperial_gallon" | "kilogram" | "kilowatt_hour" | "liter" | "other" | "pound" | "us_gallon"; unit_cost_decimal?: string }; lodging?: { check_in_at?: number; nights?: number }; receipt?: ({ description?: string; quantity?: string; total?: number; unit_cost?: number })[]; reference?: string };
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Expire a test-mode authorization
   */
  postTestHelpersIssuingAuthorizationsAuthorizationExpire: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Finalize a test-mode authorization's amount
   */
  postTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmount: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The final authorization amount that will be captured by the merchant. This amount is in the authorization currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    final_amount: number;
    /** Fleet-specific information for authorizations using Fleet cards. */
    fleet?: { cardholder_prompt_data?: { driver_id?: string; odometer?: number; unspecified_id?: string; user_id?: string; vehicle_number?: string }; purchase_type?: "fuel_and_non_fuel_purchase" | "fuel_purchase" | "non_fuel_purchase"; reported_breakdown?: { fuel?: { gross_amount_decimal?: string }; non_fuel?: { gross_amount_decimal?: string }; tax?: { local_amount_decimal?: string; national_amount_decimal?: string } }; service_type?: "full_service" | "non_fuel_transaction" | "self_service" };
    /** Information about fuel that was purchased with this transaction. */
    fuel?: { industry_product_code?: string; quantity_decimal?: string; type?: "diesel" | "other" | "unleaded_plus" | "unleaded_regular" | "unleaded_super"; unit?: "charging_minute" | "imperial_gallon" | "kilogram" | "kilowatt_hour" | "liter" | "other" | "pound" | "us_gallon"; unit_cost_decimal?: string };
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Respond to fraud challenge
   */
  postTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespond: (input: {
    /** Whether to simulate the user confirming that the transaction was legitimate (true) or telling Stripe that it was fraudulent (false). */
    confirmed: boolean;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Increment a test-mode authorization
   */
  postTestHelpersIssuingAuthorizationsAuthorizationIncrement: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The amount to increment the authorization by. This amount is in the authorization currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    increment_amount: number;
    /** If set `true`, you may provide [amount](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization. */
    is_amount_controllable?: boolean;
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Reverse a test-mode authorization
   */
  postTestHelpersIssuingAuthorizationsAuthorizationReverse: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The amount to reverse from the authorization. If not provided, the full amount of the authorization will be reversed. This amount is in the authorization currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    reverse_amount?: number;
    authorization: string;
  }) => Promise<IssuingAuthorization>;

  /**
   * Deliver a testmode card
   */
  postTestHelpersIssuingCardsCardShippingDeliver: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * Fail a testmode card
   */
  postTestHelpersIssuingCardsCardShippingFail: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * Return a testmode card
   */
  postTestHelpersIssuingCardsCardShippingReturn: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * Ship a testmode card
   */
  postTestHelpersIssuingCardsCardShippingShip: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * Submit a testmode card
   */
  postTestHelpersIssuingCardsCardShippingSubmit: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    card: string;
  }) => Promise<IssuingCard>;

  /**
   * Activate a testmode personalization design
   */
  postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    personalization_design: string;
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * Deactivate a testmode personalization design
   */
  postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivate: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    personalization_design: string;
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * Reject a testmode personalization design
   */
  postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignReject: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The reason(s) the personalization design was rejected. */
    rejection_reasons: { card_logo?: ("geographic_location" | "inappropriate" | "network_name" | "non_binary_image" | "non_fiat_currency" | "other" | "other_entity" | "promotional_material")[]; carrier_text?: ("geographic_location" | "inappropriate" | "network_name" | "non_fiat_currency" | "other" | "other_entity" | "promotional_material")[] };
    personalization_design: string;
  }) => Promise<IssuingPersonalizationDesign>;

  /**
   * Create a test-mode settlement
   */
  postTestHelpersIssuingSettlements: (input: {
    /** The Bank Identification Number reflecting this settlement record. */
    bin: string;
    /** The date that the transactions are cleared and posted to user's accounts. */
    clearing_date: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The total interchange received as reimbursement for the transactions. */
    interchange_fees_amount?: number;
    /** The total net amount required to settle with the network. */
    net_total_amount: number;
    /** The card network for this settlement. One of ["visa", "maestro", "mastercard"] */
    network?: "maestro" | "visa";
    /** The Settlement Identification Number assigned by the network. */
    network_settlement_identifier?: string;
    /** The total transaction amount reflected in this settlement. */
    transaction_amount?: number;
    /** The total number of transactions reflected in this settlement. */
    transaction_count?: number;
  }) => Promise<IssuingSettlement>;

  /**
   * Complete a test-mode settlement
   */
  postTestHelpersIssuingSettlementsSettlementComplete: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The settlement token to mark as complete. */
    settlement: string;
  }) => Promise<IssuingSettlement>;

  /**
   * Create a test-mode force capture
   */
  postTestHelpersIssuingTransactionsCreateForceCapture: (input: {
    /** The total amount to attempt to capture. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    amount: number;
    /** Card associated with this transaction. */
    card: string;
    /** The currency of the capture. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened. */
    merchant_data?: { category?: "ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards"; city?: string; country?: string; name?: string; network_id?: string; postal_code?: string; state?: string; terminal_id?: string; url?: string };
    /** Additional purchase information that is optionally provided by the merchant. */
    purchase_details?: { fleet?: { cardholder_prompt_data?: { driver_id?: string; odometer?: number; unspecified_id?: string; user_id?: string; vehicle_number?: string }; purchase_type?: "fuel_and_non_fuel_purchase" | "fuel_purchase" | "non_fuel_purchase"; reported_breakdown?: { fuel?: { gross_amount_decimal?: string }; non_fuel?: { gross_amount_decimal?: string }; tax?: { local_amount_decimal?: string; national_amount_decimal?: string } }; service_type?: "full_service" | "non_fuel_transaction" | "self_service" }; flight?: { departure_at?: number; passenger_name?: string; refundable?: boolean; segments?: ({ arrival_airport_code?: string; carrier?: string; departure_airport_code?: string; flight_number?: string; service_class?: string; stopover_allowed?: boolean })[]; travel_agency?: string }; fuel?: { industry_product_code?: string; quantity_decimal?: string; type?: "diesel" | "other" | "unleaded_plus" | "unleaded_regular" | "unleaded_super"; unit?: "charging_minute" | "imperial_gallon" | "kilogram" | "kilowatt_hour" | "liter" | "other" | "pound" | "us_gallon"; unit_cost_decimal?: string }; lodging?: { check_in_at?: number; nights?: number }; receipt?: ({ description?: string; quantity?: string; total?: number; unit_cost?: number })[]; reference?: string };
  }) => Promise<IssuingTransaction>;

  /**
   * Create a test-mode unlinked refund
   */
  postTestHelpersIssuingTransactionsCreateUnlinkedRefund: (input: {
    /** The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    amount: number;
    /** Card associated with this unlinked refund transaction. */
    card: string;
    /** The currency of the unlinked refund. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened. */
    merchant_data?: { category?: "ac_refrigeration_repair" | "accounting_bookkeeping_services" | "advertising_services" | "agricultural_cooperative" | "airlines_air_carriers" | "airports_flying_fields" | "ambulance_services" | "amusement_parks_carnivals" | "antique_reproductions" | "antique_shops" | "aquariums" | "architectural_surveying_services" | "art_dealers_and_galleries" | "artists_supply_and_craft_shops" | "auto_and_home_supply_stores" | "auto_body_repair_shops" | "auto_paint_shops" | "auto_service_shops" | "automated_cash_disburse" | "automated_fuel_dispensers" | "automobile_associations" | "automotive_parts_and_accessories_stores" | "automotive_tire_stores" | "bail_and_bond_payments" | "bakeries" | "bands_orchestras" | "barber_and_beauty_shops" | "betting_casino_gambling" | "bicycle_shops" | "billiard_pool_establishments" | "boat_dealers" | "boat_rentals_and_leases" | "book_stores" | "books_periodicals_and_newspapers" | "bowling_alleys" | "bus_lines" | "business_secretarial_schools" | "buying_shopping_services" | "cable_satellite_and_other_pay_television_and_radio" | "camera_and_photographic_supply_stores" | "candy_nut_and_confectionery_stores" | "car_and_truck_dealers_new_used" | "car_and_truck_dealers_used_only" | "car_rental_agencies" | "car_washes" | "carpentry_services" | "carpet_upholstery_cleaning" | "caterers" | "charitable_and_social_service_organizations_fundraising" | "chemicals_and_allied_products" | "child_care_services" | "childrens_and_infants_wear_stores" | "chiropodists_podiatrists" | "chiropractors" | "cigar_stores_and_stands" | "civic_social_fraternal_associations" | "cleaning_and_maintenance" | "clothing_rental" | "colleges_universities" | "commercial_equipment" | "commercial_footwear" | "commercial_photography_art_and_graphics" | "commuter_transport_and_ferries" | "computer_network_services" | "computer_programming" | "computer_repair" | "computer_software_stores" | "computers_peripherals_and_software" | "concrete_work_services" | "construction_materials" | "consulting_public_relations" | "correspondence_schools" | "cosmetic_stores" | "counseling_services" | "country_clubs" | "courier_services" | "court_costs" | "credit_reporting_agencies" | "cruise_lines" | "dairy_products_stores" | "dance_hall_studios_schools" | "dating_escort_services" | "dentists_orthodontists" | "department_stores" | "detective_agencies" | "digital_goods_applications" | "digital_goods_games" | "digital_goods_large_volume" | "digital_goods_media" | "direct_marketing_catalog_merchant" | "direct_marketing_combination_catalog_and_retail_merchant" | "direct_marketing_inbound_telemarketing" | "direct_marketing_insurance_services" | "direct_marketing_other" | "direct_marketing_outbound_telemarketing" | "direct_marketing_subscription" | "direct_marketing_travel" | "discount_stores" | "doctors" | "door_to_door_sales" | "drapery_window_covering_and_upholstery_stores" | "drinking_places" | "drug_stores_and_pharmacies" | "drugs_drug_proprietaries_and_druggist_sundries" | "dry_cleaners" | "durable_goods" | "duty_free_stores" | "eating_places_restaurants" | "educational_services" | "electric_razor_stores" | "electric_vehicle_charging" | "electrical_parts_and_equipment" | "electrical_services" | "electronics_repair_shops" | "electronics_stores" | "elementary_secondary_schools" | "emergency_services_gcas_visa_use_only" | "employment_temp_agencies" | "equipment_rental" | "exterminating_services" | "family_clothing_stores" | "fast_food_restaurants" | "financial_institutions" | "fines_government_administrative_entities" | "fireplace_fireplace_screens_and_accessories_stores" | "floor_covering_stores" | "florists" | "florists_supplies_nursery_stock_and_flowers" | "freezer_and_locker_meat_provisioners" | "fuel_dealers_non_automotive" | "funeral_services_crematories" | "furniture_home_furnishings_and_equipment_stores_except_appliances" | "furniture_repair_refinishing" | "furriers_and_fur_shops" | "general_services" | "gift_card_novelty_and_souvenir_shops" | "glass_paint_and_wallpaper_stores" | "glassware_crystal_stores" | "golf_courses_public" | "government_licensed_horse_dog_racing_us_region_only" | "government_licensed_online_casions_online_gambling_us_region_only" | "government_owned_lotteries_non_us_region" | "government_owned_lotteries_us_region_only" | "government_services" | "grocery_stores_supermarkets" | "hardware_equipment_and_supplies" | "hardware_stores" | "health_and_beauty_spas" | "hearing_aids_sales_and_supplies" | "heating_plumbing_a_c" | "hobby_toy_and_game_shops" | "home_supply_warehouse_stores" | "hospitals" | "hotels_motels_and_resorts" | "household_appliance_stores" | "industrial_supplies" | "information_retrieval_services" | "insurance_default" | "insurance_underwriting_premiums" | "intra_company_purchases" | "jewelry_stores_watches_clocks_and_silverware_stores" | "landscaping_services" | "laundries" | "laundry_cleaning_services" | "legal_services_attorneys" | "luggage_and_leather_goods_stores" | "lumber_building_materials_stores" | "manual_cash_disburse" | "marinas_service_and_supplies" | "marketplaces" | "masonry_stonework_and_plaster" | "massage_parlors" | "medical_and_dental_labs" | "medical_dental_ophthalmic_and_hospital_equipment_and_supplies" | "medical_services" | "membership_organizations" | "mens_and_boys_clothing_and_accessories_stores" | "mens_womens_clothing_stores" | "metal_service_centers" | "miscellaneous_apparel_and_accessory_shops" | "miscellaneous_auto_dealers" | "miscellaneous_business_services" | "miscellaneous_food_stores" | "miscellaneous_general_merchandise" | "miscellaneous_general_services" | "miscellaneous_home_furnishing_specialty_stores" | "miscellaneous_publishing_and_printing" | "miscellaneous_recreation_services" | "miscellaneous_repair_shops" | "miscellaneous_specialty_retail" | "mobile_home_dealers" | "motion_picture_theaters" | "motor_freight_carriers_and_trucking" | "motor_homes_dealers" | "motor_vehicle_supplies_and_new_parts" | "motorcycle_shops_and_dealers" | "motorcycle_shops_dealers" | "music_stores_musical_instruments_pianos_and_sheet_music" | "news_dealers_and_newsstands" | "non_fi_money_orders" | "non_fi_stored_value_card_purchase_load" | "nondurable_goods" | "nurseries_lawn_and_garden_supply_stores" | "nursing_personal_care" | "office_and_commercial_furniture" | "opticians_eyeglasses" | "optometrists_ophthalmologist" | "orthopedic_goods_prosthetic_devices" | "osteopaths" | "package_stores_beer_wine_and_liquor" | "paints_varnishes_and_supplies" | "parking_lots_garages" | "passenger_railways" | "pawn_shops" | "pet_shops_pet_food_and_supplies" | "petroleum_and_petroleum_products" | "photo_developing" | "photographic_photocopy_microfilm_equipment_and_supplies" | "photographic_studios" | "picture_video_production" | "piece_goods_notions_and_other_dry_goods" | "plumbing_heating_equipment_and_supplies" | "political_organizations" | "postal_services_government_only" | "precious_stones_and_metals_watches_and_jewelry" | "professional_services" | "public_warehousing_and_storage" | "quick_copy_repro_and_blueprint" | "railroads" | "real_estate_agents_and_managers_rentals" | "record_stores" | "recreational_vehicle_rentals" | "religious_goods_stores" | "religious_organizations" | "roofing_siding_sheet_metal" | "secretarial_support_services" | "security_brokers_dealers" | "service_stations" | "sewing_needlework_fabric_and_piece_goods_stores" | "shoe_repair_hat_cleaning" | "shoe_stores" | "small_appliance_repair" | "snowmobile_dealers" | "special_trade_services" | "specialty_cleaning" | "sporting_goods_stores" | "sporting_recreation_camps" | "sports_and_riding_apparel_stores" | "sports_clubs_fields" | "stamp_and_coin_stores" | "stationary_office_supplies_printing_and_writing_paper" | "stationery_stores_office_and_school_supply_stores" | "swimming_pools_sales" | "t_ui_travel_germany" | "tailors_alterations" | "tax_payments_government_agencies" | "tax_preparation_services" | "taxicabs_limousines" | "telecommunication_equipment_and_telephone_sales" | "telecommunication_services" | "telegraph_services" | "tent_and_awning_shops" | "testing_laboratories" | "theatrical_ticket_agencies" | "timeshares" | "tire_retreading_and_repair" | "tolls_bridge_fees" | "tourist_attractions_and_exhibits" | "towing_services" | "trailer_parks_campgrounds" | "transportation_services" | "travel_agencies_tour_operators" | "truck_stop_iteration" | "truck_utility_trailer_rentals" | "typesetting_plate_making_and_related_services" | "typewriter_stores" | "u_s_federal_government_agencies_or_departments" | "uniforms_commercial_clothing" | "used_merchandise_and_secondhand_stores" | "utilities" | "variety_stores" | "veterinary_services" | "video_amusement_game_supplies" | "video_game_arcades" | "video_tape_rental_stores" | "vocational_trade_schools" | "watch_jewelry_repair" | "welding_repair" | "wholesale_clubs" | "wig_and_toupee_stores" | "wires_money_orders" | "womens_accessory_and_specialty_shops" | "womens_ready_to_wear_stores" | "wrecking_and_salvage_yards"; city?: string; country?: string; name?: string; network_id?: string; postal_code?: string; state?: string; terminal_id?: string; url?: string };
    /** Additional purchase information that is optionally provided by the merchant. */
    purchase_details?: { fleet?: { cardholder_prompt_data?: { driver_id?: string; odometer?: number; unspecified_id?: string; user_id?: string; vehicle_number?: string }; purchase_type?: "fuel_and_non_fuel_purchase" | "fuel_purchase" | "non_fuel_purchase"; reported_breakdown?: { fuel?: { gross_amount_decimal?: string }; non_fuel?: { gross_amount_decimal?: string }; tax?: { local_amount_decimal?: string; national_amount_decimal?: string } }; service_type?: "full_service" | "non_fuel_transaction" | "self_service" }; flight?: { departure_at?: number; passenger_name?: string; refundable?: boolean; segments?: ({ arrival_airport_code?: string; carrier?: string; departure_airport_code?: string; flight_number?: string; service_class?: string; stopover_allowed?: boolean })[]; travel_agency?: string }; fuel?: { industry_product_code?: string; quantity_decimal?: string; type?: "diesel" | "other" | "unleaded_plus" | "unleaded_regular" | "unleaded_super"; unit?: "charging_minute" | "imperial_gallon" | "kilogram" | "kilowatt_hour" | "liter" | "other" | "pound" | "us_gallon"; unit_cost_decimal?: string }; lodging?: { check_in_at?: number; nights?: number }; receipt?: ({ description?: string; quantity?: string; total?: number; unit_cost?: number })[]; reference?: string };
  }) => Promise<IssuingTransaction>;

  /**
   * Refund a test-mode transaction
   */
  postTestHelpersIssuingTransactionsTransactionRefund: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). */
    refund_amount?: number;
    transaction: string;
  }) => Promise<IssuingTransaction>;

  /**
   * Expire a pending refund.
   */
  postTestHelpersRefundsRefundExpire: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    refund: string;
  }) => Promise<Refund>;

  /**
   * Simulate presenting a payment method
   */
  postTestHelpersTerminalReadersReaderPresentPaymentMethod: (input: {
    /** Simulated on-reader tip amount. */
    amount_tip?: number;
    /** Simulated data for the card payment method. */
    card?: { cvc?: string; exp_month: number; exp_year: number; number: string };
    /** Simulated data for the card_present payment method. */
    card_present?: { number?: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Simulated data for the interac_present payment method. */
    interac_present?: { number?: string };
    /** Simulated payment type. */
    type?: "card" | "card_present" | "interac_present";
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Simulate a successful input collection
   */
  postTestHelpersTerminalReadersReaderSucceedInputCollection: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** This parameter defines the skip behavior for input collection. */
    skip_non_required_inputs?: "all" | "none";
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * Simulate an input collection timeout
   */
  postTestHelpersTerminalReadersReaderTimeoutInputCollection: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    reader: string;
  }) => Promise<TerminalReader>;

  /**
   * List all test clocks
   */
  getTestHelpersTestClocks: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TestHelpersTestClock)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a test clock
   */
  postTestHelpersTestClocks: (input: {
    /** Existing customer this test clock will be attached to. Once attached, customers can't be removed from a test clock. */
    customer?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The initial frozen time for this test clock. */
    frozen_time: number;
    /** The name for this test clock. */
    name?: string;
  }) => Promise<TestHelpersTestClock>;

  /**
   * Delete a test clock
   */
  deleteTestHelpersTestClocksTestClock: (input: {
    body?: { [key: string]: unknown };
    test_clock: string;
  }) => Promise<DeletedTestHelpersTestClock>;

  /**
   * Retrieve a test clock
   */
  getTestHelpersTestClocksTestClock: (input: {
    body?: { [key: string]: unknown };
    test_clock: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TestHelpersTestClock>;

  /**
   * Advance a test clock
   */
  postTestHelpersTestClocksTestClockAdvance: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The time to advance the test clock. Must be after the test clock's current frozen time. Cannot be more than two intervals in the future from the shortest subscription in this test clock. If there are no subscriptions in this test clock, it cannot be more than two years in the future. */
    frozen_time: number;
    test_clock: string;
  }) => Promise<TestHelpersTestClock>;

  /**
   * Test mode: Fail an InboundTransfer
   */
  postTestHelpersTreasuryInboundTransfersIdFail: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about a failed InboundTransfer. */
    failure_details?: { code?: "account_closed" | "account_frozen" | "bank_account_restricted" | "bank_ownership_changed" | "debit_not_authorized" | "incorrect_account_holder_address" | "incorrect_account_holder_name" | "incorrect_account_holder_tax_id" | "insufficient_funds" | "invalid_account_number" | "invalid_currency" | "no_account" | "other" };
    id: string;
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * Test mode: Return an InboundTransfer
   */
  postTestHelpersTreasuryInboundTransfersIdReturn: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * Test mode: Succeed an InboundTransfer
   */
  postTestHelpersTreasuryInboundTransfersIdSucceed: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * Test mode: Update an OutboundPayment
   */
  postTestHelpersTreasuryOutboundPaymentsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about network-specific tracking information. */
    tracking_details: { ach?: { trace_id: string }; type: "ach" | "us_domestic_wire"; us_domestic_wire?: { chips?: string; imad?: string; omad?: string } };
    id: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Test mode: Fail an OutboundPayment
   */
  postTestHelpersTreasuryOutboundPaymentsIdFail: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Test mode: Post an OutboundPayment
   */
  postTestHelpersTreasuryOutboundPaymentsIdPost: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Test mode: Return an OutboundPayment
   */
  postTestHelpersTreasuryOutboundPaymentsIdReturn: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Optional hash to set the return code. */
    returned_details?: { code?: "account_closed" | "account_frozen" | "bank_account_restricted" | "bank_ownership_changed" | "declined" | "incorrect_account_holder_name" | "invalid_account_number" | "invalid_currency" | "no_account" | "other" };
    id: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Test mode: Update an OutboundTransfer
   */
  postTestHelpersTreasuryOutboundTransfersOutboundTransfer: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about network-specific tracking information. */
    tracking_details: { ach?: { trace_id: string }; type: "ach" | "us_domestic_wire"; us_domestic_wire?: { chips?: string; imad?: string; omad?: string } };
    outbound_transfer: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Test mode: Fail an OutboundTransfer
   */
  postTestHelpersTreasuryOutboundTransfersOutboundTransferFail: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    outbound_transfer: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Test mode: Post an OutboundTransfer
   */
  postTestHelpersTreasuryOutboundTransfersOutboundTransferPost: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    outbound_transfer: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Test mode: Return an OutboundTransfer
   */
  postTestHelpersTreasuryOutboundTransfersOutboundTransferReturn: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Details about a returned OutboundTransfer. */
    returned_details?: { code?: "account_closed" | "account_frozen" | "bank_account_restricted" | "bank_ownership_changed" | "declined" | "incorrect_account_holder_name" | "invalid_account_number" | "invalid_currency" | "no_account" | "other" };
    outbound_transfer: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Test mode: Create a ReceivedCredit
   */
  postTestHelpersTreasuryReceivedCredits: (input: {
    /** Amount (in cents) to be transferred. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount to send funds to. */
    financial_account: string;
    /** Initiating payment method details for the object. */
    initiating_payment_method_details?: { type: "us_bank_account"; us_bank_account?: { account_holder_name?: string; account_number?: string; routing_number?: string } };
    /** Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://docs.stripe.com/treasury/money-movement/timelines) to learn more about money movement timelines for each network type. */
    network: "ach" | "us_domestic_wire";
  }) => Promise<TreasuryReceivedCredit>;

  /**
   * Test mode: Create a ReceivedDebit
   */
  postTestHelpersTreasuryReceivedDebits: (input: {
    /** Amount (in cents) to be transferred. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount to pull funds from. */
    financial_account: string;
    /** Initiating payment method details for the object. */
    initiating_payment_method_details?: { type: "us_bank_account"; us_bank_account?: { account_holder_name?: string; account_number?: string; routing_number?: string } };
    /** Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://docs.stripe.com/treasury/money-movement/timelines) to learn more about money movement timelines for each network type. */
    network: "ach";
  }) => Promise<TreasuryReceivedDebit>;

  /**
   * Create a CVC update token
   */
  postTokens: (input: {
    /** Information for the account this token represents. */
    account?: { business_type?: "company" | "government_entity" | "individual" | "non_profit"; company?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; directors_provided?: boolean; directorship_declaration?: { date?: number; ip?: string; user_agent?: string }; executives_provided?: boolean; export_license_id?: string; export_purpose_code?: string; name?: string; name_kana?: string; name_kanji?: string; owners_provided?: boolean; ownership_declaration?: { date?: number; ip?: string; user_agent?: string }; ownership_declaration_shown_and_signed?: boolean; ownership_exemption_reason?: "" | "qualified_entity_exceeds_ownership_threshold" | "qualifies_as_financial_institution"; phone?: string; registration_date?: { day: number; month: number; year: number } | ""; registration_number?: string; representative_declaration?: { date?: number; ip?: string; user_agent?: string }; structure?: "" | "free_zone_establishment" | "free_zone_llc" | "government_instrumentality" | "governmental_unit" | "incorporated_non_profit" | "incorporated_partnership" | "limited_liability_partnership" | "llc" | "multi_member_llc" | "private_company" | "private_corporation" | "private_partnership" | "public_company" | "public_corporation" | "public_partnership" | "registered_charity" | "single_member_llc" | "sole_establishment" | "sole_proprietorship" | "tax_exempt_government_instrumentality" | "unincorporated_association" | "unincorporated_non_profit" | "unincorporated_partnership"; tax_id?: string; tax_id_registrar?: string; vat_id?: string; verification?: { document?: { back?: string; front?: string } } }; individual?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; dob?: { day: number; month: number; year: number } | ""; email?: string; first_name?: string; first_name_kana?: string; first_name_kanji?: string; full_name_aliases?: (string)[] | ""; gender?: string; id_number?: string; id_number_secondary?: string; last_name?: string; last_name_kana?: string; last_name_kanji?: string; maiden_name?: string; metadata?: { [key: string]: string | undefined } | ""; phone?: string; political_exposure?: "existing" | "none"; registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; relationship?: { director?: boolean; executive?: boolean; owner?: boolean; percent_ownership?: number | ""; title?: string }; ssn_last_4?: string; verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } } }; tos_shown_and_accepted?: boolean };
    /** The bank account this token will represent. */
    bank_account?: { account_holder_name?: string; account_holder_type?: "company" | "individual"; account_number: string; account_type?: "checking" | "futsu" | "savings" | "toza"; country: string; currency?: string; payment_method?: string; routing_number?: string };
    /** The card this token will represent. If you also pass in a customer, the card must be the ID of a card belonging to the customer. Otherwise, if you do not pass in a customer, this is a dictionary containing a user's credit card details, with the options described below. */
    card?: { address_city?: string; address_country?: string; address_line1?: string; address_line2?: string; address_state?: string; address_zip?: string; currency?: string; cvc?: string; exp_month: string; exp_year: string; name?: string; networks?: { preferred?: "cartes_bancaires" | "mastercard" | "visa" }; number: string } | string;
    /** Create a token for the customer, which is owned by the application's account. You can only use this with an [OAuth access token](https://docs.stripe.com/connect/standard-accounts) or [Stripe-Account header](https://docs.stripe.com/connect/authentication). Learn more about [cloning saved payment methods](https://docs.stripe.com/connect/cloning-saved-payment-methods). */
    customer?: string;
    /** The updated CVC value this token represents. */
    cvc_update?: { cvc: string };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Information for the person this token represents. */
    person?: { additional_tos_acceptances?: { account?: { date?: number; ip?: string; user_agent?: string | "" } }; address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; address_kana?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; address_kanji?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string; town?: string }; dob?: { day: number; month: number; year: number } | ""; documents?: { company_authorization?: { files?: (string | "")[] }; passport?: { files?: (string | "")[] }; visa?: { files?: (string | "")[] } }; email?: string; first_name?: string; first_name_kana?: string; first_name_kanji?: string; full_name_aliases?: (string)[] | ""; gender?: string; id_number?: string; id_number_secondary?: string; last_name?: string; last_name_kana?: string; last_name_kanji?: string; maiden_name?: string; metadata?: { [key: string]: string | undefined } | ""; nationality?: string; phone?: string; political_exposure?: "existing" | "none"; registered_address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string }; relationship?: { authorizer?: boolean; director?: boolean; executive?: boolean; legal_guardian?: boolean; owner?: boolean; percent_ownership?: number | ""; representative?: boolean; title?: string }; ssn_last_4?: string; us_cfpb_data?: { ethnicity_details?: { ethnicity?: ("cuban" | "hispanic_or_latino" | "mexican" | "not_hispanic_or_latino" | "other_hispanic_or_latino" | "prefer_not_to_answer" | "puerto_rican")[]; ethnicity_other?: string }; race_details?: { race?: ("african_american" | "american_indian_or_alaska_native" | "asian" | "asian_indian" | "black_or_african_american" | "chinese" | "ethiopian" | "filipino" | "guamanian_or_chamorro" | "haitian" | "jamaican" | "japanese" | "korean" | "native_hawaiian" | "native_hawaiian_or_other_pacific_islander" | "nigerian" | "other_asian" | "other_black_or_african_american" | "other_pacific_islander" | "prefer_not_to_answer" | "samoan" | "somali" | "vietnamese" | "white")[]; race_other?: string }; self_identified_gender?: string }; verification?: { additional_document?: { back?: string; front?: string }; document?: { back?: string; front?: string } } };
    /** The PII this token represents. */
    pii?: { id_number?: string };
  }) => Promise<Token>;

  /**
   * Retrieve a token
   */
  getTokensToken: (input: {
    body?: { [key: string]: unknown };
    token: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Token>;

  /**
   * List all top-ups
   */
  getTopups: (input: {
    body?: { [key: string]: unknown };
    /** A positive integer representing how much to transfer. */
    amount?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return top-ups that have the given status. One of `canceled`, `failed`, `pending` or `succeeded`. */
    status?: "canceled" | "failed" | "pending" | "succeeded";
  }) => Promise<{ data: (Topup)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a top-up
   */
  postTopups: (input: {
    /** A positive integer representing how much to transfer. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The ID of a source to transfer funds from. For most users, this should be left unspecified which will use the bank account that was set up in the dashboard for the specified currency. In test mode, this can be a test bank token (see [Testing Top-ups](https://docs.stripe.com/connect/testing#testing-top-ups)). */
    source?: string;
    /** Extra information about a top-up for the source's bank statement. Limited to 15 ASCII characters. */
    statement_descriptor?: string;
    /** A string that identifies this top-up as part of a group. */
    transfer_group?: string;
  }) => Promise<Topup>;

  /**
   * Retrieve a top-up
   */
  getTopupsTopup: (input: {
    body?: { [key: string]: unknown };
    topup: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Topup>;

  /**
   * Update a top-up
   */
  postTopupsTopup: (input: {
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    topup: string;
  }) => Promise<Topup>;

  /**
   * Cancel a top-up
   */
  postTopupsTopupCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    topup: string;
  }) => Promise<Topup>;

  /**
   * List all transfers
   */
  getTransfers: (input: {
    body?: { [key: string]: unknown };
    /** Only return transfers that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return transfers for the destination specified by this account ID. */
    destination?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return transfers with the specified transfer group. */
    transfer_group?: string;
  }) => Promise<{ data: (Transfer)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a transfer
   */
  postTransfers: (input: {
    /** A positive integer in cents (or local equivalent) representing how much to transfer. */
    amount?: number;
    /** Three-letter [ISO code for currency](https://www.iso.org/iso-4217-currency-codes.html) in lowercase. Must be a [supported currency](https://docs.stripe.com/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The ID of a connected Stripe account. <a href="/docs/connect/separate-charges-and-transfers">See the Connect documentation</a> for details. */
    destination: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** You can use this parameter to transfer funds from a charge before they are added to your available balance. A pending balance will transfer immediately but the funds will not become available until the original charge becomes available. [See the Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-availability) for details. */
    source_transaction?: string;
    /** The source balance to use for this transfer. One of `bank_account`, `card`, or `fpx`. For most users, this will default to `card`. */
    source_type?: "bank_account" | "card" | "fpx";
    /** A string that identifies this transaction as part of a group. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for details. */
    transfer_group?: string;
  }) => Promise<Transfer>;

  /**
   * List all reversals
   */
  getTransfersIdReversals: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (TransferReversal)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a transfer reversal
   */
  postTransfersIdReversals: (input: {
    /** A positive integer in cents (or local equivalent) representing how much of this transfer to reverse. Can only reverse up to the unreversed amount remaining of the transfer. Partial transfer reversals are only allowed for transfers to Stripe Accounts. Defaults to the entire transfer amount. */
    amount?: number;
    /** An arbitrary string which you can attach to a reversal object. This will be unset if you POST an empty value. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** Boolean indicating whether the application fee should be refunded when reversing this transfer. If a full transfer reversal is given, the full application fee will be refunded. Otherwise, the application fee will be refunded with an amount proportional to the amount of the transfer reversed. */
    refund_application_fee?: boolean;
    id: string;
  }) => Promise<TransferReversal>;

  /**
   * Retrieve a transfer
   */
  getTransfersTransfer: (input: {
    body?: { [key: string]: unknown };
    transfer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<Transfer>;

  /**
   * Update a transfer
   */
  postTransfersTransfer: (input: {
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    transfer: string;
  }) => Promise<Transfer>;

  /**
   * Retrieve a reversal
   */
  getTransfersTransferReversalsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    transfer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TransferReversal>;

  /**
   * Update a reversal
   */
  postTransfersTransferReversalsId: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    id: string;
    transfer: string;
  }) => Promise<TransferReversal>;

  /**
   * List all CreditReversals
   */
  getTreasuryCreditReversals: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return CreditReversals for the ReceivedCredit ID. */
    received_credit?: string;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return CreditReversals for a given status. */
    status?: "canceled" | "posted" | "processing";
  }) => Promise<{ data: (TreasuryCreditReversal)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a CreditReversal
   */
  postTreasuryCreditReversals: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The ReceivedCredit to reverse. */
    received_credit: string;
  }) => Promise<TreasuryCreditReversal>;

  /**
   * Retrieve a CreditReversal
   */
  getTreasuryCreditReversalsCreditReversal: (input: {
    body?: { [key: string]: unknown };
    credit_reversal: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryCreditReversal>;

  /**
   * List all DebitReversals
   */
  getTreasuryDebitReversals: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return DebitReversals for the ReceivedDebit ID. */
    received_debit?: string;
    /** Only return DebitReversals for a given resolution. */
    resolution?: "lost" | "won";
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return DebitReversals for a given status. */
    status?: "canceled" | "completed" | "processing";
  }) => Promise<{ data: (TreasuryDebitReversal)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a DebitReversal
   */
  postTreasuryDebitReversals: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The ReceivedDebit to reverse. */
    received_debit: string;
  }) => Promise<TreasuryDebitReversal>;

  /**
   * Retrieve a DebitReversal
   */
  getTreasuryDebitReversalsDebitReversal: (input: {
    body?: { [key: string]: unknown };
    debit_reversal: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryDebitReversal>;

  /**
   * List all FinancialAccounts
   */
  getTreasuryFinancialAccounts: (input: {
    body?: { [key: string]: unknown };
    /** Only return FinancialAccounts that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** An object ID cursor for use in pagination. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit ranging from 1 to 100 (defaults to 10). */
    limit?: number;
    /** An object ID cursor for use in pagination. */
    starting_after?: string;
    /** Only return FinancialAccounts that have the given status: `open` or `closed` */
    status?: "closed" | "open";
  }) => Promise<{ data: (TreasuryFinancialAccount)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a FinancialAccount
   */
  postTreasuryFinancialAccounts: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Encodes whether a FinancialAccount has access to a particular feature. Stripe or the platform can control features via the requested field. */
    features?: { card_issuing?: { requested: boolean }; deposit_insurance?: { requested: boolean }; financial_addresses?: { aba?: { requested: boolean } }; inbound_transfers?: { ach?: { requested: boolean } }; intra_stripe_flows?: { requested: boolean }; outbound_payments?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } }; outbound_transfers?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } } };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The nickname for the FinancialAccount. */
    nickname?: string | "";
    /** The set of functionalities that the platform can restrict on the FinancialAccount. */
    platform_restrictions?: { inbound_flows?: "restricted" | "unrestricted"; outbound_flows?: "restricted" | "unrestricted" };
    /** The currencies the FinancialAccount can hold a balance in. */
    supported_currencies: (string)[];
  }) => Promise<TreasuryFinancialAccount>;

  /**
   * Retrieve a FinancialAccount
   */
  getTreasuryFinancialAccountsFinancialAccount: (input: {
    body?: { [key: string]: unknown };
    financial_account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryFinancialAccount>;

  /**
   * Update a FinancialAccount
   */
  postTreasuryFinancialAccountsFinancialAccount: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Encodes whether a FinancialAccount has access to a particular feature, with a status enum and associated `status_details`. Stripe or the platform may control features via the requested field. */
    features?: { card_issuing?: { requested: boolean }; deposit_insurance?: { requested: boolean }; financial_addresses?: { aba?: { requested: boolean } }; inbound_transfers?: { ach?: { requested: boolean } }; intra_stripe_flows?: { requested: boolean }; outbound_payments?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } }; outbound_transfers?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } } };
    /** A different bank account where funds can be deposited/debited in order to get the closing FA's balance to $0 */
    forwarding_settings?: { financial_account?: string; payment_method?: string; type: "financial_account" | "payment_method" };
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The nickname for the FinancialAccount. */
    nickname?: string | "";
    /** The set of functionalities that the platform can restrict on the FinancialAccount. */
    platform_restrictions?: { inbound_flows?: "restricted" | "unrestricted"; outbound_flows?: "restricted" | "unrestricted" };
    financial_account: string;
  }) => Promise<TreasuryFinancialAccount>;

  /**
   * Close a FinancialAccount
   */
  postTreasuryFinancialAccountsFinancialAccountClose: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A different bank account where funds can be deposited/debited in order to get the closing FA's balance to $0 */
    forwarding_settings?: { financial_account?: string; payment_method?: string; type: "financial_account" | "payment_method" };
    financial_account: string;
  }) => Promise<TreasuryFinancialAccount>;

  /**
   * Retrieve FinancialAccount Features
   */
  getTreasuryFinancialAccountsFinancialAccountFeatures: (input: {
    body?: { [key: string]: unknown };
    financial_account: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryFinancialAccountFeatures>;

  /**
   * Update FinancialAccount Features
   */
  postTreasuryFinancialAccountsFinancialAccountFeatures: (input: {
    /** Encodes the FinancialAccount's ability to be used with the Issuing product, including attaching cards to and drawing funds from the FinancialAccount. */
    card_issuing?: { requested: boolean };
    /** Represents whether this FinancialAccount is eligible for deposit insurance. Various factors determine the insurance amount. */
    deposit_insurance?: { requested: boolean };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Contains Features that add FinancialAddresses to the FinancialAccount. */
    financial_addresses?: { aba?: { requested: boolean } };
    /** Contains settings related to adding funds to a FinancialAccount from another Account with the same owner. */
    inbound_transfers?: { ach?: { requested: boolean } };
    /** Represents the ability for the FinancialAccount to send money to, or receive money from other FinancialAccounts (for example, via OutboundPayment). */
    intra_stripe_flows?: { requested: boolean };
    /** Includes Features related to initiating money movement out of the FinancialAccount to someone else's bucket of money. */
    outbound_payments?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } };
    /** Contains a Feature and settings related to moving money out of the FinancialAccount into another Account with the same owner. */
    outbound_transfers?: { ach?: { requested: boolean }; us_domestic_wire?: { requested: boolean } };
    financial_account: string;
  }) => Promise<TreasuryFinancialAccountFeatures>;

  /**
   * List all InboundTransfers
   */
  getTreasuryInboundTransfers: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return InboundTransfers that have the given status: `processing`, `succeeded`, `failed` or `canceled`. */
    status?: "canceled" | "failed" | "processing" | "succeeded";
  }) => Promise<{ data: (TreasuryInboundTransfer)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an InboundTransfer
   */
  postTreasuryInboundTransfers: (input: {
    /** Amount (in cents) to be transferred. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount to send funds to. */
    financial_account: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The origin payment method to be debited for the InboundTransfer. */
    origin_payment_method: string;
    /** The complete description that appears on your customers' statements. Maximum 10 characters. Can only include -#.$&*, spaces, and alphanumeric characters. */
    statement_descriptor?: string;
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * Retrieve an InboundTransfer
   */
  getTreasuryInboundTransfersId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * Cancel an InboundTransfer
   */
  postTreasuryInboundTransfersInboundTransferCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    inbound_transfer: string;
  }) => Promise<TreasuryInboundTransfer>;

  /**
   * List all OutboundPayments
   */
  getTreasuryOutboundPayments: (input: {
    body?: { [key: string]: unknown };
    /** Only return OutboundPayments that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** Only return OutboundPayments sent to this customer. */
    customer?: string;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return OutboundPayments that have the given status: `processing`, `failed`, `posted`, `returned`, or `canceled`. */
    status?: "canceled" | "failed" | "posted" | "processing" | "returned";
  }) => Promise<{ data: (TreasuryOutboundPayment)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an OutboundPayment
   */
  postTreasuryOutboundPayments: (input: {
    /** Amount (in cents) to be transferred. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** ID of the customer to whom the OutboundPayment is sent. Must match the Customer attached to the `destination_payment_method` passed in. */
    customer?: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The PaymentMethod to use as the payment instrument for the OutboundPayment. Exclusive with `destination_payment_method_data`. */
    destination_payment_method?: string;
    /** Hash used to generate the PaymentMethod to be used for this OutboundPayment. Exclusive with `destination_payment_method`. */
    destination_payment_method_data?: { billing_details?: { address?: { city?: string; country?: string; line1?: string; line2?: string; postal_code?: string; state?: string } | ""; email?: string | ""; name?: string | ""; phone?: string | "" }; financial_account?: string; metadata?: { [key: string]: string | undefined }; type: "financial_account" | "us_bank_account"; us_bank_account?: { account_holder_type?: "company" | "individual"; account_number?: string; account_type?: "checking" | "savings"; financial_connections_account?: string; routing_number?: string } };
    /** Payment method-specific configuration for this OutboundPayment. */
    destination_payment_method_options?: { us_bank_account?: { network?: "ach" | "us_domestic_wire" } | "" };
    /** End user details. */
    end_user_details?: { ip_address?: string; present: boolean };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount to pull funds from. */
    financial_account: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** The description that appears on the receiving end for this OutboundPayment (for example, bank statement for external bank transfer). Maximum 10 characters for `ach` payments, 140 characters for `us_domestic_wire` payments, or 500 characters for `stripe` network transfers. Can only include -#.$&*, spaces, and alphanumeric characters. The default value is "payment". */
    statement_descriptor?: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Retrieve an OutboundPayment
   */
  getTreasuryOutboundPaymentsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * Cancel an OutboundPayment
   */
  postTreasuryOutboundPaymentsIdCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    id: string;
  }) => Promise<TreasuryOutboundPayment>;

  /**
   * List all OutboundTransfers
   */
  getTreasuryOutboundTransfers: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return OutboundTransfers that have the given status: `processing`, `canceled`, `failed`, `posted`, or `returned`. */
    status?: "canceled" | "failed" | "posted" | "processing" | "returned";
  }) => Promise<{ data: (TreasuryOutboundTransfer)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create an OutboundTransfer
   */
  postTreasuryOutboundTransfers: (input: {
    /** Amount (in cents) to be transferred. */
    amount: number;
    /** Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). */
    currency: string;
    /** An arbitrary string attached to the object. Often useful for displaying to users. */
    description?: string;
    /** The PaymentMethod to use as the payment instrument for the OutboundTransfer. */
    destination_payment_method?: string;
    /** Hash used to generate the PaymentMethod to be used for this OutboundTransfer. Exclusive with `destination_payment_method`. */
    destination_payment_method_data?: { financial_account?: string; type: "financial_account" };
    /** Hash describing payment method configuration details. */
    destination_payment_method_options?: { us_bank_account?: { network?: "ach" | "us_domestic_wire" } | "" };
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount to pull funds from. */
    financial_account: string;
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined };
    /** Statement descriptor to be shown on the receiving end of an OutboundTransfer. Maximum 10 characters for `ach` transfers or 140 characters for `us_domestic_wire` transfers. The default value is "transfer". Can only include -#.$&*, spaces, and alphanumeric characters. */
    statement_descriptor?: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Retrieve an OutboundTransfer
   */
  getTreasuryOutboundTransfersOutboundTransfer: (input: {
    body?: { [key: string]: unknown };
    outbound_transfer: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * Cancel an OutboundTransfer
   */
  postTreasuryOutboundTransfersOutboundTransferCancel: (input: {
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    outbound_transfer: string;
  }) => Promise<TreasuryOutboundTransfer>;

  /**
   * List all ReceivedCredits
   */
  getTreasuryReceivedCredits: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount that received the funds. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** Only return ReceivedCredits described by the flow. */
    linked_flows?: { source_flow_type: "credit_reversal" | "other" | "outbound_payment" | "outbound_transfer" | "payout" };
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return ReceivedCredits that have the given status: `succeeded` or `failed`. */
    status?: "failed" | "succeeded";
  }) => Promise<{ data: (TreasuryReceivedCredit)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a ReceivedCredit
   */
  getTreasuryReceivedCreditsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryReceivedCredit>;

  /**
   * List all ReceivedDebits
   */
  getTreasuryReceivedDebits: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** The FinancialAccount that funds were pulled from. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return ReceivedDebits that have the given status: `succeeded` or `failed`. */
    status?: "failed" | "succeeded";
  }) => Promise<{ data: (TreasuryReceivedDebit)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a ReceivedDebit
   */
  getTreasuryReceivedDebitsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryReceivedDebit>;

  /**
   * List all TransactionEntries
   */
  getTreasuryTransactionEntries: (input: {
    body?: { [key: string]: unknown };
    /** Only return TransactionEntries that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    effective_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The results are in reverse chronological order by `created` or `effective_at`. The default is `created`. */
    order_by?: "created" | "effective_at";
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return TransactionEntries associated with this Transaction. */
    transaction?: string;
  }) => Promise<{ data: (TreasuryTransactionEntry)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a TransactionEntry
   */
  getTreasuryTransactionEntriesId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryTransactionEntry>;

  /**
   * List all Transactions
   */
  getTreasuryTransactions: (input: {
    body?: { [key: string]: unknown };
    /** Only return Transactions that were created during the given date interval. */
    created?: { gt?: number; gte?: number; lt?: number; lte?: number } | number;
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Returns objects associated with this FinancialAccount. */
    financial_account: string;
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** The results are in reverse chronological order by `created` or `posted_at`. The default is `created`. */
    order_by?: "created" | "posted_at";
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
    /** Only return Transactions that have the given status: `open`, `posted`, or `void`. */
    status?: "open" | "posted" | "void";
    /** A filter for the `status_transitions.posted_at` timestamp. When using this filter, `status=posted` and `order_by=posted_at` must also be specified. */
    status_transitions?: { posted_at?: { gt?: number; gte?: number; lt?: number; lte?: number } | number };
  }) => Promise<{ data: (TreasuryTransaction)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Retrieve a Transaction
   */
  getTreasuryTransactionsId: (input: {
    body?: { [key: string]: unknown };
    id: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<TreasuryTransaction>;

  /**
   * List all webhook endpoints
   */
  getWebhookEndpoints: (input: {
    body?: { [key: string]: unknown };
    /** A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list. */
    ending_before?: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10. */
    limit?: number;
    /** A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list. */
    starting_after?: string;
  }) => Promise<{ data: (WebhookEndpoint)[]; has_more: boolean; object: "list"; url: string }>;

  /**
   * Create a webhook endpoint
   */
  postWebhookEndpoints: (input: {
    /** Events sent to this endpoint will be generated with this Stripe Version instead of your account's default Stripe Version. */
    api_version?: "2011-01-01" | "2011-06-21" | "2011-06-28" | "2011-08-01" | "2011-09-15" | "2011-11-17" | "2012-02-23" | "2012-03-25" | "2012-06-18" | "2012-06-28" | "2012-07-09" | "2012-09-24" | "2012-10-26" | "2012-11-07" | "2013-02-11" | "2013-02-13" | "2013-07-05" | "2013-08-12" | "2013-08-13" | "2013-10-29" | "2013-12-03" | "2014-01-31" | "2014-03-13" | "2014-03-28" | "2014-05-19" | "2014-06-13" | "2014-06-17" | "2014-07-22" | "2014-07-26" | "2014-08-04" | "2014-08-20" | "2014-09-08" | "2014-10-07" | "2014-11-05" | "2014-11-20" | "2014-12-08" | "2014-12-17" | "2014-12-22" | "2015-01-11" | "2015-01-26" | "2015-02-10" | "2015-02-16" | "2015-02-18" | "2015-03-24" | "2015-04-07" | "2015-06-15" | "2015-07-07" | "2015-07-13" | "2015-07-28" | "2015-08-07" | "2015-08-19" | "2015-09-03" | "2015-09-08" | "2015-09-23" | "2015-10-01" | "2015-10-12" | "2015-10-16" | "2016-02-03" | "2016-02-19" | "2016-02-22" | "2016-02-23" | "2016-02-29" | "2016-03-07" | "2016-06-15" | "2016-07-06" | "2016-10-19" | "2017-01-27" | "2017-02-14" | "2017-04-06" | "2017-05-25" | "2017-06-05" | "2017-08-15" | "2017-12-14" | "2018-01-23" | "2018-02-05" | "2018-02-06" | "2018-02-28" | "2018-05-21" | "2018-07-27" | "2018-08-23" | "2018-09-06" | "2018-09-24" | "2018-10-31" | "2018-11-08" | "2019-02-11" | "2019-02-19" | "2019-03-14" | "2019-05-16" | "2019-08-14" | "2019-09-09" | "2019-10-08" | "2019-10-17" | "2019-11-05" | "2019-12-03" | "2020-03-02" | "2020-08-27" | "2022-08-01" | "2022-11-15" | "2023-08-16" | "2023-10-16" | "2024-04-10" | "2024-06-20" | "2024-09-30.acacia" | "2024-10-28.acacia" | "2024-11-20.acacia" | "2024-12-18.acacia" | "2025-01-27.acacia" | "2025-02-24.acacia" | "2025-03-01.dashboard" | "2025-03-31.basil" | "2025-04-30.basil" | "2025-05-28.basil" | "2025-06-30.basil" | "2025-07-30.basil" | "2025-08-27.basil" | "2025-09-30.clover" | "2025-10-29.clover" | "2025-11-17.clover" | "2025-12-15.clover" | "2026-01-28.clover" | "2026-02-25.clover" | "2026-03-25.dahlia" | "2026-04-22.dahlia" | "2026-05-27.dahlia";
    /** Whether this endpoint should receive events from connected accounts (`true`), or from your account (`false`). Defaults to `false`. */
    connect?: boolean;
    /** An optional description of what the webhook is used for. */
    description?: string | "";
    /** The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection. */
    enabled_events: ("*" | "account.application.authorized" | "account.application.deauthorized" | "account.external_account.created" | "account.external_account.deleted" | "account.external_account.updated" | "account.updated" | "application_fee.created" | "application_fee.refund.updated" | "application_fee.refunded" | "balance.available" | "balance_settings.updated" | "billing.alert.triggered" | "billing.credit_grant.created" | "billing_portal.configuration.created" | "billing_portal.configuration.updated" | "billing_portal.session.created" | "capability.updated" | "cash_balance.funds_available" | "charge.captured" | "charge.dispute.closed" | "charge.dispute.created" | "charge.dispute.funds_reinstated" | "charge.dispute.funds_withdrawn" | "charge.dispute.updated" | "charge.expired" | "charge.failed" | "charge.pending" | "charge.refund.updated" | "charge.refunded" | "charge.succeeded" | "charge.updated" | "checkout.session.async_payment_failed" | "checkout.session.async_payment_succeeded" | "checkout.session.completed" | "checkout.session.expired" | "climate.order.canceled" | "climate.order.created" | "climate.order.delayed" | "climate.order.delivered" | "climate.order.product_substituted" | "climate.product.created" | "climate.product.pricing_updated" | "coupon.created" | "coupon.deleted" | "coupon.updated" | "credit_note.created" | "credit_note.updated" | "credit_note.voided" | "customer.created" | "customer.deleted" | "customer.discount.created" | "customer.discount.deleted" | "customer.discount.updated" | "customer.source.created" | "customer.source.deleted" | "customer.source.expiring" | "customer.source.updated" | "customer.subscription.created" | "customer.subscription.deleted" | "customer.subscription.paused" | "customer.subscription.pending_update_applied" | "customer.subscription.pending_update_expired" | "customer.subscription.resumed" | "customer.subscription.trial_will_end" | "customer.subscription.updated" | "customer.tax_id.created" | "customer.tax_id.deleted" | "customer.tax_id.updated" | "customer.updated" | "customer_cash_balance_transaction.created" | "entitlements.active_entitlement_summary.updated" | "file.created" | "financial_connections.account.account_numbers_updated" | "financial_connections.account.created" | "financial_connections.account.deactivated" | "financial_connections.account.disconnected" | "financial_connections.account.reactivated" | "financial_connections.account.refreshed_balance" | "financial_connections.account.refreshed_ownership" | "financial_connections.account.refreshed_transactions" | "financial_connections.account.upcoming_account_number_expiry" | "identity.verification_session.canceled" | "identity.verification_session.created" | "identity.verification_session.processing" | "identity.verification_session.redacted" | "identity.verification_session.requires_input" | "identity.verification_session.verified" | "invoice.created" | "invoice.deleted" | "invoice.finalization_failed" | "invoice.finalized" | "invoice.marked_uncollectible" | "invoice.overdue" | "invoice.overpaid" | "invoice.paid" | "invoice.payment_action_required" | "invoice.payment_attempt_required" | "invoice.payment_failed" | "invoice.payment_succeeded" | "invoice.sent" | "invoice.upcoming" | "invoice.updated" | "invoice.voided" | "invoice.will_be_due" | "invoice_payment.paid" | "invoiceitem.created" | "invoiceitem.deleted" | "issuing_authorization.created" | "issuing_authorization.request" | "issuing_authorization.updated" | "issuing_card.created" | "issuing_card.updated" | "issuing_cardholder.created" | "issuing_cardholder.updated" | "issuing_dispute.closed" | "issuing_dispute.created" | "issuing_dispute.funds_reinstated" | "issuing_dispute.funds_rescinded" | "issuing_dispute.submitted" | "issuing_dispute.updated" | "issuing_personalization_design.activated" | "issuing_personalization_design.deactivated" | "issuing_personalization_design.rejected" | "issuing_personalization_design.updated" | "issuing_token.created" | "issuing_token.updated" | "issuing_transaction.created" | "issuing_transaction.purchase_details_receipt_updated" | "issuing_transaction.updated" | "mandate.updated" | "payment_intent.amount_capturable_updated" | "payment_intent.canceled" | "payment_intent.created" | "payment_intent.partially_funded" | "payment_intent.payment_failed" | "payment_intent.processing" | "payment_intent.requires_action" | "payment_intent.succeeded" | "payment_link.created" | "payment_link.updated" | "payment_method.attached" | "payment_method.automatically_updated" | "payment_method.detached" | "payment_method.updated" | "payout.canceled" | "payout.created" | "payout.failed" | "payout.paid" | "payout.reconciliation_completed" | "payout.updated" | "person.created" | "person.deleted" | "person.updated" | "plan.created" | "plan.deleted" | "plan.updated" | "price.created" | "price.deleted" | "price.updated" | "product.created" | "product.deleted" | "product.updated" | "promotion_code.created" | "promotion_code.updated" | "quote.accepted" | "quote.canceled" | "quote.created" | "quote.finalized" | "radar.early_fraud_warning.created" | "radar.early_fraud_warning.updated" | "refund.created" | "refund.failed" | "refund.updated" | "reporting.report_run.failed" | "reporting.report_run.succeeded" | "reporting.report_type.updated" | "reserve.hold.created" | "reserve.hold.updated" | "reserve.plan.created" | "reserve.plan.disabled" | "reserve.plan.expired" | "reserve.plan.updated" | "reserve.release.created" | "review.closed" | "review.opened" | "setup_intent.canceled" | "setup_intent.created" | "setup_intent.requires_action" | "setup_intent.setup_failed" | "setup_intent.succeeded" | "sigma.scheduled_query_run.created" | "source.canceled" | "source.chargeable" | "source.failed" | "source.mandate_notification" | "source.refund_attributes_required" | "source.transaction.created" | "source.transaction.updated" | "subscription_schedule.aborted" | "subscription_schedule.canceled" | "subscription_schedule.completed" | "subscription_schedule.created" | "subscription_schedule.expiring" | "subscription_schedule.released" | "subscription_schedule.updated" | "tax.settings.updated" | "tax_rate.created" | "tax_rate.updated" | "terminal.reader.action_failed" | "terminal.reader.action_succeeded" | "terminal.reader.action_updated" | "test_helpers.test_clock.advancing" | "test_helpers.test_clock.created" | "test_helpers.test_clock.deleted" | "test_helpers.test_clock.internal_failure" | "test_helpers.test_clock.ready" | "topup.canceled" | "topup.created" | "topup.failed" | "topup.reversed" | "topup.succeeded" | "transfer.created" | "transfer.reversed" | "transfer.updated" | "treasury.credit_reversal.created" | "treasury.credit_reversal.posted" | "treasury.debit_reversal.completed" | "treasury.debit_reversal.created" | "treasury.debit_reversal.initial_credit_granted" | "treasury.financial_account.closed" | "treasury.financial_account.created" | "treasury.financial_account.features_status_updated" | "treasury.inbound_transfer.canceled" | "treasury.inbound_transfer.created" | "treasury.inbound_transfer.failed" | "treasury.inbound_transfer.succeeded" | "treasury.outbound_payment.canceled" | "treasury.outbound_payment.created" | "treasury.outbound_payment.expected_arrival_date_updated" | "treasury.outbound_payment.failed" | "treasury.outbound_payment.posted" | "treasury.outbound_payment.returned" | "treasury.outbound_payment.tracking_details_updated" | "treasury.outbound_transfer.canceled" | "treasury.outbound_transfer.created" | "treasury.outbound_transfer.expected_arrival_date_updated" | "treasury.outbound_transfer.failed" | "treasury.outbound_transfer.posted" | "treasury.outbound_transfer.returned" | "treasury.outbound_transfer.tracking_details_updated" | "treasury.received_credit.created" | "treasury.received_credit.failed" | "treasury.received_credit.succeeded" | "treasury.received_debit.created")[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The URL of the webhook endpoint. */
    url: string;
  }) => Promise<WebhookEndpoint>;

  /**
   * Delete a webhook endpoint
   */
  deleteWebhookEndpointsWebhookEndpoint: (input: {
    body?: { [key: string]: unknown };
    webhook_endpoint: string;
  }) => Promise<DeletedWebhookEndpoint>;

  /**
   * Retrieve a webhook endpoint
   */
  getWebhookEndpointsWebhookEndpoint: (input: {
    body?: { [key: string]: unknown };
    webhook_endpoint: string;
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
  }) => Promise<WebhookEndpoint>;

  /**
   * Update a webhook endpoint
   */
  postWebhookEndpointsWebhookEndpoint: (input: {
    /** An optional description of what the webhook is used for. */
    description?: string | "";
    /** Disable the webhook endpoint if set to true. */
    disabled?: boolean;
    /** The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection. */
    enabled_events?: ("*" | "account.application.authorized" | "account.application.deauthorized" | "account.external_account.created" | "account.external_account.deleted" | "account.external_account.updated" | "account.updated" | "application_fee.created" | "application_fee.refund.updated" | "application_fee.refunded" | "balance.available" | "balance_settings.updated" | "billing.alert.triggered" | "billing.credit_grant.created" | "billing_portal.configuration.created" | "billing_portal.configuration.updated" | "billing_portal.session.created" | "capability.updated" | "cash_balance.funds_available" | "charge.captured" | "charge.dispute.closed" | "charge.dispute.created" | "charge.dispute.funds_reinstated" | "charge.dispute.funds_withdrawn" | "charge.dispute.updated" | "charge.expired" | "charge.failed" | "charge.pending" | "charge.refund.updated" | "charge.refunded" | "charge.succeeded" | "charge.updated" | "checkout.session.async_payment_failed" | "checkout.session.async_payment_succeeded" | "checkout.session.completed" | "checkout.session.expired" | "climate.order.canceled" | "climate.order.created" | "climate.order.delayed" | "climate.order.delivered" | "climate.order.product_substituted" | "climate.product.created" | "climate.product.pricing_updated" | "coupon.created" | "coupon.deleted" | "coupon.updated" | "credit_note.created" | "credit_note.updated" | "credit_note.voided" | "customer.created" | "customer.deleted" | "customer.discount.created" | "customer.discount.deleted" | "customer.discount.updated" | "customer.source.created" | "customer.source.deleted" | "customer.source.expiring" | "customer.source.updated" | "customer.subscription.created" | "customer.subscription.deleted" | "customer.subscription.paused" | "customer.subscription.pending_update_applied" | "customer.subscription.pending_update_expired" | "customer.subscription.resumed" | "customer.subscription.trial_will_end" | "customer.subscription.updated" | "customer.tax_id.created" | "customer.tax_id.deleted" | "customer.tax_id.updated" | "customer.updated" | "customer_cash_balance_transaction.created" | "entitlements.active_entitlement_summary.updated" | "file.created" | "financial_connections.account.account_numbers_updated" | "financial_connections.account.created" | "financial_connections.account.deactivated" | "financial_connections.account.disconnected" | "financial_connections.account.reactivated" | "financial_connections.account.refreshed_balance" | "financial_connections.account.refreshed_ownership" | "financial_connections.account.refreshed_transactions" | "financial_connections.account.upcoming_account_number_expiry" | "identity.verification_session.canceled" | "identity.verification_session.created" | "identity.verification_session.processing" | "identity.verification_session.redacted" | "identity.verification_session.requires_input" | "identity.verification_session.verified" | "invoice.created" | "invoice.deleted" | "invoice.finalization_failed" | "invoice.finalized" | "invoice.marked_uncollectible" | "invoice.overdue" | "invoice.overpaid" | "invoice.paid" | "invoice.payment_action_required" | "invoice.payment_attempt_required" | "invoice.payment_failed" | "invoice.payment_succeeded" | "invoice.sent" | "invoice.upcoming" | "invoice.updated" | "invoice.voided" | "invoice.will_be_due" | "invoice_payment.paid" | "invoiceitem.created" | "invoiceitem.deleted" | "issuing_authorization.created" | "issuing_authorization.request" | "issuing_authorization.updated" | "issuing_card.created" | "issuing_card.updated" | "issuing_cardholder.created" | "issuing_cardholder.updated" | "issuing_dispute.closed" | "issuing_dispute.created" | "issuing_dispute.funds_reinstated" | "issuing_dispute.funds_rescinded" | "issuing_dispute.submitted" | "issuing_dispute.updated" | "issuing_personalization_design.activated" | "issuing_personalization_design.deactivated" | "issuing_personalization_design.rejected" | "issuing_personalization_design.updated" | "issuing_token.created" | "issuing_token.updated" | "issuing_transaction.created" | "issuing_transaction.purchase_details_receipt_updated" | "issuing_transaction.updated" | "mandate.updated" | "payment_intent.amount_capturable_updated" | "payment_intent.canceled" | "payment_intent.created" | "payment_intent.partially_funded" | "payment_intent.payment_failed" | "payment_intent.processing" | "payment_intent.requires_action" | "payment_intent.succeeded" | "payment_link.created" | "payment_link.updated" | "payment_method.attached" | "payment_method.automatically_updated" | "payment_method.detached" | "payment_method.updated" | "payout.canceled" | "payout.created" | "payout.failed" | "payout.paid" | "payout.reconciliation_completed" | "payout.updated" | "person.created" | "person.deleted" | "person.updated" | "plan.created" | "plan.deleted" | "plan.updated" | "price.created" | "price.deleted" | "price.updated" | "product.created" | "product.deleted" | "product.updated" | "promotion_code.created" | "promotion_code.updated" | "quote.accepted" | "quote.canceled" | "quote.created" | "quote.finalized" | "radar.early_fraud_warning.created" | "radar.early_fraud_warning.updated" | "refund.created" | "refund.failed" | "refund.updated" | "reporting.report_run.failed" | "reporting.report_run.succeeded" | "reporting.report_type.updated" | "reserve.hold.created" | "reserve.hold.updated" | "reserve.plan.created" | "reserve.plan.disabled" | "reserve.plan.expired" | "reserve.plan.updated" | "reserve.release.created" | "review.closed" | "review.opened" | "setup_intent.canceled" | "setup_intent.created" | "setup_intent.requires_action" | "setup_intent.setup_failed" | "setup_intent.succeeded" | "sigma.scheduled_query_run.created" | "source.canceled" | "source.chargeable" | "source.failed" | "source.mandate_notification" | "source.refund_attributes_required" | "source.transaction.created" | "source.transaction.updated" | "subscription_schedule.aborted" | "subscription_schedule.canceled" | "subscription_schedule.completed" | "subscription_schedule.created" | "subscription_schedule.expiring" | "subscription_schedule.released" | "subscription_schedule.updated" | "tax.settings.updated" | "tax_rate.created" | "tax_rate.updated" | "terminal.reader.action_failed" | "terminal.reader.action_succeeded" | "terminal.reader.action_updated" | "test_helpers.test_clock.advancing" | "test_helpers.test_clock.created" | "test_helpers.test_clock.deleted" | "test_helpers.test_clock.internal_failure" | "test_helpers.test_clock.ready" | "topup.canceled" | "topup.created" | "topup.failed" | "topup.reversed" | "topup.succeeded" | "transfer.created" | "transfer.reversed" | "transfer.updated" | "treasury.credit_reversal.created" | "treasury.credit_reversal.posted" | "treasury.debit_reversal.completed" | "treasury.debit_reversal.created" | "treasury.debit_reversal.initial_credit_granted" | "treasury.financial_account.closed" | "treasury.financial_account.created" | "treasury.financial_account.features_status_updated" | "treasury.inbound_transfer.canceled" | "treasury.inbound_transfer.created" | "treasury.inbound_transfer.failed" | "treasury.inbound_transfer.succeeded" | "treasury.outbound_payment.canceled" | "treasury.outbound_payment.created" | "treasury.outbound_payment.expected_arrival_date_updated" | "treasury.outbound_payment.failed" | "treasury.outbound_payment.posted" | "treasury.outbound_payment.returned" | "treasury.outbound_payment.tracking_details_updated" | "treasury.outbound_transfer.canceled" | "treasury.outbound_transfer.created" | "treasury.outbound_transfer.expected_arrival_date_updated" | "treasury.outbound_transfer.failed" | "treasury.outbound_transfer.posted" | "treasury.outbound_transfer.returned" | "treasury.outbound_transfer.tracking_details_updated" | "treasury.received_credit.created" | "treasury.received_credit.failed" | "treasury.received_credit.succeeded" | "treasury.received_debit.created")[];
    /** Specifies which fields in the response should be expanded. */
    expand?: (string)[];
    /** Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. */
    metadata?: { [key: string]: string | undefined } | "";
    /** The URL of the webhook endpoint. */
    url?: string;
    webhook_endpoint: string;
  }) => Promise<WebhookEndpoint>;
};

export * from "./schemas.js";
