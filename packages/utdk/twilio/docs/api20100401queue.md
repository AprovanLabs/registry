# Api20100401Queue

5 operations · `@utdk/twilio`

```ts
import twilio from "@utdk/twilio";
```

## `twilio.listQueue`

Retrieve a list of queues belonging to the account used to make the request — [Provider docs](https://support.twilio.com)

```ts
twilio.listQueue(input: {
  /** The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Queue resources to read. */
  AccountSid: string;
  /** How many resources to return in each list page. The default is 50, and the maximum is 1000. */
  PageSize?: number;
  /** The page index. This value is simply for client state. */
  Page?: number;
  /** The page token. This is provided by the API. */
  PageToken?: string;
}): Promise<{ end?: number; first_page_uri?: string; next_page_uri?: string | null; page?: number; page_size?: number; previous_page_uri?: string | null; start?: number; uri?: string; queues?: (ApiV2010AccountQueue)[] }>
```

<sub>`GET /2010-04-01/Accounts/{AccountSid}/Queues.json` · `ListQueue`</sub>

## `twilio.createQueue`

Create a queue — [Provider docs](https://support.twilio.com)

```ts
twilio.createQueue(input: {
  /** A descriptive string that you created to describe this resource. It can be up to 64 characters long. */
  FriendlyName: string;
  /** The maximum number of calls allowed to be in the queue. The default is 1000. The maximum is 5000. */
  MaxSize?: number;
  /** The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that will create the resource. */
  AccountSid: string;
}): Promise<ApiV2010AccountQueue>
```

<sub>`POST /2010-04-01/Accounts/{AccountSid}/Queues.json` · `CreateQueue`</sub>

## `twilio.deleteQueue`

Remove an empty queue — [Provider docs](https://support.twilio.com)

```ts
twilio.deleteQueue(input: {
  /** The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Queue resource to delete. */
  AccountSid: string;
  /** The Twilio-provided string that uniquely identifies the Queue resource to delete */
  Sid: string;
}): Promise<undefined>
```

<sub>`DELETE /2010-04-01/Accounts/{AccountSid}/Queues/{Sid}.json` · `DeleteQueue`</sub>

## `twilio.fetchQueue`

Fetch an instance of a queue identified by the QueueSid — [Provider docs](https://support.twilio.com)

```ts
twilio.fetchQueue(input: {
  /** The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Queue resource to fetch. */
  AccountSid: string;
  /** The Twilio-provided string that uniquely identifies the Queue resource to fetch */
  Sid: string;
}): Promise<ApiV2010AccountQueue>
```

<sub>`GET /2010-04-01/Accounts/{AccountSid}/Queues/{Sid}.json` · `FetchQueue`</sub>

## `twilio.updateQueue`

Update the queue with the new parameters — [Provider docs](https://support.twilio.com)

```ts
twilio.updateQueue(input: {
  /** A descriptive string that you created to describe this resource. It can be up to 64 characters long. */
  FriendlyName?: string;
  /** The maximum number of calls allowed to be in the queue. The default is 1000. The maximum is 5000. */
  MaxSize?: number;
  /** The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Queue resource to update. */
  AccountSid: string;
  /** The Twilio-provided string that uniquely identifies the Queue resource to update */
  Sid: string;
}): Promise<ApiV2010AccountQueue>
```

<sub>`POST /2010-04-01/Accounts/{AccountSid}/Queues/{Sid}.json` · `UpdateQueue`</sub>

Named result types are exported from the package — hover them in your editor, or browse `types/schemas.ts`.

<!-- prompt-hash:
8c3694991a4c289225f05a4e8f1e098cc74d085a088d7dffd82f00d93797b7f8
-->
