{
  "name": "@aprovan/registry-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CDK infrastructure for the Aprovan registry gateway",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aprovan/main": "^0.1.0",
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "aws-cdk": "^2.180.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.3",
    "vitest": "2.1.5"
  }
}
