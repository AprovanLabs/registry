# Apps and Workflows — the domain

This document previously described the pre-`app-model-split` model (the
Personal pseudo-app, `dataScope`, name-keyed identity, a three-namespace
`NATIVE_APP_NAMESPACES`). That model is superseded and this file is retained
only so [platform.md](./platform.md)'s inbound link keeps resolving.

For the current, normative model see:

- `aprovan/docs/app-data.md` — records vs. files, ULID identity
  (`appId`/`installId`), partition layout (current truth).
- `aprovan/docs/native-surfaces.md` — how native services (including Apps)
  present as first-class surfaces in the chat app (current truth).
- `aprovan/openspec/changes/IW-9-APP-FIRST.md` — forward direction for the
  app-first platform.

_2026-07-24. Companion to [vcs-and-sessions.md](./vcs-and-sessions.md); see
[platform.md](./platform.md) for the repo/deployment map._
